# CLAUDE.md — KOTU 프로젝트 에이전트 운영 규칙

> **이 파일의 성격**: Claude 세션의 프로젝트 메모리(로컬 저장)를 저장소로 미러링한 것.
> 다른 컴퓨터·다른 계정에서 이 저장소를 열어도 같은 규칙으로 이어서 일하기 위함이다.
> **동기화 규칙(2026-08-14 사용자 지시): 메모리를 갱신하면 이 파일도 함께 갱신·커밋한다.**
> 프로젝트 상태의 정본은 `docs/HANDOVER.md`(인수인계) + `docs/REQUIREMENTS.md`(백로그) —
> 이 파일에는 상태를 중복 기록하지 않고 **일하는 방식**만 담는다.

## 1. 협업 규칙 (사용자 확정)

1. **"구현/수정해줘" 요청은 실개발이 아니라 `docs/REQUIREMENTS.md`에 항목 추가만 한다.**
   실제 개발은 **"미반영 A?? 진행"** 형태의 명시적 지시가 있을 때만 착수.
2. **A번호는 영구 ID** — 재정렬·재사용 금지. 완료되면 본문에서 상세를 정리하고 결번 기록.
3. **커밋은 건별(버전 명기: `v0.X.0 [A??] ...`), push는 커밋마다 즉시 1회.**
   (2026-08-21 사용자 개정 — 매 버전 빌드·릴리스가 바로 나오게. 종전 "묶음 끝 1회·몰아 push 시
   마지막만 태그" 방식은 폐지. 버전 커밋마다 push → 태그·릴리스가 버전별로 발행된다.)
4. **권장 모델 병기**: 항목 등재 시 난이도로 Fable/Opus 중 권장 모델을 `[미반영 An · 모델]`로
   병기한다. Fable = 설계·상태머신·원인 불명 디버깅·조사 / Opus = 사양 확정된 기계적 구현.
   **단 하위 에이전트 위임은 Opus를 기본으로 한다(2026-09-03 사용자 지시 — "하위 에이전트는 가급적
   Fable을 쓰지 말 것").** 위 기준은 난이도 판단용으로 그대로 두되, 등재상 Fable 항목이라도
   **위임 시에는 Opus로 지정**하고, **Fable이 꼭 필요해 보이면 위임 전에 사용자에게 먼저 묻는다**
   (2026-09-03 A309가 첫 적용 — 등재는 Fable 항목이었으나 사용자 확인을 받아 Opus로 진행).
   ※ **오케스트레이션 세션 자체를 Fable로 구동한다는 아래 조항은 판단 보류** — 사용자가 그것까지
   바꾸라고 하지는 않았으므로 임의로 고치지 않는다.
   **실개발 위임(2026-08-14 사용자 확정)**: 모델별 새 세션 대신 **오케스트레이터 세션이
   서브에이전트 호출의 model 파라미터로 권장 모델을 명시 지정해 위임**한다(Opus 항목 → opus /
   Fable 항목 → fable). **오케스트레이션 세션 자체는 Fable로 구동한다(2026-08-20 사용자 확정
   — 함정 선지목·서브에이전트 보고 검수 품질 때문. Opus 오케스트레이션은 쓰지 않는다).** 종전 "모델별 새 세션" 방식도 병행 가능. 한 항목이 에이전트 하나의
   컨텍스트를 넘칠 크기면 배치를 쪼갠다. 오케스트레이션 세션의 컨텍스트가 차면 HANDOVER
   갱신·커밋 후 **다음 세션에서 오케스트레이션을 이어가도록 사용자에게 한 줄로 안내**한다.
5. **다이어트 규칙**: 완료 상세는 미진행 항목의 수행에 도움이 되는 동안만 REQUIREMENTS 본문에,
   아니면 `docs/REQUIREMENTS-ARCHIVE.md`로 이관(한 줄 요약 + 포인터만 남김). **완료 처리 때마다 적용**한다
   — 미루면 쌓인다(2026-08-28 실사례: 3,763줄까지 불어 일괄 재적용). 부록 A 표의 완료 행·부록 B의
   무관해진 결정도 같은 기준으로 ARCHIVE로 보낸다.
6. 같은 내용을 사용자가 재차 지시하면 모순이 아니라 **재확인**으로 기록한다(사용자 명시).
7. 애매한 지시는 해석과 결정 필요 사항을 함께 등재하고, 확인 질문은 "부록 B 확인 필요"에 모은다.
8. **주기 작업을 UI 스레드에서 돌리지 않는다(2026-08-29 사용자 확정)** — 사용자가 체감 지연을
   느끼지 못했더라도 금지다("UI 스레드에서 도는 건 절대 금지"). 타이머·폴링·네트워크 체크는 워커에서
   하고 UI 반영만 디스패처로 되돌린다. 최초 적용 대상 = A278(업데이트 체크 2분 주기).
9. **"부록 B 확인 필요"는 답을 받아 소진시킨다.** 2026-08-13~15에 올린 14건이 2주 넘게 방치되다
   2026-08-29에야 일괄 정리됐다(대부분 "이제 안 거슬린다"로 종결 — 그 사이 코드가 여러 번 바뀌어
   질문 자체가 낡아 있었다). 항목이 쌓이면 다음 세션 시작 때 사용자에게 물어 비울 것.

## 2. 개발 파이프라인 — 오케스트레이터 + 서브에이전트 직렬

병렬 금지. 오케스트레이터가 배치 정의·diff 검수·커밋·push·CI 확인, 서브에이전트는 구현만(커밋 금지).
서브에이전트 모델은 항목의 권장 모델(규칙 1.4)을 호출 시 명시 지정한다.

서브에이전트 프롬프트에 반드시 넣을 것:
1. 저장소 경로 2종(파일 도구용 / bash용 — 다르다)
2. **dotnet 없음 → CI가 유일한 컴파일러**, push 전 정적 자체 검사 의무
3. `TreatWarningsAsErrors=true` — 미사용 private 필드도 빌드를 깬다
4. XML/XAML 주석 안 하이픈 2개 금지, 레거시 식별자 불가침, 실명 금지
   + **주석은 실제 한글 문자로 저장(`\uXXXX` 이스케이프 표기 금지 — 2026-08-24 실사고: 파일 편집
   도구가 한글 주석을 이스케이프로 저장. 커밋 전 `grep -E '\\u(A[C-F]|[BCD][0-9A-F])[0-9A-F]{2}'`
   검사 상비. 따옴표 안 글리프 리터럴("" 등)은 의도된 것이니 오탐 아님)**
5. 커밋·푸시 금지
6. 새 API는 기존 사용례 grep으로 실재 증빙, 선례 없으면 CI 1순위 위험 후보 + 최소 복구법 보고
7. "이 배치의 최대 함정"을 오케스트레이터가 먼저 찾아 적어 줄 것
8. "구현 시 결정"은 미리 답을 준다 — 수치·키 이름·문구·실패 시 동작까지(되물을 수 없다)
9. "지시가 코드와 어긋나면 코드를 따르고 보고하라"
10. 직렬 연쇄 배치면 앞 배치에 "뒤 배치가 얹힐 훅"을 심게 명시. 문자 키를 셸 전역 키로 쓸 땐
    3종 세트(오토리피트 무시·텍스트 입력 통과·PassThroughTag 통과)를 못 박을 것.

가장 잘 통하는 장치 = **배치마다 "통과 조건"을 이름 붙여 표로 요구**(대조표·grep 표·수명 감사표).
오케스트레이터 주의: 신규 파일은 `git diff`에 안 보임(`git status`의 `??` 별도 검수), 제거 배치는
고아 심볼(CS0414)이 위험, 파생 수치는 함께 고치게 지시.

## 3. 빌드·배포 환경

- 샌드박스(Linux)에 dotnet 없음 — 정적 검사(tree-sitter·grep·XML 검증)까지만 가능,
  실빌드는 push 후 GitHub Actions(windows-latest). **tree-sitter는 using 누락·catch 타입 순서를
  못 잡는다** — 유사 파일과 대조 검사할 것.
- **WPF에서 되는 XAML 문법이 WinUI에서 된다고 가정하지 말 것**(v0.169.1 실사례: `PathGeometry.Figures`에
  path 미니언어 문자열 → `XamlCompiler error WMC0055`. `Data="M ..."`처럼 **Geometry 타입 속성**에 거는
  문자열 변환만 성립한다. **v0.174.1 실사례 추가: WinUI Geometry는 부모가 하나뿐이라 공유 리소스를
  PathIcon.Data에 걸면 런타임 XamlParseException으로 앱이 죽는다 — CI는 컴파일만 하므로 런타임
  XAML 파스·할당 실패 부류를 못 잡는다. 도형은 사용처마다 인라인 문자열 또는 코드 빌더로 새
  인스턴스를 만들 것**). 규칙: **저장소 안에 실제로 쓰이고 있는 형태만 복제한다.** 선례가 0건이면
  "CI 1순위 위험 후보 + 최소 복구법"으로 반드시 보고하게 할 것.
- **master push만으로 태그+릴리스 자동**(release.yml이 `Directory.Build.props`의 Version을 읽어
  멱등 발행). 작업 완료 = 버전 올린 커밋 + push 1회.
- push 인증은 저장소 로컬 git 설정에 이미 구성돼 있음(비밀 값은 저장소에 없음). 401이 나면
  사용자에게 토큰 재발급 요청. **새 PC에 clone하면 두 가지를 로컬 설정에 직접 넣어야 샌드박스가
  커밋·push할 수 있다(2026-08-29 GitHub Desktop PC 실사례)**: ① identity — 샌드박스 리눅스 git은
  Windows 전역 `.gitconfig`를 못 봐서 `user.name`·`user.email`이 빈 값이다(`ZP Studios` /
  `zpstudiosdev@gmail.com`을 `--local`로 박을 것. 안 하면 커밋 자체가 거부된다) ② PAT — Windows
  자격 증명 관리자도 못 읽으므로 remote URL에 토큰을 넣는다(`https://<PAT>@github.com/zpstudios/kotu.git`
  — 소스트리는 remote 설정, GitHub Desktop은 Repository settings → Remote). 검증은
  `git push --dry-run origin master`(401/403이 아니면 인증 성공).
  저장소가 OneDrive 안이라 토큰이 담긴 `.git/config`가 클라우드로 동기화된다 — PAT는 fine-grained로
  `zpstudios/kotu` 한 저장소·Contents Read/write까지만 좁혀 발급할 것.
- CI 로그는 web_fetch로 못 읽음 — 사용자 Chrome(브라우저 MCP)으로 Actions 잡 페이지를 열어 읽는다.
  사용자의 활성 탭을 뺏지 말 것.
- 샌드박스 git이 `.git/*.lock` 삭제에 "Operation not permitted"로 막히면 파일 삭제 권한 도구
  (allow_cowork_file_delete)로 권한을 켠 뒤 rm.
- **사용자 노출 UI 문자열은 영어만**(코드 주석·커밋 메시지는 한국어).

## 4. 검증 분업

사용자는 매 릴리스를 직접 테스트하지 않는다. Claude가 코드 정독+흐름 시뮬레이션으로 자체 검수
패스를 돌리고(라우팅 재진입·상태 전이·계약 위반), **실기기에서만 판별 가능한 것**(렌더 모양·네이티브
재생·체감 성능·비공식 OS 동작)만 "실기기 확인 포인트"로 모아 HANDOVER에 적는다. 사용자가 이슈를
보고하면 그 유형을 다음 자체 검수 체크리스트에 추가한다. 실기기 확인 대기 목록의 정본 = HANDOVER §4(최신 완주 묶음)·§5 + `docs/DEVICE-CHECKS.md`(그 이전 누적분).
새 묶음을 완주하면 HANDOVER §4의 기존 내용을 DEVICE-CHECKS.md 상단으로 밀어내고 §4에는 새 묶음만 둔다.
**종결 규칙(2026-08-29 사용자 확정)**: 이 목록은 사용자가 매 릴리스를 확인하지 않는 이상 구조적으로 줄지 않는다
— **약 100릴리스 이상 뒤로 밀린 구간은 `docs/DEVICE-CHECKS-ARCHIVE.md`로 "미확인 종결" 이관**한다(그 사이 방향
전환으로 확인 대상 동작 자체가 바뀌어 지금 확인해도 의미가 없다). 1차 적용 = v0.161.0 이하 20묶음. 이관은
삭제가 아니라 조회용 보관이며, 종결분에 묶여 REQUIREMENTS 본문에 존치되던 완료 상세도 함께 풀어 준다.

## 5. 브랜딩·식별자 (불가침 규칙 포함)

- 앱 이름 **KOTU (King Of The Util)**, 실행 파일 **KOTU.exe**, 저장소 **zpstudios/kotu**.
  이름 이력: WinUtil(초기 코드명) → ZP(v0.33.0) → KOTU(v0.86.0~). 로컬 폴더명만 `winutil`.
- **절대 KOTU로 바꾸면 안 되는 것**(과거를 가리키는 값 — 바꾸면 구 등록 청소가 깨진다):
  `ExplorerIntegration.LegacyBrands = ["ZP", "WinUtil"]`, `%AppData%\WinUtil`·`%AppData%\ZP`·
  `WinUtil-Main`·`ZP-Main`을 언급하는 주석. 리브랜딩 스캔은 **모든 과거 이름**으로 할 것.
- 다음 리네이밍 시 손댈 지점: `Branding.AppName`, `ExplorerIntegration.Brand`+`LegacyBrands`,
  `AssemblyName`, 아이콘 접두사(`kotu-*.ico`)+생성 스크립트, Velopack packId, release.yml mainExe.
- **저작 주체 = ZP Studios(회사) / 제품 = KOTU** — 층을 섞지 않는다. **개인 실명·개인 메일은
  배포물·저장소·문서 어디에도 쓰지 않는다**(이 파일 포함). 커밋은 저장소 로컬 config identity
  그대로 — `-c user.name=...` 일회성 오버라이드 금지. 표기 변경 시 4곳 동기:
  `Directory.Build.props`·LICENSE·release.yml packAuthors·site 푸터.
- csproj 등 XML 주석에 하이픈 2개(`--`) 연속 금지 — MSB4025로 빌드가 깨진다(실제 사례 있음).

## 6. 인수인계 문서 정책

컨텍스트가 차서 세션 전환이 필요해 보이면 **묻지 말고** `docs/HANDOVER.md`를 갱신하고 커밋한다
("정리할까요?" 금지 — 사용자가 지침으로 확정). 갱신 시점 = 압축 임박·작업 묶음(릴리스) 종료·대화
장기화. 기준 = "다음 세션이 이 문서만 읽고 이어서 일할 수 있는가". 저장소가 이미 기록하는 것
(코드 구조·커밋 이력)은 중복해 적지 않는다. 사용자에게는 한 줄로만 알린다.
**문서 분업(2026-08-28 정리)**: HANDOVER §1은 "지금 상태"만(수십 줄) — 세션별 작업 일지는
`docs/SESSION-LOG.md` 최상단에 붙인다(HANDOVER에 일지를 쌓지 말 것 — 2,000줄까지 불어난 실사례).
낙수(등재 후보) 목록의 정본 = REQUIREMENTS 부록 A-2.

## 7. 메모리 ↔ CLAUDE.md 동기화

세션 메모리(로컬)에 프로젝트 관련 내용을 쓰거나 고치면, **같은 작업 묶음 안에서 이 파일에도
반영해 커밋한다**(2026-08-14 사용자 지시 — 계정·컴퓨터 이관 대비). 새 환경에서 이 저장소를 열면:
① 이 파일이 자동 적용되고 ② `docs/HANDOVER.md`를 읽는 것으로 온보딩이 끝난다.
