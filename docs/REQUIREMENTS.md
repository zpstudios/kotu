# KOTU 요구사항 — 남은 작업 정리

> ※ 앱 이름은 v0.86.0(A46)에서 **ZP → KOTU (King Of The Util)** 로 변경 완료.
> 아래에 남은 "ZP" 표기는 변경 **이전** 이력을 가리키는 것이다.

> **운용 규칙**
> 1. "~~~ 구현/수정해줘" 요청은 이 문서에 항목 추가만 한다 (실개발 X).
> 2. 실제 개발은 "미반영 A?? 진행" 지시가 있을 때만 착수한다.
> 3. **A번호는 영구 ID** — 재정렬·재사용 금지. 완료되면 문서에서 항목을 제거하고 그 번호는 결번 처리. 신규 항목은 마지막 번호 다음을 부여.
> 4. **우선순위 정책 (2026-08-10 사용자 확정)**: **UI·코어 기능을 우선** 개발한다.
>    부가 기능군(녹화/녹음 A14·A15, HW 랭킹 A19, 포터블 배포 A43, HWP 뷰어 A45 등)은 후순위.
> 5. **권장 모델 병기 (2026-08-14 사용자 지시)**: 항목 등재 시 난이도·복잡도로 판단해
>    **Fable / Opus** 중 권장 모델을 `[미반영 An · 모델]` 형태로 함께 적는다.
>    실개발은 오케스트레이터 세션이 서브에이전트에 그 모델을 명시 지정해 위임한다
>    (2026-08-14 확정 — 종전 "모델별 새 세션" 방식도 병행 가능).
>    기준(HANDOVER §6과 동일) — **Fable**: 설계 판단·상태 머신·원인 불명 디버깅·API 조사 등
>    잘못 잡으면 되돌리기 비싼 것 / **Opus**: 사양이 표·수치로 확정돼 기계적으로 옮기면 되는 것.
>
> 상태 표기:
> - `[미반영 An · 모델]` 아직 안 함 — 지시가 오면 그 모델의 새 세션에서 착수 (부록 A 번호)
> - `[확인]` 구현은 됐으나 실기기 확인/재등록 확인이 남음
> - `[보류]` 후순위 (조사·선행 작업 필요)
>
> **다이어트 규칙 (2026-08-13 사용자 지시 · 2026-08-14 재확인 + 일괄 적용)**: 완료(결번) 항목의
> 상세는 **미진행 항목의 수행에 도움이 되는 동안만 본문에 남긴다.** 참조가 끊긴 상세는
> `docs/REQUIREMENTS-ARCHIVE.md`로 옮기고 본문엔 한 줄 요약 + 포인터만 둔다.
> 앞으로 항목을 완료 처리할 때도 같은 기준으로 판단할 것.
> (2026-08-14 일괄 적용으로 22건 이관 — 남긴 완료 상세는 전부 미진행건이 참조하는 것:
> A34 표=A87 / A49·A30·A98=A83·A41 / A61·A62·A73·A74·A17·A18·A29·A54·A42=A60·A70·A71·A72 /
> A93·A57=A85·A94 / A95·A76·A78=A43 / A38·A21·A44=A47·A48 / A40·A97·A65=치수·진입로 전제.)

---

## 0. 브랜딩 · 명칭 (전역)

- ※ A46(**앱 이름 ZP → KOTU (King Of The Util)**, v0.86.0) 완료 — 결번. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A52(**모듈 표시명 정리** — `DisplayName` = Image/Video/Audio/Document/Archive·`BrandName` KOTU-archive·우클릭 라벨은 brandLabel 인자화, v0.87.0) 완료 — 결번.
  ※ 셸에 이미 등록된 verb 라벨·ProgID 설명은 **재등록될 때만** 갱신(7장 [확인]과 연관). (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A64(**리브랜딩 2단계 — exe·어셈블리·프로젝트·네임스페이스 전면 rename**, v0.88.0) 완료 — 결번. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A79(**브랜드 에셋 단계형 적용** — `branding.assetLevel` 0~3·기본 0·설정 화면 비노출, 단일 매핑 = `BrandAssets.cs`(+`packaging/brand.py` 동기), v0.119.0) 완료 — 결번.
  ※ 커밋되는 산출물(.ico·splash.png)은 **언제나 레벨 0**으로 생성. 원본 시트 = `docs/assets/kotu-brand-sheet.png`(재배포물엔 조각만). (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A82(**개인 실명 노출 제거 — 저작 주체 = `ZP Studios` 통일**, v0.90.0) 완료 — 결번.
  **원칙 유지: 개인 실명·개인 메일은 배포물·저장소·문서 어디에도 표시하지 않는다(이 문서 포함).**
  표기 변경 시 4곳(Directory.Build.props·LICENSE·release.yml·site 푸터)을 함께 볼 것.
  커밋은 저장소 로컬 config가 정답 — `-c user.name=...` 일회성 오버라이드 금지. (상세 → docs/REQUIREMENTS-ARCHIVE.md)

## 1. 셸 · 시작 메뉴

- ※ A1(메뉴 힌트 지연 툴팁, v0.57.0)·A2(인스턴스 배지, v0.58.0)·A3(아이콘 반전, v0.59.0)·A23(확장자별 파일 아이콘, v0.60.0)·A24(다중 인스턴스+새 창, v0.65.0)·A31(메뉴 히트 영역, v0.66.0)·A32(숫자 단독 전환+오버레이 Ctrl 회귀, v0.66.0) 완료 — 결번.
  A32 확정 내역: 1~5=모듈·0=Settings·`=메뉴 전부 단독 키, 텍스트 입력 컨트롤 포커스 시 통과(예외 처리),
  정보 오버레이 홀드·2연타 고정 = Ctrl(Shift는 A24 새 창 더블클릭 담당).
  A24 확정 내역: Ctrl+N·시작 메뉴 "New window" = 현재 모듈 빈 인스턴스 새 창 /
  탐색기 Shift+더블클릭·우클릭 "Open in new window" = 무조건 새 창 /
  설정 Windows 섹션 "Always open files in a new window" 토글(기본 off=재사용, 적용 범위=내장 탐색기+외부 열기,
  빈 셸 재사용은 유지, 창 대상 드래그&드롭·모듈 내부 탐색(◀/▶)은 규칙 무관하게 그 창에서).
- ※ **메뉴 여는 단축키 = `~`(숫자 1 왼쪽) 키** — A32(v0.66.0)에서 이미 구현됨(수정자 없는 단독 키). 추가 작업 없음.
- ※ A96(**시작 메뉴 개편** — All Readable=1번(A59 배정 대체)·구분선 2개·폭 124→136·항목 높이 44→40·광고 132×66·플라이아웃 밀착(Margin 0,0,0,-6), v0.116.0) 완료 — 결번. ⚠️ **항목 높이는 A106(v0.132.0)에서 40→36으로 재개정**(상하 패딩 10→8 동반). 나머지(폭 136·광고 132×66·밀착 −6)는 그대로. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A50(**시작 메뉴 항목 선택성 개선** — 좌측 히트 영역 확대(프레젠터 패딩 0)·구분선 여백 8→3, v0.92.0 — 치수는 A96이 개정) 완료 — 결번. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A53(**"New window" → "New Instance"** 표기 통일, v0.87.0) 완료 — 결번. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A65(**시작 메뉴에서 "New Instance" 항목 제거**, v0.92.0) 완료 — 결번.
  메뉴 항목과 전용 핸들러(AddNewInstanceItem)만 제거. 나머지 진입로는 전부 유지(사용자 확정):
  `Ctrl+N`, 탐색기 Shift+더블클릭, 우클릭 "Open in new instance",
  설정 "Always open files in a new instance" 토글. 다중 인스턴스 기능 자체도 유지(A70 의존).
- ※ A68(**인스턴스 색상 코딩 확장** — A2의 9색 순환(`InstanceIcon.ColorFor` 공유), 창 2개↑면 창·트레이 아이콘에 색 테두리 링, v0.103.0) 완료 — 결번.
  ※ 트레이 아이콘의 원형 번호 배지는 A54(v0.118.0)에서 제거(테두리만 유지), 창 아이콘 배지는 존치. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
  ※ **창 아이콘 배지·인스턴스 링은 A102(v0.130.0)가 대체**(배지 제거·링 = 모듈 색·창 개수 조건 폐지) — 9색 팔레트는 타이틀바 배지(A2) 전용으로만 남았다.
- ※ A69(**최소화 = 트레이로 숨김** — 전 모듈, 작업표시줄·Alt+Tab 제거, 마지막 창까지 숨겨도 프로세스 유지(닫기만 종료 경로), v0.104.0) 완료 — 결번. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A123(**작업표시줄 아이콘 좌클릭 → 창·아이콘 동시 소실 보고** — 사양 재확인, 코드 변경 0,
  2026-08-16) 종결 — 결번. 판정: ① 포커스된 창의 작업표시줄 버튼 좌클릭 = 최소화(**OS 표준 토글**
  — 우리 코드 아님) ② A69가 모든 최소화를 트레이 숨김으로 전환(작업표시줄·Alt+Tab 제거) — 두
  사양의 합성이 보고된 현상 그대로다. 최소화 출처 구분(작업표시줄 클릭 vs 캡션 ─ 버튼)은 둘 다
  SC_MINIMIZE라 신뢰성 있게 불가(선조사). **사용자: "당시 요구대로 구현된 거면 현상 유지" — 단
  판단 보류 명시**(부록 B 61). 재론 시 대안 = 일반 최소화 + 트레이 숨김 별도 제스처 분리.
  연관: 트레이 아이콘 미표시 환경(A100 §4 확인 대기)에서는 복귀 수단이 소실된다 — A100 확인 선행.
- ※ A54(**트레이 아이콘 정비** — 인스턴스당 정확히 1개 + 모듈별 표시 내용, v0.118.0) 완료 — 결번.
  ※ 센서 아이콘은 A101(v0.137.0)에서 폐지 — 창별 아이콘이 H/W 선택값을 표시한다(별도 축 소멸).
  ※ H/W 선택의 화면 쪽 토글 UI(구 하단 카드)는 A60 3차(v0.138.0)가 센터 타일/긴 그래프로 대체 —
  트레이 배선(ITrayStatusProvider·선택 = 표시)은 무변경.
  확정·구현 내역:
  - **원칙: 인스턴스 1개당 트레이 아이콘 정확히 1개**(인스턴스 5개 = 아이콘 5개). A18 센서 아이콘은
    **프로세스당 1개의 별도 축**으로 그대로 병존(개수 계산에서 제외 — 값 표시가 우선이라 손대지 않음).
    → A101이 이 별도 축을 폐지해 원칙만 남았다(전 화면 아이콘 = 창별 1개뿐).
  - **수명 감사 결과**: 생성(창 생성자 1회)·삭제(`Closed` → `Dispose` → NIM_DELETE)·탐색기 재시작
    (`TaskbarCreated` → NIM_ADD, `_added` 유지라 중복 없음)·`SetIcon` 재호출(소유 핸들만 `DestroyIcon`,
    캐시 핸들은 보존)은 모두 원칙을 지키고 있었다. **위반 1건만 수정**: 창을 닫지 않고 프로세스가
    내려가는 경로(정보 모듈 "Restart as admin"의 `Application.Exit`)에서 아이콘이 잔존할 수 있어
    `TrayIcon`에도 `ProcessExit` 정리를 걸었다(A18 `SensorTray`가 이미 쓰던 같은 방어선).
  - **표시 내용**: 열림 = 비디오 해상도·비트레이트 / 사진·문서 확장자·용량 / 오디오 시간·이퀄라이저 /
    압축 종류·압축률(작업 중엔 진행률). 유휴 = 3자 표기 **IMG/VID/AUD/DOC/ARC/ALL**.
    **유휴 = 1줄 중앙·저채도 / 열림 = 2줄·모듈 색**으로 상태를 이중 구분(DOC vs .doc 충돌 무해화).
  - **구조**: 계약 `ITrayStatusProvider`(+`TrayStatus`·`TrayFormat`)를 `KOTU.Core.Contracts`에 두고
    모듈이 값만 내준다(App → 모듈 단방향 — `IBottomBarProvider`·`IDriveStripHost`와 같은 방향).
    아이콘 합성은 셸(`TrayStatusIcon`, GDI+ — A18과 같은 도구). **A59 All Readable은 자식 상태를 그대로 중계**
    (자식 교체 시점에도 이벤트를 쏴 옛 값에 머물지 않게, 자식 없으면 `ALL`).
  - **구현 시 결정**:
    ① **이퀄라이저 = 실제 주파수 분석 아님** — libvlc 오디오 콜백 비용이 16px 장식에 비해 과해
       막대 4개의 **의사 패턴**을 그린다. 재생 중에만 1초 주기로 높이가 바뀌고 일시정지·정지면 낮게 고정
       (그때 타이머도 멈춘다).
    ② **A68 원형 번호 배지는 트레이에서 제거**(테두리만 유지) — 2줄 텍스트를 덮어 판독 불가.
       창 아이콘 배지는 존치, 번호는 제목 `[n]`(A56)이 계속 알려 준다.
       ※ A102(v0.130.0): 창 아이콘 배지도 제거되고 테두리는 모듈 색이 됐다. 제목 형식은 A103.
    ③ **표기 축약**: 용량 `1.2M`·`340K`(1024 단위, 10 이상은 소수 버림) / 비트레이트 `4.2M`(1000 단위,
       **파일 크기 ÷ 길이의 평균값** — 순간값보다 안정적이고 이미 가진 값으로 구해진다) /
       해상도 `1080p`(표준 등급 +6% 이내면 그 등급으로 스냅) / 확장자 대문자 4자까지 /
       압축률 `0.00~1.00`·진행률 `45%` / 값 없음은 `—`(A18과 동일).
    ④ **갱신**: 오디오만 1초 주기, 나머지는 이벤트 기반(열기·전환·길이 확정·압축 진행률·자식 교체).
       값이 그대로면 재합성하지 않는다 — A18 `ComposeKey`(문자열 키 비교) 그대로.
    ⑤ **정보(하드웨어) 모듈 유휴 표기 = `INF`** (3자 규칙 준수 + BrandName `KOTU-info`와 정합.
       2자 `HW`는 규칙에서 벗어나고 `HWM`은 조어라 채택 안 함). 표시 값이 상수라 계약을 구현하지 않고
       셸의 모듈 ID → 3자 표기 표가 담당한다. **A18 센서 2값 아이콘과는 별개 축**(혼동 금지).
       ※ A101(v0.137.0)이 대체: HardwareView가 계약을 구현해 선택값을 표시하고, 선택 0개면 같은
       `INF` 유휴를 직접 돌려준다(표의 hardware 행은 안전망으로 존치 — 별개 축은 소멸).
    ⑥ 표에 없는 화면(설정·미지원 파일 안내)은 중립 모듈 `.ico` + 인스턴스 테두리로 폴백 —
       "인스턴스당 아이콘 1개"는 어떤 화면에서도 유지된다.
- ※ A100(**창별 트레이 아이콘 미표시 수정** — ① 식별 결정화 v0.126.0 + ② IsPromoted 자동 승격 v0.128.0) 완료 — 결번.
  남긴 근거: A101(창별 아이콘 값 표시)·A102(아이콘 합성 개편)·A104(슬롯 상한 가정 점검)·A105(창별
  태스크바 축)가 이 구조를 전제한다. (조사 경위·리스크 평가 → docs/REQUIREMENTS-ARCHIVE.md)
  - 원인: 콜백 창 클래스명이 랜덤 GUID → Win11 NotifyIconSettings 식별(exe 경로+창 정보 해시)이
    실행·창마다 새로 생겨 항목 증식, 새 항목은 기본 '끔'. guidItem은 exe 경로 결박(개발 빌드/설치본
    병행 시 NIM_ADD 실패)으로 배제. Velopack은 `{root}\current\KOTU.exe` 고정 경로라 exe 축은 안전.
  - ① 식별 결정화(v0.126.0): 클래스명·uID = 프로세스 내 **단조 증가 슬롯**(`KOTUTrayWnd_{n}`·uID 100+n,
    SensorTray는 싱글턴이라 고정 이름 `KOTUSensorTrayWnd`·uID 2 — 대역 분리). 슬롯을 표시용 인스턴스
    번호에 묶지 않은 이유 = 번호는 창 닫힘 시 재배정되지만 클래스명은 생성 후 불변 + 재사용 시점에
    옛 창 클래스와 등록 충돌. 등록 실패 시 잔재 해제 후 재시도 → 랜덤 접미사 폴백(미표시보다 churn).
  - ② IsPromoted 자동 승격(v0.128.0): `src/KOTU.App/Integration/TrayPromotion.cs` — NIM_ADD 후 약
    1/4/10초 3회 HKCU `Control Panel\NotifyIconSettings` 스캔, 자기 exe 항목의 `IsPromoted`(DWORD)=1
    쓰기. 매칭 = `ExecutablePath` 값이 known-folder GUID 접두 형태일 수 있어 **ProcessPath 파일명
    접미사 비교**(하드코딩 없음 — 리브랜딩 무관). 이미 1이면 안 씀(churn 방지), 실패는 전부 조용히
    무시(비공식 키 — A38 성격의 폴백, 실패 시 ①의 슬롯당 1회 수동 켜기로 후퇴), 항목별 개별 try/catch.
    호출 4곳 = 생성자·TaskbarCreated 재등록 × TrayIcon/SensorTray — **SensorTray는 첫 NimAdd 전이만**
    (Render가 200ms 폴링이라 NimModify 경로에서 부르면 스캔 태스크 증식). 조기 종료·디바운스 없음(멱등).
    ※ A101(v0.137.0): SensorTray 폐지로 호출은 **TrayIcon 2곳만** 남았다(② 불변식은 그대로).
  - 실기기 확인 포인트 = HANDOVER v0.128.0 절.
- ※ A101(**H/W 트레이 표시 이원화 해소** — A18 센서 아이콘 폐지 → 창별 아이콘이 그 창 선택값 표시, v0.137.0) 완료 — 결번.
  **사양 변경(2026-08-14 사용자 확정 — A18 축 폐지)**: A18의 "앱 실행 중 상시 표시"는 **소멸** —
  H/W 창이 하나도 없으면 트레이에 센서 값도 없다. 창별 아이콘 표시가 그 자리를 대체한다.
  - `HardwareView`가 `ITrayStatusProvider` 구현(A54 셸 배선에 그대로 얹힘 — 셸 무수정):
    이 창 선택(`HardwareInstanceState.Selection`, 최대 2)을 `FormatCompact` 2줄로 표시,
    값 없는 줄 `—` 승계. **선택 1개 = 아래 줄 `—`**(계약상 열림 = 2줄 고정 — 단줄 열림 없음),
    **선택 0개 = 유휴 `INF`**(셸 폴백 표 `IdleTrayLabel`과 같은 결과 — 이중 규칙 방지, 그 행은
    안전망으로 존치). 줄별 채널 색·툴팁은 싣지 않음 — `TrayStatus`에 자리가 없고 **Core 계약
    확장 금지**(A60 3차에서 필요해질 때 한 번에), 글자는 모듈 액센트 1색(셸 합성).
  - 통지는 뷰 **UI 스레드 깔때기 1곳**(`NotifyTrayStatus`): 스냅샷 반영(`ApplySnapshot`)·선택
    토글이 표기 키(채널 ID+압축 표기 — 구 `ComposeKey` 방식) 비교 후 바뀔 때만 쏜다.
    워커 단계(OnSnapshot 디스패치 전) 발화 금지 — `GetTrayStatus`는 UI 스레드 계약이라 어긋난다.
  - 삭제: `SensorTray.cs`(+`App.xaml.cs` 초기화)·`TraySensors.cs`(파사드)와 고아 연쇄 =
    `HardwareModule.SubscribeSensors`(센서 전용 구독)·`HardwareInstanceState.ClearGlobalSelection`·
    `WindowManager.ShowHardware`(센서 아이콘 좌클릭 전용). 구 트레이 메뉴("Hide tray sensors"·
    "Open H/W monitor")도 함께 소멸 — 창별 아이콘 클릭은 TrayIcon 기존 동작(그 창 활성화) 그대로.
    `TrayPromotion` 호출은 TrayIcon 2곳만 남음(A100 ② 불변식 유지 — TrayIcon 경로 무수정).
  - 저장 정책 불변(A70): 전역 1벌·마지막 커밋·키 이름(`hardware.traySensors`) 유지 — 새 창
    초기값·재실행 복원이 이것이다. 커밋 깔때기는 이제 **둘 다 무통지**.
  - 실기기: 구 SensorTray의 Win11 NotifyIconSettings 잔재 항목(`KOTUSensorTrayWnd`·uID 2)이
    사용자 기기에 남을 수 있음 — 무해(설정 목록에 표시만 남는 항목).
- ※ A126(**창 아이콘(파비콘) 본체 글리프가 링에 가려짐 — 본체 inset 합성**, v0.148.0) 완료 — 결번.
  원 보고 2026-08-14: "글꼴이 너무 커서 테두리 색깔에 가려짐, 타이틀바 속 아이콘도 같은 문제" —
  당시 트레이 값 글자만 0.85 축소(A102)했고 창 아이콘 본체는 미조치로 남아 있었다.
  - `InstanceIcon.Compose`가 **링이 있을 때만** 본체를 링 두께만큼 안쪽으로 줄여 그린다:
    공통 기하(`thickness`·`radius`) 계산을 본체 그리기 **위로** 올린 뒤
    `g.TranslateTransform(offset, offset)` → `BrandIcons.DrawBase(g, icoPath, inner, accent)` →
    `g.ResetTransform()`. `inner = (int)Math.Round(size - thickness * 2)` ·
    `offset = (size - inner) / 2`. 링 없는 합성(중립·정보 모듈)은 전폭 그대로.
  - 크기 대조: 32px+링 → 본체 24px(`.ico`에 실재하는 프레임이라 스케일 손실 없음) ·
    16px+링 → 본체 12px · 링 없음 → 전폭 무변경. `DrawBase`에 사각 오버로드를 새로 만들지 않고
    좌표계를 옮긴 이유 = 다른 호출처(`BrandIcons.GetBranded`)의 픽셀을 건드리지 않기 위해.
  - A79 브랜드 레벨 표식은 `DrawBase` 안에서 그려지므로 본체와 **같이** inset된다(의도 — 주석 명기).
  - 캐시 키 무변경(링 ARGB가 이미 들어 있어 같은 키의 픽셀만 바뀐다). A105 라벨의 자체 inset 계산도
    무변경 — 링이 있으면 라벨 inset과 본체 inset이 같은 값이 되어 글자 줄과 본체가 같은 안쪽 사각을
    공유한다(정합 개선). 트레이 **값 렌더**(TrayStatusIcon)는 `DrawBase`·`GetComposed`를 아예 쓰지
    않는 별도 경로라 무영향. 반면 트레이 **폴백 아이콘**(TrayIcon.SetIcon의 링 있는 분기)은 같은
    `Compose`를 타므로 같은 inset이 적용된다 — 창 아이콘과 한 모습을 유지하는 쪽이 맞다.
  - 실기기 확인 포인트: 16px에서 본체 12px 글리프("KO/TU" 2줄 또는 발바닥) 가독 — A46 하한 재검.
- ※ A102(**트레이·창 아이콘 표기 개편** — 배지 제거·링 = 모듈 색·H/W 무링·글자 0.85배, v0.130.0) 완료 — 결번.
  아이콘 합성 구조(A101·A104·A105가 전제):
  - **링 = 그 창 모듈의 액센트 색**(`Branding.IconRing` 단일 판단 — 액센트가 곧 링 색, 예외는 아래 둘).
    **창 개수 조건 폐지** — 링의 목적이 인스턴스 구분에서 모듈 식별로 바뀌어 창이 하나여도 그린다.
    링 없음 = ⓐ 중립 아이콘 화면(설정·미지원 안내 — 구분할 모듈이 없다) ⓑ **정보(H/W) 모듈**
    (유휴 INF·값 표시 모두 — 값 2줄을 채우는 유일한 모듈이라 링이 글자를 먹는다).
  - **원형 번호 배지 = 창·트레이 모두 렌더 코드째 제거**(A68의 "창 아이콘 존치" 결정 대체).
    번호 표기는 창 제목(A103)과 타이틀바 배지(A2) 둘만 남는다 — **아이콘 합성은 번호를 안 받는다**
    (`InstanceIcon.GetComposed(icoPath, size, accent, ring)`, 캐시 키도 번호 대신 링 ARGB).
    ※ A105(v0.143.0)가 label 인자(하단 3자 표기)를 추가하고 ring을 nullable로 바꿨다 —
    캐시 키에도 3자 코드가 명시로 들어간다.
    9색 팔레트 `InstanceIcon.ColorFor`는 타이틀바 배지 전용으로 존치(A104가 참조).
  - **경로 분기**: 링 있음 → `InstanceIcon.GetComposed`(모듈 .ico + 링, 프로세스 수명 캐시) /
    링 없음 → `BrandIcons.GetBranded`(A79, 무변경) → 실패 시 원본 .ico. 창(WindowIcon)·트레이
    폴백(TrayIcon.SetIcon)·트레이 값 렌더(TrayStatusIcon) 세 경로가 같은 규칙을 쓴다.
    ※ 창(WindowIcon) 경로만 A105(v0.143.0)에서 "링 **또는 3자 표기** 있음 → InstanceIcon"으로
    넓어졌다(정보 모듈이 링 없이 INF를 얹는 경우) — 트레이 두 경로는 그대로다.
  - **글자 0.85배**(`TrayStatusIcon.FontScale`, 1줄·2줄 공통. 폭 초과 축소 루프의 하한 5px은 유지) —
    **실기기 미세조정 지점**(링에 글자가 물리는 정도를 눈으로 보고 이 상수만 건드린다).
  - 번호가 아이콘에 영향을 주지 않으므로 `SetInstanceNumber`의 아이콘 재합성 호출을 제거했다.
- ※ A103(**타이틀바 제목 표기 개편** — `2KOTU - 파일명`, v0.130.0) 완료 — 결번. A56 형식 대체.
  - 형식: 파일 있음 `KOTU - 파일명` / 없음(모듈만·설정·빈 셸) `KOTU`. 창이 2개 이상이면 번호를
    브래킷·공백 없이 앞에 붙여 `2KOTU - 파일명`. 미저장 표시 `● `는 그대로 맨 앞(A37).
    번호 표시 조건은 A56 그대로(창 2개 이상) — 형식만 교체했다.
  - **모듈명 단어 전부 제거**(Document·Image·H/W Info…). 설정 화면의 "Settings"도 같은 규칙으로
    빠졌다(파일 없는 화면 = `KOTU`) — 모듈 구분은 아이콘 링 색(A102)이 담당한다.
  - 조립 지점을 `MainWindow.FileTitle(path)` 한 곳으로 모았다(파일 열기 3경로 공용).
  - 구분자 em-dash → ASCII `-`. **UI 문자열 전수 점검**에서 21곳을 함께 교체(툴팁·안내·설정 문구).
    **제외 2종**: ⓐ "값 없음" 자리표시 글리프 `"—"`(`TrayStatus.Unknown`·H/W 카드·SensorTray —
    구분자가 아니라 16px에서 읽히는 기호) ⓑ `Branding.MissionStatement`(커밋된 `packaging/splash.png`와
    한 벌인 문구 — 코드만 바꾸면 이미지와 어긋난다. 바꾸려면 gen_splash.py 재생성과 한 배치로).
- ※ A104(**인스턴스 개수 무제한 — 상한 부재 증빙**, v0.143.0) 완료 — 결번. 전수 조사 결과
  **인위적 상한 없음 — 제거할 코드 없음**: 창 목록(`WindowManager._windows`·`_ordered`)은 무제한
  List, 번호 당겨오기(A2 `UpdateInstanceNumbers`)는 개수 무관, 제목 접두 번호(A103 `2KOTU`)는
  문자열 조립이라 자릿수 제한 없음(`10KOTU`도 그대로). 타이틀바 배지가 10번째부터 숨는 것
  (`SetInstanceNumber`의 `> 9`)과 9색 팔레트 순환은 사양이라 유지(A2/A56·부록 B 32번 — 제목
  번호가 전담). 무제한 전제의 가정 점검: A100 트레이 슬롯(클래스명 `KOTUTrayWnd_{n}`·uID
  100+n)은 단조 증가 int라 실질 무제한(이론 한계 = int 오버플로 약 21억 — 프로세스 수명 내
  도달 불가, 형 변환 uID(uint)도 그 안에서 안전), 아이콘 합성 캐시(InstanceIcon·BrandIcons·
  WindowIcon)는 키가 전부 모듈 축(경로×크기×액센트×링×3자)이라 유계 — 인스턴스 수와 무관.
  트레이 값 아이콘(A54 TrayStatusIcon)은 캐시 없이 즉시 회수라 증식 없음.
- ※ A105(**태스크바 인스턴스별 분리(AppUserModelID) + 창 아이콘 모듈 3자 표기**, v0.143.0) 완료 — 결번.
  ① 창별 AUMID = `KOTU.Instance.{n}` — n은 **A100 트레이 슬롯 번호 재사용**(`TrayIcon.Slot`
  신설 — 창 생성 단조 시퀀스·수명 불변. 표시 번호(A2)는 재배정되어 그룹이 재편되므로 부적격).
  신규 `src/KOTU.App/Integration/TaskbarIdentity.cs`(저장소 최초 COM 인터페이스 interop:
  `SHGetPropertyStoreForWindow` + IPropertyStore + 수동 PROPVARIANT(VT_LPWSTR) + PropVariantClear +
  FinalReleaseComObject)가 MainWindow 생성자(Activate 전)에서 1회 지정. 실패는 전부 조용히
  무시(A100 스타일 — 공유 AUMID로 후퇴, 앱 동작 무영향). **그룹 분리 실효는 실기기에서만 판별
  가능**(실기기 확인 포인트 — HANDOVER). 수용된 트레이드오프(코드 대응 없음): 작업표시줄
  고정(pin)·점프리스트가 인스턴스 단위로 갈라지고(고정 버튼은 다음 실행의 같은 슬롯 창하고만
  재결합), Velopack 바로가기 AUMID와 달라 시작 메뉴 그룹 연결이 느슨해질 수 있음.
  ② 창(태스크바·Alt+Tab) **32px 아이콘 하단에 모듈 3자 표기** — 매핑은 트레이 유휴 표기
  `IdleTrayLabel`(A54: IMG/VID/AUD/DOC/ARC/ALL/INF)을 단일 출처로 재사용(중복 표 없음).
  렌더는 링 상수 파생 자리(링 두께 안쪽·안쪽 폭 절반 줄)에 TrayStatusIcon과 같은 다크 판 +
  모듈 액센트 Lighten 0.30 글자(폰트 산정도 A54 식 — 32px 기준 약 9.6px). 정보(H/W) 모듈은
  링 없이 표기만 얹는다(`InstanceIcon.GetComposed`의 ring이 nullable로, label 인자 신설).
  **16px 소형(타이틀바) 아이콘은 글자 생략** — 같은 산정으로 폰트가 약 4.8px가 되어 A54 실증
  하한(5px) 아래(뭉개짐 방지, 구현 시 결정 준수). 캐시 키에 3자 코드 명시 추가(무링 2종
  INF·설정 구분 — 링 색 프록시 금지). 설정·빈 셸은 글자 없음. 트레이 아이콘(A54·A101) 무수정.
- ※ A55(**창 크기+위치 저장/복원** — 마지막 닫힌 창 우선·최대화 저장·화면 밖 보정 클램프·`TrackNormalBounds`, v0.95.0) 완료 — 결번.
  ※ 다중 인스턴스 계단식 오프셋은 A89(v0.114.0)가 폐기(그대로 승계). (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A89(**새 창 크기·위치 = 마지막에 닫은 창 그대로 승계** — A55 오프셋 제거, 살아 있는 창과 겹쳐도 그대로(사용자 확정), v0.114.0) 완료 — 결번. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A56(**제목표시줄 정리 + 인스턴스 번호 접두**, v0.87.0) 완료 — 결번. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
## 2. 내장 탐색기 · 오버레이

- ※ A5(리스트 정렬 이름/크기/수정일 — 그리드도 동일 순서 적용, explorer.sort 저장, v0.69.0
  ※ **만든 날짜 키는 A117(v0.136.0)이 추가** — 저장 값 `"created"`)·
  A6(리스트 길이 컬럼 — 미디어만, System.Media.Duration 지연 로드+캐시, v0.70.0)·
  A7(파일 종류 필터 — 확장자별 체크 드롭다운+Show all, 세션 한정, 활성 시 아이콘 강조, v0.71.0)·
  A8(현재 경로 우측 정렬 표시+전체 경로 툴팁, v0.72.0) 완료 — 결번.
- [확인] 오버레이 반투명(아크릴, v0.31.0) — 실기기 확인 대기. 비디오 표면(스왑체인) 위에서 안 비칠 가능성.
- ※ A33(오버레이 전용 약틴트 아크릴 OverlayAcrylicBrush — TintOpacity 0.1·LuminosityOpacity 0.4,
  좌측 정보 오버레이 30% 균등 폭(InfoOverlayRoot 3*/7*), v0.73.0) 완료 — 결번.
- [확인] Alt 좌측(A57에서 스왑) 리스트가 줌(Zoom) 화면공유에서 안 보임 — 고정 상태로 우회 가능한지 확인
  (v0.32.0의 2연타 고정은 A58(v0.100.0)에서 대체됨: 이제 **2초 홀드 = 반투명 고정 / 2연타 = 불투명 밀어내기**).
- [보류 A4] 자체 탐색기 유지 확정. 파일 읽기/쓰기 권한(보호 폴더 등) 문제 조사 후 OS 탐색기 임베드 재검토.
- ※ A57(**좌/우 30% 오버레이 전면 개편**, v0.98.0 ①②③ + v0.99.0 ④) 완료 — 결번. 확정·구현 내역:
  - ① 좌우 스왑 — **좌 = 파일 리스트(Alt) / 우 = 정보(Ctrl)**로 배치 교체 (v0.98.0).
  - ② 공통 모듈화 — `MainWindow.xaml(.cs)`의 `AltOverlayRoot`·`InfoOverlayRoot`를
    `src/KOTU.App/Overlays/`의 공용 컨트롤 `FileListOverlay`·`ContentInfoOverlay`로 추출,
    셸이 모듈 컨텍스트(SupportedExtensions 필터·IContentInfoProvider 정보)를 주입
    (적용: 파일 모듈만 — 정보(H/W)·설정 모듈 제외. v0.98.0 — 입력 처리(A32)는 셸 유지·A58에서
    대체 예정, OverlayAcrylicBrush(A33)는 App.xaml 앱 리소스로 이동).
    ※ A119(v0.145.0)가 정보 모듈 편입(ISidePanelProvider·SidePanelHost) — "정보 모듈 제외"는 설정만 남음.
  - ③ 모듈별 필터 — 모듈 필터(`IModule.SupportedExtensions`)가 기본값, A7 드롭다운은
    그 안에서 추가 좁히기 (v0.98.0).
  - ④ 좌측 리스트 오버레이 상단 25% = 디스크 계층 트리 (v0.99.0) — `FileListOverlay` 내부 세로
    1*/3* 고정 분할(스플리터 없음), WinUI `TreeView` 폴더 전용(드라이브 노드 = 이름+종류 간단 표기),
    노드 펼침 시점 지연 로드(전체 스캔 없음, 숨김/시스템 제외·접근 불가 폴더 조용히 생략),
    Show() 시 현재 폴더까지 자동 펼침·선택·스크롤, 트리 선택 → `ExplorerPane.NavigateTo`
    재사용으로 하단 75% 리스트 이동(A5 정렬·A7 필터·A8 경로 표시 유지).
- ※ A58(**오버레이 입력 4상태 머신** — 홀드=반투명 덮기 / 2초=반투명 고정 / 2연타=불투명 도크(3:4:3)·홀드 취소 안전장치·`SetDockedState` API, v0.100.0) 완료 — 결번.
  ※ 키는 A86(v0.121.0)이 Z/X로 대체 — 현행 사양 = `docs/A86-keymap.md`. 전이·안전장치는 그대로 유효. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A81(**오버레이 기본 상태 = 진입 경로별** — 파일 인자 시작=없음 / 모듈만 실행=좌·우 불투명 도크(창 생성 1회 주입), 이후 사용자 상태 유지·세션 간 기억 안 함, 빈 모듈도 오버레이 컨텍스트 인정, v0.101.0) 완료 — 결번. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
  ⚠️ **모듈 전환 시의 "사용자 상태 유지"는 A109(v0.136.0)가 대체** — 전환마다 좌·우 사이드바 기본이 다시 적용된다. 파일 인자 시작=무사이드바는 그대로.
- ※ A85(**자체 탐색기 더블클릭 열기 불량 수정**, v0.127.0) 완료 — 결번. 확정·구현 내역:
  - **원인은 라우팅이 아니라 더블클릭 판정이었다**: 중앙 `ThumbnailExplorer`·좌 `ExplorerPane`이
    "500ms 내 `ItemClick` 2회"로 더블클릭을 판정했는데, 실기기 입력 스택은 두 번째 클릭을
    **더블탭 제스처로 소비**해 두 번째 ItemClick이 오지 않는다 — 열기 요청이 셸(OpenFileRouted)에
    **도달하지 못한 채 침묵**(예외 없음). 등재 당시의 "같은-모듈 조기 반환" 가설은 코드 정독으로
    **기각**(`ShowModule`은 같은 모듈에도 항상 새 뷰 교체·조기 반환 없음). "이미지 모듈에서만"으로
    보인 것은 그 표면이 담당 확장자만 표시해서다. 우클릭 새 인스턴스는 판정을 안 타 성립했다.
  - 수정: 두 탐색기의 항목 컨테이너에 **`DoubleTapped` 열기 경로 신설**(압축 모듈 ArchiveView와
    같은 배선 — 그 표면은 원래 증상이 없었다) + 공통 종착점 `Activate`(폴더 진입/파일 열기,
    Shift=새 창·A24). ItemClick 쌍 판정은 보조로 유지하되 **같은 경로 연속 발화를 500ms 창에서
    1회로 억제**(두 경로가 다 발화하는 환경에서 A24 on일 때 창 2개 방지). F2 편집 상자 안
    더블클릭은 텍스트 선택에 양보.
  - 부수 수리: **좌 리스트 단발 Enter 열기 구멍** — 셸 `OnShellEnter`는 "리스트 포커스 = 선택 항목
    열기 우선"으로 양보하는데(A86 keymap) `ExplorerPane`에 표면 핸들러가 없어 무동작이었다 →
    `OnSurfaceKeyDown`에 Enter 추가(선택 파일=열기/폴더=진입, 선택 없으면 셸로 통과).
  - 무변경: 우클릭 새 인스턴스·인포/창 드랍·파일 인자·A24 분기·미지원 안내. 실패 표시 UI는
    만들지 않음(열기 성공이 곧 해결). 실기기 확인 포인트는 HANDOVER v0.127.0 절.
- ※ A86(**오버레이 동작·단축키 재정의** — 좌 Z/우 X·열림 상태 1회=닫기·Enter 일괄 토글(원 기능 우선)·경계 버튼·셸 6상태(S1~S4) 판정, v0.121.0) 완료 — 결번.
  **사양 원본 = `docs/A86-keymap.md`**(2026-08-13 확정 — A87 단축키 장의 원본이기도 하다). (상세 → docs/REQUIREMENTS-ARCHIVE.md)
  ※ 좌/우 키는 A107(v0.134.0, Alt+Z/X)을 거쳐 **A118(v0.144.0)이 F1/F2로 대체** — 전이·Enter·경계 버튼은 그대로.
- ※ A63(탐색기 미리보기 아이콘 기준 크기) — **2026-08-13 지시로 A93에 흡수, 결번.**
  종전 "아이콘 크기 고정·열 수 가변(1600×900 기준 5개)"은 **"열 수 고정(오버레이 열림 4 / 닫힘 8)·크기 가변"으로
  대체**됐다(부록 B 49). 세부는 A93 참조 — **v0.120.0에서 A93과 함께 구현 완료**(ThumbnailExplorer).
- ※ A90(**'오픈 파일' 버튼 = S4 탐색 모드** — 하단 바 버튼(☰ 옆), 없는 구획만 반투명 추가·중앙 전용 썸네일, Esc/재누름=진입 전 복귀·콘텐츠 바뀌면 자동 종료, S1에서는 강조만, v0.122.0) 완료 — 결번.
  사양 = `docs/A86-keymap.md` S4 열. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A91(**좌 오버레이 주소·필터 줄을 패널 최상단으로** — `DetachPathBar`, 행 구성 Auto/1*/3*/Auto, v0.115.0) 완료 — 결번. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A92(**도크·고정 안내 문구 2.5초 표시 후 페이드아웃**, v0.115.0 — 문구는 A86에서 Z/X 기준 갱신) 완료 — 결번. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A93(**자체 탐색기 3구획 기본 UI + 중앙 썸네일 뷰 신설**, v0.120.0) 완료 — 결번. 확정·구현 내역:
  - S1(콘텐츠 없음·모듈만 실행) 기본 UI = **좌(트리&리스트) / 중(썸네일 뷰) / 우(인포)** 3구획
    **25:50:25** 불투명 도크. 도크 폭은 상태별(`ApplyOverlayStates`): S1 = 25% / 콘텐츠 상태 = 종전
    30%(A57) — 오버레이 내부 분할도 `SetPanelPercent`로 같은 값(도크 컬럼과 픽셀 정렬 유지).
    파일 인자 시작(S2)·반투명 덮기 동작은 변경 없음(A81·A58 그대로).
  - 중앙 = 신규 `Controls/ThumbnailExplorer`(GridView — ItemsRepeater는 저장소 선례가 없어 배제):
    열 수 **좌우 도크 둘 다 열림 4 / 하나라도 닫힘 8**(A63 대체), 타일 한 변 = floor(실폭/열수) —
    SizeChanged마다 ItemsWrapGrid ItemWidth/ItemHeight 재계산. 타일 = 이미지 확장자
    (ImageFolderNavigator 목록 재사용)는 BitmapImage+DecodePixelWidth 256 실제 미리보기(디코드
    실패 시 확장자 타일 폴백) / 폴더 = E8B7 글리프 / 그 외 = 담당 모듈 액센트 색 + 확장자 대문자.
    캡션 = 파일명 1줄 말줄임.
  - **좌 리스트와 폴더·필터(A7)·정렬(A5) 상태 공유** — 좌 도크 리스트(ExplorerPane)가 단일 원본:
    표시 목록이 `ViewChanged`로 흘러 썸네일 뷰가 같은 목록을 그리고, 썸네일 쪽 폴더 더블클릭은
    `FileListOverlay.NavigateList`(Show에서 분리 — 표시와 무관하게 리스트만 항해)로 되돌아온다.
    파일 열기는 기존 라우팅(OpenFileRouted·A24) 재사용. 구 중앙 ExplorerPane(_emptyExplorer)은 제거 —
    A81의 "좌 도크 열림 시 중앙 탐색기 숨김"도 **대체**(중앙이 리스트가 아니라 중복으로 안 보인다).
  - **드랍 규칙**: 우(인포) = 그 파일 열기(콘텐츠 없으면 OpenFile의 라우터 A59가 모듈 전환 후 열기) /
    콘텐츠 열림 상태의 콘텐츠 영역 = 같은 종류는 그 자리, 다른 종류는 담당 모듈 새 인스턴스
    (`OpenFileInNewWindow` 재사용) / **좌·중(탐색기 영역) = 무동작**(이동/복사는 A94 몫 —
    AcceptedOperation=None으로 소비만 해서 창 전체 "열기" 폴백에 안 넘어가게).
    → **v0.124.0(A94 1차)에서 좌·중이 실동작(이동/복사)으로 전환** — 우·콘텐츠 영역 규칙은 무변경.
  - 구현 시 결정: 등재 당시 "중앙에 Drop here 문구만 있다"는 **착오**였다(실제 중앙 = v0.25.0
    ExplorerPane, "Drop here" 문구는 코드에 없음). 취지대로 인포 플레이스홀더("No file open")에
    "Drop a file here to open it"을 더해 **인포에 아무것도 표시 중이 아닐 때만** 보이게 구현.
  - 키 체계(Z/X·Enter)는 A86 몫 — 현행 Alt/Shift(A58)·A92 안내 문구 그대로 두었다
    (→ **v0.121.0 A86이 반영 완료**).
- ※ A94(**자체 탐색기 동작 = 윈도우 탐색기 동등** — v0.124.0~v0.153.0, **6차 완주**) 완료 — 결번.
  (2026-08-13 등재, 부록 B 49. 좌(트리&리스트)·중(썸네일 뷰) 두 표면을 탐색기와 최대한 같게.)
  - **대조표 = `docs/A94-matrix.md`가 영구 기록이다** — 동작별 지원 여부·차수·구현 메모가
    전부 거기 있다. 아래는 차수 요지만 남긴다(다이어트 규칙).
  - **1차 v0.124.0**: 다중선택(Extended — Ctrl 토글·Shift 범위·Ctrl+A) / 드래그 아웃(StorageItems) /
    좌·중 드랍 이동·복사(같은 볼륨 이동·다른 볼륨 복사·Ctrl/Shift 강제) / 클립보드 Ctrl+C·X·V.
    공용 로직 = `src/KOTU.App/ExplorerFileOps.cs`.
  - **2차 v0.125.0**: F2 인라인 이름변경(`ExplorerRenameBox` — 거부+원복 규칙) / 새 폴더 Ctrl+Shift+N /
    Del = 휴지통 삭제 / 항목 우클릭 메뉴에 Rename·Delete.
  - **3차 v0.150.0**: 이름 충돌 대화상자(`ExplorerConflictDialog` — Replace/Skip/Keep both + "남은 충돌 전부" 축약,
    Esc = 취소) / 대량 조작 진행 문구("Copying 3 of 12...").
  - **4차 v0.151.0**: Shift+Del 영구 삭제(확인창) / 잘라내기 원본 반투명 표시 / 접근 거부(UAC) 구분 안내 +
    관리자 재시작 제안(runas 흐름 공용화 = `AdminRelaunch` + Core 훅, 대화상자 게이트 = `ExplorerDialogs`).
  - **5차 v0.152.0**: 폴더 감시 자동 갱신(FileSystemWatcher + 디바운스 400ms → 전체 재스캔 1회,
    이름변경 편집 중 보류, 폴더 소실 = 가장 가까운 상위로 이동).
  - **6차 v0.153.0 (마지막)**: 빈 영역 우클릭 메뉴(New folder / Paste / Refresh — 두 표면 공통,
    항목 메뉴와의 이중 발화는 ContextFlyout 규칙이 차단) / 항목 메뉴에 Cut·Copy 추가 +
    **폴더 항목에 Paste(대상 = 그 폴더)** / **다중 선택 일괄 열기**(더블클릭·Enter — 첫 파일은 재사용
    규칙(A24), 나머지는 새 인스턴스. 폴더 제외, 상한 10개 + 안내 문구. 진입점 4곳 공통) /
    **OS 탐색기 클립보드 상호운용**("Preferred DropEffect" 쓰기 = 잘라내기 2·복사 5 /
    읽기 = 있으면 그것이 우선, 없거나 못 읽으면 종전 RequestedOperation 판정).
  - **잔여 = 미완이 아니라 스코프 아웃(2026-08-16 확정)**: ① OS 셸 확장 컨텍스트 메뉴 ·
    ② Alt 드래그 = 바로가기 만들기 — 둘 다 COM 대형(필요해지면 **별도 항목으로 새로 등재**) /
    ③ 드래그 중 항목 수 배지 — WinUI DragUI 제약 대비 가치 낮음 / ④ UAC 승격 뒤 자동 재시도 —
    승격 헬퍼 필요, "Restart as admin" 제안으로 대체 / ⑤ 별도 진행률 대화상자 — 문구 채널로 대체.
  - A4(탐색기 파일 권한 조사)는 이 항목과 별개로 부록 A에 남아 있다(후순위).
- ※ A108(**'사이드바 vs 오버레이' 개념 확정 + 표시 정리**, v0.135.0) 완료 — 결번. 확정·구현 내역:
  - **용어 확정**: 사이드바 = 불투명·메인을 줄이며 옆에 섬(구 "불투명 도크/밀어내기") /
    오버레이 = 반투명·메인 위에 덮임(홀드/2초 고정). 소스 주석·UI 문구·`docs/A86-keymap.md`(용어 절
    신설 + 표기 정리) 일괄 반영. **식별자는 rename 없이 유지 + 주석 병기**(`OpaqueDocked` 등 —
    구현 시 결정: diff 폭발·회귀 위험 대비 이득 없음). UI 문구는 `OverlayHints`(A107 단일 출처)에서
    "Docked -" → **"Sidebar -"**, "Pinned"는 유지(오버레이 고정). 경계 버튼 툴팁도 sidebar 표기로.
  - **틴트 약화(A33 개정)**: `OverlayAcrylicBrush`(App.xaml — 좌우 패널·S4 중앙 공유 단일 브러시)의
    불투명도를 약 60% 수준으로 — TintOpacity 0.1→0.06 · TintLuminosityOpacity 0.4→0.24 ·
    폴백 알파 B3→6B, **다크/라이트 딕셔너리 양쪽 동일 적용**. 실기기 미세조정 단일 지점으로 주석 명기.
    사이드바(불투명 배경)는 무변경.
  - **안내 문구 위치**: 패널 상/하단(A92 현행) → **경계 버튼의 화면 안쪽 옆**(좌 패널 = 버튼 오른쪽 /
    우 패널 = 버튼 왼쪽, 세로 중앙 동일)으로 이동 — XAML 배치만 이동(PinnedText를 패널 밖
    RestColumn으로), 타이밍(2.5초+300ms 페이드)·문구 장치는 무변경. 버튼이 근접 표시로 숨어 있어도
    문구만 그 자리에 표시(강제 표시 없음 — 사용자 확정). 좌 패널의 A94 실패 안내(같은 요소 재사용)도
    같은 자리에 뜬다.
- ※ A109(**모듈 전환 시 기본 화면 = 좌·우 사이드바**, v0.136.0) 완료 — 결번.
  같은 인스턴스에서 다른 모듈로 넘어가면(Alt+숫자·시작 메뉴) 좌·우 **사이드바**(불투명)가 뜬 기본
  상태로 표시 — 구현은 `ShowModule(..., defaultSidebars: true)`가 뷰 교체를 마친 뒤 A81의
  `SetDockedState(true, true)`를 재사용한다(신규 상태 없음). 파일 여는 경로(OpenFile·OpenVerb)는
  false라 **파일 인자 직접 열기 = 무사이드바(A81) 유지**. 같은 모듈 재선택(`OpenModuleById` no-op
  가드)은 무동작 그대로. 순서: `SetContentState`(S4 자동 종료·A90) **뒤**에 사이드바 기본이 얹힌다.
  ※ A119(v0.145.0)가 정보 모듈 편입 — 같은 경로로 정보 모듈도 진입 기본 = 양쪽 사이드바가 실표시된다.
- ※ A110(**[확인] 다중 인스턴스 오버레이/사이드바 상태·키 꼬임 검증** — 코드 감사분, v0.139.0 시점) 완료 — 결번.
  - **감사 결과: 실버그 0건·코드 변경 0.** 상태 소유 감사표 30행 전수 — 오버레이/사이드바/셸 키
    도메인의 상태는 전부 창별(A58 4상태·2연타·`_lastBatchStates`·S4 `_s4Restore`·A107 `_altComboUsed`
    — Deactivated에서 리셋), 전역은 전부 의도됨(A70 하드웨어 1벌·A55/A89 창 크기·`explorer.sort`(A5)·
    `lastFolder.*`·아이콘 캐시 — 캐시 키에 렌더 입력 전부 포함 확인).
  - **스레드 모델 확정: 모든 창 = 단일 UI 스레드**(창 생성 = `WindowManager.Create` 1지점, 호출자 전수
    UI 스레드 — "UI 스레드 전용" 주석 static들의 전제 성립).
  - **오버레이/사이드바 열림·S4 상태는 설정에 저장 안 함 = 현행 사양 확인**(저장 키 전수 grep).
  - 포커스 경계 시뮬레이션 3종(Alt 홀드 중 창 전환·홀드 2초 중 비활성화·경계 버튼 중 포커스 상실)
    전부 꼬임 없음 — 비활성 창 경계 버튼 표시 잔존만 코스메틱(방어선: Deactivated에 `HideEdgeButtons()`
    한 줄 — 실기기에서 거슬리면 등재). 실기기 확인 포인트 4건 = HANDOVER §4.
  - ※ 감사표의 좌/우 키 서술(Alt+Z/X)은 당시 기준 — **A118(v0.144.0)이 F1/F2로 대체**(상태 소유 구조는 무변경).
- ※ A118(**오버레이/사이드바 키 = F1(좌)/F2(우) 전면 교체 — Alt+Z/X 폐지**, v0.144.0) 완료 — 결번.
  A119(같은 키 적용 — 전 모듈 공통, 부록 B 59)가 참조하므로 요지를 본문에 남긴다.
  - 좌 = **F1** / 우 = **F2** 단독 키(Alt+Z/X 폐지 — 병행 없음). **전이는 기존 그대로**(홀드 =
    오버레이 · 2초 홀드 = 핀 · 2연타 = 사이드바 · 열림 1회 = 닫기 · F1+F2 동시 = 구 Z+X 규칙 승계 ·
    S4 무동작 · 오버레이 컨텍스트 없는 화면(설정·H/W) 무소비 — A119 편입 전까지 현행 유지).
    ※ A119(v0.145.0)가 정보 모듈 편입 — H/W도 소비·동작하며, 무소비로 남는 화면 = 설정·빈 셸·미지원 안내.
  - 키 정본 = `MainWindow.LeftPanelKey/RightPanelKey` 상수 단일 지점(시험적 변경 — 되돌리기 지점).
    분기 위치는 텍스트 입력 가드보다 **앞**(F키는 문자 비생성 — 문서 에디터 편집 중에도 동작).
  - **탐색기 표면 F2 = 이름변경(A94)이 원 기능 우선**: 표면이 선택 항목으로 소비(Handled)하면
    셸이 양보(A86 Enter와 동일 규칙 — 선택 없으면 표면이 안 삼켜 패널 키로 동작).
  - Alt 부기(`_altComboUsed`·단독 up 조건 소비)는 Alt+\`·Alt+0~7용으로 존치 — Z/X 게이트만 제거.
  - 연쇄 갱신 완료: `OverlayHints`("F1"/"F2") · `docs/A86-keymap.md` 개정 · A34 키 맵 표 ·
    `docs/USER-GUIDE.md`·`site/guide.html`(As of v0.144.0).
- ※ A116(**[버그] 사이드바 25:50:25 비율 — 원인 규명·수정**, v0.135.0) 완료 — 결번.
  - **원인**: 리사이즈 재계산 문제가 아니었다 — 도크 컬럼·`SetPanelPercent`는 전부 star(GridLength
    Star)라 리사이즈 추종이 원래 성립하고, 픽셀로 굳는 지점은 코드에 없다(전수 추적 결과).
    실원인 = 도크 폭이 **상태별 2값**(S1 = 25% / 콘텐츠·S4 = 30% — A57 3:4:3 유산)이라, 같은
    3구획 화면이 파일 열기(S3B)나 S4('오픈 파일') 진입에서 **소리 없이 30:40:30으로 전환**됐다.
    울트라와이드 최대화 시점에 두꺼워진 사이드바가 눈에 띄어 "리사이즈가 원인"으로 관측된 것.
    관측치 30:40:30이 코드의 30% 분기 값과 정확히 일치하는 것이 근거.
  - **수정**: 도크 폭을 전 상태 공통 상수 `SidebarPercent = 25`로 단일화(`MainWindow.xaml.cs`) —
    도크 컬럼·패널 내부 분할(`SetPanelPercent`)·S4 스페이서·경계 버튼 x가 전부 이 상수를 사용.
    콘텐츠 상태의 종전 30%(A57·A93 "콘텐츠 상태 = 30%" 명기)는 **이 항목이 25%로 대체**
    (사용자 기대 "사이드바 = 25:50:25"가 우선). 어느 상태든 사이드바 연 채 리사이즈 시 25:50:25(양쪽)
    /25:75(한쪽) 유지.
  - ※ A119(v0.145.0)가 정보 모듈 편입 — 정보 모듈 패널 호스트(SidePanelHost)도 같은 25% 상수를 공유한다.
- ※ A117(**탐색기 정렬에 '만든 날짜(date created)' 추가**, v0.136.0) 완료 — 결번.
  정렬 키(A5)에 `SortKey.Created` 추가 — date modified와 **같은 방식으로 병존**(날짜 범위 필터 아님):
  메뉴 `Date created`(ExplorerPane.xaml) · 비교자(폴더도 파일도 최신부터, 동률 이름순) ·
  저장 키 `explorer.sort` = `"created"`(모르는 값은 이름순 폴백) · 목록 원본
  `ExplorerListing.Entry.Created`(DirectoryInfo/FileInfo.CreationTime). 좌 리스트가 폴더 상태 단일
  원본(A93)이라 중앙 썸네일은 `ViewChanged`로 자동 추종 — 별도 배선 없음.
- ※ A133(**힌트 문구에 다크 반투명 판** — 경계 버튼 옆 안내가 배경 콘텐츠 글씨와 겹쳐 안 읽히던
  문제, v0.155.0) 완료 — 결번. A92 타이밍 장치 **세 벌 전부**에 같은 판을 씌웠다.
  - **판 규격(세 벌 공통)**: `Background #CC202020` · `CornerRadius 4` · `Padding 10,6` ·
    글씨 `White` 고정(테마 무관) · 요소 `Opacity 1` — A12 비디오 시작 칩(`VideoPlayerView`의
    `StartOverlay`)과 같은 값. **`HintOpacity` 0.6 → 1로 개정**(등재 사양에 없던 판단): 판이 0.6이면
    배경이 비쳐 판을 씌운 의미가 없다 — 칩처럼 요소는 불투명, 반투명은 배경 브러시 알파(CC) 담당.
    세 파일의 상수와 XAML `Opacity` 값이 항상 함께 움직여야 한다.
  - **적용 지점**: `FileListOverlay.xaml`·`ContentInfoOverlay.xaml`에 `PinnedText`를 감싼
    `PinnedPlate`(Border) 신설 + `SidePanelHost`(A119 재현분)에 `_pinnedPlate` 필드 신설
    (Child·정렬·Margin·컬럼 연결은 `Initialize` — 필드 초기화자는 다른 인스턴스 필드를 못 본다).
    표시·숨김·페이드 대상이 문구 → 판으로 승격(각 벌 5곳: ShowHint 2·HideHint 1·FadeOutHint 2 —
    `Storyboard.SetTarget`도 판). 문구 대입만 `PinnedText`가 받는다(식별자 보존).
  - **위치·정렬·타이밍 무변경**(2.5초 + 300ms 페이드·재시작 규칙·Margin 14). 단 Margin 14는 이제
    판의 바깥 모서리 기준이라 글자는 칩 패딩(10)만큼 안쪽에 앉는다.
  - A94 실패 안내(`ShowTransientNotice`)가 같은 요소 채널을 재사용하므로 그 문구도 같은 판을 얻는다
    — 가독성이 목적이라 **의도된 결과**(주석 명기).
  - **실기기 확인 포인트**: ① 판과 문구가 한 덩어리로 300ms 페이드되는지(Opacity 애니메이션 대상이
    TextBlock → Border로 바뀐 경로 — CI 검증 불가 부류) ② 라이트 테마에서 다크 판 + 흰 글씨 대비
    ③ 세 벌(좌 파일 리스트 · 우 정보 · H/W 패널 호스트) 모양이 같은지.
  - 되돌리기(장식만 조용히 오프): Border를 걷어내고 코드의 판 참조 5곳을 문구 요소로 되돌리면
    A92 원형 — 타이밍 장치는 손대지 않았다(각 파일 주석에 절차 명기).

- ※ A129(**S4/오버레이 반투명이 비디오 모듈에서 불투명으로 표시 — 원인 판별 + 전 모듈 검수·통일**,
  v0.156.0) 완료 — 결번. 통일 기준(사용자 확정) = **반투명이 정답**(A90·A108).
  - **판별 = ⓑ(렌더 한계) — ⓐ(상태 버그) 기각**: S4 진입(`EnterOpenFileBrowsing`)·상태 분배
    (`ApplyOverlayStates`)·브러시 대입(4표면 `SetState`류) 전 경로에 모듈 분기가 없다 — 이미지와
    비디오가 **같은 셸 경로·같은 `OverlayAcrylicBrush`**를 받는다(대입 지점 전수: FileListOverlay·
    ContentInfoOverlay·SidePanelHost `SetState` + ThumbnailExplorer `UseTranslucentBackground`).
    남는 차이는 중앙 렌더뿐: 비디오는 libvlc VideoView의 **D3D 스왑체인**(별도 합성 비주얼)이고,
    WinUI 3 인앱 `AcrylicBrush`는 **같은 XAML 렌더 결과만 배경 샘플**하므로 스왑체인 프레임이
    샘플에 없다. 아크릴 출력 = 그 샘플의 **불투명 합성**이라 비디오 위에서 "불투명 판"이 된다
    (이미지는 XAML `Image`가 그려 샘플에 들어가므로 정상 반투명 — 실기기 관측과 정합).
    HANDOVER §4의 v0.31.0 묵은 확인("아크릴이 비디오 스왑체인 위에서 비치는지")의 답 = **안 비친다**.
  - **처방(v0.156.0)**: 스왑체인 위 한정 **반투명 단색 폴백** — 진짜 알파 합성이라 투과 성립,
    블러는 포기(주석 명기). ① `App.xaml`에 `OverlayTranslucentFallbackBrush` 다크/라이트 쌍 신설
    (`#6B202020`/`#6BF9F9F9` — 아크릴 FallbackColor(A108 알파 6B)와 동일 값이 상수 출처) ②
    브러시 선택 단일 지점 **`Overlays/OverlayBackdrop.Pick(docked, overSwapChain)` 신설**(분산 대입
    금지 — 반투명 브러시 키 문자열은 이 파일과 App.xaml에만) ③ 4표면 `SetState`/
    `UseTranslucentBackground`에 `overSwapChain` 인자 추가, 값은 셸 `IsSwapChainContent` 하나를
    `ApplyOverlayStates`(상태 변경 단일 종착점)가 매번 일괄 재푸시 — **모듈 전환·파일 닫기 시
    아크릴 원복이 구조적으로 누락 불가**(S4 중앙 인스턴스 재사용분도 S4 절에서 매번 재푸시).
  - **판별 신호(기존 신호 재사용 — 새 계약 없음)**: `_currentFilePath != null` + (모듈 ID가
    비디오/오디오이거나, All Readable(A59)이면 `_router.Resolve(파일)`의 담당 모듈이 비디오/오디오 —
    S4 타일 액센트 `ModuleIdForFile`과 같은 관용구. A59 자식 선택과 라우터는 같은 우선순위 규칙).
    파일 없는(빈 모듈) 상태는 중앙이 불투명 ExplorerHost(A93)라 아크릴이 정상 — 신호 거짓이 옳다.
  - **검수 중 발견 — 오디오도 스왑체인**(등재문의 "스왑체인 = 비디오" 전제와 코드 불일치 →
    코드를 따름): 오디오 모듈 중앙(`AudioSurface`)도 같은 libvlc VideoView에 **파형 시각화를 상시**
    로 그린다. 아크릴이 파형을 못 비추는 같은 원인(뒤가 검정 XAML 배경이라 티가 덜 났을 뿐) —
    같은 처방에 편입(`IsSwapChainModule` = video·audio 한 곳). 비디오 한정으로 좁히려면 그 한 줄만 고치면 된다.
  - **전 모듈 검수 대조표**(파일 열림 기준. 반투명 = TranslucentOver(홀드·고정) 브러시, S4 중앙 포함 /
    사이드바 = OpaqueDocked. 아크릴 = OverlayAcrylicBrush, 폴백 = OverlayTranslucentFallbackBrush,
    불투명 = SolidBackgroundFillColorBase — 전부 OverlayBackdrop.Pick 산출):

    | 모듈 | 중앙 렌더 | S4 중앙 | 좌 오버레이 | 우 오버레이 | 좌/우 사이드바 |
    |---|---|---|---|---|---|
    | 이미지 | XAML Image | 아크릴 ✓ | 아크릴 ✓ | 아크릴 ✓ | 불투명 ✓ |
    | 비디오 | 스왑체인 | **폴백**(이번 수리) | **폴백** | **폴백** | 불투명 ✓ |
    | 오디오 | 스왑체인(파형) | **폴백**(검수 편입) | **폴백** | **폴백** | 불투명 ✓ |
    | 문서 | XAML(TextBox·PdfPane) | 아크릴 ✓ | 아크릴 ✓ | 아크릴 ✓ | 불투명 ✓ |
    | 압축 | XAML 리스트 | 아크릴 ✓ | 아크릴 ✓ | 아크릴 ✓ | 불투명 ✓ |
    | H/W | XAML 그래프(파일 없음) | S4 없음(파일 가드) | 아크릴 ✓(SidePanelHost) | 아크릴 ✓(〃) | 불투명 ✓(〃) |
    | All Readable | 자식 뷰 따름 | 자식 비디오/오디오 = 폴백, 그 외 아크릴 | 〃 | 〃 | 불투명 ✓ |

    그 밖의 편차 없음(빈 모듈 상태는 전 모듈 아크릴 — 중앙이 불투명 XAML이라 정상 샘플).
  - **실기기 확인 포인트(ⓑ는 코드로 최종 검증 불가 — 합성기 동작)**: ① 비디오 재생 중 S4/오버레이에
    영상이 비치는지(블러 없는 반투명이 정상) ② 오디오 재생 중 파형이 비치는지(편입 판단의 실검증 —
    종전에는 안 비쳤을 것) ③ 비디오→이미지 전환·파일 닫기 후 아크릴(블러) 원복, S4 재진입 중앙 포함
    ④ 다크/라이트 각각에서 폴백 농도가 아크릴과 이질적이지 않은지(6B 쌍 — 조정은 App.xaml 한 곳)
    ⑤ HANDOVER §4 v0.31.0 묵은 확인 항목은 이 판정으로 해소(다음 HANDOVER 갱신 때 정리).
- [미반영 A134 · Opus] **좌 패널 리스트 폴더 진입 시 상단 트리 미동기** (2026-08-16 실기기 보고:
  리스트에서 폴더로 들어가도 위 트리(A57 ④ 25% 디스크 트리)의 선택·펼침이 안 따라옴).
  - **원인(등재 시 특정 — 배선 누락)**: 트리 동기 `ExpandToFolderAsync`(FileListOverlay — 경로
    지연 펼침·선택·스크롤·`_expandSeq` 재진입 방어까지 완비)가 **Show() 시에만** 호출된다(주석에
    명기된 당시 사양). 리스트 항해는 `_list.ViewChanged`로 중앙 썸네일(A93)에는 흐르지만 트리
    동기 호출이 없다 — 구현 완비·배선 1곳 누락.
  - 사양: `ViewChanged` 구독(또는 같은 지점)에서 `ExpandToFolderAsync(folder)` 호출. 현재 트리
    선택 경로와 같으면 조기 반환(불필요 재펼침 방지 — 감시(A94 5차) 재스캔의 같은 폴더 재통지
    에서도 조용).
  - 함정(선지정): ① **트리 클릭 → 리스트 항해 → 재동기 루프** — 트리 쪽 항해 트리거가
    SelectionChanged 기반이면 `SelectedNode` 재대입이 이벤트를 재발화할 수 있다(ItemInvoked
    기반이면 무해). 코드 실상을 확인하고 같은 경로 조기 반환으로 루프 원천 차단. ② 비동기 펼침
    중 연속 항해는 기존 `_expandSeq`가 방어(재사용). ③ UNC 등 트리 밖 볼륨은 기존 "드라이브만
    안다" 조기 반환 그대로.
## 3. 이미지 모듈

- ※ A9(하단 바 정보 확충 — 파일명 옆 용량·종류(확장자·비트뎁스)·EXIF 요약(촬영일·카메라·노출) 인라인 표시, (상세 → docs/REQUIREMENTS-ARCHIVE.md)
## 4. 영상 모듈

- ※ A10(오디오 모듈 분리 ZP-audio — 표시명 Music·단축키 3번 삽입(1이미지 2영상 3오디오 4문서 5압축 6HW)· (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- [미반영 A11 · Fable] 재생 목록 루프 옵션: 목록 루프(기본 켬) / 현재 영상 루프(기본 끔) / 루프 횟수 1·3·무한(기본 무한). ※ 폴더 기반 연속 재생(목록 개념)부터 설계 필요.
- ※ A12(재생 시작 시 좌상단 "파일명 · 1080p" 3초 오버레이 — 새 미디어 첫 Playing에서만, 해상도 미파싱 시 파일명만, v0.76.0) 완료 — 결번. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A13(전체화면 조작 피드백 — 중앙 칩 0.9초: 볼륨 "Volume n%"/뮤트 "Muted"/시킹 "위치/길이"/재생 ▶·❚❚, (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- [확인] 창 크기 동일 시 시크바 길이 불변 — 가변 폭 컨트롤 고정폭화(v0.49.0~v0.53.0) 후 실기기 확인.
- ※ A28([버그] 뮤트/소리 아이콘 반전 — 원인: libvlc Mute 게터가 설정 직후 이전 값 반환(비동기 반영), (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A30(Fit 버튼 상태형 — 본체에 마지막 옵션 표시(A 텍스트/E8AB/E8CB)·본체 클릭=그 옵션 적용,
  플라이아웃 3옵션(Auto-fit/Fit width/Fit height), 영상 바뀌면 A 회귀·기억 안 함, 1:1 별도 유지, v0.79.0) 완료 — 결번.
  ⚠️ **A83·A111(v0.133.0)에서 개정**: 플라이아웃 4옵션(100%·Contain·Fit width·Fit height)·1:1 별도 버튼 흡수·
  본체 A 표기 소멸(Contain 아이콘 E9A6 / 100%는 1:1 텍스트). 상태형 UX 자체와 "영상 바뀌면 회귀"는 그대로.

- ※ A130(**EOF 후 창 리사이즈 비디오 미추종 — 원인 판별·수리**, v0.157.0) 완료 — 결번.
  - **판별(libvlc 3.0 소스 대조로 확정)**: EOF에서 libvlc는 vout을 재사용 대비로 살려 두되
    flush가 재표시 시계를 무효화해(video_output.c ThreadFlush → displayed.date 무효) **80ms
    재표시 펌프가 멎고**, MediaPlayer.Scale 쓰기도 활성 vout 목록이 비어(resource.c에서 제거)
    닿지 않는다 → **EOF에서는 어떤 재적용도 화면을 못 그린다**. 후보 ⓐ의 "틱 부재" 골격은
    맞되 틱의 정체는 우리 코드가 아닌 libvlc 내부 펌프(= C#에서 재적용 1회·프레젠터 훅으로
    보충 불가 — 두 수리 방향 모두 무효, 코드 우선 규칙으로 방향 변경). ⓑ "최대화만 핸들러가
    안 도는 분기"는 부존재 확인(SizeChanged는 최대화에도 정상 발화). 재생·수동 일시정지는 같은
    펌프가 살아 있어 전 리사이즈 정상(→ "수동 일시정지+최대화 = 정상"이 이 모델의 예측).
    "EOF 경계 드래그는 정상" 관찰만은 코드로 확정 불가한 잔여 — 재도장이 아니라 정지 프레임의
    합성층 스트레치 또는 Contain "축소만" 구간의 무변화-기대(원래 안 변하는 게 정답인 크기 대역)로
    설명되는 외관일 가능성이 가장 정합.
  - **수리**: Ended 상태에서 뷰포트 SizeChanged 시 `VideoView.Clear()`(LibVLCSharp 3.10.0 동봉,
    vlc 이슈 23667 공식 워크어라운드)로 잔상 프레임을 검정 정리 — 표면 배경도 검정이라 크기
    무관 이음새 없음, 종료 상태는 하단 바(▶·끝 위치)가 알린다. 최대화 전용이 아닌 **단발·연속
    전 경로 공통**(경계 드래그·복원·Win+화살표 스냅·F11 포함). 재생 재개(Ended → PlayCurrent)
    시 Playing 핸들러의 ApplyFitMode가 새 크기로 재도장(기존 Fit 경로 재사용 — 신규 계산식·
    타이머·디스패처 후행 틱 불요).
  - **A11 승계**: 잔상 정리는 이벤트 훅이 아니라 "Ended 상태 + 크기 변경" 조건이라 루프 옵션이
    EOF를 Ended에 머물지 않게 재정의하면 자연 비활성 — EndReached에 승계 주석을 심어 둠.
  - 실기기 확인 포인트: ① EOF → 최대화/스냅/드래그 = 잔상이 검정으로 정리되는지 ② 이후 ▶
    재시작 = 새 창 크기에 맞게 재생되는지 ③ 수동 일시정지 + 최대화 = 정상 추종(모델 예측 —
    어긋나면 재조사).
## 4.1 오디오 모듈

- ※ A66(**샘플 mp3 기본 동봉** — `Assets\sample.mp3`를 Content `CopyToOutputDirectory`로 동봉(비디오 (상세 → docs/REQUIREMENTS-ARCHIVE.md)
## 5. 녹화 · 녹음 (신규 기능군)

- [미반영 A14 · Fable] **화면 녹화**: 윈도우 화면 녹화기처럼. 소리는 시스템 오디오 기본 + 마이크 입력 있으면 믹스해서 함께 녹화.
- [미반영 A15 · Fable] **녹음**: 마이크 녹음.
- 소속 모듈 미정: 별도 모듈(ZP-rec?) vs 오디오/비디오 모듈 부속 — 설계 시 결정.

## 6. 문서 모듈 (ZP-doc)

- ※ A16(PDF 뷰어 — OS 내장 Windows.Data.Pdf 렌더(외부 의존성 없음)·.pdf 확장자 라우팅 추가, (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- [보류 A45] **HWP·오픈오피스(ODT 등) 뷰어** (A16에서 분리): C#에서 쓸 만한 렌더러가 없어 조사 선행 —
  hwplib(Java)·LibreOffice 변환 경유·hwpx(XML 직접 파싱) 등 방식 검토 후 착수. 스텁 연결 금지 원칙 유지.
- ※ A37(텍스트 편집·저장 — 뷰어→에디터 승격(TextBox)·.ini 확장자 추가·Ctrl+S+저장 버튼, (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A49(**문서 모듈에 맞춤 보기(Fit) 도입 + 전 모듈 점검**, v0.96.0) 완료 — 결번.
  ⚠️ 아래 옵션 구성은 **A83·A111(v0.133.0)에서 개정**됐다(4옵션·1:1 버튼 흡수·Auto-fit → Contain,
  이미지 구규격 정리 = 여기서 "후속 필요"라고 적어 둔 몫). 배율 계산·추종·수동 줌 해제 규칙은 그대로. 확정·구현 내역:
  - PDF 뷰어(A16, PdfPane)에 **1:1 / Fit width / Fit height** 추가 — UX는 A30(v0.79.0) 규격 그대로:
    하단 바 상태형 Fit 버튼(2칸 90×40, 본체=마지막 옵션 표시·클릭 시 재적용, 플라이아웃 3옵션
    Auto-fit/Fit width/Fit height), 1:1은 별도 1칸 버튼, **파일이 바뀌면 Auto-fit으로 회귀·기억 안 함**.
    텍스트 에디터 모드(A37)는 Fit 대상이 아니라 PDF 모드에서만 버튼 표시.
  - 구현 시 결정: **Fit height 기준 = 현재 페이지 전체가 세로로 보이게** — 페이지 스냅 스크롤은
    도입하지 않음(연속 스크롤 유지). **Auto-fit = 페이지가 뷰포트보다 크면 전부 보이게 줄이고,
    작으면 100%(원본 크기)** — 비디오 A30 의미론과 일치. **Ctrl+휠/핀치 수동 줌(A16)을 하면
    Fit "적용 중" 상태만 해제**(창 크기 추종 중단) — 버튼 표시는 마지막 옵션 유지.
    Fit 적용 중에는 뷰포트 크기 변화에 배율 추종(비디오 v0.41.0과 동일 규칙).
  - A40 규칙 준용: 하단 바 폭 760 미만이면 드라이브 정보를 숨김 — 버튼 추가분(1:1+Fit=140px)이
    최소 창 폭 720(바 폭 약 656)에서 잘리지 않게.
    ※ **A22(v0.108.0)에서 이 규칙은 제거**됐다 — 드라이브 표시가 Auto 폭 텍스트에서
    남는 폭(star 칸) 슬롯으로 바뀌어 더는 버튼을 밀어내지 않는다(넘치면 줄 자체가 스크롤).
  - **전 모듈 점검 결과**: Video = A30으로 보유. **Image = v0.41.0/v0.53.0의 1:1+Fit 플라이아웃 보유하나
    A30 이전 규격**(본체 클릭=항상 Auto-fit, 마지막 옵션 미표시, 플라이아웃 2옵션) — 규격 통일은 후속 필요.
    Audio(커버아트 표시 기능 자체가 없음)·Archive(시각 미리보기 없음, 목록만) = Fit 대상 없음(해당 없음).

- ※ A80(문서 뷰어 우측 약 30% 빈 공간 버그 수정 — 원인은 오버레이가 아니라 텍스트 에디터 자체. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A113(**문서 모듈 정합성·편집 범위 확정 + 런타임 체크 ⓐ~ⓓ**, v0.141.0) 완료 — 결번. 구현 요지:
  - **저장 순서(SaveAsync)** = ⓓ 외부 변경(열 때·저장 때 기록한 수정 시각·크기 스탬프 비교 →
    "File changed on disk", Overwrite/Cancel) → ⓑ 라운드트립 손실 예고(로드 시 1회 판정 —
    "Saving will normalize this file", 사유 = 줄바꿈 통일/문자 대체, Save anyway/Cancel) →
    인코딩(기존 CP949→UTF-8 전환 확인) → 쓰기 → ⓐ 재검증(다시 읽어 쓴 바이트와 대조 —
    "Save verification failed", Retry/Save as.../Cancel). 어느 취소든 저장 전체 취소(더티 유지,
    닫기 가드 경유면 닫기도 취소). 강행 금지 원칙에 따라 파괴적인 ⓓ만 기본 버튼이 Cancel.
  - **ⓑ 판정 = 로드 워커에서 1회**: 원본 byte[] 참조 보관(잘림 제외) + "정규화→줄바꿈 복원→
    EncodeStrict == 원본 바이트"로 `_lossyAtLoad`+사유(`RoundTripLoss`). **ⓐ Save as...** =
    FileSavePicker(ArchiveView 피커 패턴: InitializeWithWindow+GetHwnd, 확장자 목록 = 현재
    확장자 1개) → 성공 시 `_path`·`_shownPath` 갱신 + ContentOpened/TrayStatusChanged 발화.
  - **저장 성공 후 재기준화**: 원본 바이트=쓴 바이트·기준 텍스트=저장 내용·손실 해제·ⓓ 스탬프
    재기록(누락 시 다음 저장마다 ⓓ 오탐) + 더티는 내용 재판정(쓰는 동안 들어온 입력이 ● 없이
    새던 종전 동작 수리).
  - **ⓒ 더티 = 내용 비교**(등재 당시 "버퍼 해시" 표현 — 해시 대신 원문 비교로 확정): 기준 =
    로드·저장 시점의 \n 정규화 텍스트. TextChanged마다 길이가 다르면 즉시 dirty, 같으면 250ms
    디바운스(DispatcherTimer — 지시의 DispatcherQueueTimer 대신 저장소 관용구를 따름) 후
    string 비교 — undo 원복이면 ●가 꺼진다. 종전 "1회 set 후 조기 반환" 최적화 제거. 뷰
    Unloaded·파일 전환·PDF 전환 시 타이머 정지.
  - **① 정합성 수리 2건**: 저장·닫기 판단 직전 보류 중 디바운스 판정을 확정(250ms 창의 치환
    편집이 "변경 없음"으로 새는 구멍 — HasUnsavedChanges/SaveAsync 진입부) · 저장 흐름 대화상자와
    닫기 가드의 ContentDialog 동시 표시 충돌 방지(`_saving` 중 닫기 보류). **② 편집 범위**: PDF
    뷰 전용(`_path` null)·잘림 읽기 전용은 현행이 이미 충족 — 클래스 헤더 주석에 명문화만.
    ⓐ~ⓓ 전부 잘림·PDF 비적용.
  - 한계(보고): Save as... 후 창 제목(A103 "KOTU - 파일명")은 갱신되지 않는다 — 셸 OnContentOpened가
    제목을 만지지 않는 기존 동작(뷰 내부 열기 공통, 트레이·기준 경로는 갱신됨). 필요 시 셸 측 후속.
- [미반영 A115 · Fable] **문서 편집기 표시 개선** (2026-08-14 등재 → v0.142.0에서 ①~③ 구현,
  **④만 불가 판정으로 잔여**): ① 라인 가이드 ② 비가시 문자(¶·EOF) ③ 옅게 = 완료.
  - **조사 판정(2026-08-15)**: ⑴ TextBox 캐럿 색 공개 API·테마 리소스 **없음** — 캐럿 색은
    배경색에서 내부 유도(흑/백만 가능), microsoft-ui-xaml #9005(RichEditBox도 동일, Closed as
    not planned). ⑵ 문자 좌표 API는 **있음** — `TextBox.GetRectFromCharacterIndex(int, bool)`
    (WinAppSDK 공식, 반환 Rect = TextBox 로컬 좌표·스크롤 반영). ⑶ 내부 ScrollViewer
    (ContentElement) 취득+ViewChanged = PdfPane `FindDescendant<ScrollViewer>` 선례 그대로 가능.
    ⑷ TextBox에 LineHeight·LineStackingStrategy **없음**(TextBlock 전용) → 균일 줄 높이 강제
    불가 — 가이드는 고정 간격 산술 대신 **문자 rect 실측**으로 그린다(한글·이모지 혼재 줄도 정합).
  - **구현(EditorDecor.cs + DocumentView 배선)**: EditorBox 위 z-겹침 Canvas(IsHitTestVisible
    =False, Padding 안쪽 클립 — 포커스된 TextBox는 Focused 상태가 불투명 배경을 그려(A80) z-아래
    레이어가 가려지므로 위에 얹는다). 뷰포트 안 줄만 걷는다 — 첫 표시 인덱스 이진 탐색 + 줄 단위
    지수/이진 전진(패스당 rect 호출 수백 회 수준). 하드 개행 ¶·파일 끝 ·EOF(0.25), 줄 윗변/밑변
    가이드(0.08 — 다크=흰/라이트=검, 상수는 EditorDecor 상단 한 곳). 무효화 = TextChanged(플래그만
    — A113 디바운스에 무게 없음)·SizeChanged·ViewChanged·테마 변경. PDF·빈 화면 미렌더,
    잘림(읽기 전용) 렌더, 0바이트 파일은 잴 문자가 없어 EOF 표지 생략(한계). 새 설정 키·UI 문구 없음.
  - **폴백(오케스트레이터 지정 함정)**: 내부 ScrollViewer 취득은 템플릿 구조 의존 — WinAppSDK
    업데이트에 깨질 수 있는 부류(v0.113.1 지연 로딩 스타일 급). 취득 실패·렌더 중 예외는 장식만
    조용히 끈다(Disable — 편집 본기능 무영향). rect 좌표 기준(뷰포트/콘텐츠)도 런타임 1회 자가
    판별로 이중 방어.
  - **④ 잔여(불가 판정·대안)**: 형광 녹색+검정 테두리 캐럿은 현행 TextBox·RichEditBox 모두
    불가(색 API 자체가 없고 2색 캐럿은 더더욱 — 본문 Foreground 변조 같은 근사는 사양 ③ 위반이라
    배제). 대안 = ⓐ 커스텀 캐럿 오버레이(좌표는 GetRectFromCharacterIndex로 확보되나 **네이티브
    캐럿을 숨길 API가 없어 이중 캐럿** 문제) ⓑ RichEditBox 이관(#9005상 캐럿 색은 동일 한계 —
    이관 실익 없음) ⓒ Windows 설정(접근성 > 텍스트 커서 표시기)은 사용자 OS 설정의 몫.
    진행하려면 별도 항목 등재 필요 — 본문을 해치지 않는 구현 경로가 현재 없다.
- ※ A120(**문서 본문 = 중앙 컬럼 배치 — "콘텐츠는 항상 가운데" 원칙**, v0.146.0) 완료 — 결번.
  원칙("이후 새 콘텐츠 표면 기본 = 중앙 배치", 부록 B 61 사용자 확정)이 후속 항목의 전제라 요지를 남긴다.
  - **구현**: `DocumentView.xaml`의 EditorBox(TextBox)와 DecorLayer(Canvas)에 **`MaxWidth="900"`
    한 속성씩만** 추가. 그리기 산술·코드는 무변경(수정 파일 1개).
  - **구현 시 결정 ① — `HorizontalAlignment="Center"`는 쓰지 않는다**(등재 사양의 문구를 배치
    시점에 정정). Center를 명시하면 배치 폭이 **콘텐츠 폭으로 접혀** 900 컬럼 자체가 무너진다.
    기본값 Stretch를 두고 MaxWidth만 걸면 레이아웃 규약(Stretch의 constrained fallback)이 남는
    폭을 좌우 균등 배분해 **결과가 곧 중앙 배치**다 — 사용자 사양("본문 컬럼 중앙 배치")은 그대로
    충족하고 폭만 지킨다. 근거는 두 요소 주석에 명기.
  - **A80(v0.91.0)과의 구분**: A80이 걷어낸 것은 "MaxWidth 900 + **Left**"의 **비대칭**(여백이
    우측에만 몰려 포커스 배경이 한쪽만 빈 증상)이었고, 이번은 Stretch+MaxWidth라 여백이 좌우
    대칭이다. **900 이하 폭 창은 제약이 걸리지 않아 A80 이후 현행과 픽셀 동일**(회귀 기준 충족).
  - **함정 해소(등재 시 선지정)**: A115 장식 레이어 좌표 — EditorDecor는
    `GetRectFromCharacterIndex`(EditorBox 로컬)를 DecorLayer(Canvas) 로컬에 그대로 찍으므로
    **두 요소의 레이아웃 제약이 같아야 원점이 겹친다**. 그래서 Canvas에도 같은 900을 걸었다
    (오프셋 보정 추가 없음). 두 요소 주석에 상호 의존을 못 박아 한쪽만 바꾸는 사고를 막는다.
  - 무변경 확인: PdfPane·PlaceholderText·StatusBar(전폭 유지) · EditorDecor 코드 0줄.
- ※ A121(**PDF 키보드 스크롤 — ↑/↓·PageUp/Down·Home/End**, v0.146.0) 완료 — 결번.
  - **구현**: `PdfPane.TryHandleNavKey(VirtualKey)` 신설(비율 상수 `ArrowScrollRatio` 0.125 ·
    `PageScrollRatio` 0.9 = 파일 상단 한 곳) + `DocumentView`가 **루트 `PreviewKeyDown`(터널링)**
    으로 수신. ↑/↓ = 뷰포트 높이 1/8, PgUp/Dn = 90%, Home/End = 0 / `ScrollableHeight`.
    `ChangeView(null, y, null, disableAnimation: false)` — 가로·배율 null(무변경)이라 배율 불변이고
    Fit 추종 해제(A49)에도 걸리지 않는다. **부드러운 스크롤(false)** 은 줌 경로의 `true`와
    의도가 갈리는 지점이라 주석으로 구분해 뒀다. ←/→는 미배정 유지.
  - **구현 시 결정 ① 수신 경로 = KeyboardAccelerator 아님**: 액셀러레이터의 조건부 통과
    (Handled 되돌리기)는 화살표에서 불안정해 **에디터 캐럿 이동을 뺏을 위험**이 있다. 터널링
    PreviewKeyDown은 PdfPane 안쪽 ListView·ScrollViewer 내장 키 처리보다 먼저 도착해 우선순위가
    구조적으로 성립한다. **저장소 첫 사용 API**(선례 0) — 되돌리기 지점 = 생성자의 구독 한 줄을
    `KeyDown`으로 바꾸면 배선은 그대로 살고 우선순위만 내려간다.
  - **게이트(전부 통과 시에만 소비)**: ⓐ PDF 표시 중(`_pdfPane.Visibility`) — 에디터·4MB 잘림
    뷰·빈 화면은 즉시 통과라 **TextBox의 화살표·PgUp/Dn·Home/End 원 기능 불간섭**(이 배치의 합격선)
    ⓑ `e.OriginalSource`가 TextBox·PasswordBox·RichEditBox면 통과 ⓒ `HotkeySupport.ShouldPassThrough`
    (A34 공용 — 텍스트 입력 포커스 + PassThroughTag 표면) 통과. 셸 오버레이는 이 뷰의 조상 경로
    밖이라 터널링이 닿지도 않는다(ⓒ는 이중 방어선) ⓓ 수정키(Ctrl·Shift·Alt·Win) 조합은 통과 —
    Ctrl+Home 등을 뺏지 않는다.
  - **오토리피트는 걸러내지 않는다**(`WasKeyDown` 미조회) — 꾹 누르면 연속 스크롤이 사양이다.
    토글·상태 전이 키(A86 문자 3종 세트·A118 F1/F2)가 오토리피트를 버리는 것과 반대인 이유
    ("연사 금지" 대 "연속 이동")를 주석에 명기.
  - **셸 전역 키 무충돌 재확인**(배선 후 grep): 저장소 전체 `.cs`에 `VirtualKey.Up/Down/PageUp/
    PageDown/Home/End` 참조 0건 · XAML 액셀러레이터의 Up/Down/Left/Right는 오디오·이미지 모듈
    전용(문서 모듈 미보유, 뷰가 공존하지 않음) · A118 F1/F2·A34 문자 핫키·Alt 조합에 해당 키 없음.
  - 연쇄 갱신 완료: `docs/A86-keymap.md`(매트릭스 행 + 포커스 예외 절) ·
    `docs/USER-GUIDE.md`·`site/guide.html`(2.5 PDF 절 + 모듈 키 표, As of v0.146.0).
## 7. 압축 모듈 (ZP-zip)

- [확인] 우클릭 메뉴 표시명 **ZP-zip** — 코드상은 v0.33.0에서 교체 완료. 실기기에서 "WinUtil…"로 보이면:
  설정에서 우클릭 메뉴 토글 껐다 켜서 재등록 → 그래도 WinUtil로 나오면 버그로 처리.

- ※ A131(**압축 모듈 탐색기 zip 더블클릭 무반응**, v0.158.0) 완료 — 결번. 판별·수리 내역:
  - **판별(전수 추적)**: 표면(ThumbnailExplorer·ExplorerPane)·셸(OpenFileRouted → OpenFile →
    Resolve → ShowModule) 전 구간에 모듈·확장자 분기 0건 — zip은 파일로 열거·필터 통과·archive로
    라우팅(좌 리스트 행은 zip/jpg가 문자 그대로 같은 경로, 중앙 타일은 미리보기 내용물만 다르고
    이벤트 배선 동일). 셸 경로는 도달만 하면 `SetTitle`이 `CreateView`보다 먼저라 제목이 반드시
    바뀐다 → "아무 변화 없음"은 **FileActivated 미발화 확정**(선조사의 가중 그대로). 기각한 가설:
    같은 모듈 스킵 가드·필터 분류 이탈·표면 미구독·Resolve 오배정·오버레이 히트 차단(전 층 검증 —
    중앙 상공 히트 요소 없음)·IFileOpenTarget 우회·침묵 예외(전역 핸들러가 메시지박스를 띄운다).
  - **원인(A85 잔여 구멍)**: 두 더블클릭 판정(컨테이너 DoubleTapped·ItemClick 쌍)이 **둘 다 항목
    컨테이너 수명에 묶여 있다** — 두 클릭 사이·클릭 도중에 목록 재구축(A94 5차 폴더 감시 재스캔:
    디바운스 400ms, 활동 중인 폴더면 연속 재구축. ShowEntries/Fill이 항목을 전부 새로 만든다)이
    끼면 눌림·뗌이 다른 요소가 되어 클릭이 성립하지 않고(ItemClick 침묵) 새 컨테이너에는 제스처
    상태가 없어 DoubleTapped도 안 뜬다 → 완전 침묵. **압축만인 것은 코드 분기가 아니라 데이터
    상관** — 압축 탐색기 시작 폴더(zip이 사는 다운로드류 활동 폴더)는 감시 이벤트가 잦고
    이미지·영상 라이브러리는 조용하다. 좌/중 동시 무반응도 정합(두 표면은 같은 ViewChanged로
    같은 순간 함께 재구축된다).
  - **수리**: 항목 수명과 무관한 **원시 눌림(PointerPressed) 쌍 폴백** — 경로 키·500ms·왼쪽 눌림
    전이만(A112 관용구)·Ctrl 눌림 제외(다중 선택 토글)·이름변경 TextBox 제외·Handled 불변(순수
    관찰 — 선택·드래그 무간섭). 기존 종착점 `Activate` 재사용(새 열기 경로 0), 기존
    `_lastActivation` 억제(A85)가 정상 환경의 이중 발화를 1회로 누른다(발화 시점 = 두 번째 눌림 —
    탐색기 WM_LBUTTONDBLCLK 관례와 동일). 두 표면(ThumbnailExplorer — S4 인스턴스 포함 ·
    ExplorerPane 그리드/리스트)에 같은 한 벌 — 같은 원인이 전 파일 모듈 공통이라 모듈 무관 폐쇄.
  - 시뮬레이션: S1 좌/중 더블클릭·Enter·다중 일괄 열기(A94 6차)·Shift+더블클릭(새 창)·S4 전부
    기대 동작 유지(폴백은 기존 판정과 겹치면 억제로 잉여, 재구축이 끼면 유일 생존 경로).
    실기기 확인 포인트: 압축 모듈 S1에서 zip 더블클릭 열림(파일 변동 잦은 폴더에서도).
    ※ "맥OS식 자동 해제" 전환은 종전대로 이 항목 범위 밖(원하면 별도 등재).
## 8. 하드웨어(정보) 모듈

- ※ A119(**H/W 좌/우 구획의 사이드바/오버레이 체계 편입 + 정사각형 그래프·타일**, v0.145.0) 완료 — 결번.
  전제(부록 B 59·60 — 사용자 확정): 패널은 전 모듈 공통 개념, 좌 = 큰 그래프 / 우 = 스펙 텍스트,
  진입 기본 = 양쪽 사이드바, 어느 형태(오버레이/사이드바)든 내용 동일, 설정 모듈 제외. 구현 요지:
  - **셸 계약 2건**: Core 신설 `ISidePanelProvider`(뷰가 좌/우 패널 요소를 object로 내준다 —
    IBottomBarProvider와 같은 "컨트롤은 셸에·계약은 Core에·모듈은 슬롯만" 패턴, 구현 = HardwareView뿐)
    + `ISidebarAwareView` 개정(bool "양쪽 열림" → **도크 수 0/1/2** — 소비처가 H/W뿐이라 개정 안전.
    A93 썸네일의 4/8열은 별개 경로라 무변경).
  - **셸 호스트 = 신규 `Overlays/SidePanelHost`**(코드 전용 컨트롤 좌/우 2개, MainWindow.xaml에
    파일 오버레이와 같은 층으로 선언): 반투명(OverlayAcrylicBrush)/불투명 배경·홀드 클릭 통과·
    힌트(2.5초+페이드, OverlayHints 공유)·25% 내부 분할을 파일 오버레이와 같은 규칙으로 재현 —
    배경·상태 적용이 FileListOverlay/ContentInfoOverlay 내부에 있어 셸 층으로 뽑는 대신 호스트에
    재현하는 쪽을 택했다(A92 타이밍 장치가 세 벌이 됐다 — 세 파일 상호 주석으로 봉인).
  - **게이트 확장 = `HasPanelContext` 단일 판정**(파일 열림·빈 파일 모듈(A81)·패널 제공 뷰) —
    F1/F2 소비·상태 전이·경계 버튼·힌트가 전부 이걸 탄다. `CurrentShellState`는 패널 제공 뷰를
    좌/우 조합대로 S2/S3*에 분류(Enter 일괄 토글 성립 — '오픈 파일'은 파일 가드가 걸러 무동작
    유지). **설정·빈 셸·미지원 안내는 종전대로 무소비**. A118의 "(A119 전까지)" 표식 지점 소진.
  - **모듈 전환 시 셸이 호스트를 반드시 비운다**(`ClearModulePanels` — ModuleBarHost 교체와 같은
    자리 3곳: ShowModule·설정 진입·미지원 안내). 이전 모듈 패널 잔존(A59급 회귀)·요소 수명 누수 방지.
  - **정보 모듈 화면 개편**: 뷰 본체 = 센터 타일 그리드만(세로 ScrollViewer) — 구 좌/우 구획은
    **코드 분리 생성** 요소로 옮겨 셸에 내준다(XAML에 두지 않아 reparent 자체가 없다 — §3.4 이중
    Add 부류 원천 차단. x:Name이던 BigGraphPanel·BigGraphHint·Root는 같은 이름의 코드 필드로 승계 —
    렌더 경로 무변경). 진입 기본 사이드바는 A109 경로(ShowModule defaultSidebars)가 이미 적용 —
    수리 0건(게이트는 표시 층에만 있었고 상태는 원래 들어오고 있었다).
  - **정사각형**: 타일 한 변 = (센터 실폭 − 간격×(열−1)) / 열 내림, 높이 = 한 변 — 열 수 = 도크
    2/1/0 → **2/3/4열**(반투명 오버레이는 계수 안 함). 좌 대형 한 변 = 패널 실폭(선택 2개 세로
    적층). 둘 다 명시 픽셀 + "같은 변이면 조기 반환"(SizeChanged 재계산 — 레이아웃 사이클 방지,
    A93 선례) + 변이 바뀌면 즉시 재렌더. 행 Auto + 세로 ScrollViewer(센터·좌 패널 각각) =
    **넘침은 스크롤·축소 없음**(타일 10개 2열 = 5줄 등). 축 라벨·글꼴 규칙(A74·A62)은 승계.
  - 불변 확인: 하단 바 긴 그래프 2개·타일 상호작용(클릭 선택·드래그 순서·핀)·채널 정의·
    HardwareInstanceState(A70 — 새 static 없음)·전체화면 SensorStrip 이관·A61 접힘 전부 그대로.
    **맥박 임계 676 재계수 결과 = 변화 없음**(하단 바 BarGrid 구성을 A119가 건드리지 않았다 —
    임계의 항은 긴 그래프 312 + BarFixedWidth 364뿐, 센터 폭과 무관).
- [미반영 A19 · Fable] HW 랭킹 제출 버튼 — 서버·개인정보 정책 설계 필요 (장기).
- ※ A20(네트워크 상태 — Network 섹션(Storage와 System 사이): 연결 여부·대표 어댑터(게이트웨이 기준)· (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A39(Always on top — ⛶ 바로 왼쪽 핀(E718) ToggleButton, OverlappedPresenter.IsAlwaysOnTop, (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A29(리프레시 주기 100/300/1000ms 선택 — 기본 300(사용자 확정), 2칸(90×40) DropDownButton이
  기존 "200 ms" 표기 자리(우측 정렬), 설정 저장(hardware.refreshMs)·PollingWorker.Interval 런타임 변경
  (변경 즉시 1회 폴링), 그 왼쪽에 맥박(EKG) 그래프 — 스냅샷 실제 도착 타이밍마다 QRS풍 스파이크(5초 창,
  90×36), SMART는 폴링 횟수 기반→10초 시간 기반으로 교체(주기 무관 일정 부하), v0.84.0) 완료 — 결번.
  ※ 이 중 **주기 목록(100/300/1000ms·기본 300)은 A73(v0.110.0)이 50/200/500/1000/2000/5000ms·기본 500으로
  대체**했다. 나머지(드롭다운 위치·설정 키·런타임 변경·맥박 그래프·SMART 10초 시간 기반)는 그대로 유효하다.
- ※ A17(센서 모니터링 하단 그래프 10개 — LibreHardwareMonitor, 저하 모드 + Restart as admin 버튼, v0.63.0) 완료 — 결번.
  ※ 하단 카드 10개는 A60 3차(v0.138.0)가 **센터 타일 10개 + 하단 긴 그래프 2개**로 대체 —
  채널 정의(SensorChannels)·렌더 경로(스파크라인·A74 축)·저하 모드·admin 버튼은 승계.
- ※ A18(선택 센서 최대 2개 트레이 표시 — 카드 클릭 토글, 아이콘 1개에 2값 세로, 앱 실행 중 상시, v0.64.0) 완료 — 결번.
  ※ 센서 아이콘은 A101(v0.137.0)에서 폐지 — 창별 아이콘이 그 창의 선택값을 표시한다.
  "앱 실행 중 상시"도 함께 소멸(사양 변경 — H/W 창이 없으면 트레이에 센서 값 없음).
  카드 클릭 토글·최대 2개·2값 세로 표기·`—` 관례는 창별 표시가 그대로 승계.
  ※ A54(v0.118.0) 재확인(→ A101이 대체): 당시 센서 트레이(`SensorTray`)는 **프로세스당 1개의 별도
  축**으로 병존했다 — "인스턴스당 아이콘 1개" 계산에 넣지 않았다. 2값 세로 표기·`ComposeKey`
  관용구는 A54의 창별 아이콘이 그대로 재사용했다(정보 모듈 창의 유휴 표기 `INF`와는 별개).
- [미반영 A47 · Fable] **"Restart as admin" 없이 센서 읽기** — 다른 모니터링 앱들은 재실행 없이도 동작하는 것으로 보임.
  현재: `SensorService.IsElevated`가 false면 저하 모드 + `runas`로 재실행 버튼(A17, v0.63.0).
  조사·구현 방향(구현 시 택일):
  ① LibreHardwareMonitor의 커널 드라이버(WinRing0)를 **서비스로 1회 설치** — 이후 비관리자에서도 열기 가능
     (설치 시점에만 관리자 권한. 설치본에 서비스 등록 옵션 추가). 대부분의 상용 모니터링 앱이 쓰는 방식.
  ② 관리자 권한이 **필요 없는 소스로 최대한 대체** — CPU/메모리/네트워크/디스크는 PerfCounter·WMI로,
     온도·팬·전압만 저하 처리(현행 유지).
  ③ 앱 시작 시 UAC로 한 번만 승격(manifest `requireAdministrator`) — **비권장**(전체 앱이 관리자로 뜨면
     드래그&드롭·기본 앱 연동에 부작용).
  ※ 드라이버 설치는 **백신 오탐·서명 이슈** 검토 필요 (A38과 같은 성격의 리스크).
- ※ A51(**맥박 그래프 창 길이 = refreshMs×2**(5초 고정 대체)·주기 변경 즉시 재렌더, v0.93.0) 완료 — 결번.
  ※ A73의 50~5000ms 범위에서도 성립 — 스파이크 폭은 픽셀 상수라 클램프 추가 금지(의도가 깨진다). (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A88(**맥박 그래프 연속 스크롤** — 기록/렌더 분리, `CompositionTarget.Rendering` 매 프레임, v0.114.0) 완료 — 결번.
  ⚠️ static 이벤트라 `Unloaded`에서 해제 필수 — 빠뜨리면 뷰·창이 통째로 누수. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A60(**정보 모듈 화면 재구성 — 3구획 그래프 중심**(3차) — A71·A72 흡수, v0.138.0) 완료 — 결번.
  구현 확정 내역(뒤 항목이 참조할 골격만):
  ① **구획 = 좌 1* : 센터 2* : 우 1***(셸 사이드바 25:50:25와 동형 — 전부 star라 DPI 무관,
  A40 최소 720×540 기준 꽉 참). 좌 = 선택 그래프 확대(줄당 1개, 최대 2 — 선택 0이면
  "Select graphs in the grid" 안내 1줄) / 센터 = 전 채널(10) 타일 그리드, **클릭 = 선택 토글
  (최대 2)·드래그 = 순서 변경** / 우 = 스펙 텍스트 리스트(기존 Render()·서명 생략·Busy 링
  그대로 이동 — 라벨 폭만 170→120, 구획 폭 25%에 맞춤).
  ② **시간 창 = 좌 10초 / 센터 30초 / 하단 5분(A71 대체)** — 전부 `min(사양 창, 링 600 × 주기)`
  계산값(`WindowFor` — A74 ③ 원칙 그대로: 50ms 주기면 하단 5분도 30초로 줄고 x축 표기도 계산값.
  기본 500ms에서 링 600 = 정확히 5분).
  ③ **하단 바 = 선택 긴 그래프 2개가 A17 카드 10개를 대체**(2026-08-14 사용자 확정 ②):
  각 폭 152 고정(= 구 카드 최소 폭 76 × 2 — A62 배수 비적용: 배수 대상 목록(글씨·선 굵기·높이
  클램프)에 폭이 없고, L × 최소 창 720에서 star 칸을 넘치기 때문), SensorGrid 가운데 정렬,
  선택 0개면 조용히 빈다. 고정 요소(Copy·Busy·맥박·주기·크기·핀·⛶, `BarFixedWidth` 364)·바
  두께 44(A40)·A61 접힘·A88 맥박 루프·전체화면 SensorStrip 이관(PlaceSensorGrid)은 무변경 승계.
  카드 수 축소 로직(`LayoutSensorCards`·`MinCardWidth` 계열)은 소멸(긴 그래프 2개 고정) —
  맥박 숨김 임계만 `긴 그래프 폭 합(312) + BarFixedWidth = 676`으로 재구성(최소 창 720에서는
  맥박이 내려가 긴 그래프 자리가 난다). 접힌 바(A61)에 긴 그래프 2개가 남는 것이 A72 흡수의
  핵심 가치.
  ④ **선택 = `HardwareInstanceState.Selection` 단일 개념**(A70·A101 축 완성): 센터 타일 클릭
  토글 하나로 핀 배지(카드 → 타일로 이동)·좌 대형·하단 긴 그래프·창별 트레이(A101)가 함께
  갱신된다. 기본 선택 cpuTemp·cpuPower 승계(확정 ④), A18 규칙(최대 2·오래된 것 밀림) 그대로.
  ⑤ **순서 = 신규 설정 키 `hardware.channelOrder`**(전 채널 ID 콤마 목록): 인스턴스 필드 +
  전역 1벌 무통지 커밋 깔때기(A70 원칙 그대로). 로드 시 정규화 = 미지 ID 버림 + 빠진 채널
  기본 순서(SensorChannels.All) 뒤 보충(채널 개편 내성). 드래그는 A94의 CanDrag+DragStarting
  선례 준용(페이로드 = 채널 ID 텍스트, 타일에 드롭 = 그 자리로 삽입), **커밋 = 드롭 확정 시
  1회**(드래그 중 Save 난사 없음). 클릭 토글과 제스처 충돌 없음(드래그 시작 = 탭 판정 탈락).
  ⑥ **4/8열 신호 = Core 신설 `ISidebarAwareView`**(셸 → 뷰 푸시): 셸 ApplyOverlayStates(사이드바
  상태 변경의 단일 종착점)가 "불투명 도크 양쪽 열림" bool을 밀고, 뷰가 열 수 4/8로 환산한다
  (A93 썸네일과 같은 판정). 미구현 뷰는 캐스트 실패로 no-op. ※ 현행 셸은 파일 컨텍스트 없는
  정보 모듈에 사이드바를 안 띄우므로(IsFileModule 게이트) 지금은 상시 8열 — 셸 정책이 바뀌면
  4열이 저절로 사는 배선이다(지시·코드 불일치는 코드 우선 원칙으로 이렇게 정리).
  ※ A119(v0.145.0)가 정보 모듈 편입 — 이 계약은 도크 수(0/1/2) 전달·열 수 2/3/4로 개정됐고
  화면도 자체 3구획에서 "센터 타일 + 셸 좌/우 패널"로 재편됐다(위 ①⑦의 3구획 서술은 그때까지의 형태).
  ⑦ **전체화면(F11) = 같은 3구획** — v0.42.0 섹션 카드 대시보드 폐기(`RenderDashboard`·
  `MakeSectionCard`·`AccentFor`·`DashboardScroller` 제거): 우측 텍스트가 상시 보여 중복이었다.
  ⑧ 축 상태(A74 상한·흐림)는 **채널별 공유(`ChannelScale`)** — 세 표면(타일·좌·하단)의 눈금이
  늘 같고, 선택을 껐다 켜도(표면 재생성) 단조 증가가 채널 단위로 이어진다.
- ※ A71(하단 바 시간 스케일 차등)·A72(모드 전환 버튼) — **2026-08-14 지시로 A60 3차에 흡수, 결번.**
  좌 10초/센터 30초/하단 5분 3단 차등(A71 대체) + 좌측 상시 큰 그래프(A72 대체) —
  **v0.138.0에서 실행됨**. 세부는 위 A60 완료 항목 참조.
- ※ A73(**리프레시 주기 목록 교체** — **50 / 200 / 500(기본) / 1000 / 2000 / 5000 ms**로
  A29(v0.84.0)의 100/300/1000·기본 300을 대체. `HardwareModule.RefreshChoices`·`DefaultRefreshMs`.
  구현 결정과 이유:
  ① **구값 이관 = "목록 밖이면 그보다 크거나 같은 가장 가까운 값으로 올린다"**(100→200, 300→500,
  v0.51.0 잔재 200→200, 목록 최대 초과는 5000). 기존의 "목록 밖이면 기본값으로 리셋"을 이 규칙으로 교체한
  이유는, 내림(예: 300→200)이면 사용자가 고른 적 없는 더 잦은 폴링이 되어 체감 부하가 갑자기 나빠지기
  때문이다. 이관은 `NormalizeRefreshMs`가 **읽을 때 1회 정규화 후 곧바로 되쓰기** 해서
  다음 실행부터는 그냥 통과한다(0·음수 저장값도 최단 50으로 올라가 폴러가 0 간격으로 도는 사고가 없다).
  ② **50ms 부하 경고 = 드롭다운 항목 툴팁** `Very frequent polling — higher CPU load`.
  항목 텍스트는 `50 ms` 그대로 두었다(숫자가 정보다). **상한 제한은 걸지 않는다** — 실측 부하는
  기기마다 달라 앱이 대신 판단할 근거가 없고, 확인은 사용자 몫이다.
  ③ 표기 폭: 가장 긴 `5000 ms`는 구 `1000 ms`와 **같은 7글자**라 2칸(84×40, A27 → A97에서 84×36 → A106에서 84×32) 버튼 폭 여유가 그대로다.
  ④ A51(맥박 창 = 주기 × 2)은 새 범위에서도 그대로 — 클램프 미추가(위 A51 항목의 재확인 메모 참조).
  ⑤ SMART(Storage) 조회는 A29에서 이미 **10초 시간 기반**이라 폴링 횟수와 무관 — 50ms(초당 20회)에서도
  SMART 부하는 10초에 1회로 동일함을 코드로 확인(`SensorService.StorageInterval`).
  ⑥ 부수 확인: 이력 링 600개는 50ms에서 30초치뿐이라 그래프 창(60초)보다 짧다 → A74에서 창을
  `min(60초, 링 용량 × 주기)`로 묶어 좌측 절반이 비지 않게 했다, v0.110.0) 완료 — 결번.
- ※ A74(**그래프 x·y축 스케일 표시** — 센서 그래프에 모서리 최소 표기로 축을 넣는다.
  **적용 대상 = A17 센서 그래프 셀 10개**(값을 그리는 그래프는 현재 이것뿐이다).
  **제외 대상과 이유**: ① **하단 바 전용 축약 표시** — A40으로 하단 바 두께가 44px에 1줄로 고정이라
  축 라벨을 얹을 세로 공간이 없다. 지금은 A17 셀이 그 자리에 살고 있어 같은 컨트롤이므로,
  "공간이 없으면 숨긴다"를 **폭 규칙(그래프 폭 90px 미만이면 라벨 숨김, A40의 좁은 폭 축약 관례와 동일)**
  으로 구현했다 — 좁은 창·카드 10개가 다 들어간 상태에서는 자동으로 사라진다.
  ② **맥박(EKG) 그래프(A29)** — 값이 아니라 스냅샷 **도착 타이밍**을 그리는 장식이라 y축이 존재하지 않는다.
  ※ A60 3차(v0.138.0)가 실현: 센터 타일·좌 대형·하단 긴 그래프가 같은 렌더 경로(공용 표면)로
  이 표기를 물려받았다 — 대형 셀은 폭 90px 규칙에 걸리지 않아 항상 보이고, 시간 창 3단 차등
  (A71 흡수)의 x 표기도 같은 계산식(`min(사양 창, 링 × 주기)`)이다.
  구현 결정과 이유:
  ① **표기 방식 = 모서리 최소 표기**(눈금선·축선을 그리지 않는다 — 36px 높이의 작은 셀에 격자를 그리면
  그래프 자체가 안 읽힌다): 좌상단 = y 최대값 + 단위(`100°C`·`65W`·`100%`), 우하단 = x 시간 범위(`60s`),
  10px·`Opacity` 0.55, 그래프 선 위에 겹쳐 배치. **y 하한 0은 자명하므로 적지 않는다.**
  단위는 값 표기(`62 °C`)와 달리 **공백 없이** 붙인다(`SensorChannel.AxisUnit` 신설) — 좁은 셀에서 한 글자가 아깝다.
  ② **y 상한 규칙**: 백분율(%) = 0~100 **고정**(100%가 정의상 최대라 넘겨도 확장 안 함) /
  온도(℃) = 0~100 고정 + **초과 관측 시에만 10 단위 확장**(여기에 1·2·5 눈금을 쓰면 105℃ 한 번에
  축이 200℃가 되어 그래프가 바닥에 깔린다) / 그 외(W·RPM·MHz) = 관측 최대를 **1·2·5 × 10ⁿ 눈금으로 올림**.
  셋 다 **세션 중 단조 증가**(한 번 올라간 상한은 내려오지 않는다) — 값이 튈 때마다 그래프 전체가
  출렁이는 것을 막기 위해서다. 채널별(카드별)로 적용. 기존 A17의 "관찰 최대 ×1.1" 자동 스케일을 대체한다.
  ③ **x 범위는 계산값**(하드코딩 금지): 창 길이 = `min(60초, 이력 링 600개 × 리프레시 주기)`.
  A73의 50ms에서는 30초가 되고, 200ms 이상에서는 60초다. 주기를 바꾸면 **다음 스냅샷을 기다리지 않고**
  즉시 다시 그린다(`RerenderSparklines`, A51의 `RerenderPulse`와 같은 계열) — 5000ms에서 최대 5초 동안
  옛 표기가 남는 것을 막기 위해서다. 60초를 넘는 창은 분 표기(`3m`)로 떨어진다.
  ④ 값이 한 번도 없던 채널(권한 부족·미지원)은 축 라벨도 숨긴다 — 축만 떠 있으면 값이 있는 것처럼 보인다.
  ⑤ 렌더 수단은 기존과 동일(`Polyline`/`Polygon` 위에 `TextBlock` 2개를 같은 `Grid`에 겹침) —
  새 렌더링 스택을 들이지 않았다, v0.110.0) 완료 — 결번.
- ※ A75(**정보 화면 컨트롤 정리** — 수동 Refresh 버튼 제거(주기 폴링이 있어 불필요, 사용자 확정). (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A70(**하드웨어 모듈 다중 인스턴스 + 인스턴스별 상태 분리** — 런타임 창별 독립·저장 전역 1벌, v0.131.0) 완료 — 결번.
  남긴 근거: A60 3차·A101·A107 등이 이 인스턴스 상태 구조(`HardwareInstanceState`)를 전제한다.
  - **공유/분리 경계**: 인스턴스(창)별 = ① 센서 선택(A18 카드 핀, 최대 2) ② 하단 바 크기(A62 S/M/L).
    신규 `src/KOTU.Module.Hardware/HardwareInstanceState.cs` = **인스턴스 상태 + 전역 1벌 스토어 +
    저장 깔때기의 단일 소스** — 뷰가 `CreateForView()`(전역 현재값 복사)로 소유한다.
    공유 유지 = 폴러·섹션 캐시·구독 API(A42 확정 그대로)와 `BarScaleSteps` 상수 표.
    창 간 동기화 구독 2쌍(`TraySensors.Changed`→핀, `BarScaleChanged`→배율)은 **제거** —
    인스턴스 독립이 사양이 됐으므로 다른 창을 따라가면 안 된다(`BarScaleChanged` 이벤트 자체 폐지,
    뷰 내부 갱신은 토글 직후 직접 호출). HardwareView의 배율 static 헬퍼 연쇄
    (AxisMinWidthNow·MinCardWidthNow·UpdateCard·RenderSparkline)는 인스턴스 멤버로 승격.
  - **리프레시 주기(A29/A73)는 프로세스 공유 유지**(구 "구현 시 확인 필요"의 답 — 확정):
    폴러가 하나라 주기는 폴러의 속성이다. 인스턴스별 주기는 폴러 분할이 필요해 비용 대비 이득이 없다.
  - **저장 정책(부록 B 42 그대로)**: 전역 1벌·마지막 커밋(조작) 우선. 설정 키 이름 불변
    (`hardware.traySensors`·`hardware.barScale`) → **마이그레이션 0**. 모든 변경은 즉시 커밋
    (닫힘 훅 불필요). 재실행·새 창의 초기값 = 저장된 1벌. 쓰기는 스토어의 항목별 깔때기 2곳뿐 —
    선택 커밋만 `TraySensors.Changed`를 쏘고(SensorTray 소비), 바 크기 커밋은 무통지
    (창 간 동기화 폐지 + 트레이는 바 크기를 안 쓴다).
  - **A18 트레이 아이콘(SensorTray)은 계속 전역 1벌(마지막 커밋)을 따른다** — 폐지·창별 이관은 A101의 일.
    `TraySensors`는 App 호환 파사드로 존치(MaxCount·Selected·Changed·Clear — `Selected`의 volatile
    불변 스냅샷 규약 유지). `Toggle`/`IsSelected`는 인스턴스로 이사, `Initialize`는 스토어에 흡수.
    ※ A101(v0.137.0)이 실행: SensorTray·TraySensors 폐지 — 전역 1벌은 저장·새 창 초기값 전용으로 남았다.
  - **Clear("Hide tray sensors") = 전역 1벌만 비운다** — 열린 창들의 런타임 선택은 유지(허용된 발산).
    다음에 어느 창에서든 토글하면 그 창의 선택 전체가 커밋되어 아이콘이 되살아난다.
    ※ 이 메뉴·`ClearGlobalSelection`도 A101에서 아이콘과 함께 소멸(전부 해제는 창에서 핀을 끄면 된다).
  - **새 창 흐름 확인(구 Ctrl+N — A84에서 Shift+N으로 전환): 성립.** `MainWindow.RegisterShortcuts`의
    `AddShortcut(VirtualKey.N, ...)`(Shift 수식) → 하드웨어 창은 `CurrentModuleId`="hardware" →
    `WindowManager.OpenNewWindow`가 **항상 새 셸**을 만들어 `OpenModuleById("hardware")`로 빈
    하드웨어 인스턴스를 띄운다(창 재사용 규칙 FindReusable을 타지 않는다).
- ※ A61(**정보 모듈 Always on top → 하단 바만 표시** — A39의 핀을 켜면 메인 영역을 접어
  **타이틀바 + 하단 바 44px만 남는 얇은 상시 표시 바**가 되고, 끄면 접기 전 크기로 복원한다.
  구현 결정과 이유:
  ① **접힘의 단일 소스 = 핀**(별도 토글을 만들지 않는다). 접힘 여부는 들고 다니는 플래그가 아니라
  **"핀 ON && 전체화면 아님"으로 매번 계산하는 파생 상태**(`HardwareView.ShouldCollapse`)다 —
  핀·전체화면을 어떤 순서로 눌러도 상태가 어긋나지 않는다. A39의 "뷰가 내려가면 자동 해제"도 그대로여서
  모듈을 바꾸면 접힘도 함께 풀린다(펼 수단이 없는 납작한 창을 남기지 않는다 — A39와 같은 이유).
  ② **타이틀바는 유지**한다 — 드래그 이동·닫기 수단이고 인스턴스 번호 `[n]`(A56) 표기가 거기 있다.
  접힌 높이 = 비클라이언트(타이틀바 + 테두리) + 하단 바 44 DIP이며 **타이틀바 높이를 하드코딩하지 않는다**:
  `AppWindow.Size.Height − 클라이언트 높이(ScaleHost.ActualHeight × RasterizationScale)`로 실측한다
  (UI 배율 오버라이드(v0.24.0)가 걸리면 바도 실제로 그만큼 커지므로 그 배수를 함께 곱한다).
  ③ ⚠️ **A40 최소 창 크기(720×540 DIP)의 런타임 오버라이드가 필수** — `WindowMinSize`의
  `WM_GETMINMAXINFO` 하한이 540 DIP라 그대로 두면 접기가 통째로 무동작이 된다. 접힘 동안만 창별로
  최소 **높이**를 접힌 높이로 낮추고(`WindowMinSize.SetMinHeightOverride` — DIP로 들고 있어 모니터를
  옮겨도 DPI 환산이 옳다) 해제 시 540으로 되돌린다. **폭 하한 720은 접힘 중에도 유지** —
  하단 바가 그 폭을 전제로 축약 설계돼 있다(LayoutSensorCards·UpdateBarDensity).
  ※ **A40 수치를 만질 때 이 오버라이드를 같이 봐야 한다.**
  ④ **폭·위치는 접을 때 건드리지 않는다**(옮기지 않는다). 복원도 높이만 되돌리므로 접힌 채 폭을 바꿔도
  그 폭이 남는다. 최대화 상태에서 켜면 최대화된 창은 Resize가 먹지 않으므로 먼저 Restore한 뒤 접고,
  끄면 다시 최대화한다(되돌릴 높이는 A55가 추적해 둔 일반 높이).
  ⑤ **A55(창 크기·위치 저장)와의 상호작용 = 접힌 크기는 절대 저장하지 않는다**:
  `TrackNormalBounds`가 접힘 중에는 **높이만 동결**하고(폭·위치는 계속 추적해 접힌 채 옮겨도 반영),
  `SaveWindowBounds`에 접힘 분기를 더해 접힌 채 닫혀도 **접기 전 높이**가 저장된다.
  ⑥ **전체화면(⛶)과의 관계**: 접힌 채로 들어가지 않는다 — 먼저 펼치고 전환하며, 나오면 파생 상태
  재계산(`UpdateViewMode`)으로 (핀이 여전히 켜져 있으므로) 다시 접힌다.
  ⑦ **A69(최소화 → 트레이 숨김)·A68(인스턴스 색 테두리·번호)** 는 접힘과 무관하게 그대로 동작한다 —
  최소화 판정은 프레젠터 상태, 아이콘은 창 수명 기준이라 접힘이 끼어들 지점이 없다(코드로 확인).
  ⑧ 구조: 모듈 프로젝트는 셸을 참조할 수 없으므로(App → 모듈 단방향) **판단은 뷰, 실행은 셸**로 나눴다 —
  새 계약 `IWindowCollapseSource`(KOTU.Core.Contracts)로 뷰가 접기/펼치기를 요청하고
  `MainWindow.SetBarOnlyCollapsed`가 실제 창을 조작한다. 접기 요청은 지금 보이는 뷰의 것만 받고,
  펼치기는 뷰가 내려간 뒤(Unloaded)에도 받아준다.
  핀 툴팁도 새 동작에 맞춰 `Always on top (collapses to the bar)`로 갱신, v0.111.0) 완료 — 결번.
- ※ A62(**정보 모듈 하단 바 표시 크기 조절** — A61의 상시 표시 바에서 가독성을 확보하는 것이 목적.
  구현 결정과 이유:
  ① **단계 = S / M(기본) / L 3단계, 배수 0.85 / 1.0 / 1.25**. 설정 키 `hardware.barScale`(값은 `S`·`M`·`L`),
  **프로세스 공유 → A70(v0.131.0)에서 인스턴스별 분리됨**(런타임 창별 독립, 저장은 전역
  1벌·마지막 커밋) — 저장값이 목록 밖이면 M으로 떨어진다.
  ② **UI = 하단 바 1칸(A27 `BottomBarButtonStyle` — A97에서 36×36 → A106에서 32×32) 버튼 하나**로 **S → M → L → S 순환**.
  2칸 +/− 방식을 쓰지 않은 이유는 하단 바 폭이 이미 빡빡하기 때문이다(A40에서 720px 잘림을 따로 처리했다).
  글리프는 **E71E(Zoom, 돋보기)** — 방향이 없어 순환 버튼에 맞는다(ZoomIn/ZoomOut은 한 방향을 암시).
  툴팁은 `Bottom bar size: Medium` 형태로 클릭 때마다 갱신하고, **아이콘 자체도 단계만큼 커져**
  툴팁을 열지 않아도 지금 단계가 보인다. 위치는 주기 드롭다운(A29·A73)과 핀(A39) 사이.
  ③ **바 두께 44px는 불변(A40)** — 배수는 바 **안** 요소에만 곱한다: 카드 제목 11 / 값 13 /
  축 라벨·트레이 핀 10(A74·A18) / 선 굵기 1.5 / 카드 높이 36. 카드 높이는 **최대 40으로 클램프**해
  L(45가 될 값)에서도 44px 안에 들어간다. 버튼·드롭다운의 글씨는 그대로 둔다 — 규격이
  A27(40×40·84×40)로 고정이라 키우면 그 안에서 잘린다.
  ※ **A97(v0.116.0) 반영**: 버튼 규격이 36×36·84×36이 되면서 `MaxCardHeight`도 **40 → 36**(버튼과
  같은 높이여야 한 줄이 어긋나지 않는다). 기준 높이와 상한이 같아져 **L 단계 카드 높이는 M과 동일**하고,
  L은 글씨·선 굵기만 키운다.
  ※ **A106(v0.132.0) 재개정**: 버튼이 32×32·84×32이 되어 `MaxCardHeight` **36 → 32**. 상한이 기준
  높이(`BaseCardHeight` 36 — 무변경)보다 작아져 **M·L 두 단계 모두 카드 높이 32**, S만 30.6이다.
  ④ **폭 임계값도 함께 스케일**한다(글씨가 커지면 더 이른 폭에서 축약돼야 자연스럽다):
  카드 최소 폭 76(A40)·축 라벨 표시 임계 90(A74)에 배수를 곱한다. 맥박 그래프(A29)를 숨기는 바 폭
  임계는 **카드 전체 최소 폭(배수 적용) + 고정 요소 `BarFixedWidth`**로 분해했다 — 고정 요소
  (A27 규격 버튼들)는 배수를 타지 않기 때문이다. v0.111.0 값은 458(= 1240 − 832 + 크기 버튼 50)로
  M 단계 임계 1290이었다. ※ **A97(v0.116.0)에서 380으로 재산정** — 버튼 36·간격 6 기준으로
  BarGrid 요소를 하나씩 세어 정확한 합(338 + 42)으로 바꿨다(구 458은 역산 근사치라 34 컸다).
  M 단계 임계는 832 + 380 = 1212. ※ **A106(v0.132.0)에서 364로 재산정**(버튼 32 기준 요소 합
  322 + 간격 42) — M 단계 임계는 832 + 364 = **1196**.
  ⑤ **전역 UI 배율(A41)과는 별개의 배수** — `UiScale`은 건드리지 않고 하단 바 요소에만 곱한다.
  ⑥ 변경은 즉시 반영(재시작 불필요)이고, 설정이 프로세스 공유라 `HardwareModule.BarScaleChanged`로
  **열려 있는 다른 정보 창도 같은 단계를 따라온다**(TraySensors.Changed와 같은 방식 —
  ※ 이 창 간 동기화는 A70(v0.131.0)이 인스턴스 독립으로 대체·이벤트 폐지), v0.111.0) 완료 — 결번.
  ※ 배수 적용 대상이던 카드 10개는 A60 3차(v0.138.0)가 **하단 긴 그래프 2개**로 대체(높이
  클램프 32·글씨·선 굵기·축 라벨 임계에 그대로 적용, 폭 152는 고정) — 센터 타일·좌 대형은
  바 밖이라 배수 비적용. 카드 수 축소 임계(④의 "카드 전체 최소 폭")는
  `긴 그래프 폭 합(312) + BarFixedWidth = 676`으로 재구성.

- ※ A127(**H/W y축 최대치 라벨 "max" 접두** — 현재값 오독 방지, v0.154.0) 완료 — 결번.
  A128과 한 배치(같은 메서드를 두 번 만지지 않으려고 묶었다 — 등재 시 예고한 대로).
  - **구현**: `HardwareView.RenderSparkline`의 y축 라벨 조립 1곳 →
    `graph.YAxisText.Text = $"max {max:0}{graph.Channel.AxisUnit}";` (예: "max 100%", "max 100°C",
    "max 5000MHz"). x축 라벨(`FormatSpan`)·헤더 채널명·현재값(`UpdateGraph`의 `FormatFull`)은 무변경.
  - **적용 범위**: A128로 바 표면이 라벨을 아예 안 그리게 돼 **접두가 보이는 곳은 센터 타일·좌 대형뿐**.
  - 표시 임계 `AxisMinWidth` 90은 **유지**(등재 시 지정한 대로 임계 상향·폰트·위치 불변) — 배수를 타던
    바 표면이 판정에서 빠져 넉넉한 표면만 남았다. **실기기 확인 포인트**: 4열(사이드바 0)에서 타일이
    가장 좁을 때 최장 라벨("max 5000MHz" 급)이 우하단 x축 표기와 가로로 부딪히는지.
- ※ A128(**하단 바 긴 그래프 = 축 라벨 숨김** — 32px 바에서 채널명·현재값과 겹치던 문제, v0.154.0)
  완료 — 결번. 부록 B 64의 "새 구조" 대신 숨김 택1 그대로.
  - **구현**: `RenderSparkline`의 showAxis 판정에 `!graph.InBar`를 앞세운 1줄 —
    `var showAxis = !graph.InBar && w >= AxisMinWidth && scale.HasEverHadValue;`
    바 표면은 y·x축 라벨 둘 다 끝까지 Collapsed(바 = 흐름 확인용, 채널명+현재값만. 최대치·시간축은
    센터 타일·좌 대형 몫 — x축 "5m" 표기도 바에서는 소멸).
  - **무변경 확인**: `ClearSparkline`(이미 라벨을 비운다)·A54의 inBar 헤더 수직 중앙 회피·
    A122의 비-바 `header.SizeChanged` 배선.
  - **부수 정리(고아 제거)**: 바 표면이 판정에서 빠지면서 A62·A70의 `AxisMinWidthNow`
    (= `AxisMinWidth × _state.BarScale`)가 **적용 지점을 잃어** 제거했다 — 참조가 `RenderSparkline`
    1곳뿐이었음을 grep으로 확인. 상수 `AxisMinWidth`는 비-바 전용으로 존치.
    `UpdateSensors → UpdateGraph → RenderSparkline` 연쇄를 A70이 인스턴스 메서드로 승격한 근거가
    이 프로퍼티였지만 **서명은 그대로 뒀다**(호출부를 흔들 이유가 없다 — 사유는 주석에 이력으로 남김).
    `ApplyScaleToLongGraph`의 축 라벨 FontSize 배수 2줄도 표면 구성이 공용(`MakeGraph`)이라 존치.
- ※ A122(**그래프 좌상단 채널명·y축 라벨 겹침 수리** — 비-바 표면의 y축 라벨을 헤더 줄 아래로,
  v0.147.0) 완료 — 결번. 원인은 A119(v0.145.0)로 타일이 정사각형이 되며 축 라벨 표시 임계
  (`AxisMinWidth` 90)가 충족돼 잠복이 드러난 것(구 8열 ~40px 타일에서는 라벨이 숨어 안 보였다).
  - **구현**: `HardwareView.MakeGraph`(등재 시 표기한 `CreateSensorGraph`는 실코드에 없는 이름 —
    이 메서드가 타일·좌 대형·하단 긴 그래프 표면의 공용 생성부다) 끝에 **비-바 표면만**
    `header.SizeChanged`를 걸어 `yAxisText.Margin = new Thickness(0, header.ActualHeight, 0, 0)`.
    실측이라 A62 배수·글꼴 변화에 자동 추종한다(수치 하드코딩 없음). 하단 바 표면(헤더 수직
    중앙 회피)·xAxisText(우하단)는 무변경, 수정 파일 1개·XAML 0줄.
  - **배선은 생성부 1회**: 렌더 루프(RenderSparkline)는 축 라벨의 표시/숨김·글자만 만지고 Margin은
    건드리지 않는다(매 틱 레이아웃 무효화 금지 — 등재 시 선지정한 함정).
  - **A119 조기 반환과의 무간섭**: 핸들러에 **같은 값 가드**(0.5px 허용 오차 — A119
    `ApplySquareTileSizes`/`ApplySquareBigSizes`와 같은 관례)를 둬, 헤더 폭만 바뀌는 리사이즈
    (= 한 변 재계산 경로)에서는 첫 대입 뒤 전부 조기 반환이다. 표면 크기는 A119가 명시 픽셀
    (Root.Width/Height)로 잡아 두므로 라벨이 내려가도 표면이 커지지 않아 한 변 계산에 되먹임이 없다.
  - **구독 해제 불요**: header·yAxisText는 같은 표면의 자식이라 수명이 동일하고 **인스턴스 이벤트**다
    (§3.4 static 이벤트 누수 규칙 = A88 `CompositionTarget.Rendering` 부류와 무관).
- ※ A124(**"Restart as admin" 후 창 세트 복원** — 종료 직전 창 세트를 세션 파일로 넘겨 승격
  프로세스가 재현, v0.149.0) 완료 — 결번. (2026-08-16 실기기 보고: 창 2개 이상에서 Restart as
  admin 시 1창만 되살아남. **전 창 종료 자체는 불가피** — 승격은 프로세스 단위·모든 창이 한
  프로세스(A68/A100). 본 항목은 재기동 후 재현. A47이 해결되면 발동 빈도는 급감하나 버튼이 남는 한 유효.)
  - **설계 확정 5건**: ① 직렬화 범위 = **모듈 ID가 있는 창 전부**(설정 화면·빈 셸·미지원 안내
    창은 `CurrentModuleId`가 null → 자연 제외) × {모듈 ID, 파일 경로(null 가능), 위치·크기·
    최대화 — A55 저장 항목·선택 규칙 준용(공용 `BoundsForPersist`), 휘발 상태(미저장 편집·재생
    위치·오버레이)는 제외}. ② 전달 매체 = `%AppData%\KOTU\restart-session.json`
    (`RestartSessionFile`) — UTC 타임스탬프(ISO "O") **2분 이내만 유효**, 읽는 쪽이 읽는 즉시
    삭제(정리 책임 = 읽는 쪽), 무효 파일도 조용히 삭제·무시. ③ 복원 = 시작 시 유효 세션이 있으면
    기본 1창 대신 생성 순서대로 재현(`WindowManager.TryRestoreSession`) — 파일 있으면
    OpenFileInNewWindow와 같은 순서(Create→OpenFile→Activate, 확장자 라우팅 재사용), 없으면
    OpenNewWindow와 같은 순서(Create→OpenModuleById→A81 기본 도크→Activate). 기하는
    `ApplySessionBounds`가 A40 최소 클램프 + A55 화면 밖 보정(ClampToWorkArea)을 재사용.
    ④ 실패 폴백 전부 조용히 — 직렬화 실패 = 세션 파일 없이 종전 1창 재시작, 파싱 실패·창별 복원
    실패 = 그 창만 건너뜀, 전부 실패 = 기본 1창. ⑤ 미저장 가드(A37)는 **현행 유지** — 현행 runas
    경로는 가드를 타지 않는다(Application.Exit — 묻지 않고 닫힘). 스냅샷도 가드와 무관하게 기록.
  - **함정 대응 기록**: 쓰기는 runas 직전·창 전부 생존 시점(하드웨어 모듈 → Core `RestartSession`
    훅, 모듈은 Core만 의존하므로 셸(App)이 Writer/Discarder 배선), **UAC 취소 catch에서 파일
    되지움**(Discarder — 취소 후 2분 내 일반 재시작이 세트를 재현하는 사고 방지). 승격 프로세스의
    %AppData%는 같은 사용자 프로필이라 파일 교환 성립 — 세션 파일은 읽고 지울 뿐, 설정 저장
    경로는 불관여. 복원 창 연속 생성은 단일 UI 스레드(A110)에서 동기 완결(빈 창의 미저장 가드가
    완료 태스크 → await 동기 연속) — 경합 없음, 마지막 창 포커스만 남는 것은 수용. A55 저장은
    현행대로(재시작 종료 때 창마다 저장·복원 창이 다시 덮는 것 모두 정상 — 세션 파일이 우선).
    JSON은 System.Text.Json 직접 직렬화(SponsorAds.Entry DTO 관례·새 패키지 없음). 기록 파일이
    사라진 창은 그 모듈의 빈 컨텍스트로 후퇴, 모듈 ID까지 무효면 그 창은 건너뜀.
## 8.1 All Readable 모듈 (신규)

- ※ A59(**All Readable 통합 모듈 신규** — 모든 지원 형식을 한 창에서 열어보는 모듈, v0.113.0) 완료 — 결번.
  ⚠️ **번호 배정(7번·"1~6 근육기억 유지")은 A96(v0.116.0)이 대체·구현 완료** — All Readable = **번호 1**,
  기존 모듈은 순서 그대로 한 칸씩 밀려 이미지 2 ~ 정보 7. A59 기록의 "번호 7"은 구현 당시 값으로 읽을 것
  (현행 번호의 원본은 A34 키 맵 표).
  (구현 상세·신규 모듈 추가 대조표 → docs/REQUIREMENTS-ARCHIVE.md)
## 9. 설정

- ※ A21(현재 윈도우 디스플레이 배율 % 표시 — Display 섹션, 모니터별·변경 추종, v0.67.0) 완료 — 결번.
- ※ A44(A21 보강 — 배율 목록 항목 옆 "(current Windows setting)" 표기, 일치 없으면 별도 안내 줄, 기존 텍스트 줄 제거, v0.68.0) 완료 — 결번.
- [미반영 A48 · Fable] **윈도우 디스플레이 배율을 앱에서 직접 변경** (A21/A44 확장) — 현재는 현재 배율 **조회·표시**만 가능.
  우리 앱에서 Windows 쪽 배율 설정까지 바꿀 수 있게.
  조사 필요(구현 전 선행): Windows에 공개 API가 없어 실무상 두 갈래 —
  ① 비공식 `SetDisplayConfigDeviceInfo`(DISPLAYCONFIG_DEVICE_INFO_SET_DPI_SCALE, 문서화되지 않은 구조체) —
     즉시 적용되지만 비공식(A38 UserChoice와 같은 성격의 리스크: OS 업데이트로 깨질 수 있음),
  ② 레지스트리 `HKCU\Control Panel\Desktop\PerMonitorSettings\{모니터}` 의 `DpiValue`(권장 배율 대비 상대 단계) 기록 —
     **로그오프/재로그인 후 적용**되는 경우가 많음,
  ③ 폴백: `ms-settings:display` 딥링크로 Windows 설정 화면을 열어주기(A25와 같은 보조 자동화 방식).
  구현 시 결정: 모니터별 적용 대상(현재 창이 있는 모니터 vs 선택), 즉시 적용 실패 시 안내 문구,
  실패 시 ③ 폴백 자동 전환. ※ 앱 자체 UI 배율(A41)과는 별개 기능 — 설정 화면에서 구분되게 표기.
- ※ A38(연결 토글 ON 시 UserChoice(ProgId+Hash) 직접 쓰기로 기본 앱 강제 지정 — 해시는 UserChoiceHash로
  자체 구현(외부 exe 없음, Mozilla WindowsUserChoice·PS-SFTA 알고리즘), 부모 키에서 UserChoice 삭제·재생성으로
  ACL 우회, 쓴 뒤 실제 LastWrite 기준으로 재검증(분 경계 최대 3회 재시도)·실패 확장자는 A25 설정 딥링크로 폴백,
  Mozilla gtest 실측 벡터 5종으로 알고리즘 검증(UserChoiceHashTests), v0.85.0) 완료 — 결번.
  ※ 알려진 리스크(수용): 비공식 — Windows 업데이트로 해시가 바뀌면 무력화 가능. Win11 23H2+ UCPD가 일부
  보호 확장자(http/https/.pdf 등) 쓰기를 차단(문서 모듈 .pdf는 폴백 예상) — 검증+폴백이 안전망. 백신 오탐 유의.
- ※ A35(**연결 섹션 순서 변경**, v0.87.0) 완료 — 결번. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A36(**설정에 "Open settings.json" 버튼** — 설정 파일을 KOTU-doc 에디터로 직접 열기, v0.109.0) 완료 — 결번. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A26(**업데이트 주기 체크 + 토스트**, v0.105.0) 완료 — 결번. ⚠️ **A95(v0.117.0)가 전량 제거 — 현행은 A95를 볼 것.**
  남아 있는 것: 전역 싱글턴 `UpdateCoordinator` · `update.lastCheckError`. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A76(**업데이트 섹션 보강 — 오토체크 토글 + 마지막 확인 시각**, v0.105.0) 완료 — 결번. A26과 함께 구현.
  ⚠️ **오토체크 토글(`update.autoCheck`)은 A95(v0.117.0)에서 제거됐다. 마지막 확인 시각만 남는다.**
  남은 설정 키(공유): `update.lastCheckedAt`(ISO 8601 **UTC** `O`, 없으면 빈 문자열) ·
  `update.lastCheckError`(빈 문자열 = 성공).
  표시는 로컬 시각으로 변환(저장은 UTC): `Last checked: 2026-08-12 14:32`, 없으면 `Last checked: never`,
  실패면 `Last checked: 14:32 — failed (사유)` — 사유는 예외 메시지 첫 줄만, 80자 초과 시 말줄임.
  업데이트 불가 빌드에서는 표시를 **숨기지 않고 비활성(Disabled)** 으로 둔다.
- ※ A95(**업데이트 체크 수동 전환 — 주기 검사·토스트·오토체크 토글 제거**, v0.117.0) 완료 — 결번.
  ※ 같은 주제의 **세 번째 정책 반전**이다: v0.17.0 "주기 체크 금지, 설정 진입 시에만" →
  v0.27.0 "설정 화면 체류 중 1분 카운트다운" → v0.105.0(A26·A76) 주기 체크+토스트 도입 →
  **v0.117.0 재폐기(현행)**. 사용자가 이력을 아는 위에서 내린 최신 지시다. 또 뒤집기 전에 이 줄을 읽을 것.
  - 설정 업데이트 섹션 = 위에서부터 **현재 버전 · 최신 버전 · 마지막 확인 시각 · [Check now][Update to vX] ·
    안내 문구**(안내는 버튼 줄 **밑**). 최신 버전 줄은 `Latest version: vX` / 한 번도 확인 전이면
    `not checked yet` / 확인 실패면 `unknown`. 최신일 때만 `You are on the latest version.`을 띄우고,
    확인 전에는 안내를 비운다. 확인 실패는 기존대로 `Update check failed: …`.
  - **제거**: 타이머(`FirstCheckDelay` 30초·`CheckInterval` 10분·`DispatcherTimer`) · `AppNotification`
    토스트 발행/등록/클릭 처리 · 오토체크 토글(`update.autoCheck`·`SetAutoCheck`) ·
    `update.lastNotifiedVersion` · 토스트 클릭 전용 스크롤 경로(`WindowManager.ShowSettings` ·
    `MainWindow.ShowSettings(scrollToUpdates)` · `SettingsView.ScrollToUpdates`) — 다른 진입로가 없어 함께 삭제.
    구 설정 키는 settings.json에 남아도 **읽지 않는 잔여값**이라 마이그레이션은 넣지 않았다.
  - **유지**: 전역 `UpdateCoordinator`(확인 중 여부·마지막 확인 시각·발견된 업데이트를 여러 창이 공유 —
    설정 화면은 표시만 하고 확인은 전역 서비스가 소유) · 마지막 확인 시각 표시(A76) ·
    업데이트 불가 빌드의 비활성 처리 · `DownloadAndInstallAsync`(다운로드 → 확인 대화상자 → 적용, 자동 재시작 없음).
  - 체크는 **Check now를 눌렀을 때만** 돈다. 앱 시작 시에는 저장된 마지막 확인 결과를 읽기만 한다.
  - ⚠️ **A114(v0.136.0)가 4차 반전으로 대체** — 2분 주기 타이머 + 설정 진입 1회가 다시 들어왔다
    (토스트·오토체크 토글은 계속 없음). 이 항목의 "수동 전용"은 더 이상 현행이 아니다.
- ※ A114(**업데이트 체크 정책 4차 개정**, v0.136.0) 완료 — 결번. ⚠️ **같은 주제 네 번째 반전**
  (v0.17.0 → v0.27.0 → v0.105.0 → v0.117.0 → **v0.136.0**). 또 뒤집기 전에 A95 항목과 함께 읽을 것.
  - 확인 경로 3종이 전부 `UpdateCoordinator.CheckNowAsync` 하나로 모인다: **① 상시 2분 간격
    자동 확인**(A26의 DispatcherTimer 구조 복원 — 단 **첫 확인도 2분 뒤**, 30초 아님: 시작 직후
    네트워크 러시 회피) · **② 설정 화면 진입 시 즉시 1회**(SettingsView 생성마다) ·
    ~~**③ Check now**~~(A125/v0.148.0에서 제거 — 아래 ※. 현행 경로는 ①② 둘).
    확인 중이면 조용히 반환 = 중복 요청 방지. 타이머는 **프로세스당 1개**(정적 필드 + Initialize 1회 가드,
    창 수와 무관). 업데이트 불가 빌드에서는 타이머를 시작하지 않는다.
  - **알림 = (b) 조용히 반영만**: 팝업·토스트 없음(A26의 `AppNotification` 경로는 **복원 금지** —
    등록·발행·클릭 처리 전부 그대로 폐기 상태. `update.autoCheck`·`update.lastNotifiedVersion`도 부활 없음).
    새 버전은 설정 화면 업데이트 섹션(최신 버전 줄·[Update to vX])에만 나타난다.
  - 유지: 마지막 확인 시각(A76) · 다중 창 동기(`Changed`) · Velopack 확인 API 사용부
    (`UpdateService.CheckAsync` 재사용 — 새 API 없음) · 다운로드/적용 흐름.
    ※ **Check now 버튼은 A125/v0.148.0에서 제거**(자동 확인 ①②는 그대로 — 부록 B 61).
    이 항목의 "유지: Check now 버튼"은 더 이상 현행이 아니다.
- ※ A125(**업데이트 Check now 버튼 제거** — 확인 경로 ③만 삭제, ①② 자동 확인 유지, v0.148.0)
  완료 — 결번. ⚠️ 이 주제 **다섯 번째 변경**이지만 정책 반전은 아니다: 확인 빈도(2분 주기 +
  설정 진입 1회)는 그대로고 **사람이 확인을 시키는 손잡이만** 없앴다(2026-08-16 사용자 지시 —
  부록 B 61. 저장 기록(부록 B 57·A114 완료 서술)에는 버튼 유지로 남아 있었으나 현 지시가 우선).
  - 삭제: `SettingsView.BuildUpdatesSection`의 `checkNow` 버튼 · 클릭 핸들러 · `Render()`의
    `checkNow.IsEnabled` 갱신, 그리고 버튼이 하나만 남아 쓸모가 없어진 가로 `buttonRow` StackPanel.
    전부 **메서드 지역 요소**(지역 변수 2·람다 1·문장 1)라 고아 필드(CS0414 부류 — A95 교훈)는
    생기지 않았고, 저장소 전수 grep 잔존 참조 0건. 2차 고아도 없다 —
    `available`·`UpdateCoordinator.IsChecking`은 안내 문구 분기가 계속 쓴다.
  - `UpdateCoordinator.CheckNowAsync`는 **존치**(①②의 단일 종착점). 이름만 수동 버튼 유래로 남는다.
  - 레이아웃: `[Update to vX]` 버튼을 `Root`에 직접 넣고 `HorizontalAlignment=Left` 지정
    ("Open settings.json" 버튼과 같은 처리). 숨겨진 버튼 하나만 든 빈 가로 줄을 남기면 `Root`의
    Spacing 12가 위아래로 두 번 붙어 헛간격이 생긴다. 섹션 순서(현재 버전 · 최신 버전 ·
    마지막 확인 시각 · [Update to vX] · 안내 문구)는 그대로.
  - `You are on the latest version.`은 **버튼 전용이 아니었다** — `LastCheckedAt`이 채워졌는지로만
    갈려 ①②의 확인 결과에도 그대로 뜬다. 그래서 함께 지우지 않았다(코드 확인 결과).
  - 문서 연쇄(A87): docs/USER-GUIDE.md 8장 · site/guide.html Updates 절에서 "whenever you press
    Check now"를 걷고 "여는 것이 곧 확인"으로 갱신. docs/A86-keymap.md에는 해당 서술 없음(확인 완료).
- ※ A77(**확장자 연결 토글의 진행 상태·결과 즉시 반영**, v0.106.0) 완료 — 결번. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A78(exe 경로 변화 감지 시 탐색기 연결 자동 재등록, v0.89.0) 완료 — 결번.
  구현 시 결정은 부록 B #25 참조.
- ※ A25(기본 앱 지정 보조 자동화, v0.61.0) 완료 — 결번.

## 10. 아키텍처 (구조 작업)

- ※ A42(모듈별 설계 + UI/워커 스레드 분리 구조 정리, v0.62.0) 완료 — 결번.
  Core `WinUtil.Core.Threading`(ModuleWorker/PollingWorker/WorkContext)로 계약 통일,
  Hardware 폴러만 프로세스 공유(사용자 확정)·파일 모듈은 뷰별 워커, 임시 Task.Run 전량 제거,
  ARCHITECTURE.md §8 스레드 모델 문서화.

## 10.1 공통

- ※ A22(**드라이브 정보 표시 전면 개편** — no file open 시에만·전 드라이브·WMI 종류·사용률 막대·자동 스크롤(마퀴), 공용 `DriveStrip` + `IDriveStripHost` 계약, v0.108.0) 완료 — 결번. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A27(**하단 바 버튼 통일 — 1칸/2칸 2종 규격, 전역 스타일 5종 + SplitButton 인라인 3곳(이미지·문서·영상)**, v0.107.0) 완료 — 결번.
  ⚠️ **치수 현행 = A106(v0.132.0): 1칸 32×32 · 2칸 84×32 · 간격 6**(A97/v0.116.0의 36×36·84×36을 재개정) — 구 40×40·36×36 기록을 보고 되돌리지 말 것. 바 두께 44(A40)는 불변. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A34(**모든 버튼류에 핫키 부여 + 툴팁 표기**, v0.112.0) 완료 — 결번.
  전 모듈 하단 바·툴바 버튼을 훑어 **단독 문자 키**를 배정하고, 툴팁 끝에 `(키)`를 붙였다.
  아래 표가 **키 맵의 원본**이다(A87 단축키 장의 원본, A86 합의 표와의 충돌 검사 기준).

  **전역(셸) — 모듈과 무관하게 항상 같은 키**

  | 키 | 동작 | 비고 |
  | --- | --- | --- |
  | `` ` `` | 시작 메뉴 열기 | A32. **A34에서 툴팁 표기만 추가** — 툴팁이 `` Menu (`) `` 가 된다 |
  | 1 ~ 7 | 모듈 전환 — **1 All Readable · 2 이미지 · 3 영상 · 4 오디오 · 5 문서 · 6 압축 · 7 정보(H/W)** | A32. 번호는 **A96(v0.116.0)** 배열(A59의 7번 배정을 대체). 시작 메뉴 항목 툴팁에 이 숫자가 표기된다 |
  | 0 | Settings | A32. 번호 배열과 무관하게 고정 |
  | Shift+N | 새 창 | A84(구 Ctrl+N). 앱에 남는 Ctrl 조합은 문서 Ctrl+S 하나 |
  | F1 / F2 | 좌 파일 리스트 / 우 정보 패널 — 홀드/2초/2연타(A58 전이) + 열림 상태 1회 = 닫기 | A118(v0.144.0)이 A107의 Alt+Z/X를 대체(계보: A58 Alt/Shift → A86 Z/X → A107 Alt+Z/X). 문자 비생성이라 텍스트 입력 중에도 동작. 탐색기 표면 F2 = 이름변경(A94)이 원 기능 우선 |
  | Enter | 좌/우 오버레이 일괄 토글(상태별 분배 = `docs/A86-keymap.md`) | A86(v0.121.0). 원 기능 우선: 영상 전체화면·에디터 줄바꿈·탐색기 선택 열기 |

  **모듈별(동시에 두 모듈이 뜨지 않으므로 키 네임스페이스는 독립)**

  | 모듈 | 키 | 동작 | 눌렀을 때 | 신규/기존 |
  | --- | --- | --- | --- | --- |
  | 이미지 | R | 90° 회전 | 즉시 실행 | 기존(v0.29.0) — 표기 유지, 통과 규칙만 추가 |
  | 이미지 | A | 1:1 원본 크기 | 즉시 실행 | 신규 |
  | 이미지 | F | Auto-fit | **본체 클릭과 같은 동작**(플라이아웃 아님) | 신규 |
  | 이미지 | ←/→ · Delete · F11 · Esc | 이전·다음 / 휴지통 / 전체화면 / 복귀 | 즉시 실행 | 기존 — Enter 전체화면은 A86(v0.121.0)에서 제거(셸 Enter에 양보) |
  | 영상 | M | 음소거 | 토글 | 기존(v0.21.0) — 통과 규칙만 추가 |
  | 영상 | S | 재생 속도 | **드롭다운을 연다** | 신규 |
  | 영상 | C | 자막 | **플라이아웃을 연다** | 신규(S를 배속이 먼저 써서 캡션 관습 키 C) |
  | 영상 | A | 1:1 원본 크기 | 즉시 실행 | 신규 |
  | 영상 | F | Fit(마지막 옵션 재적용) | **본체 클릭과 같은 동작** | 신규 |
  | 영상 | Space · ←/→ · ↑/↓ · Enter/F11 · Esc | 재생/일시정지 · 5초 · 볼륨 · 전체화면 · 복귀 | 즉시 실행 | 기존 — Enter/F11 전체화면은 콘텐츠 열림 시만(A86: 빈 모듈에서는 셸 Enter 일괄 토글이 듣는다) |
  | 오디오 | M | 음소거 | 토글 | 기존(v0.75.0) |
  | 오디오 | S | 재생 속도 | **드롭다운을 연다** | 신규 |
  | 오디오 | Space · ←/→ · ↑/↓ · F11 · Esc | 영상과 동일 | 즉시 실행 | 기존 — Enter 전체화면은 A86(v0.121.0)에서 제거(셸 Enter에 양보) |
  | 문서 | A | 1:1 원본 크기(PDF) | 즉시 실행. 텍스트 모드에서는 버튼이 없어 무동작 | 신규 |
  | 문서 | F | Fit(PDF, 마지막 옵션 재적용) | **본체 클릭과 같은 동작** | 신규 |
  | 문서 | Ctrl+S · F11 · Esc | 저장 · 전체화면 · 복귀 | 즉시 실행 | 기존(A37·A84) |
  | 압축 | E | Extract here | 즉시 실행 | 신규 |
  | 압축 | T | Extract...(폴더 지정) | 폴더 대화상자 | 신규(E를 Extract here가 먼저 써서 "Extract **T**o"의 T) |
  | 압축 | C | New archive | **플라이아웃(ZIP/7z)을 연다** | 신규 |
  | 압축 | U | 상위 폴더로 | 즉시 실행 | 신규 |
  | 압축 | F11 · Esc | 전체화면 · 복귀 | 즉시 실행 | 기존 |
  | 정보(H/W) | C | Copy all | 즉시 실행 | 신규 |
  | 정보(H/W) | I | 센서 갱신 주기 | **플라이아웃을 연다** | 신규 |
  | 정보(H/W) | B | 하단 바 크기 S→M→L 순환 | 즉시 실행 | 신규 |
  | 정보(H/W) | P | Always on top(핀) | 토글 | 신규(Pin — 다른 모듈 A와 뜻이 겹치지 않게) |
  | 정보(H/W) | F11 · Esc | 전체화면 대시보드 · 복귀 | 즉시 실행 | 기존 |

  ※ O(열기)는 A99(v0.129.0)에서 버튼과 함께 소멸 — 파일 열기는 셸 S4 'Open file'(A90)로 일원화.

  **Z·X는 어느 모듈에도 배정하지 않았다** — A86(오버레이 좌/우 재정의)이 예약한 키였고,
  **v0.121.0에서 실제로 오버레이 키가 됐다**(위 전역 표 참고).
  ※ A118(v0.144.0)이 오버레이 키를 F1/F2로 재교체 — Z·X는 현재 다시 미배정(빈 키)이다.

  - 배정 규칙: ① 단독 문자 키만(조합키 신설 없음 — A32 관례). ② 니모닉 우선(동작 영어 이름 첫 글자,
    이미 찼으면 두 번째 글자나 관습 키). ③ **뜻이 같은 동작은 모듈 간 같은 키**(O 열기 · A 1:1 · F Fit ·
    M 음소거 · S 배속). ④ 토글 버튼은 누를 때마다 토글, 플라이아웃형은 목록을 열고,
    Fit·1:1처럼 본체 클릭이 곧 주 동작인 것은 본체 클릭과 같게 했다(표의 "눌렀을 때" 열).
  - **통과 규칙**(A32/A84 재사용 + A34 확장): 판정을 공용 파일 `src/Shared/HotkeySupport.cs` 하나로
    추출해 셸·전 모듈이 같은 코드를 쓴다(KOTU.Core는 UI 비의존이라 어셈블리 대신 csproj `Compile Link`로 공유).
    ① 텍스트 입력(TextBox·PasswordBox·RichEditBox) 포커스 → 키를 삼키지 않는다(문서 에디터 타이핑 우선).
    ② **탐색기 파일 리스트·폴더 트리 포커스 → 삼키지 않는다**(첫 글자 점프 우선). 표시는 셸이 거는
    `Tag = PassThroughTag` 하나로, 모듈은 셸 타입을 몰라도 판정된다. 압축 모듈의 **항목 목록은 제외** —
    행이 표시 전용 객체(ArchiveRow)라 첫 글자 점프 대상이 아니고, 항목을 고른 뒤 바로 E를 눌러야 하기 때문.
    ③ 셸 키(숫자·`` ` ``·Shift+N)는 ①만 적용 — 파일 리스트를 보면서도 모듈 전환은 되어야 한다(A32 동작 유지).
    ④ 버튼이 비활성·숨김이면 키도 무동작(키를 삼키지도 않는다).
  - **표기 규칙**: 키 문자는 `VirtualKey` 값 한 곳에서만 정하고 툴팁을 `"설명 (키)"`로 조립한다
    (`HotkeySupport.Tip`). 표시가 상태를 따라 바뀌는 버튼(Fit·바 크기)도 같은 상수로 조립해
    키와 표기가 어긋날 수 없다. 이 때문에 해당 버튼의 XAML 툴팁 속성은 제거하고 코드가 유일한 출처가 됐다.
    단독 문자 키의 액셀러레이터 선언도 XAML에서 코드로 옮겼다(R·M 포함 — **의미·키는 무변경**,
    통과 규칙과 표기 단일화를 위해). Space·화살표·Enter·F11·Esc·Delete·Ctrl+S는 XAML에 그대로 둔다.
  - **제외 대상과 이유**: 설정 화면의 토글·버튼(수가 많고 레지스트리 등 부작용이 크다. ⛶는 이미 F11),
    이미지 Delete 같은 파괴적 동작(이미 Delete 키가 있고 새 문자 키를 더하지 않는다),
    압축 Cancel(진행 중에만 뜨는 버튼 — 스친 한 글자로 긴 작업이 끊기는 손해가 크다),
    탐색기 툴바(위로·필터·정렬 — 통과 규칙상 그 표면에 포커스가 있으면 키를 흘려보내야 해서 자기모순),
    드라이브 줄(A22)의 동적 버튼(고정 동작이 아니다), 하단 바 슬라이더·텍스트(버튼류가 아니다).
  - 남은 특이점: 영상·문서 Fit 버튼 본체에 표시되는 `A`(Auto-fit 상태 표시, A30)와 **1:1 키 A**는
    글자만 같고 뜻이 다르다. 니모닉(Actual size)을 우선했고, 표기가 `Actual size (100%) (A)`로 명확해
    혼동은 툴팁에서 해소된다고 판단했다. A83(Auto-fit 재정의) 착수 시 이 표시를 함께 볼 것.
    ※ **A83·A111(v0.133.0)에서 해소** — 본체 `A` 표기 자체가 사라졌다(Contain 아이콘 / 1:1 텍스트).
    같은 개정으로 위 모듈별 표의 A·F 행은 **대상 버튼과 옵션 이름만** 바뀌었다(1:1 버튼 → Fit 버튼,
    Auto-fit → Contain. 키·눌렀을 때 동작은 무변경).
- ※ A84(**전역 단축키 Ctrl→Shift 전환**, v0.102.0) 완료 — 결번. 현재 유효: **Shift+N(새 창)·Ctrl+S(유일한 Ctrl 예외)**·홀드 취소 트리거(휠 포함).
  ※ 휠 줌 항목은 A98(v0.123.0)이 대체 — Ctrl+휠 복귀. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A97(**하단 바 버튼 10% 축소 + 간격 균일화** — A27 규격 개정, v0.116.0) 완료 — 결번.
  ⚠️ **아래 치수는 A106(v0.132.0)에서 32로 재개정됐다**(1칸 32×32 · 2칸 84×32 · 파생 수치 전부 재산정) —
  이 항목의 36을 현행으로 읽지 말 것. 확정·구현 내역:
  - **1칸 40×40 → 36×36, 2칸 84×40 → 84×36**(폭 84 유지 — "1칸 차지해야 한다고 말한 버튼들에 한해서"가
    사용자 원문). `App.xaml` 전역 스타일 5종 + **SplitButton 인라인 3곳**(이미지·문서·영상) 전부.
  - **간격 = 전 지점 6으로 통일**(구 4/10 혼재). 각 모듈 바의 부모 Grid `ColumnSpacing="6"` 한 곳에서
    정하고 개별 `Margin`은 걷어냈다(이미지·All Readable이 개별 Margin 10/12를 쓰고 있었다).
    바 좌우 **가장자리 여백은 간격이 아니므로 유지**(StartButton 좌 6, ModuleBarHost 우 12).
  - 연쇄 반영: `ModuleBarHost.Margin` 52 → **48**(= 좌 6 + 버튼 36 + 간격 6) ·
    인스턴스 배지(A2) `Margin` 32,2 → **28,4**(36px 버튼 우상단 유지) ·
    A62 `MaxCardHeight` 40 → **36** · A62 `BarFixedWidth` 458 → **380**
    (BarGrid 요소 재계수: 36+20+90+84+36+36+36 = 338, ColumnSpacing 6×7 = 42. 구 458은 근사치였다).
  - 하단 바 두께 **44(A40)는 불변**. A34 툴팁은 문자열만 다루고 위치는 WinUI 기본이라 영향 없음(확인 완료).
- ※ A98(**휠 줌 재정의 — Ctrl+휠 복귀 + 사진 휠 단독 줌 + 영상 줌 신규, Shift+휠 줌 제거**, v0.123.0)
  완료 — 결번. 확정 동작 표는 `docs/A86-keymap.md` 키 × 상태 매트릭스의 휠 2행. 구현 내역:
  - **이미지**: 콘텐츠 프레젠터 휠 = Ctrl 유무와 무관하게 줌(사진 특례 — 휠 단독으로도).
    Shift+휠 줌은 제거(Shift는 기본 처리에 양보). 줌 파라미터(노치당 10%, 10%~800%, 포인터 고정점) 현행 유지.
    구 **휠 이전/다음 탐색(v0.41.0)은 휠 단독=줌이 대체** — `OnScrollerWheel` 제거, 탐색 자체는
    ←/→ 키·하단 바 버튼(`MoveAsync`)으로 그대로 남는다.
  - **PDF**: 줌 수정자 Shift → Ctrl만 교체. 휠 단독=스크롤·Fit 추종 해제(A49)·노치당 10% 유지.
  - **영상(신규)**: Ctrl+휠 = **A30 libvlc `MediaPlayer.Scale` 기반 명시 배율 줌**(`VideoFitMode.Manual` 추가) —
    VideoView는 스왑체인이라 XAML transform 줌 불가. 현재 유효 배율(Scale==0 자동 맞춤이면 뷰포트·트랙
    크기로 레터박스 배율을 직접 계산 — FitWidth 계산식 재활용)에서 노치당 ×1.1, 0.1~8 클램프,
    수동 줌 시 Fit 추종 해제(A49 UX와 동일). 트랙 정보 전이면 무동작(안전). 휠 단독=볼륨 유지.
    줌 시 A13 칩 `Zoom n%`(볼륨 칩과 같은 전체화면 한정 패턴). 새 미디어 로드 시 리셋은 기존
    OpenPath의 Fit 회귀 + Playing 핸들러의 ApplyFitMode 재적용이 자동 수행(추가 코드 없음 — 확인 완료).
  - A84의 구 Ctrl+휠 차단 분기(이미지·PDF `e.Handled` 소비) 제거. **Shift+N·Ctrl+S·홀드 취소 트리거
    (Ctrl+휠 포함 — 위 A86 항목)는 무변경**(grep 확인).
  - 썸네일 뷰·오버레이는 ScrollViewer ZoomMode 기본(Disabled)이라 Ctrl+휠 줌 없음 — 코드 추가 없이 확인만.
    랜딩 페이지 표기 Shift+wheel → Ctrl+wheel 갱신.
- ※ A83(**Fit 옵션 4종 공통 — 이미지·PDF·영상**, v0.133.0) 완료 — 결번. A111과 한 배치로 구현.
  확정·구현 내역(2026-08-12의 "조건 적응형 재정의"는 2026-08-14 사용자 확정이 **대체** —
  규칙 표 초안 프로세스(부록 B 48)·보류 종결, `docs/A83-autofit-draft.md`는 폐기됨):
  - 옵션 = **100%(1:1) · Contain · Fit width · Fit height** 넷, 세 모듈 공통(텍스트 에디터 모드 제외).
    초기·리셋 모드는 3모듈 모두 **Contain**(구 Auto-fit/Fit 자리 승계).
  - **Contain = 축소만** — 뷰포트보다 클 때만 줄이고 작은 파일은 확대하지 않는다.
    이미지(MaxWidth/MaxHeight 상한)·PDF(zoom = min(1:1 배율, 가로, 세로))는 **이미 그 의미라 이름만 교체**.
    **영상만 실동작 변경**: 구 Auto-fit은 libvlc `Scale=0`이라 작은 영상을 확대했다 →
    레터박스 유효 배율(A98 계산식 공유, `ContainScale()`)이 **1 이상이면 `Scale=1`(원본 고정),
    1 미만이면 `Scale=0`(자동 축소)**. 트랙·표면 크기를 모르는 시점(파싱 전)에는 종전대로 `Scale=0`
    (안전) — 크기를 알게 되는 Playing·SizeChanged에서 재판정된다.
    **SizeChanged 추종 목록에 Contain 추가**(구 FitWidth/FitHeight만) — 안 하면 작은 창에서 100%로 굳는다.
    `CurrentEffectiveScale()`(Ctrl+휠 줌 시작 배율)의 Contain 분기도 1 상한을 함께 적용.
  - Fit width/Fit height는 현행 의미 유지(확대·축소 양방향). 줌(A98)·Fit 추종 해제 경로는 무변경.
  - 모드 기억은 현행 유지(신규 영속화 없음): 영상·PDF = 새 파일이면 Contain 회귀, 이미지 = 세션 안에서 유지.
- ※ A67(**시작 메뉴 스폰서 이미지 클릭 → 링크** — `Assets/sponsors.json`(파일명 매칭·http/https만), 4건 = GitHub 릴리스 URL(v0.115.0), 실패는 조용히 무시, v0.109.0) 완료 — 결번. (상세 → docs/REQUIREMENTS-ARCHIVE.md)
- ※ A40(**창을 작게 줄여도 하단 바는 유지 — 최소 720×540 + 하단 바 두께 고정**, v0.94.0) 완료 — 결번.
  확정(2026-08-10): 최소 창 크기 **720 × 540 DIP**, 하단 바 두께 **44px 고정**(창 크기와 무관.
  UI 배율(A41)에 따른 변화는 별개). 구현 내역:
  - 최소 크기는 `OverlappedPresenter.PreferredMinimum*`(WinAppSDK 1.4+, 현재 1.8) 대신
    **HWND 서브클래싱(`WM_GETMINMAXINFO`, WindowMinSize.cs)** 채택. 이유 두 가지:
    ① Preferred* 는 DPI 미인지 버그(microsoft-ui-xaml #10475, 1.7 기준 미수정)로 값이 물리 픽셀로
    쓰여 150% 모니터에서 실효 최소가 480×360 DIP로 줄어든다. ② `SetPresenter`(전체화면 왕복)가
    OverlappedPresenter를 재생성하면 값이 초기화된다(A39의 IsAlwaysOnTop과 같은 원인).
    서브클래스는 HWND에 붙어 **프레젠터 재생성과 무관 — 재적용 불필요**하고, DPI를 메시지 시점마다
    GetDpiForWindow로 읽어 모니터 이동·배율 변경에도 옳다(720×540 DIP × 배율 = 물리 px 하한).
    창 생성 경로가 MainWindow 생성자 하나뿐이라 다중 인스턴스 전부 적용. 창 크기 복원(v0.55.0)도
    구버전 저장값이 최소 미만이면 올려서 연다(프로그램 Resize는 이 메시지가 안 막으므로 별도 클램프).
    ※ **A61(v0.111.0): 하단 바만 남기고 접힌 동안에는 최소 높이 제약이 일시적으로 낮아진다**
    (`WindowMinSize.SetMinHeightOverride` — 폭 720은 그대로, 펼치면 540 복귀).
    여기 수치를 바꿀 때 A61의 오버라이드 경로를 같이 볼 것.
  - 하단 바: BottomBarRow `Auto`+MinHeight 44 → **고정 44**(전체화면 0은 유지). 이에 따라
    H/W 센서 카드의 2·3줄 접힘(v0.64.2)을 **항상 1줄 + 폭 부족 시 뒤 순서 카드부터 생략**으로 교체
    (카드 순서 = 사용자 확정 순서 = 우선순위, 폭이 돌아오면 복원. 카드 최소 폭 76 유지).
    맥박 그래프(A29)는 카드 10개가 다 안 들어가는 폭(바 폭 < 1240px)에서 먼저 숨긴다 — 정보가 장식보다 우선.
  - 720 폭 잘림 점검: 이미지·문서·압축·오디오·설정·H/W(위 처리 후)는 잘림 없음(가운데 텍스트 칸이
    말줄임 담당). **비디오만** 고정 요소 합이 초과해 **좁은 바(< 760px)에서 볼륨 슬라이더·시간 텍스트를
    숨김** — 볼륨은 ↑/↓·휠·음소거 버튼, 위치는 시크 슬라이더가 대신하므로 기능 손실 없음.
  - ※ A63(미리보기 아이콘 기준 1600×900)과 함께 창 크기 관련 수치의 상·하한이 정해진 셈.
- [미반영 A41 · Fable] **UI 배율 라이브 조절** (※ 2026-08-10 사용자가 **Ctrl + 마우스 스크롤**을 재차 요청 — 우선순위 상승):
  **Ctrl + +/−** 및 **Ctrl + 마우스 휠**로 UI 스케일(UiScale)을 즉석에서 조절.
  단계는 설정 Display의 배율 목록을 따르고, 변경 시 설정에 저장(모든 창 즉시 반영). Ctrl+0 이 Settings 단축키와
  겹치므로 배율 초기화 키는 별도 지정(예: Ctrl+*). ※ 이미지 줌(Ctrl+휠)·영상 볼륨 휠과의 충돌 정리 필요 —
  모듈 콘텐츠 위에서는 기존 동작 유지, 하단 바·빈 영역에서만 배율 조절 등의 규칙을 구현 시 확정.
- [미반영 A43 · Fable] **진짜 포터블 단일 파일 실행본** (설치본과 별개 배포물):
  - 현행 zip 방식의 한계(사용자 판단): 레지스트리 등록은 없어 "미설치"이긴 하나, 받아서 실행하면 압축이
    풀린 뒤 쓰는 방식이고 자동 업데이트·설정 저장이 설치본과 동일하게 동작함.
  - 요구 3가지: ① **단일 파일인 채로 바로 실행** (수동 압축 해제 없이 exe 하나)
    ② **자동 업데이트 대상 아님** (업데이트 체크·알림·적용 전부 비활성)
    ③ **완전 스탠드얼론 — 설정값 저장도 안 함** (%AppData%·레지스트리 등 시스템에 흔적 남기지 않기.
    설정은 세션 한정 메모리 유지 또는 기본값 고정 — 구현 시 확정)
  - 구현 아이디어(사용자 제안): 외부 라이브러리 사용권 요구사항 때문에 물리적 단일 파일이 어렵다면,
    **실행 시점에 임시/가상 공간에 자체 압축 해제 후 실행**하는 방식 검토.
  - 구현 시 조사할 것: .NET single-file publish의 네이티브 라이브러리 자체 추출
    (IncludeNativeLibrariesForSelfExtract — 임시 폴더 추출이라 사용자 제안과 같은 계열),
    libvlc처럼 플러그인 디렉터리 구조를 요구하는 네이티브 의존성 처리, WinUI 3 unpackaged
    single-file 지원 여부, MPL-2.0(LHM 계열)·LGPL(libvlc) 고지 파일 동봉 방법(추출물에 포함 등),
    포터블 모드 감지 방법(빌드 분기 vs 실행 시 판별), 임시 추출 잔여물 정리 정책.
- ※ A99(**모듈 열기 버튼 제거 — 파일 열기는 셸 S4 'Open file'(A90)로 일원화**, v0.129.0) 완료 — 결번.
  A107(O 소멸 예외)·A87(단축키 표)이 참조하므로 본문에 남긴다.
  - 원인(2026-08-14 사용자 보고): ☰ 옆 셸 'Open file'(A90 S4 진입)과 모듈 자체 'Open ...'(O 키,
    네이티브 파일 대화상자) 버튼이 같은 계열 아이콘이라 구분 불가 — 전 파일 모듈 공통 중복.
  - 확정(부록 B 55·58): **왼쪽(셸 S4)만 유지, 모듈 열기 버튼은 전 모듈에서 제거.** 네이티브 파일
    대화상자 진입로 소멸은 수용(S4 자체 탐색기가 대체). **S4 버튼에 새 핫키는 배정하지 않는다.**
  - 구현: 6표면(이미지·영상·오디오·문서·압축·All Readable 빈 상태 바)에서 버튼+칸 제거·Grid.Column
    재번호·`PickAndOpenAsync`류와 피커 using 정리. 압축의 풀기/새 압축 피커(`GetHwnd`)는 무관 — 유지.
  - 연쇄: **O(열기) 핫키 소멸**(A107 "문자 핫키 전부 유지"의 유일한 예외 — 부록 B 58), A34 키 맵
    표에서 모듈별 O 행 5개 제거(각주로 기록), 영상 압축 표시 임계 **760→718**(열기 버튼 제거로
    고정 요소 합 42px 감소 — 다른 모듈에는 하드코딩 폭 상수 없음), keymap(A86-keymap.md)은 O 언급
    자체가 없어 무변경. A87 단축키 장은 이 상태를 기준으로 쓰면 된다.
- ※ A106(**하단 바 버튼·시작 메뉴 10% 추가 축소** — A97·A96 개정, v0.132.0) 완료 — 결번.
  **1칸 32×32 · 2칸 84×32**(폭 84·바 두께 44(A40) 불변) = `App.xaml` 전역 스타일 5종 +
  SplitButton 인라인 3곳(이미지·문서·영상). **시작 메뉴 항목 40 → 36**(폭 136·구분선 여백 3·
  플라이아웃 밀착 −6은 유지. `MinHeight`만으로는 안 줄어서 A96 선례대로 상하 패딩 10 → 8도 함께).
  파생 수치 재산정: `ModuleBarHost.Margin` 90 → **82**(6+32+6+32+6) · `OpenFileButton.Margin`
  48 → **44**(6+32+6) · 인스턴스 배지(A2) 28,4 → **24,6** · A62 `MaxCardHeight` 36 → **32**
  (`BaseCardHeight` 36은 M 기준값이라 유지 → M·L 모두 32로 클램프) · A62 `BarFixedWidth` 380 →
  **364**(BarGrid 요소 직접 계수 32+20+90+84+32+32+32 = 322 + 간격 6×7 = 42 → 맥박 숨김 임계
  832+364 = **1196**) · 맥박 그래프 초기 높이 36 → **32** · 영상 볼륨·시간 숨김 임계 718 → **698**
  (1칸 버튼 5개 × −4). 1칸·토글 버튼 `Padding` 4 → **2** — 32에서 4를 두면 콘텐츠 상자가 22px라
  FontIcon 18(줄 높이 약 24)이 잘린다(2면 26px = A97과 같은 여유).
- ※ A107(**전역 키 체계 Alt 조합 전환** — A32·A86 전역 키 대체, v0.134.0) 완료 — 결번.
  A110(다중 인스턴스 키 꼬임 검증)·A108(문구·용어 재정리 — OverlayHints가 얹힐 자리)·
  A87(단축키 장)이 참조하므로 상세를 본문에 남긴다. `docs/A86-keymap.md` 개정 포함.
  - **키 대응표(구 → 신)**: \`(단독) → **Alt+\`** = 시작 메뉴 / 1~7·0(단독) → **Alt+1~7·Alt+0** =
    모듈 전환·Settings(배열은 A96 그대로) / Z·X(단독) → **Alt+Z·Alt+X** = 좌/우 오버레이.
    **단독 \`·숫자·Z/X는 폐지**(무동작 — 문자 입력·리스트 첫 글자 점프에 양보).
    무변경: A34 모듈 문자 핫키 전부(2026-08-14 확정 — O만 A99로 기소멸)·Enter 일괄 토글(A86)·
    Shift+N·Ctrl+S·F11·Esc·경계 버튼·휠(A98).
  - **전이 체계는 A86 그대로**(키만 교체): 홀드 = 반투명 오버레이 · 2초 홀드 = 고정(pinning) ·
    2연타 = 불투명 사이드바. **열림 상태에서 키 1회 = 닫기는 재검토 결과 유지** — 사용자가
    재검토를 언급했으나 폐지 확정이 없어 현행 유지로 확정(오케스트레이터, 2026-08-15).
  - **Alt+Z+X 동시 누름 = 좌·우 동시 호출 신설**: 상태머신의 "다른 키 개입 = 홀드 취소"에서
    **Z↔X 상호만 예외** — 한쪽 홀드 중 다른 쪽 down이면 그쪽도 독립적으로 같은 전이를 시작
    (순서 무관, 2초 고정·2연타 판정은 원래 사이드 객체별 분리라 그대로 독립). 부산물: 구 Z+X
    상호 취소용 `OverlaySide.KeyIsDown` 필드 제거(읽는 곳 소멸 — CS0414 예방).
  - **Alt 단독 OS 메뉴 모드 회피 재도입**(A86이 제거한 A58 방식의 개작): **"이번 Alt 세션에서
    우리 조합(Alt 액셀러레이터·Alt+Z/X)이 발화했으면 Alt 단독 up을 소비"**(`_altComboUsed` 플래그,
    KeyDown/KeyUp 루트 핸들러 층 — A58과 동일 층). A58의 "down을 본 Alt up 전부 소비"와 달리
    깨끗한 Alt 단독 탭은 통과(OS 메뉴 모드 보존)하고, down은 소비하지 않는다(메뉴 모드는 up 발동 —
    down 무조건 소비는 시스템 조합을 건드리는 반대 함정). Alt+Tab·Alt+F4·Alt+Space 무영향.
    Alt 자체는 홀드 취소 트리거("다른 키")에서 제외 — 수식키가 홀드를 즉사시키지 않게.
  - 구현 갈래: \`·숫자·0 = 기존 KeyboardAccelerator(AddShortcut)에 **Modifiers=Menu** 추가(A32
    텍스트 입력 통과 예외는 Menu 조합에 비적용 — 어디서나 동작. 툴팁 표기도 "Alt+n"으로) /
    Z·X = 기존 KeyDown/KeyUp 상태머신에 **Alt 눌림 게이트**(`GetKeyStateForCurrentThread(Menu)` —
    ExplorerFileOps.IsCtrlDown 선례와 동일 API. Ctrl+X 잘라내기와 수식키로 실구분). 단독 Z/X
    폐지로 Q4 예외(리스트 첫 글자 점프 양보)·텍스트 입력 통과의 Z/X 특례가 코드에서 소멸 —
    "외부 탐색기에서 파일을 열어 시작하면 Z/X 무반응"(에디터·리스트 포커스가 삼키던 증상)이
    구조적으로 해소(A107의 계기). A92 문구는 **OverlayHints(신설)로 단일화** —
    "press Alt+Z/X to close"(XAML 기본 문구 리터럴 제거).
  - ※ 좌/우 키 Alt+Z/X는 **A118(v0.144.0)이 F1/F2 단독 키로 대체** — Alt+\`·Alt+숫자와 Alt 단독 up 조건 소비(`_altComboUsed`)는 그대로 유효.
- ※ A111(**Fit·1:1 버튼 전 모듈 규격 통일** — A49 "전 모듈 점검"의 후속, v0.133.0) 완료 — 결번.
  A83과 한 배치. 확정·구현 내역:
  - **A30 상태형 SplitButton으로 3모듈 통일**: 본체 = 마지막으로 고른 옵션 표시(1:1 텍스트 /
    Contain E9A6 / Fit width E8AB / Fit height E8CB) · 본체 클릭 = 그 옵션 재적용 · 화살표 =
    A83 4옵션 플라이아웃(위에서 `100%` · `Contain` · `Fit width` · `Fit height`. **100%만 아이콘 없음** —
    검증된 새 글리프를 쓰지 않는다). 이미지 모듈의 구규격(비상태형·2옵션)이 여기서 정리됐다.
    텍스트 에디터 모드는 종전대로 버튼 자체가 Collapsed.
  - **1:1 별도 버튼(`ActualSizeButton`)은 3모듈 모두 제거**(100%가 플라이아웃 옵션이라 중복 — A99 계열).
    Grid.Column 재번호: 이미지 5칸(Fit 4→3·⛶ 5→4) · 영상 10칸(Fit 9→8·⛶ 10→9) · 문서 6칸(Fit 5→4·⛶ 6→5).
  - 연쇄: **영상 볼륨·시간 숨김 임계 698 → 660**(1칸 버튼 32 + 칸 간격 6 감소 = −38. 요소 직접 계수는
    `UpdateCompactTransport` 주석 참조) · 다른 모듈에는 하드코딩 폭 상수 없음.
  - **A·F 키 재배선(A107 "문자 핫키 전부 유지" 준수)**: 대상 버튼만 Fit 버튼으로 옮기고 의미는 무변경 —
    **A = 100% 적용**(= 플라이아웃 100%와 같은 액션이라 본체 표시도 1:1로 바뀐다),
    **F = 본체 클릭과 동일**(마지막 옵션 재적용). 3모듈 다 `Bind`(툴팁 고정) 대신 `Register` +
    `UpdateFitButton()` 자체 툴팁으로 통일했고, 본체 툴팁이 두 키를 함께 표기한다
    (`"<옵션> (F) · 100% (A)"` — 표기는 A34 규칙대로 키 상수에서 조립).
    문서 모듈은 A의 가늠자가 구 1:1 버튼 → Fit 버튼이 됐지만 둘 다 PDF 모드에서만 보이므로 동작 조건 무변경.
  - ※ A34 키 표(위 A34 항목)의 "이미지 F = Auto-fit" 및 A 행의 "1:1 버튼" 전제는 이 항목이 갱신한다 —
    키·의미는 그대로고 **대상 버튼과 옵션 이름만** 바뀌었다(Auto-fit → Contain).
- ※ A112(**마우스 뒤로가기 = 콘텐츠 닫기(썸네일 뷰 복귀)**, v0.140.0) 완료 — 결번. 구현 요지:
  - **XButton1**(RootLayout PointerPressed, handledEventsToo — `PointerUpdateKind ==
    XButton1Pressed` 눌림 전이만) + **키보드 Browser Back**(`VirtualKey.GoBack`, OnRootKeyDown —
    오토리피트 무시·텍스트 입력 포커스 통과)이 같은 분배기 `TryNavigateBack`(MainWindow)으로 묶인다.
  - 분배(한 번에 한 층): ① 전체화면 = 해제만(`SetPresenter(Default)` — Esc의 A90 확정 순서
    "첫 회 = 전체화면 해제, 다음 회 = S4 복귀"를 셸이 직접 시행) → ② S4 = 진입 전 상태 복귀만
    (Esc와 동일) → ③ 콘텐츠 열림 = 닫기 → 그 모듈 빈 상태(S1, 마지막 폴더 유지) →
    ④ 콘텐츠 없음(S1·빈 셸·설정·H/W) = 무동작(소비도 안 함).
  - "콘텐츠 닫기"는 새 해체 코드가 아니라 **모듈 전환과 같은 `ShowModule`(빈 컨텍스트) 재사용** —
    미저장 확인(A37, 취소 시 무변경)·재생·핸들 정리(뷰 Unloaded, A59 경로)·제목 "KOTU"(A103)·
    트레이 유휴(A54)·드라이브 줄 복귀가 전부 기존 경로 몫. `defaultSidebars=false`라 A109의
    사이드바 기본 재적용을 타지 않는다 = **좌/우 사이드바 상태 그대로 보존**(요구 충족).
  - 범위 밖으로 보고만 된 후보: XButton2(앞으로 가기) 매핑 · S1에서 XButton1 = 폴더 Up.

---

## 11. 사용자 문서 (기능 · 조작법)

- ※ A87(**사용자용 기능·조작법 문서 1종 — 웹 게시 겸용**, v0.139.0) 완료 — 결번.
  - 산출물: **`docs/USER-GUIDE.md`**(마크다운 원본) + **`site/guide.html`**(웹 게시본, index.html의
    토큰·nav·footer를 그대로 복제한 단일 파일 정적 페이지) + `site/index.html` 내비에 `GUIDE` 링크 1개
    + README에 안내 1줄. 영어만·스크린샷 없이 텍스트만(부록 B 43).
  - **버전 표기 정책 = 첫머리 `As of v0.139.0` 한 줄뿐**(부록 B 58이 부록 B 43의 "버전 표기 방식만
    착수 시 확인"을 해소 — 릴리스마다 갱신할 의무 없음). 버전 문자열은 두 산출물의 머리글 각 1곳
    (`USER-GUIDE.md` 인용문 · `guide.html`의 `.asof` 배지)에만 있으므로 갱신 시 2곳만 고치면 된다.
  - 구성: 창 구조 → 모듈 7종(이미지·영상·오디오·문서(텍스트+PDF)·압축·정보·All Readable) →
    단축키 4표(전역 / 사이드바·오버레이 전이 / 모듈별 문자 키 / 마우스·휠) → 사이드바·오버레이 →
    자체 탐색기 + Open file → 파일 연결·시작 메뉴 → 다중 인스턴스 → 설정 → 트레이.
  - 서술 기준 = **v0.131.0~v0.138.0 개정 반영 후 상태**(A107 Alt 조합 · A99 O 소멸 · A111 Fit
    SplitButton · A98 휠 · A108 용어 · A109 도착 상태 · A116 25% · A60·A101 H/W 3구획 ·
    A114 업데이트 · A117 Date created · A103 제목 · A94 탐색기). 각 서술은 코드 grep으로 재확인했다.
  - **확신 부족으로 뺀 것**: ① 이미지 클릭 드래그 팬(전용 코드 없음 — 스크롤바만 언급) ·
    ② 시작 메뉴 스폰서 카드·워드마크(브랜드 에셋 레벨 ≥2 게이트, 기본 0이라 안 보임) ·
    ③ 설정 About의 Mission Statement 본문(`settings.ini`·이력 미수집 문구가 실제 구현
    `settings.json`과 어긋난다 — 문구 인용 없이 "미션 스테이트먼트가 있다"까지만 적었다.
    문구 정정은 별건) · ④ 첫 실행 환영 대화상자 · ⑤ Ctrl 홀드 정보 오버레이(A107 이후 코드에
    주석 흔적만 남고 실동작 없음) · ⑥ H/W 그래프 창 길이가 50ms·200ms에서 짧아지는 정확한 수치
    (본문은 "가장 빠른 설정에서는 더 짧다"로만 적었다).

- ※ A132(**USER-GUIDE·site 기능 명세 최신화**, v0.159.0) 완료 — 결번. 버전 표기 정책(부록 B 58 —
  릴리스마다 갱신 의무 없음)은 유지, 이번은 명시 요청에 따른 1회 최신화다. **버전 문자열은 4곳**
  (`USER-GUIDE.md` 머리 인용문 · `guide.html`의 `.asof` 배지 · `index.html`의 `.verline`과
  다운로드 `.sec-sub`) — 다음 갱신도 이 4곳만 고치면 된다. 자동화는 도입하지 않았다(수동 확정).
  - **반영한 것**: A94 3~6차 전부(충돌 대화상자 Replace/Keep both/Skip·폴더 병합·all 체크박스·
    Esc 취소 "n of m completed" / 진행 문구 "Copying 3 of 12..."(3건 이상) / Shift+Del 영구 삭제
    확인창(기본 Cancel·메뉴에는 없음) / 잘라내기 반투명 + Esc 해제 / 접근 거부 대화상자 →
    Restart as admin / 폴더 감시 400ms·폴더 소실 시 상위 이동 / 항목·폴더·빈 영역 우클릭 메뉴 3종 /
    다중 일괄 열기 상한 10 / OS 탐색기 잘라내기 왕복) · A120 900 중앙 컬럼 · A124 창 세트 복원 ·
    A127 "max" 접두 + A128 바 축 라벨 소멸 · A129 비디오·오디오 위 반투명 = 블러 없는 단색 ·
    A130 EOF 후 리사이즈 = 검정 정리 · A133 힌트 판(한 구절).
  - **범위 밖이었지만 함께 메운 누락**(A87 판이 v0.139.0 기준이라 v0.140.0~v0.145.0이 통째로
    빠져 있었다 — 전수 재검에서 드러남): A112 '뒤로'(마우스 XButton1·Browser Back, 3.6절 신설) ·
    A113 저장 전 3확인 + 저장 후 재검증 · A115 라인 가이드·¶··EOF · A105 창별 태스크바 버튼과
    32px 아이콘 3자 표기 · **A119**(정보 모듈 좌/우 = 셸 패널, 타일 2/3/4열 — 기존 "세 구획" 서술이
    낡아 정정).
  - **확신 부족으로 뺀 것**: ① A133 판의 정확한 규격(색·라운드·패딩 — 사용자에게 의미 없는 수치) ·
    ② 잘라내기 반투명이 **다른 앱이 클립보드를 바꿔도 남는** 한계(A94-matrix 명기 사항이나 사용자가
    관측할 조건이 좁고 오해를 부른다 — Esc·새 복사·붙여넣기 해제만 적었다) · ③ 폴더 감시가
    네트워크·접근 불가 경로에서 조용히 꺼지는 것(관측 불가 — "감시한다"만) · ④ A104 인스턴스 수
    상한(코드상 상한 부재 = 증빙 항목이라 문서화할 사용자 동작이 없다) · ⑤ A126 아이콘 inset 합성
    (렌더 디테일) · ⑥ A131 압축 더블클릭 수리(회귀 수리 — 사용자 서술 대상 아님) ·
    ⑦ A87이 뺐던 6건은 그대로 유지(이미지 드래그 팬·스폰서 카드·Mission 문구·환영 대화상자·
    Ctrl 홀드 오버레이·H/W 창 길이 수치).
  - **정정한 기존 서술 2건**: ① H/W "창이 세 구획" → "센터 그리드 + 좌/우는 셸 패널"(A119) ·
    ② "Restart as admin 버튼이 구획 위" → "하단 바 바로 위 줄"(SensorStrip = RootGrid Row 1).

---

## 부록 A. 할 일 후보 (확정되면 착수)

| # | 항목 | 규모 | 비고 |
|---|---|---|---|
| A4 | 자체 탐색기 파일 권한 조사 (OS 탐색기 임베드 재검토 포함) | 조사 | **후순위** |
| A11 | 영상 목록/현재 루프 옵션 (+폴더 연속 재생 설계) | 중 | |
| A14 | 화면 녹화(시스템 오디오+마이크 믹스) | 대 | 소속 모듈 미정 |
| A15 | 마이크 녹음 | 중 | A14와 함께 설계 |
| A19 | HW 랭킹 제출 | 대 | 서버 필요 |
| A41 | UI 배율 라이브 조절(Ctrl +/−, Ctrl+휠) | 중 | 이미지 줌·영상 볼륨 휠과 충돌 정리 |
| A43 | 진짜 포터블 단일 파일 실행본(업데이트·설정 저장 없음, 실행 시 자체 추출 검토) | 대 | 라이선스 고지·libvlc 구조 조사 필요 |
| A45 | HWP·오픈오피스 뷰어 (A16에서 분리) | 대 | **보류** — 렌더러 조사 선행 |
| A47 | 관리자 재실행 없이 센서 읽기(드라이버 서비스화 vs 비관리자 소스 대체) | 중 | 조사 선행, 백신·서명 리스크 |
| A48 | 설정에서 **Windows 디스플레이 배율 변경**(현재는 조회만) | 중 | 공개 API 없음 — 비공식/레지스트리/딥링크 폴백 |
| A115 | 편집기 라인 가이드·비가시 문자·형광 녹색 커서 | 중 | v0.142.0 ①~③ 구현(rect 실측 렌더) · ④ 커서 색만 잔여 — 공개 API 없음 판정(대안은 본문) |
| A134 | 좌 패널 트리-리스트 항해 동기(배선 1곳 누락) | 소 | 착수 가능 — 루프 가드 함정 본문 |
> 결번(완료): A1(v0.57.0), A2(v0.58.0), A3(v0.59.0), A23(v0.60.0), A25(v0.61.0), A42(v0.62.0), A17(v0.63.0), A18(v0.64.0), A24(v0.65.0), A31·A32(v0.66.0), A21(v0.67.0), A44(v0.68.0), A5(v0.69.0), A6(v0.70.0), A7(v0.71.0), A8(v0.72.0), A33(v0.73.0), A9(v0.74.0), A10(v0.75.0), A12(v0.76.0), A13(v0.77.0), A28(v0.78.0), A30(v0.79.0), A37(v0.80.0), A16(v0.81.0), A20(v0.82.0), A39(v0.83.0), A29(v0.84.0), A38(v0.85.0), A46(v0.86.0), A35·A52·A53·A56(v0.87.0), A64(v0.88.0), A78(v0.89.0), A82(v0.90.0), A80(v0.91.0), A50·A65(v0.92.0), A51·A75(v0.93.0), A40(v0.94.0), A55(v0.95.0), A49(v0.96.0), A66(v0.97.0), A57(v0.99.0), A58(v0.100.0), A81(v0.101.0), A84(v0.102.0), A68(v0.103.0), A69(v0.104.0), A26·A76(v0.105.0), A77(v0.106.0), A27(v0.107.0), A22(v0.108.0), A36·A67(v0.109.0), A73·A74(v0.110.0), A61·A62(v0.111.0), A34(v0.112.0), A59(v0.113.0), A88·A89(v0.114.0), A91·A92(v0.115.0), A96·A97(v0.116.0), A95(v0.117.0), A54(v0.118.0), A79(v0.119.0), A63·A93(v0.120.0), A86(v0.121.0), A90(v0.122.0), A98(v0.123.0), A85(v0.127.0), A100(v0.128.0), A99(v0.129.0), A102·A103(v0.130.0), A71·A72(A60 3차 흡수 — 2026-08-14), A70(v0.131.0), A106(v0.132.0), A83·A111(v0.133.0), A107(v0.134.0), A108·A116(v0.135.0), A109·A114·A117(v0.136.0), A101(v0.137.0), A60(v0.138.0), A87(v0.139.0), A110(코드 감사 — 코드 변경 0, 2026-08-15), A112(v0.140.0), A113(v0.141.0), A104·A105(v0.143.0), A118(v0.144.0), A119(v0.145.0), A123(사양 재확인 — 코드 변경 0, 2026-08-16), A120·A121(v0.146.0), A122(v0.147.0), A125·A126(v0.148.0), A124(v0.149.0), A94(v0.124.0~v0.153.0 — 6차 완주), A127·A128(v0.154.0), A133(v0.155.0), A129(v0.156.0), A130(v0.157.0), A131(v0.158.0), A132(v0.159.0)

## 부록 B. 확정된 결정 기록 (남은 작업 관련만)

2. "오디오 기능 분리" = 비디오/오디오 모듈 분리, 관련 전면 반영 → A10.
3. "zp-extract" = 우클릭 메뉴 표시명에 정확한 모듈명 ZP-zip → 7장 (재등록 확인 먼저).
4. 탐색기 = 자체 구현 유지, OS 권한 조사 후 재검토 (후순위) → A4.
5. 광고 = 시작 메뉴 위치가 전부. 미션 문구 = 설정 화면·설치 스플래시 현행 유지 (관련 요구 폐기 완료).
6. 메뉴 hover 도움말 = 1초 지연 툴팁 방식 → A1.
7. 메뉴 내 H/W·Settings 위치 = 현행 확정.
8. 녹화 = 화면 녹화(시스템 소리+마이크 믹스), 녹음 = 마이크 녹음 → A14/A15.
9. (2026-08-10) **앱 이름 = KOTU (King Of The Util)** → A46 (v0.86.0 완료). 이후 모든 신규 문구는 KOTU 기준으로 작성.
    범위 = **표시명 + 시스템 등록 ID**, 프로젝트·네임스페이스·exe는 `WinUtil.*` 유지.
    기존 사용자 설정·연결은 **이관하지 않는다**(구 등록만 첫 실행에 자동 청소). UserChoice는 토글 시 자동 재지정.
    앱 아이콘 중립 표식 = **"KO"/"TU" 2줄**(16px 가독성).
    ※ **A79(v0.119.0)로 갱신**: 브랜드 레벨 1부터 이 자리는 **발바닥**이 대체한다. 16px 가독성이라는
    이유는 그대로여서 발바닥도 래스터가 아니라 **벡터로 그린다**. 레벨 0(기본값)에서는 "KO/TU" 2줄 유지.
10. (2026-08-10) **개발 우선순위 = UI·코어 기능 우선** → 운용 규칙 4번. 부가 기능군은 후순위.
11. (2026-08-10) **메뉴 여는 단축키 = `~`(숫자 1 왼쪽)** — 이미 A32(v0.66.0)로 구현됨. 신규 작업 없음.
12. (2026-08-10) 표시명 **단수형 + Archive** → A52. v0.38.0 복수형·"ZIP", v0.75.0 "Music" 지정을 대체.
13. (2026-08-10) 오버레이 **좌=리스트 / 우=정보**로 스왑, 입력은 홀드/2초/더블클릭 체계 → A57·A58.
    A32의 "Ctrl 홀드 + 2연타 고정" 확정 내역은 A58로 대체된다.
14. (2026-08-10) 미리보기 아이콘은 **크기 고정·열 수 가변** → A63.
15. (2026-08-10) A56 인스턴스 번호 = **색상 배지 유지 + 제목 문자열에도 `[n]` 추가** (사용자 확정).
16. (2026-08-10) A35 연결 섹션 순서에서 **오디오는 비디오와 문서 사이** (사용자 확정 — 시작 메뉴 번호 순서와 일치).
17. (2026-08-10) **리브랜딩 범위 재확정** → A64 (v0.88.0 완료). 같은 날 A46에서 정한 "표시명 + 시스템 ID만"을 **대체**한다.
    저장소도 `zpstudios/zpro` → **`zpstudios/kotu`** 로 변경됨(사용자가 직접 수행).
    계기는 UAC 대화상자의 `WinUtil.App.exe` 노출. 사용자 지적의 핵심은 **"WinUtil이라는 단어가 나오는 것"** —
    "게시자: 알 수 없음"(코드 서명)은 **요구사항이 아니다**.
18. (2026-08-10) **최소화 = 트레이로 숨김**(작업표시줄·Alt+Tab에서 제거), **전 모듈 적용** → A69.
19. (2026-08-10) **New Instance는 시작 메뉴 항목만 제거**, Ctrl+N·Shift+더블클릭·우클릭·설정 토글은 유지 → A65.
20. (2026-08-10) 정보 모듈 좌/우 배치 = **좌 그래프(4열·정사각형) / 우 텍스트** → A60 내용 교체.
    피닝 개념 도입(기본 = CPU 온도·CPU 파워, 먼저 핀 누른 것이 앞), **피닝된 것만** 하단 바·트레이에 표시.
21. (2026-08-10) **H/W 정보를 더 보려면 인스턴스를 늘린다** (트레이 아이콘 1개 = 정보 2건이 한계) → A70.
    이 때문에 하드웨어 모듈의 프로세스 공유(static) 설계를 공유/인스턴스별로 나눠야 한다.
22. (2026-08-10) 최소 창 크기 **720 × 540**, 하단 바 두께는 창 크기와 무관하게 **일정** → A40 갱신.
23. (2026-08-10) 업데이트 체크 주기 **5분 → 10분** → A26 갱신. 오토체크 on/off는 **공유 설정** → A76.
24. (2026-08-10) A64로 exe명이 바뀌면서 드러난 구조적 문제 — **연결 등록에 exe 절대 경로가 구워진다** → A78.
    사용자 요청으로 별도 항목 등재. 이름·위치가 바뀔 때마다 수동 재등록을 요구하지 않는 것이 목표.
25. (2026-08-10) **A78 구현 결정** (v0.89.0): ① **조용히 재등록** — 알림·설정 표시 없음.
    ② 검사는 **모듈당 대표 확장자 1개** — `RegisterAssociation`이 모듈 전 확장자를 한 번에 다시 쓰므로 충분.
    ③ 실패는 무시 — 다음 실행에서 재시도. ④ 매 실행 확인, A46 청소와 같은 시작 워커
    (`App.ShellRegistrationMaintenance`)에서 수행. ⑤ 포터블 빌드의 동작 여부는 **A43 구현 때 결정**(현재 포터블 없음).
    ⑥ command를 못 읽거나 구 형태(모듈 단일 ProgID) 등록이면 어긋남으로 간주해 재등록 = 손상 복구 겸용.
    ⑦ UserChoice(A38)는 ProgID만 참조하므로 건드리지 않는다.
26. (2026-08-11) **오버레이 키에서 Ctrl을 Shift로 교체** → A58. 13번(A57 스왑)과 합치면
    **Shift = 우측 정보 오버레이 / Alt = 좌측 파일 리스트 오버레이**.
    A32(v0.66.0)의 "Ctrl 홀드" 구현과 관련 문구는 전부 Shift 기준으로 바뀐다.
    ※ 적용 범위는 **오버레이 키로 한정**해 기록했다 — 앱 전역의 다른 Ctrl 단축키(Ctrl+N, Ctrl+S, Ctrl+휠 줌)까지
    Shift로 바꾸라는 뜻인지는 확인 필요 목록에 올림.
27. (2026-08-11) **오버레이 기본 표시가 진입 경로에 따라 갈린다** → A81.
    파일 인자로 시작 = 오버레이 없음 / 인자 없이 모듈 실행 = 오버레이 있음.
28. (2026-08-11) **브랜드 이미지 에셋 제공** → A79 (v0.119.0 완료). 원본 시트는 `docs/assets/kotu-brand-sheet.png`에 보관.
    사용자 지시는 *"아이콘에 살짝씩 사용"* — **전면 교체가 아님**.
29. (2026-08-11) **개인 실명·개인 메일은 어디에도 표시되지 않는다** → A82 (v0.90.0 완료).
    저작 주체 = **`ZP Studios`** 한 이름으로 통일(제품명 KOTU와 다른 층). 커밋 메일 = `zpstudiosdev@gmail.com`.
    **git 이력은 재작성하지 않는다** — 앞으로의 커밋만 바뀐다(사용자 선택).
    앞으로 새 표기·크레딧을 만들 때는 항상 `ZP Studios`를 쓴다.
30. (2026-08-12) **A81 "오버레이 있는 상태" = 불투명 밀어내기, 좌·우 둘 다** (사용자 확정).
31. (2026-08-12) **Ctrl→Shift 교체 범위 = 앱 전역, 충돌은 개별 우회 설계로 강행** (사용자 확정) → A84 등재.
    **유일한 예외 = Ctrl+S(문서 저장)** — Shift 조합으로 성립 불가(에디터에서 대문자 입력)하여 관례 유지.
    26번의 "오버레이 키로 한정" 기록을 대체한다.
32. (2026-08-12) **A68 인스턴스 색 팔레트 = A2의 9색 유지 + 10번째부터 순환** (사용자 확정).
33. (2026-08-12) **A54(트레이 아이콘 버그)는 사용자가 증상을 확인한 뒤 재론** — 그때까지 착수 보류 유지.
34. (2026-08-12) **오버레이 키 = 좌 Z / 우 X** (사용자 지시) → A86. 26번(Shift=우/Alt=좌)과 13번의 키 부분을 **대체**한다.
    홀드·2초·더블프레스의 **동작 자체는 A58 그대로**. A84(전역 Ctrl→Shift)는 무관하게 유지 —
    오버레이가 Shift에서 손을 떼므로 Shift+N·Shift+휠과의 간섭이 오히려 사라진다.
    A81의 진입 경로별 기본 상태(파일 인자=없음 / 모듈 실행=좌우 도크)는 **재확인됨, 변경 없음**.
35. (2026-08-12) **오버레이/도크 관련 Enter 키 동작은 전면 제외** (사용자 지시, 같은 날 오후 — 오전의
    "Enter 3단계 토글" 지시를 **철회**한다). Enter는 종전 전체화면 토글만 하고 오버레이와 무관하다.
36. (2026-08-12) **A86은 착수 전 "상태 × 입력" 표로 사용자 확인을 받는다** (사용자 지시).
    지금까지의 오버레이 지시에 모순·미확정이 남아 있다는 사용자 판단 — 표가 그 검출 수단이다.
    이 표는 A87(사용자 문서)의 단축키 장으로 재활용한다.
37. (2026-08-12) **A36 = settings.json을 그대로 열고 표기만 실제 파일명에 맞춘다** (사용자 확정).
    저장 위치·포맷 이관(앱 옆 settings.ini)은 하지 않는다 — 포터블 요구는 A43 소관.
38. (2026-08-12) **3회전 배치 선정 = A26+A76 / A77 / A27 / A22** (사용자 확정, v0.105.0~v0.108.0).
    선정 기준 = 사양이 이미 확정돼 사용자 판단이 더 필요 없는 항목. A63은 A83(Auto-fit 재정의)과
    레이아웃이 겹칠 수 있어 이번 회전에서 제외.

39. (2026-08-13) **4회전 배치 선정을 오케스트레이터에 위임** (사용자 지시 — "확인 없이 자동으로 진행할 수
    있는 것들을 적당한 단위로 묶어, 한 묶음 당 하나의 서브 에이전트에서 **직렬로**"). 선정 결과 5배치 =
    **A36+A67 / A73+A74 / A61+A62 / A34 / A59** (v0.109.0~v0.113.0, CI 5/5 초록).
    선정 기준 = **사양이 확정됐거나 "구현 시 결정"을 오케스트레이터가 답할 수 있는 항목**.
    제외한 것과 이유: A86·A87(키 표 합의 선행) · A83·A63(규칙 표 합의 선행) · A85(재현 파일 필요) ·
    A79(범위·라이선스 답 필요) · A54(증상 확인 대기) · A60·A70·A71·A72(A70 저장 키가 미결) ·
    A41·A43·A47·A48·A11(조사·충돌 정리 선행).
    ※ 이 회전으로 **질문 없이 돌릴 수 있는 항목은 사실상 소진**됐다 — 다음 회전은 "확인 필요" 목록에
    답을 받는 것부터 시작해야 한다.

40. (2026-08-13) **A88 등재 — 맥박(EKG) 그래프는 연속 스크롤이어야 한다** (사용자 지시, v0.113.2 실기기
    확인 중). "그래프 출력 refresh는 가능한 많이, 최대한 부드럽게" — 데이터 갱신 주기와 렌더 주기를
    분리하라는 뜻. A29·A51의 기존 확정(창 길이·스파이크 형태)은 유지된다.
41. (2026-08-13) **A79 = 전 지점(①~⑥) 대상 + 단계형(레벨) 적용 구조** (사용자 확정 — "여러 단계를 두어
    사용자가 단계를 지정하면 바로 반영"). **에셋 출처 = 동료 반려견 사진을 재료로 한 AI 생성**(사용자
    제작·제공) — 라이선스 확인 요구 종결.
    **단계 구획·지정 UI도 같은 날 확정**(v0.119.0으로 구현): 레벨 **0/1/2/3** 4단계(0=현행 · 1=①② ·
    2=+③⑤ · 3=+④⑥), 지정은 **settings.json 키 `branding.assetLevel` 하나뿐 — 설정 화면 비노출**.
    UI 노출은 필요해지면 별도 항목으로 등재한다.
42. (2026-08-13) **A70 저장 정책 = 인스턴스별 영속 저장 폐기, 전역 1벌·마지막 조작 우선** (사용자 확정 —
    종전 "저장 키" 질문 대체). 그래프 선택은 마지막으로 조작한 인스턴스의 값 하나만 저장.
    **창 크기·위치는 마지막에 닫은 창을 새 창이 그대로 승계** → **A89 등재**(A55 계단식 오프셋 대체,
    닫기 직전 저장은 A55 현행 구조로 이미 가능).
43. (2026-08-13) **A87 = 저장소 마크다운 원본 + 웹 게시본 생성 / 영어만 / 스크린샷 없이 텍스트만**
    (사용자 확정). 버전 표기 방식만 착수 시 확인.
44. (2026-08-13) **A67 스폰서 URL = 4건 모두 GitHub 릴리스 페이지**(`https://github.com/zpstudios/kotu/releases`)
    **로 일단 연결** (사용자 확정). sponsors.json 반영은 1커밋짜리 — 진행 지시 시 적용.
45. (2026-08-13) **A54 = "인스턴스 1개당 트레이 아이콘 무조건 1개(5개면 5개)"가 기본 원칙** (사용자 확정 —
    증상 목록 수집을 대체). **각 아이콘의 모습은 사용자가 추후 상세 지시 예정** — 그 전까지 외형 관련
    구현 결정 보류.
46. (2026-08-13) **A85 재현 조건 = 이미지 모듈에서 자체 탐색기의 이미지 파일 더블클릭** (사용자 제보).
    같은-모듈 열기 경로 실패로 범위 좁혀짐 — 착수 가능.
47. (2026-08-13) **A73 50ms = 실기기에서 감당 확인** (사용자, v0.113.2) → **목록 유지 확정.** 상한 미설정
    유지, 경고 툴팁 현행 유지.
48. (2026-08-13) **A83·A86 진행 방식 확정** (사용자): 둘 다 **Claude가 초안 표를 만들어 사용자가 O/X로
    검토**하는 방식. 단 **지금 만들지 않는다** — A86은 사용자가 곧 줄 "좌우 오버레이 추가 요구사항"을
    받아 합친 뒤 다시 질문하고, A83은 오늘 나올 추가 피드백을 반영한 뒤 초안을 작성한다.
49. (2026-08-13) **오버레이·자체 탐색기 추가 요구 일괄** (사용자 지시 16건 중 1·2·3·4·5·6·8·9·16 —
    "동일한 내용을 또 이야기할 수 있으니 감안하라"는 전제와 함께):
    **A90**('오픈 파일'=자체 탐색기, 반투명 4구획, Esc/재누름=이전 상태 복귀 — 좌/우 닫기와 다른 개념) ·
    **A91**(주소·필터 줄 트리 위로) · **A92**(도크 문구 일시 표시) · **A93**(3구획 25:50:25 + 중앙 썸네일 뷰
    신설 + 드랍 규칙: 좌·중=탐색기 동일/우=열기·모듈 전환·새 인스턴스 + Drop here 인포로 이동 +
    썸네일 열 수 4/8) · **A94**(윈도우 탐색기 동등 동작) · **A98**(Ctrl+휠 줌 복귀, 사진은 휠만으로도,
    썸네일 뷰에선 줌 없음) 등재.
    **A86 갱신**: **Enter = 좌/우 일괄 토글 부활**(원 기능 우선 — **35번의 "전면 제외"를 대체**, 같은 주제
    세 번째 지시) + 경계선 마우스 버튼(근접 시 표시, 닫힘 상태에서 꺼내기 겸용).
    **A63은 A93에 흡수·결번**(크기 고정·열 수 가변 → 열 수 4/8 고정·크기 가변으로 대체).
    A81의 "중앙 탐색기 숨김" 결정도 A93이 대체.
50. (2026-08-13) **A95 등재 — 업데이트 백그라운드 주기 검사 제거, 수동 Check now 체계로** (사용자 지시).
    v0.17.0(주기 금지) → v0.105.0/A26(주기 도입, 23번) → **재폐기**의 세 번째 반전 — 이력 인지 위에서
    내려진 최신 지시. 새 버전 감지 시 업데이트 버튼 노출, 최신이면 "지금 최신 버전" 안내.
    토스트·오토체크 토글은 트리거 소멸로 함께 제거(구현 시 재확인).
51. (2026-08-13) **A96·A97 등재 — 시작 메뉴·하단 바 치수 개편** (사용자 지시 13·14·15):
    All Readable **1번**으로(A59 번호 결정 대체) + 구분선 2개(1↔2, 인포↔Settings) + 메뉴 폭 +10% ·
    항목 높이 −10% + 플라이아웃 작업표시줄 밀착(뒤 배경 비침 제거).
    하단 바 버튼 높이 −10%(40→36, 1칸 버튼은 정사각 유지해 폭도 36) + 간격 균일(A27 규격 개정).
52. (2026-08-13) **A54 트레이 표시 내용 상세 확정** (사용자 지시 10·11·12): 비디오=해상도·비트레이트 /
    사진=확장자·용량 / 오디오=시간·이퀄라이저 / 문서=확장자·용량 / 압축=종류+압축률(작업 중엔 진행률) /
    공통=콘텐츠 없으면 모듈 이름(표기는 A52의 **Archive** — 사용자의 "ZIP이 맞나?"에 대한 답).
    "인스턴스당 1개"(45번)·A70 저장 정책(42번)은 **같은 내용 재지시로 재확인** — 모순 없음.
    ※ 같은 날 후속 논의로 모듈 이름은 **3자 표기(IMG/VID/AUD/DOC/ARC/ALL)** 로 재확정 — A54 본문 참조.
53. (2026-08-13) **A86 키 동작 표 전 쟁점 확정** (`docs/A86-keymap.md` — Q1~Q10 사용자 O):
    Enter 원 기능 목록(탐색기 선택 열기·비디오 전체화면·에디터 줄바꿈, 그 외 일괄 토글) ·
    Enter 재호출 = **직전 구성 복원(세션 한정, 없으면 A81 기본)** · 리스트 포커스 Z/X = 타이핑 탐색 우선 ·
    S4(오픈 파일 탐색 모드)는 한 몸(Z/X·핫키 무시, Esc/재누름으로만 복귀) + **기존 구획 유지·없는 것만
    반투명 추가·중앙 반투명 썸네일** · 경계 버튼 = 불투명 도크 토글 · S3에서 Esc 무동작 ·
    Alt 회피 제거·홀드 취소 유지 · 상태는 좌만/우만을 분리한 6개(사용자 지적).
    36번(표 합의 선행)의 조건이 이것으로 충족 — **A86 착수 가능.**
54. (2026-08-14) **실기기 피드백 일괄 등재** (v0.126.0~v0.127.0 확인 중 — **A101~A117 신설 +
    A60 3차 교체**): 아이콘·타이틀바 표기 개편(A102·A103 — A68 배지/인스턴스 링·A56 대체),
    **전역 키 Alt 조합 전환**(A107 — A32·A86 키 대체, Alt+Z+X 동시 지원), 사이드바/오버레이 개념
    확정(A108 — "불투명·메인 축소 = 사이드바 / 반투명·덮음 = 오버레이"), 모듈 전환 기본 =
    사이드바(A109 — A81 유지 규칙 대체), **H/W 3구획 재구성**(A60 3차 — **A71·A72 흡수 결번**,
    시간 창 좌 10초/센터 30초/하단 5분), 업데이트 4차 반전(A114 — 2분 주기), H/W 트레이 이원화
    해소 방향(A101 — A18 축 폐지는 확인 후), 인스턴스 무제한(A104), 태스크바 아이콘 모듈화 질문
    → 등재(A105 — AppUserModelID로 가능 전망).
55. (2026-08-14) **54번 확인 필요 일괄 답변** (사용자): **A99** = 왼쪽(셸 S4 'Open file')만 유지,
    모듈 열기 버튼 제거("모듈별 열기는 불필요" — 이해 정확함을 Claude가 확인) / **A100 ②** =
    IsPromoted 자동 쓰기 **확정**(신규 슬롯이 기본 '끔'으로 계속 생기는 문제 — 사용자 추론 정확) /
    **A101** = A18 센서 트레이 축 폐지 확정 / **A60** = A17 카드 10개 대체 + 드래그 순서·기본
    선택(CPU 온도·파워) 승계 확정 / **A105** = 모듈별 → **인스턴스별** 정정(트레이와 정합) /
    **A107** = 홀드(오버레이·길게 = pinning)/2연타(사이드바) 전이 유지 + 문자 핫키 잠정 폐지
    / **A109** = 파일 직접 열기 무사이드바 유지 / **A117** = 정렬 키(date modified와 동일 방식).
    **A113** = "정합성 체크"의 원 의도는 개발 시점 철저 검증 + **런타임 체크 로직 추가 요청**
    (Claude 구체안 ⓐ~ⓓ 제시 — O/X 대기).
56. (2026-08-14) **추가 답변 2건** (사용자): **A113** = ⓐ~ⓓ 전부 승인 + **"경고만 하고 강행"
    금지 — 사용자에게 선택 기회를 줄 것**(저장 전 검출 = 진행/취소 대화상자, 저장 후 검출 =
    재시도/다른 이름 저장 제안으로 반영). **A83** = **이미지·PDF·영상 공통 Fit 4옵션 확정** —
    100%·Contain(한번에 다 보이게)·Fit width·Fit height. 조건 적응형(2026-08-12)·초안 표
    프로세스(48번)·사용자 보류 전부 종결, `docs/A83-autofit-draft.md` 폐기.
57. (2026-08-14) **A83 Contain = 축소만**(작은 파일 확대 안 함 — 현행 Auto-fit 의미와 동일) ·
    **A114 알림 = (b) 설정 화면 반영만, 토스트 없음** (사용자 확정). A83·A114 잔여 확인 소진 —
    둘 다 착수 가능.
58. (2026-08-14) **마지막 확인 2건 답변** (사용자): **A87 버전 표기 = "vX.Y.Z 기준"만 단다**
    (릴리스마다 갱신 의무 없음) / **A107 모듈 문자 핫키 = 전부 유지**(오전의 "아마 필요 없을 것"
    잠정 답을 대체 — O만 A99 버튼 제거에 따라 소멸). **확인 필요 목록 완전 소진 — 전 항목 착수 가능.**
59. (2026-08-15) **A118·A119 등재 확정** (사용자): 좌/우 패널 키 = **F1/F2**(Alt+Z/X **폐지** —
    "문제가 좀 많아서"), **전이는 기존 유지**(누르면 오버레이·2초 홀드=핀·2연타=사이드바 고정 —
    사용자가 기존 기획을 재확인, 홀드 임계 2초). **사이드바/오버레이는 전 모듈 공통**("파일
    모듈만"이 아니라 비디오·오디오·이미지·문서 등 전부 — 정보 모듈 포함, A119 전제). **개념
    재확인(A108과 일치)**: 불투명·메인 밀어냄 = 사이드바 / 반투명·메인 덮음 = 오버레이,
    **어느 형태든 표시 내용은 완전 동일** — 여닫는 제스처만 다름. H/W 정사각형 강제로 세로
    넘침 = **세로 스크롤**(축소 아님).

60. (2026-08-15) **A119 착수 확인 3건 답변** (사용자): ① 정보 모듈 **우 패널 = 스펙 텍스트**(현행
    우 구획 이동) ② **설정 화면은 사이드바/오버레이 대상 제외**(패널 콘텐츠 없음) ③ 정보 모듈
    진입 **기본 = 양쪽 사이드바 열림**(A109 규칙 그대로 — 메인 2개/줄로 시작). **A119 착수 가능.**

61. (2026-08-16) **실기기 보고 6건 일괄 문답** (사용자): **A120** = 본문 컬럼 중앙(각 줄 텍스트
    중앙 아님) — 최대 폭 900. **A123** = 작업표시줄 클릭→아이콘 소실이 A69 원 요구대로임을 확인
    받고 **현상 유지**("판단 보류" 명시 — 거슬리면 재론). **Check now 버튼 = 제거**(→ A125.
    사용자 회상 "며칠 전 다른 세션에서 결정"; 저장 기록 57번·A114 완료 서술은 유지였음 — 현 지시
    우선). **탐색기 더블클릭 새 창** = 설정 "Always open files in a new instance"가 ON이었음(A85
    검증 ② 테스트 잔재 추정) — OFF로 해결, 등재 불요 종결. A121·A122·A124는 제안 사양 그대로 수용.

62. (2026-08-16) **2026-08-14 실기기 피드백 프롬프트 전문 재대조** (사용자 제공 — "다른 누락
    없는지 체크"): 37개 서술 전수 대조 결과 **누락 2건** — ① 업데이트 "체크나우 날리고"(→ A125)
    ② 창 아이콘(파비콘) 글리프-링 겹침: 트레이 글자 축소(A102)만 하고 창 아이콘 본체는
    미조치(→ A126). 나머지는 전부 A101~A117·A60 3차·A110·A118/A119로 등재·구현 확인. 형태가
    바뀐 것: Alt+Z/X는 A118이 F1/F2로 재변경 / "열림 1회=닫기" 재검토는 유지 확정(A107) /
    커서 형광 녹색은 불가 판정(A115 ④ 잔여) / 그래프 DPI 불변은 "열 수 고정" 설계로 충족.

63. (2026-08-16) **A127 표기 확정** (사용자): y축 최대치 라벨 = **"max 100%" 형식**("0-100%"
    범위형 대신 max 접두 택1). 등재만 — 구현은 다음 지시 때.

64. (2026-08-16) **A128 방향 확정** (사용자): 하단 바 긴 그래프 라벨 겹침(32px)은 재배치 설계
    대신 **바에서 축 라벨 숨김** 택1 — 바는 채널명+현재값만, 축 정보는 메인·사이드 몫.

### 확인 필요 (다음 대화에서 사용자에게 물을 것)
- ※ **A119 해석 확인 3건은 부록 B 60으로 소진** — 착수 가능.
- **A115 ④ 커서 색 대안**(v0.142.0 불가 판정): 커스텀 캐럿 오버레이(이중 캐럿 문제) vs OS
  접근성 설정 안내 vs 보류 — 사용자 결정 대기.
- ※ 2026-08-14 답변으로 소진(54~58): 모든 당시 미반영 항목 착수 가능 상태였다(11회전에서 소화).
  ※ 2026-08-14 답변으로 소진된 항목: A99 방향(왼쪽 S4 버튼만 유지) · A100 ②(IsPromoted 확정) ·
  A101(A18 축 폐지 확정) · A60(A17 카드 대체·드래그/기본값 승계) · A107 전이 체계(홀드/2연타 유지) ·
  A109(무사이드바 유지) · A117(정렬 키) · A105(모듈별 → **인스턴스별** 정정).
- ※ **A83 규칙 표**는 부록 B 56(Fit 4옵션 확정)으로 종결 — v0.133.0 완료·결번, 목록에서 내림.
- ※ **A87 버전 표기**(릴리스마다 갱신 vs "vX.Y.Z 기준")는 **부록 B 58로 확정 — 목록에서 내림**.
  A87 자체도 v0.139.0으로 완료·결번.
  ※ 2026-08-13 확정으로 내린 항목: A79 범위·출처(41) · A70 저장 정책(42) · A87 형식·언어(43) ·
  A67 URL(44) · A54 원칙(45) · A85 재현 조건(46) · A73 50ms 유지(47).
  2026-08-12 확정으로 내린 항목: Ctrl→Shift 범위(31) · A81 형태(30) · A68 팔레트(32). A58 키 할당은 26번.
