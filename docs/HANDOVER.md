# KOTU 인수인계 문서

> **읽는 법**: 이 문서만 읽고 다음 세션이 이어서 일할 수 있게 유지한다. 일하는 방식은 `CLAUDE.md`,
> 남은 작업의 사양은 `docs/REQUIREMENTS.md`가 정본이고, 여기에는 **지금 상태·함정·사용자 몫·다음 수순**만 둔다.
> 세션별 작업 일지(무엇을 어떤 순서로 했는가)는 `docs/SESSION-LOG.md`, 실기기 확인 대기 누적분은
> `docs/DEVICE-CHECKS.md`로 분리했다(2026-08-28 문서 정리 — 본문은 git 이력으로 복원 가능).
>
> **최종 갱신: 2026-09-04 (v0.339.0 시점 — A345 완주(36~39차) → 결번 · **40차 A346(v0.339.0) 완료 · A347 흡수 · ✅ 사용자 실기기 확인(←/→ 순서 = 좌 리스트)** · **41차 A348(v0.340.0) 완료 — 항해 시점 통지 `ICurrentPathSource`** · **42차 A349 배치 1(v0.341.0) 완료 — 영상·오디오 ⏮⏭·Ctrl+←/→·PageUp/Down** · **43차 A349 배치 2(조사 · 코드 0) 완료 — `docs/A349-media-keys-research.md` · 권장 SMTC 단일안** · **44차 A349 배치 3(v0.342.0) 완료 — SMTC 미디어 키(`MediaTransport.cs`)** · **45차 A350(v0.343.0) 완료 — 플라이아웃 앱 이름·아이콘(사용자 스크린샷 '알 수 없는 앱' 즉시 수리)** · **다음 = 사용자 지시 대기**(A349·A350 실기기 회수 권장 · 후보: A343 ⓑ · 낙수 41) · 부록 B 확인 2건(A347 재현 여부 · A343 상한)** · A343 ⓑ 편집 상한 결정 대기 · 낙수 41 = 스트리밍 열거)** — 2026-08-31~09-04 세션에서
> ① 사용자 신규 요구 16건을 **A292~A307**로, 이어 3건을 **A310~A312**로, 마지막에 **A313~A316**을 등재하고
> (**A317·A318은 등재 없이 완료 처리** — 지시가 같은 날 구현·릴리스까지 갔다)
> ② A291 판정 스크린샷
> 2장을 수령해 **판정 종료·잔여는 A307 승계**(§4)한 뒤 ③ 여덟 묶음을 직렬로 완주했다 —
> **1차** A279·A280·A282·A278(v0.283.0~v0.286.0) · **2차** A293·A297·A298·A299·A306(v0.287.0~v0.290.0) ·
> **3차** A296·A159(v0.291.0) · **4차** A294·A295·A307(v0.292.0~v0.294.0) ·
> **5차(오디오)** A300·A301·A302·A303·A304(v0.295.0~v0.298.0) ·
> **6차(대형)** A292(v0.299.0) + A305 2배치(v0.300.0~v0.301.0) ·
> **7차(셸)** A310(v0.302.0) · A312(v0.303.0) · A311(v0.304.0) ·
> **8차** A313(v0.305.0) · **9차** A314(v0.306.0) · **10차(실기기 수리)** A315(v0.307.0) ·
> **11차** A316(v0.308.0) · **12차** A317(v0.309.0) · **13차** A318(v0.310.0 + 핫픽스 v0.310.1) ·
> **14차(문서) A308·A319 — 코드 변경 0 · 2배치**(배치 1 = 정본 `docs/USER-GUIDE.md` /
> 배치 2 = 미러 `site/guide.html`·`site/index.html`) ·
> **15차 A320(v0.311.0)** · **16차(문서) A321 — 코드 변경 0** ·
> **17차(문서) A322 — 코드 변경 0 · 3배치**(한국어 마크다운 → 한국어 사이트 + 언어 전환 → README) ·
> **18차 A309(v0.312.0)** — 낙수 31 승격, 셸 포커스 `null` 방어 ·
> **19차 A323(v0.313.0)** — 사용자 실기기 보고 2건, 좌 리스트 현재 파일 표시 + 재생성 제거(낙수 1 소진) ·
> **20차 A324·A325(v0.314.0)** — 사용자 스크린샷 1장에서 두 결함(트리·리스트 불일치 + 뷰 내부 항해
> 제목 미갱신), **한 배치 · 낙수 27 소진** ·
> **21차 A326(v0.315.0)** — Advanced options 등록 토글을 확장자 단위 → **모듈 단위**로(**A292 해석 오류의 정정**) ·
> **22차 A327(v0.316.0)** — 오디오 정보 패널에 **포맷상 정의된 속성 전부** 나열(없는 값은 빈칸 행) ·
> **23차 A328(v0.317.0)** — **영상**도 같은 방식으로(A327의 이식 · 두 축 불일치까지 해소) ·
> **24차 A329(v0.318.0)** — **문서·압축**까지(**부록 B 98의 마지막 이행** — 두 모듈은 열림 축에
> `IContentInfoProvider`가 아예 없었다) ·
> **25차 A330(v0.319.0)** — **사용자 실기기 보고 3건 수리**(시각화 잔상 · EQ 버튼 비활성 · 단일 파일
> 목록 루프. **등재 없이 구현→릴리스→완료 처리**) ·
> **26차 A331(v0.320.0 → v0.321.0 되돌림) + A332(v0.321.0)** — **이 세션 첫 되돌림**이다.
> A331은 사용자 보고대로 All Readable 좌 리스트를 자식 형식으로 좁혔다가(v0.320.0)
> **제기의 전제가 틀렸음이 확인돼 전량 원복**했고(v0.321.0 · 부록 B 101·102),
> 같은 배치에 **A332**(정보 패널 열림 축이 빈 값으로 굳는 결함 — **사용자가 신고하지 않은 결함을
> 스크린샷 배경에서 잡아낸 첫 사례**)를 실었다.
> **27차 A333(v0.322.0)** — **대용량 폴더 첫 진입에 좌 리스트·중앙이 몇 초 정지**(사용자 실기기 보고).
> **요구된 `Loading...` 표시는 A243이 이미 구현해 두었고**, 원인은 그 표시보다 **앞선 구간**의
> **UI 스레드 동기 커널 I/O**(폴더 감시자 생성·해제)였다 — **등재와 동시에 완료 처리**.
> **28차 A334~A339(v0.323.0~v0.327.0)** — **A333 뒤에도 "여전히 느리다"** 는 보고에서 시작해
> **고치지 않고 재는 배치(A334 계측판)** 를 먼저 넣고, 그 실측으로 **A335(우클릭 메뉴 즉시 조립 = 병목의
> 57%)** 와 **A339(타일 미리보기 즉시 시작)** 를 잡았다. 같은 구간에 사용자 보고·문의 3건
> (**A336** 선택 표시 둘 · **A337** 클라우드 전용 타일 사유 미표시 · **A338** PDF 기본 fit)이 실렸다.
> ⚠️ **A338은 A320의 오독 정정**이고(부록 B 108), **6건 전부 등재와 동시에 완료 처리**했다.
> ⚠️ **작업 방식의 선례** — **Opus 하위 에이전트가 529 과부하로 5회 연속 실패**해
> **사용자 확정("이 세션에서 직접")으로 오케스트레이터가 A336~A339를 직접 구현**했다.
> CLAUDE.md §2의 위임 원칙은 유지하되 **모델 용량 장애 시 직접 구현이 허용된다**(§2 아래).
> **결과 = 49건 · 46릴리스(v0.283.0~v0.327.0 — 핫픽스 포함)** — 신규 16건 + 구 항목 A159 + 2026-09-01 등재 3건
> + 2026-09-02 A313·A314·A315·A316(등재분) + A317·A318(등재 없이 완료) + **14차 A308·A319**
> + **15차 A320** + **16차 A321** + **17차 A322** + **18차 A309** + **19차 A323** + **20차 A324·A325**
> + **21차 A326** + **22차 A327** + **23차 A328** + **24차 A329** + **25차 A330**
> + **26차 A331·A332** + **27차 A333** + **28차 A334~A339**.
> ⚠️ **산식** — 종전 40건·38릴리스 + **A331(건 1 · 릴리스 2)** + **A332(건 1 · 릴리스 0)**
> = 42건 · 40릴리스 + **A333(건 1 · 릴리스 1)** = 43건 · 41릴리스
> + **28차 A334~A339(건 6 · 릴리스 5)** = **49건 · 46릴리스**.
> ⚠️ **28차의 건·릴리스가 1:1이 아니다** — **A336·A337이 v0.324.0 한 릴리스에 함께 실렸다**
> (건 6 · 릴리스 5). 버전 대응은 **A334 v0.323.0 · A336·A337 v0.324.0 · A338 v0.325.0 ·
> A335 v0.326.0 · A339 v0.327.0** 이고 **A번호 순서와 릴리스 순서가 다르다**(A338이 A335보다 먼저 나갔다 —
> 사용자 보고가 도착한 순서대로 처리했기 때문이다. **결번 줄도 이 순서로 적혀 있다**).
> ⚠️ **28차를 한 묶음으로 센 근거** = 다섯 릴리스가 **같은 대화 흐름 안에서 끊기지 않고** 이어졌고
> (계측 → 실측 회수 → 수리 → 재실측), 중간에 오케스트레이션 사이클이 새로 시작된 지점이 없다.
> 안에 **세 줄기**가 있다(성능 = A334·A335·A339 / 실기기 보고 = A336·A337 / 오독 정정 = A338).
> ⚠️ **되돌린 항목도 릴리스는 2개 나갔다** — A331은 **v0.320.0(구현) + v0.321.0(되돌림)** 으로
> 태그·릴리스가 **두 번** 발행됐고, **A332는 그 v0.321.0에 같이 실려** 릴리스를 새로 늘리지 않는다.
> 그래서 **건은 +2, 릴리스도 +2**다(A331에 2 · A332에 0). **철회는 건수에서 빼지 않는다** —
> 이 산식은 "완주한 작업 수"가 아니라 **"이 세션이 처리한 항목 수"** 를 센다(취소된 작업도 비용은 들었다).
> (그 앞은 39건·37릴리스 + A330(건 1·릴리스 1) ·
> 그 앞은 38건·36릴리스 + A329(건 1·릴리스 1) ·
> 그 앞은 37건·35릴리스 + A328(건 1·릴리스 1) ·
> 그 앞은 35건·33릴리스 + A326(건 1·릴리스 1) + A327(건 1·릴리스 1) ·
> 그 앞은 33건·32릴리스 + A324·A325(건 2·릴리스 1 — 한 배치) · 그 앞은 32건·31릴리스 + A323(건 1·릴리스 1) ·
> 그 앞은 31건·30릴리스 + A309(건 1·릴리스 1) ·
> 그 앞은 28건·29릴리스 + A320(건 1·릴리스 1) + A321·A322(각 건 1·릴리스 0)).
> 14차·16차·17차가 `docs/`·`site/`만 고친 문서 전용 묶음이라
> `Directory.Build.props`가 무변경이고 태그·릴리스가 발행되지 않았다(계산이 안 맞는 게 아니다).
> §4는 2026-09-01에 밀어내기를 실행했고, 그 위에 **7~13차 블록에 이어 15·18·19·20차 블록과
> 21·22차 합본 블록·23차 블록·24차 블록·25차 블록·26차 블록·27차 블록·28차 합본 블록까지 병치**해
> 지금 **19블록**
> (v0.295.0~v0.298.0 · v0.299.0~v0.301.0 · v0.302.0~v0.304.0 · v0.305.0 · v0.306.0 · v0.307.0 ·
> v0.308.0~v0.309.0 · v0.310.0 · v0.311.0 · v0.312.0 · v0.313.0 · v0.314.0 · v0.315.0~v0.316.0 ·
> v0.317.0 · v0.318.0 · v0.319.0 · v0.321.0 · v0.322.0 · **v0.323.0~v0.327.0**)이다.
> **다음 밀어내기 대상은 맨 앞 v0.295.0~v0.298.0 블록**이다(28차에서도 실행하지 않았다 —
> 오케스트레이터 지시가 없었고 1블록 규칙의 예외 상태가 그대로 유지된다).
> ※ **28차는 다섯 릴리스를 한 블록으로 묶었다** — 확인 시나리오가 릴리스를 가로지른다
> (①의 재측정 하나가 **A334 계측판 · A335 · A339를 동시에 판별**한다). 나누면 같은 지시를 세 번 적어야 한다.
> ※ **v0.320.0 블록은 만들지 않았다** — 그 릴리스의 내용(A331)이 v0.321.0에서 **되돌려졌으므로**
> 확인할 동작이 남아 있지 않다. 대신 **v0.321.0 블록 ⑥이 되돌림의 회귀 관문**이다.
> A291 판정 블록만 **A307 판정 대기**로 §4 맨 위에 존치.
> 직전 갱신(2026-08-29): A257~A277 21건 전량 완주(v0.257.0~v0.273.0·17릴리스)
> + A283~A291 완주(v0.274.0~v0.282.0 — 에디터 장식 9배치. **결함이 다섯**이었고 서로를 가리고 있었다:
> X축 누락(A286)·줄 탐색 정지(A287)·Y축 누락(A288)·가이드 밑변(A289)·빈 마지막 줄(A290).
> A285 계측 시설 없이는 못 잡았다).
> 태그·릴리스는 **v0.327.0까지 발행 확인(초록)** — **v0.323.0~v0.327.0 전부 초록**이다. v0.310.0은 CS0104로 한 번 깨졌고
> **v0.310.1로 수리·초록 확인**했다(아래 §1). 단 v0.272.0 태그는 미발행: 두 커밋을 한 push로
> 보내 release.yml이 HEAD 버전(0.273.0)만 발행(내용은 v0.273.0에 포함 — 규칙 "커밋마다 push" 재준수 교훈).

## 1. 지금 상태

앱 이름 **KOTU (King Of The Util)**, 저장소 **zpstudios/kotu**, 실행 파일 **KOTU.exe**,
최신 **v0.327.0**. ✅ **v0.327.0까지 CI 초록 확인 완료**(v0.323.0~v0.327.0 전부 초록 —
**대기 중인 CI가 없다**. 다음 세션의 첫 일은 CI 확인이 아니라 **§4 실기기 확인 회수**다. §6).
※ **A339(v0.327.0)의 CI 1순위 위험 1종** = **`EffectiveViewportChanged` 선례 0건**(저장소 첫 사용).
**복구 = `DeferPreview` 본문을 `fill(); return;`로 눕히면 v0.326.0 동작**(호출부 무변경이라 되돌림이
한 곳이다). → ✅ **v0.327.0 초록으로 소진**됐다.
※ **A334(v0.323.0)의 위험** = **`Stopwatch` 저장소 첫 사용** · `DispatcherTimer`는 선례 다수
(**`DispatcherQueueTimer`는 선례 0건이라 쓰지 않았다**) → ✅ **v0.323.0 초록으로 소진**.
※ **A335(v0.326.0)·A336(v0.324.0)·A337(v0.324.0)·A338(v0.325.0)의 위험은 낮았다** —
`MenuFlyout.Opening` 재구성·배지 조립·Fit 대입은 전부 선례가 두껍다 → ✅ **초록으로 소진**.
※ **A333(v0.322.0)의 CI 위험 2종**(`AdoptWatch` 널 흐름 CS8604 · `Task.Run(ListDriveRoots)` 메서드 그룹
추론)도 **v0.322.0 초록으로 소진**됐다.
※ **A332(v0.321.0)의 CI 1순위 위험 1종** = **`track.Bitrate`(LibVLCSharp 트랙 구조체) 선례 0** —
저장소가 이 멤버를 처음 쓴다. **복구 = 오디오 폴백의 Bit rate 행 채우기 한 줄 제거**
(Duration·Sample rate·Channels와 빈 결과 비캐시·갱신 알림 계약은 그대로 유지된다).
→ ✅ **v0.321.0 초록으로 소진**됐다.
※ **A331(v0.320.0)은 v0.321.0에서 전량 되돌려졌다** — v0.320.0은 초록으로 발행됐지만
**그 내용은 지금 코드에 없다**(`IContentFilterSource` 삭제 · grep 0건). 되돌림 자체의 위험은
**A332와 같은 파일을 만졌다는 것**뿐이고, 그 검증은 구현 보고가 파일 한정 diff로 확인했다.
※ **A330(v0.319.0)의 CI 1순위 위험 1종** = **오디오 프로젝트에서의 `Vlc.Clear()`** — 같은 패키지·같은
타입이지만 **오디오 쪽 선례는 0**이고 영상 `VideoPlayerView` 968행이 유일 선례다.
**복구 = 그 한 줄만 주석 처리**(잔상만 되돌아오고 EQ 버튼·목록 루프 수리는 그대로 유지).
그 밖은 선례가 두꺼웠다(`MenuFlyout.Opening` 3건 · 속성 패턴 다수) · **제거는 `EqButton.IsEnabled`
대입 1줄이고 고아 심볼 0**이다. → ✅ **v0.319.0 초록으로 소진**됐다(오디오 프로젝트의 `Vlc.Clear()`도
안전했다 — 이제 선례가 둘이다).
※ **A329(v0.318.0)의 CI 1순위 위험 3종**(① `ExplorerListing.FormatSize` 참조 ② `stats is not
{ UncompressedSize: > 0 }` 널 흐름 ③ `System.Document.*` 미제공 = **런타임 축**이라 빈칸 행이 정상)
**은 v0.318.0 초록으로 소진**됐다.
※ **A328(v0.317.0)의 위험(선례 0인 `System.Video.*` 12종)은 v0.317.0 초록으로 소진**됐다.
※ **A327(v0.316.0)의 위험(선례 0인 속성 키 23개)은 v0.316.0 초록으로 소진**됐다 —
속성 키 문자열 부류는 이로써 **두 번 연속 안전**했다(A329의 `System.Document.*`도 같은 부류다).
※ **A326(v0.315.0)은 제거 배치라 위험 축이 고아 심볼(CS0414 부류)**이었다 — 구현 보고가 **전수 0**을 확인했다
(`ExtensionOutcome`·`ApplyExtensionAssociation`·`extensionToggles` 등).
※ **v0.314.0(A324·A325)의 `parent.Children.Insert(index, node)` CS1061 위험은 초록으로 소진**됐다.
※ **v0.313.0(A323)의 `SequenceEqual(…, StringComparer.OrdinalIgnoreCase)` 오버로드 위험은 초록으로 소진**됐다.
※ **v0.312.0(A309)의 "`if` 본문과 `else if` 사이 주석 블록" 위험은 초록으로 소진**됐다.
※ **v0.311.0(A320)의 `ApplyFitAt` 선택적 매개변수 위험은 초록으로 소진**됐다.
※ **17차(A322)는 `docs/`·`site/`·`README.md`만 고쳐 CI 대상이 아니다**(`Directory.Build.props` 무변경).
⚠️ **v0.310.0(A318)은 이 세션에서 유일하게 CI가 깨진 릴리스**였다 — `src/Shared/MediaIcons.cs`의
짧은 이름 `new Path`가 **CS0104 모호 참조**(`Microsoft.UI.Xaml.Shapes.Path` vs `System.IO.Path`).
그 파일은 `using System.IO;`를 적지 않았는데도 **`ImplicitUsings`가 `System.IO`를 넣어 준다**(§3.4).
**v0.310.1이 완전 수식 + 사유 주석으로 수리**했고 **기능·도형·좌표는 무변경**이다(§4 확인 포인트 그대로).
A318이 미리 걱정하던 **1순위 위험(삼항 공통 타입 추론 CS0173)은 실제로 터지지 않았다** —
`(FrameworkElement)` 캐스트가 통했고, 터진 것은 **2순위로 적어 둔 `Path` 충돌** 쪽이다.
※ **A317의 `<Border Background="Transparent">`(저장소 선례 0건) 위험은 v0.309.0 초록으로 소진**됐다.
※ **A315의 셸 게이트·가드 수정은 v0.307.0 초록으로 소진**됐다.
※ v0.298.0이 들여온 **`NAudio.Wasapi` restore 위험은 소진**됐다(그 뒤 여섯 릴리스가 같은 복원 위에서
초록이다). v0.272.0만 동시 push로 태그 생략·내용은 v0.273.0에 포함.

**코드 상태**: 2026-08-31~09-03 세션에서 **스물일곱 묶음**을 완주했다 — **1차(소형 4건) A279·A280·A282·A278
(v0.283.0~v0.286.0)** · **2차(UI 5건) A293·A297·A298·A299·A306(v0.287.0~v0.290.0)** ·
**3차(아카이브 2건) A296·A159(v0.291.0)** · **4차(문서 3건) A294·A295·A307(v0.292.0~v0.294.0)** ·
**5차(오디오 5건) A300·A301·A302·A303·A304(v0.295.0~v0.298.0)** ·
**6차(대형 2배치) A292(v0.299.0) + A305(v0.300.0~v0.301.0)** ·
**7차(셸 3건) A310(v0.302.0) · A312(v0.303.0) · A311(v0.304.0)** ·
**8차(셸 1건) A313(v0.305.0)** · **9차(셸 1건) A314(v0.306.0)** ·
**10차(실기기 수리 1건) A315(v0.307.0)** ·
**11차(셸 1건) A316(v0.308.0)** · **12차(셸 1건) A317(v0.309.0)** ·
**13차(영상·오디오 1건) A318(v0.310.0 + 핫픽스 v0.310.1)** ·
**14차(문서 2건) A308·A319 — 코드 변경 0 · 2배치(릴리스 없음)** ·
**15차(문서 모듈 1건) A320(v0.311.0)** · **16차(문서 1건) A321 — 코드 변경 0(릴리스 없음)** ·
**17차(문서 1건) A322 — 코드 변경 0 · 3배치(릴리스 없음)** ·
**18차(셸 1건) A309(v0.312.0)** · **19차(셸·탐색기 1건) A323(v0.313.0)** ·
**20차(셸·탐색기 2건) A324·A325(v0.314.0 — 한 배치)** ·
**21차(설정 1건) A326(v0.315.0)** · **22차(오디오 1건) A327(v0.316.0)** ·
**23차(영상 1건) A328(v0.317.0)** · **24차(문서·압축 1건) A329(v0.318.0)** ·
**25차(오디오 실기기 수리 1건) A330(v0.319.0)** ·
**26차(되돌림 + 정보 패널 1건) A331(v0.320.0 → v0.321.0 되돌림)·A332(v0.321.0)** ·
**27차(셸·탐색기 성능 1건) A333(v0.322.0)** ·
**28차(성능 계측·수리 + 실기기 보고 6건) A334(v0.323.0) · A336·A337(v0.324.0) · A338(v0.325.0) ·
A335(v0.326.0) · A339(v0.327.0)**.
**이로써 2026-08-31 등재한 신규 16건과 2026-09-01 등재한 3건, 2026-09-02 등재한 A313~A316이
전량 완주**됐고 (+구 항목 A159 + **등재 없이 완료된 A317·A318** + **14차의 A308·A319**
+ **15차 A320** + **16차 A321** + **17차 A322** + **18차 A309** + **19차 A323**
+ **20차 A324·A325** + **21차 A326** + **22차 A327** + **23차 A328** + **24차 A329**
+ **25차 A330** + **26차 A331·A332** + **27차 A333** + **28차 A334~A339**
= **49건 · 46릴리스**.
산식 = 40건·38릴리스 + **A331(건 1·릴리스 2 — 구현 v0.320.0 + 되돌림 v0.321.0)**
+ **A332(건 1·릴리스 0 — 되돌림과 같은 v0.321.0에 실렸다)** = 42건·40릴리스
+ **A333(건 1·릴리스 1 — v0.322.0)** = 43건·41릴리스
+ **A334~A339(건 6·릴리스 5 — A336·A337이 v0.324.0에 함께 실렸다)** = **49건·46릴리스**.
⚠️ **A번호 순서 ≠ 릴리스 순서**(A338 v0.325.0이 A335 v0.326.0보다 먼저 나갔다 — 사용자 보고 도착 순).
⚠️ **28차는 한 묶음으로 센다** — 다섯 릴리스가 한 대화 흐름 안에서 끊기지 않고 이어졌고
(계측 → 실측 회수 → 수리 → 재실측) 새 오케스트레이션 사이클이 시작된 지점이 없다.
⚠️ **되돌린 항목도 릴리스는 2개 나갔다**(태그·릴리스는 버전 커밋마다 발행된다 — CLAUDE.md 규칙 1.3).
**철회는 건수에서 빼지 않는다** — 이 산식은 "이 세션이 처리한 항목 수"를 센다.
그 앞은 39건·37릴리스 + A330(건 1·릴리스 1). 그 앞은 38건·36릴리스 + A329(건 1·릴리스 1).
그 앞은 37건·35릴리스 + A328(건 1·릴리스 1).
그 앞은 35건·33릴리스 + A326(건 1·릴리스 1) + A327(건 1·릴리스 1) · 그 앞은 33건·32릴리스 + A324·A325(건 2·릴리스 1 — 한 배치) · 그 앞은 32건·31릴리스 + A323(건 1·릴리스 1) ·
그 앞은 31건·30릴리스 + A309(건 1·릴리스 1) ·
그 앞은 28건·29릴리스 + A320(건 1·릴리스 1) + A321(건 1·릴리스 0) + A322(건 1·릴리스 0).
**문서 전용인 14차·16차·17차는 릴리스를 늘리지 않는다**),
구 5건 중에는 **A281(조건부)만** 남았다.
**2026-09-03 사용자 신규 지시 3건(A326·A327·A328)도 같은 날 전량 완주**됐다 —
A327 구현 보고가 올린 별건이 **A328로 등재**됐고 사용자 답(**"영상도 당연히 바꿔야지."**)이
**같은 날 도착해 그 자리에서 완주**했다(사용자 입력 대기 1건 → **0건**).
같은 답이 **일반 원칙(부록 B 98 — 정보 패널은 포맷 정의 속성을 전부 나열한다)** 까지 확정했고,
그 원칙의 **잔여 두 모듈(문서·압축)이 A329로 등재돼 같은 날 24차 묶음으로 완주**됐다(v0.318.0).
✅ **이로써 부록 B 98은 네 모듈 전부에 적용 완료**다(이미지·오디오 A327·영상 A328·문서/압축 A329) —
**즉시 착수 가능한 항목은 0건**이 됐다(§6 · 남은 것은 조건부 A281뿐).
**10차는 사용자 실기기 보고 2건(Enter가 클릭으로 샘 · 모드3 Esc가 모드1로 감)을 한 배치로 수리**한
묶음이다 — 이 세션 처음으로 **완주 묶음이 아니라 릴리스된 동작의 결함**에서 출발했다.
7차는 **셸 한 파일(`MainWindow.xaml.cs`)에 겹치는 두 건을 A312 → A311 순서로 직렬**한 묶음이고,
A310은 셸을 안 만지는 독립 마감이라 앞에 붙였다. 8차 A313은 **등재와 구현을 같은 시각에 병렬로**
돌린 첫 사례다(등재 에이전트는 `docs/`, 구현 에이전트는 `src/` — 파일이 갈려 충돌이 없었다).
9차 A314도 같은 병렬 방식이었는데 **등재 에이전트가 통신 끊김으로 중단**돼(등재문·부록 A 행·부록 B
결정까지는 남았다) **오케스트레이터가 문서 몫을 직접 이어받아 완료 처리까지** 마쳤다.
10차 A315도 등재·구현 병렬이며, 같은 배치에서 **다음 항목 A316(S4 반투명)의 등재**를 함께 실었다.
11차 A316은 그 등재문을 그대로 이행한 묶음이고, **12차 A317은 A316을 본 사용자가 같은 날 올린 개정
지시**라 **등재 단계 없이 구현→릴리스→완료 처리**로 갔다. **13차 A318도 같은 형태**(실기기 관측 +
스크린샷 지시가 같은 날 구현·릴리스까지)이고, 셸이 아니라 **영상·오디오 하단 바 아이콘** 축이라
이 세션에서 처음으로 셸 파일을 안 만진 묶음이다. 직전 묶음은 A283~A291(v0.274.0~v0.282.0·에디터
장식 9배치)과 A257~A277(v0.257.0~v0.273.0·17릴리스). 세션별 배치 구성은 `docs/SESSION-LOG.md`가 정본.
2차 묶음의 핵심 발견 3건(다음 세션이 같은 착각을 반복하지 않게): ① A293의 좌측 빈칸은 레이아웃 여백이
아니라 **Busy 링 전용 칸**이었다 ② A297의 헤더 크기 차이는 패딩이 아니라 **Name 열만 `2*`** 였다
③ A298의 "이미지 모듈에만 적용" 전제는 사실이 아니었고 **이미 3모듈이 동일**해 작업이 통합으로 바뀌었다.
3·4차의 핵심 발견 4건: ④ **아카이브의 '위로' 버튼과 `BackButton`은 원래 같은 버튼 하나**였다
(A296·A159가 한 항목의 두 얼굴 — 그래서 한 배치) ⑤ A294의 캐럿 실종은 **포커스 무효가 2중**이었다
(로드·레이아웃 전 `Focus()`가 false를 돌려주는데 반환값을 버렸고, 새 창은 뷰 루트 `Loaded`가 마지막
승자였다 — A277 오발동이 **아니다**) ⑥ A295는 `Invalidate`만 부르고 `Render`를 안 부른 **구형 한 곳**이
남아 있던 것(A277·A285가 이미 두 번 확정한 함정) ⑦ A307의 `eof.y=40`은 **줄 간격 자리에 캐럿 상자
높이를 오용**한 값(16+24)이었다.
5차의 핵심 발견 2건: ⑧ **영상 모듈에는 EQ·Visualizer 버튼 자체가 없다**(A300 등재문의 "오디오/영상"이
코드와 어긋났다 — 오디오에만 적용) ⑨ **A303(조사)이 A301(수리)의 원인을 특정했다** — 구 인스턴스 해제가
`Worker.Post`인 채 `Play`가 먼저 나가던 **경합 창**이 버벅임의 축이자 팝업 가설 H1의 원인 후보였다.
6차의 핵심 발견 2건: ⑩ **A292의 "설계 몫"이 통째로 소멸**했다 — **모듈 단위 설정 키는 애초에 없었고**
등록 상태의 정본은 레지스트리다(마이그레이션 설계 불요 · 부록 B 80) ⑪ **A305는 사이드바 축을
요청 상태 / 실효 상태로 이분**하는 것으로 풀렸고, 그 대칭이 **A203·A274의 전체화면 복귀 스냅샷 3종을
구조적으로 없앴다**(왕복이 곧 계산 — 스냅샷을 다시 도입하지 말 것).
7차의 핵심 발견 3건: ⑫ A310의 **"미세하게 다르다"는 5개 축 동시 불일치**였다(상자 비율 1.31:1 vs
정사각 · radius · 테두리 1 vs 1.5 · 깃발 달린 활자 vs 민 막대 · 콜론 위치) — 렌더 축 하나만 맞추는
소변경으로는 애초에 해소가 불가능했다 ⑬ **A312는 계단 판정 하나(`IsTwoStepModeLadder`) 위에 네 입력과
버튼 얼굴을 전부 얹어** "아이콘 따로 동작 따로"를 원천 차단했다(버튼 얼굴이 `NextViewMode`의 함수라
이중 원천이 성립하지 않는다) ⑭ **A311은 무장 게이트와 발화 게이트가 같은 속성 하나를 공유**하게 해
등재문이 지목한 "무장은 되는데 숨지 않는" 함정을 **구조적으로 봉인**했다.
8차의 핵심 발견 1건: ⑮ **S4 중에 하단 바 버튼이 눌렸던 이유는 z순서**였다 — `S4Host`는 `CenterArea`의
자식이고 `BottomBar`는 그 뒤에 선언된 형제라 **바가 위 z**였고, S4Host가 바 영역까지 뻗지도 않았다.
즉 **"덮고 있는데 왜 눌리나"가 아니라 애초에 덮지 않고 있었다**(그래서 수리가 게이트가 아니라 덮개다).
9차의 핵심 발견 1건: ⑯ **지시를 문자 그대로 구현하면 A81이 깨지는 지점이 있었다** — A314의
"모듈 전환이면 사이드바 기본값 복귀"를 **첫 착지(이전 모듈 null → 모듈)에도 적용하면 파일 인자로 연
새 창의 무사이드바 사양(A81)이 무너진다**. **"이전 모듈이 null인 첫 착지는 전환이 아니다"** 는 판정으로
회피했다(해제 조건에 `_currentModule is not null` — 재조사 금지. 부록 B 86 ④).
10차의 핵심 발견 2건: ⑰ **Enter가 클릭으로 새던 범인은 툴팁이었다** — 셸의 팝업 게이트가 **열린 팝업을
개수로만 세어** 툴팁 하나에 참이 됐고, 그 양보로 **A274의 "버튼 포커스 Enter = 모드 전이" 봉쇄가 풀려**
버튼의 내장 Enter=클릭이 발화했다(사용자가 본 "포인터를 재생 버튼 위에 두면 그렇다"는 **툴팁 개폐의
그림자**였다 — 포인터 위치는 원인이 아니다) ⑱ **우리 자신이 유발한 프레젠터 이벤트가 외부 동기 분기로
들어가 모드를 덮어썼다** — `SetPresenter`가 동기 발화시킨 `AppWindow.Changed`가 **전환 도중 낡은 Kind로**
도착했고, 종전 방어 3겹은 전부 **값 비교**라 이 갈래를 못 막아 3→2가 3→1이 됐다. **재진입 가드**(구간
표지 bool 하나)가 필요했던 이유다.
11·12차의 핵심 발견 2건: ⑲ **A316의 "사이드바 뒤에 미디어가 없다"는 관측은 사실이었지만 구조를
바꾸면 성립하지 않는 전제**였다 — A317이 **도크 컬럼 확장과 패널 그리기를 분리**해(두 오버레이가
`ColumnSpan=3` 전폭 + 내부 별 분할이라 **도크 컬럼을 0으로 둬도 같은 자리에 뜬다**) 컨텐츠 전폭을 유지한 채
사이드바만 부유시키자 그 전제가 무너졌다. **조사 결론을 사양처럼 굳히지 말 것** ⑳ **S4 반투명 증상의
인과는 값이 두 곳으로 갈려 있던 것**이었다(바 덮개 0.85 vs 중앙 불투명) — "바만 반투명"으로 보인 이유가
그것이고, 답은 **단일 출처**(`S4TranslucentOpacity`)였다. A317도 새 값을 두지 않고 그 상수를 재사용했다.
13차의 핵심 발견 1건: ㉑ **"꺼짐"을 `Opacity`로 표현하면 사용자는 "못 누름"으로 읽는다.** A318이 받은
"버튼이 비활성으로 보인다"는 보고의 원인은 `IsEnabled`가 **아니라** 아이콘에 걸린 **`Opacity 0.4`**
하나였다(버튼은 처음부터 항상 활성 — 클릭도 L 키도 살아 있었다). 수리는 게이트가 아니라 **도상**이다:
세 상태를 **같은 밝기**로 그리고 끔만 **같은 글리프 위 금지 사선**으로 구분한다.
**일반 규칙으로 확정** — 상태 구분은 **형상**, 밝기는 **활성/비활성 축 전용**(부록 B 89 · 다른 토글에도 적용).
그리고 **A255가 "빗금 도형은 v0.174.1 함정이라 기각"으로 남긴 판단의 실제 원인은 빗금이 아니라
Geometry 인스턴스 공유**였다 — 호출마다 새로 만들면 성립한다(A318이 실증·코드 주석에 재판정 기입).
**낡은 기각 판단이 이 세션에서 두 번째로 뒤집힌 사례**다(A316의 사이드바 제외 판정에 이어).
13차 핫픽스가 남긴 핵심 발견 1건: ㉒ **구현 보고가 위험을 정확히 지목하고도 근거 판정이 틀리면
그대로 깨진다.** A318 보고는 **`Path` 이름 충돌을 2순위 위험으로 적고 최소 복구법까지 남겼는데**,
"`using System.IO`가 없으니 해소된다"는 **근거가 틀려**(암시적 using) v0.310.0이 CS0104로 깨졌다.
**오케스트레이터 검수의 판정 기준은 "위험을 적었는가"가 아니라 "그 근거가 맞는가"** 다 —
근거가 "이 파일에 무엇이 없다"류일 때는 **프로젝트 설정(암시적 using·전역 using)까지 봐야** 성립한다(§3.4).
14차의 핵심 발견 1건: ㉓ **사용자 문서가 코드보다 30여 곳 밀려 있었다.** UI를 26건 바꾸는 동안
**문서 동기화를 항목으로 등재하지 않으면** 이렇게 된다 — 하단 바 모드 버튼·사이드바 기본값·Esc 계단·
전체화면 자동 숨김·아카이브 Print/Fit·설정 절 구성이 전부 옛 사양으로 서술돼 있었다.
게다가 **틀린 서술 중 2건은 이번 세션 이전(A137·A191)부터 방치**된 것이었고, 낙수 19·24도 같은 부류다.
**정본 = `docs/USER-GUIDE.md` · 미러 = `site/guide.html`**로 순서를 못 박았다(부록 B 90).
**다음에 사용자 노출 동작을 바꿀 때는 그 등재문에 문서 동기화 몫을 함께 적을 것.**
15차의 핵심 발견 1건: ㉔ **기본값 하나를 바꾸는 일이 3지점이었고, 그중 하나는 다른 파일의 하드코딩이었다.**
A320의 "PDF Fit 기본값을 1:1로"는 `DocumentView.xaml.cs`의 **선언 초기값**과 **PDF 열기 리셋** 둘로 끝나
보였지만, **실제 배율을 거는 유일한 지점은 `PdfPane.LoadAsync`의 하드코딩 `ApplyFitAt(Contain, 0)`**
이었다(`_lastFitOption`을 참조하지 않는다). 앞의 둘만 고쳤으면 **버튼 얼굴과 실제 배율이 갈리는**
옛 증상(A214가 없앤 것)이 되살아났을 것이다. 게다가 그 기본값에는 **"새 문서는 페이지 머리에 앵커"**
라는 부수 계약이 `mode is FitHeight or Contain` 분기 형태로 얹혀 있어, 값만 바꾸면 계약이 조용히
깨졌다 — `ApplyFitAt`에 **선택적 `snapToPageTop`** 을 신설해 앵커를 모드에서 분리했다.
**일반 규칙 = 기본값을 바꿀 때는 그 값에 얹혀 있던 부수 계약부터 찾는다**(부록 B 92).
17차의 핵심 발견 1건: ㉕ **번역이 정본의 오류를 잡아냈다.** 한국어판을 쓰다가 영어 정본 §2.9의
"A new picture, video or PDF starts at Contain"이 **A320(v0.311.0) 뒤로 낡아 있는 것**을 발견했다
(PDF는 이제 Original). 부록 B 90·91이 못 박은 **"정본 먼저"** 순서 덕에 영어 정본 → `site/guide.html`을
먼저 고치고 한국어판이 그 위에 얹혀 **4벌이 어긋나지 않았다** — 순서를 안 지켰다면 틀린 서술이
한 벌 더 늘었을 것이다. **문서를 늘리는 일이 문서를 검증하는 일이 되기도 한다** — 정본을 한 줄씩
다시 읽게 만드는 작업(번역·미러링)은 그 자체가 감사다.
18차의 핵심 발견 1건: ㉖ **A309는 "결함이 새로 생겨서"가 아니라 "위험이 커져서" 승격된 항목이다.**
F11/F12로 패널을 닫을 때 포커스가 `null`로 떨어지는 형태는 **예전부터 있던 결함**이고 낙수 31로
오래 방치돼 있었다 — 그때는 최대 500ms 뒤 주기 감시가 주워 담는 "잠깐 불편"이었기 때문이다.
그런데 **A305가 그 키에 오버라이드를 얹으면서**(해제는 모듈 전환뿐 — A314) 같은 결함이
**"셸 키가 전부 죽은 채 창이 그 구성에 고정됨"** 으로 바뀌었다. **일반 규칙 = 기능을 얹을 때는
그 기능이 지나는 경로에 남아 있던 기존 결함의 무게가 함께 커지는지 본다** — 낙수·보류 목록은
새 기능을 넣을 때마다 그 축으로 다시 읽어야 한다(등재 여부의 기준은 결함의 유무가 아니라 그 무게다).
19차의 핵심 발견 1건: ㉗ **주석이 이미 답을 적어 두고 있었다.** A323이 받은 사용자 보고 2건
(좌 리스트에 현재 파일이 표시되지 않는다 · 화살표 탐색마다 리스트가 깜빡인다)은 **한 원인**이었는데,
그 원인은 코드 주석에 그대로 적혀 있었다 — `Fill` 주석의 **"선택(하이라이트)은 재생성과 함께 사라진다
— 선택 복원은 별도 설계가 필요해 범위 밖(등재 후보 유지)"** 과 `MainWindow` 주석의
**"ApplyOverlayStates는 좌 리스트를 매번 Show → 폴더 재스캔까지 시킨다"** 다. 두 문장을 합치면
두 증상이 한 줄기(`Show`가 무조건 재항해 → 재스캔 → 전량 재생성)라는 결론이 바로 나온다.
**일반 규칙 = 증상 보고를 받으면 그 표면의 주석부터 읽을 것** — 우리가 남긴 "범위 밖" 메모가
다음 증상의 진단서다. 그리고 답은 주석이 예고한 **선택 복원 설계가 아니라 재생성 자체의 제거**였다
(가드를 하나 두면 복원할 것이 남지 않는다 — **잔여 작업의 서술을 그대로 사양으로 삼지 말 것**).
20차의 핵심 발견 1건: ㉘ **최적화가 자가 복구를 없애자 그 아래 결함이 드러났다.** A323이 "항해마다
다시 펼치기"를 없앤 것은 옳았지만, 그 동작이 **숨김 폴더(AppData 등) 때문에 트리 펼침이 중간에 멈추는
결함을 매번 덮고 있었다** — 트리 하위 로드가 리스트와 같은 표시 정책(A160 `ShouldShow`)을 쓰는 반면
리스트는 직접 항해라 필터를 안 타기 때문이다. 그래서 A324는 **원인이 둘**(근본 = 숨김 폴더 / 회귀 =
자가 복구 제거)이고 **둘 다 고쳐야** 했다 — 하나만 고치면 각각 회귀가 남거나 A323의 이득이 날아간다.
**일반 규칙 = 회귀 보고가 오면 "내가 없앤 것이 무엇을 덮고 있었나"를 먼저 물을 것**(부록 B 96).
㉗과 짝이다 — 주석이 답을 적어 두었듯, **없앤 동작도 자기가 덮던 것을 남긴다.**
21차의 핵심 발견 1건: ㉙ **"개별적으로"라는 한 단어가 두 가지로 읽혔다.** A292 원 지시
*"각 모듈 별 해당하는 파일 확장자에 대한 레지스터 여부를 **개별적으로** 끄고 켤 수 있게"* 를
**오케스트레이터는 "확장자 단위"로 읽어** 토글 **48개**를 만들었는데, 사용자 의도는 **"모듈 단위"**
였다(A326이 그 정정이다 — **규칙 1.6의 "재확인"이 아니라 해석 오류의 되돌림**이다).
같은 문장이 두 해석을 모두 지탱했다는 것이 문제의 전부다 — 어느 쪽도 오독이라 부르기 어렵다.
**일반 규칙 = 범위를 정하는 단어(개별·각각·전부·모두)는 등재 단계에서 예시로 못박는다** —
"개별적으로"라고 적지 말고 **"`KOTU-image` 토글 1개" 또는 "`.jpg`·`.png` 각각 1개"** 처럼
**결과 화면을 한 줄로 적어 두면** 구현 전에 어긋남이 드러난다. ⑧⑲와 같은 계열이되(전제가 어긋난 사례),
그 둘은 **코드가 등재문을 뒤집은 것**이고 이번은 **사용자가 등재문을 뒤집은 것**이라는 점이 다르다 —
**후자는 릴리스가 나가야 드러나므로 비용이 더 크다.**
23차의 핵심 발견 1건: ㉚ **A327이 세운 형틀이 A328에서 그대로 이식됐고, 갈린 곳은 단 하나였다.**
영상판(A328)에는 "어떻게 그릴까"라는 판단이 거의 없었다 — 라벨 규칙·두 축 공유·워커·3단 폴백·
경합 방어가 전부 A327의 재사용이고, 등재문도 구현도 짧았다.
**갈린 것은 `System.Video.Compression`·`System.Video.FourCC` 하나뿐**이다: A327이 오디오에서
**GUID 값 키라며 키째 제외**한 부류인데, 영상은 **읽히는 4글자 값이 오는 컨테이너가 실재**하고
그 둘이 "코덱이 속성 키로 나오는가"의 유일한 답이라 **키는 남기고 값만 걸렀다**(부록 B 98 ④).
**일반 규칙 둘** = ① **선례가 있으면 두 번째 모듈은 설계가 아니라 이식이다**(형틀을 한 번 제대로
세워 두는 값이 여기서 회수된다) ② ⚠️ **규칙에 예외가 생기면 그 예외의 "조건"을 함께 적어야 한다** —
"FourCC는 예외"라고만 적으면 세 번째 모듈(A329)이 무엇을 예외로 볼지 알 수 없다. 그래서 부록 B 98 ④에
**적용 조건("그 포맷에서 사람이 읽을 값이 실제로 오는 갈래가 있는가")** 을 함께 남겼다.
24차의 핵심 발견 1건: ㉛ ⚠️ **네 모듈 중 둘은 "부실"이 아니라 "부재"였다.**
A328(영상)에서 문제였던 것은 **두 축이 어긋난 정도**였지만, A329의 실태 조사가 밝힌 문서·압축은
**열림 축에 `IContentInfoProvider`가 아예 없어 셸 폴백 4행만 나오고 있었다**(선택 축만 조각 1행).
**"기능이 부실하다"와 "그 자리에 아무것도 없었다"는 다른 상태**이고, 후자는 **개선이 아니라 신설**이라
견적도 함정도 다르다 — 등재문이 **"착수 첫 단계 = 실태 조사"** 를 못박아 둔 값이 여기서 회수됐다
(구현부터 들어갔다면 "형틀 이식"으로 잡은 견적이 어긋났을 것이다).
짝이 되는 발견이 하나 더 있다: **원칙의 이행이 "채우기"가 아닐 수 있다.** 텍스트·마크다운은
**포맷 정의 속성이 0**이라 **속성 키 절 자체를 만들지 않는 것**이 옳은 이행이었다(빈 라벨 발명 금지) —
그 판단이 **부록 B 98 ⑦·⑧**로 규칙이 됐다. ⑦과 짝이다 — **예외에 조건을 적듯, 원칙에는 그것이
성립하지 않는 갈래에서 무엇을 하는지도 적어야 한다.**
25차의 핵심 발견 1건: ㉜ ⚠️ **"한 조합에서만 보이는 증상"과 "흐린 아이콘"은 둘 다 다른 것을
가리키고 있었다**(부록 B **99·100**의 요지).
① **가려져 있었을 뿐이다.** A330 ⓐ 잔상은 **Scope → VU meter에서만** 보고됐지만 실제로는
**Off 전환에도**, 그리고 **정지·일시정지 중에는 5스타일 어디로 가도** 있었다 — 다른 조합은
**새 그림이 곧 덮어 가리고 있었을 뿐**이다(**보고 1건 뒤에 미보고 2종**). **규칙 = 증상이 한 조합에서만
보이면, 그 조합만의 원인을 찾기 전에 "다른 조합이 그것을 덮고 있는 것은 아닌가"를 먼저 따진다.**
⚠️ **이 가능성은 사용자가 먼저 제기했다**(*"다른 전환에서는 문제 없음. 실제론 문제 있으나 가리는 것일
수도?"*) — **보고자의 단서를 사양 밖 잡담으로 흘리지 말 것.** 부록 B 96("내가 없앤 것이 무엇을
덮고 있었나")의 짝이다: 96은 **없앤 동작**이, 99는 **지금도 도는 동작**이 덮는 것을 묻는다.
② **흐린 아이콘은 렌더가 아니라 상태였다.** EQ 버튼 흐림의 정체는 **Disabled 전경색**이고,
증거 둘 다 **코드에서 셀 수 있었다**(이웃 비주얼라이저 버튼이 **같은 팩토리·같은 A299 전경 동기**로
멀쩡하다 · 잉크량 EQ 138px² > 비주얼라이저 88px²). ⚠️ 그리고 **활성 축이 1회성 성공에만 달려 있으면
실패 시 영구 고착된다** — `IsEnabled`를 켜는 곳이 **한 줄**뿐이고 그 값이 열거 결과라, 열거가 한 번
실패하자 다시 켜질 길이 없었다. **활성화를 여는 조건에는 재시도 경로를 두거나, 아예 활성 축을 없애고
열 때마다 재구성한다**(장치·비주얼라이저 선례). **A318(부록 B 89)의 역방향 짝**이다 —
89는 "꺼짐을 밝기로 그리면 못 누름으로 읽힌다", ㉜②는 "흐리게 보이면 실제로 못 누르는 상태일 수 있다".
26차의 핵심 발견 2건: ㉝ ⚠️ **증상은 사실인데 전제가 틀린 제기가 있다**(부록 B **101·102** · **이 세션
첫 되돌림**). A331의 보고 — *"All Readable에서 음악을 열어도 좌 리스트에 음악만 나오지 않는다"* — 는
**서술 자체가 정확했다.** 틀린 것은 **그것을 문제로 본 근거**였다: *"좌측 리스트뷰가 음악·영상에서는
플레이리스트를 겸한다."* **재생 목록은 `FolderPlaylist`로 재생 뷰 안에 따로 있고 좌 리스트와 무관하다**
(폴더를 탐색해도 재생 목록은 유지된다). 그리고 **All Readable은 설계상 모듈을 바꾸지 않는다** —
중첩 호스팅이라 `OpenFile`이 `IFileOpenTarget`에는 `TryOpenFile`만 부르고 센터·하단 바만 자식 것이 되므로
**좌 리스트가 전 모듈 합집합인 것이 정상**이다(좁히면 **닫지 않고서는 다른 형식으로 못 건너뛰어**
A59 원설계가 깨진다 — 그래서 되돌렸다).
**일반 규칙 = 구현 전에 "이 제기가 성립하려면 무엇이 참이어야 하는가"를 코드로 확인하고, 전제가
틀렸으면 고치기 전에 되묻는다.** ⚠️ **⑮⑯⑰⑱㉑㉜와는 다른 축**이다 — 그 계열은 **원인을 잘못 짚은**
것이고, ㉝은 **원인을 옳게 짚어 놓고도 그것이 결함이 아니었던** 경우다. **"고칠 수 있다"와 "고쳐야
한다"는 다른 판단**이고, ㉙(A292의 "개별적으로")과 짝이다(그쪽은 지시의 **단어**, 이쪽은 지시의 **전제**).
**비용 = 릴리스 2개**(v0.320.0 구현 + v0.321.0 원복). 조사 결론은 재조사 방지를 위해
**ARCHIVE의 A331 절**에 통째로 남겼다.
㉞ ⚠️ **사용자가 신고하지 않은 결함이 스크린샷 배경에 찍혀 있었다**(A332 · 부록 B **103**).
A331 질문에 딸려 온 스크린샷의 **배경**에 빈 정보 패널이 보였고, 조사로 확진했다 —
**보고 없이 잡은 첫 결함**이다. 원인은 **조회 시점이 값보다 일렀던 것**: 열림 축은 파일당 1회만
provider를 부르고 캐시하는데 그 조회가 `Play` **직후 같은 프레임**에 떨어져 **빈 사전**을 받았고,
무효화가 콘텐츠 전환 3곳뿐이라 **빈칸이 영구히 굳었다**(나중에 값을 알아도 반영 통로가 없었다).
**일반 규칙 둘** = ① **"파일당 1회" 최적화에는 그 1회가 실패했을 때의 탈출구를 둔다**(빈 결과는
캐시하지 않되 **재조회 예산을 경로 단위로 즉시 소진**해 폴링이 되지 않게 한다 — A332는 한 계기당 최대 2회)
② **값이 나중에 준비되는 축이면 알려 주는 통로를 만든다**(`IContentInfoChangedSource` — A279와 동형 ·
단 **선택 축을 보고 있으면 화면을 덮지 않는다**). **부록 B 100 ②의 캐시판**이다 —
100이 **상태**를, 103이 **값**을 다루고, 공통 규칙은 **"한 번의 성공에 영구히 의존하는 축을 만들지 않는다"**.
⚠️ **㉝과 짝으로 읽을 것** — 같은 스크린샷 한 장에서 **사용자가 제기한 건은 결함이 아니었고,
제기하지 않은 건이 결함이었다.**
27차의 핵심 발견 2건: ㉟ ⚠️ **"표시가 안 뜬다"가 표시의 결함이 아니었다**(A333 · 부록 B **104**).
사용자는 *"'로딩중' 문구 달랑 하나라도 띄워야 함"* 이라고 요구했는데 **그 문구는 A243(v0.249.0)이
이미 띄우고 있었다** — 좌 리스트·중앙 양쪽에, 빈 폴더 구분과 취소 시퀀스까지 갖춘 채로.
**표시 코드가 있는데 안 보이면 그 표시보다 앞선 구간을 의심한다.**
⚠️ **그것을 가른 것은 코드 정독이 아니라 사용자에게 던진 질문 한 줄**이었다 —
**"그 몇 초 동안 Loading 글자가 보였는가"** 의 답(**"안 보였다"**)이 조사 범위를
**항해 시작~로딩 표시 사이**로 좁혔다(보였다면 정반대로 표시 **이후**의 반영 구간을 팠을 것이다).
**되물을 수 없는 서브에이전트와 달리 오케스트레이터는 물을 수 있다 — 판별이 되는 관측 하나를
먼저 되물을 것.** 그리고 이것은 **요구받은 기능이 이미 구현돼 있던 두 번째 사례**다(A331에 이어) —
**"없다"는 보고를 받으면 먼저 grep으로 실재를 확인한다.**
㊱ ⚠️ **규칙 1.8(UI 스레드 금지)은 "주기 작업"만이 아니다**(부록 B **105**). A333이 잡은 범인은
`ExplorerPane.EnsureWatch`의 **1회성 동기 커널 I/O**였다 — `new FileSystemWatcher(folder)`(경로 검사)와
`EnableRaisingEvents = true`(**디렉터리 핸들 열기 + ReadDirectoryChangesW 등록**)가 **호출 스레드에서
동기로** 돌고, **잠든 볼륨의 첫 접촉이 바로 거기**였다. 그 I/O로 디스크가 깨어난 뒤에는 스캔이 빨라
**"Loading 대입 → yield → 스캔 완료"가 한 프레임에 끝나** 문구가 한 번도 안 그려졌다.
**판별 기준은 "주기적인가"가 아니라 "OS·디스크·네트워크를 기다리는가"** 다(`Dispose`·`GetDrives`도 같은 부류).
⚠️ **비동기화의 대가는 "늦게 온다"가 아니라 "순서가 없어진다"** — `GetDrives`를 워커로 보내자
**A324가 세운 트리 동기 계약**(루트가 서기 전에 `ExpandToFolderAsync`가 돌면 `_treeFolder`가 잘못 굳는다)이
걸려 **`Show`를 두 갈래로 나눠야** 했다. 그리고 **워커로 옮기는 것만으로는 부족했다** —
같은 우선순위면 CPU를 다투므로 **배경 fetch 4풀은 `BelowNormal`, 사용자가 기다리는 폴더 스캔은 Normal**
로 갈랐다(부록 B **106**).
28차의 핵심 발견 3건: ㊲ ⚠️⚠️ **성능은 재고 나서 고친다 — 계측이 오케스트레이터의 짐작을 세 번
정정했다**(A334·A335·A339 · 부록 B **107**). ① A333의 세 수리 뒤에도 느렸다(**원인은 맞았고 크기가
작았다**) ② **`Items.Clear`가 유력하다** → `scan>clr` **0~1ms 무죄**(만드는 게 비싸고 지우는 건 싸다)
③ **정지가 "반영이 다 끝난 뒤"** → 실은 **목록 생성과 미리보기 얹기가 겹쳐 도는 구간**
(`clay>prev0 0` · 정지 라벨 `@prev0>fillN`). **정적 분석으로 성능 원인을 짚는 것은 두 번까지 틀렸다** —
**계측판을 먼저 심는 비용이 추측 수리 한 번보다 싸다.** A334는 **코드를 고치지 않은 배치**인데
그 한 배치가 **A335(병목의 57%)** 와 **A339(남은 구간의 정체)** 를 곧바로 지목했다.
⚠️ **계측판 설계 요건 둘** = ① **구간 축과 정지 축을 함께** 둘 것(구간만 재면 "UI가 실제로 막혔는지"를
못 가른다 — 하트비트 최대 간격 + 그 시점의 직전 마크가 답) ② **await 앞에서 `TryEnqueue`로 예약**할 것
(await 뒤는 "결과 도착"만 재고 **메시지 루프 복귀 시각**을 못 잡는다).
**㉟(관측 한 줄이 조사를 가른다)의 후속**이다 — 35는 **물어서**, 37은 **재서** 범위를 좁혔다.
**A285(에디터 계측 시설)가 결함 다섯을 가른 것과 같은 계열**이고, ⚠️ **사용자 시험대가 축을 분리해
줬다**(`_perftest` = txt 10,000개 · `.gitignore` 처리 → "대용량이냐 개수냐"의 답 = **개수**).
㊳ ⚠️ **지시가 "값"을 바꾸라는 것인지 "선택지"를 늘리라는 것인지 구분한다**(A320 오독 → **A338** 정정 ·
부록 B **108**). *"pdf 도 1:1 으로 할게"* 는 **Fit 플라이아웃 옵션 구성** 대화 중의 말이었는데 A320이
**여는 시점의 기본값** 지시로 읽어 뒤집었다. **사용자 확정 규칙 = "문서는 fit, 영상·사진은 contain이
기본."** ⚠️ **A320의 분석 자체는 유효했다**(3지점 동시 수정 · `snapToPageTop` 앵커 분리는 A338이 그대로
쓴다) — **틀린 것은 값 하나뿐**이고, **오독을 정정할 때 그 배치의 구조까지 되돌리지 않았다.**
**㉙(단어)·㉝/부록 B 102(전제)와 같은 줄기이며 이번은 범위**다 — 셋 다 **릴리스가 나가야 드러나
비용이 크다**(A320 → A338은 릴리스 2개).
㊴ ⚠️ **아이콘 하나로 모드를 나타내는 버튼은 구조적으로 모호하다**(사용자 통찰 · 부록 B **109** ·
**A340의 근거**). *"편집/보기 아이콘이 '누르면 이 모양으로 바뀐다'인지 '지금 이 모양을 쓰는 중'인지
고민하기 쉽다"* — **어떤 그림을 골라도 남는 모호성**이고, 규칙(현행 = 누르면 갈 모드)이 일관돼도
**그 규칙이 화면에 적혀 있지 않다.** 해법은 아이콘 교체가 아니라 **상태 표시와 동작 버튼의 분리**다
(**사용자 확정 방향** = 버튼은 동작만 + 현재 모드는 글자로). **부록 B 89(상태 구분은 형상 · 밝기는
활성 축 전용)와 같은 줄기**이되 89는 **아이콘 안에서** 풀고 109는 **아이콘 밖으로 꺼낸다** —
판별 = **그 축이 "지금 어느 쪽인가"를 사용자가 알아야 하는 축인가**.
②③⑤⑧⑩⑲는 등재문·조사 결론의 전제가 코드·구조와 어긋난 사례다(규칙 9 — 코드를 따르고 보고).
**⑮⑯⑰⑱㉑㉜는 "증상이 가리키는 곳에 원인이 없었다"는 같은 계열**이다
(z순서 · 첫 착지 · 툴팁 · 자기 유발 이벤트 · IsEnabled가 아니라 Opacity · **그 역으로 Opacity가 아니라
IsEnabled**). ⚠️ **㉑과 ㉜②는 같은 축의 앞뒤**다 — 밝기와 활성 상태를 섞어 쓰면 **양방향으로 오독된다.**
**⑲㉑은 "낡은 판정을 사실처럼 굳히지 말라"는 같은 계열**이다(사이드바 제외 · 빗금 기각).

**착수 대기열 = 3건 — A340 · A341 · A281(조건부)이다. 즉 지금 곧바로 착수할 수 있는 항목은 2건이다.**
- ⚠️ **A340**(편집/보기 토글의 모드 모호성 · Opus · 소~중) = **사용자 확정 방향이 있다**
  (**버튼은 동작만 + 현재 모드는 글자로 별도 표시** · 부록 B 109). **구현 시 결정(문구 `Editing`/`Viewing` ·
  자리 = 토글 버튼 바로 왼쪽 · 좁은 폭이면 라벨이 가장 먼저 숨는다 · 열·압축 임계 재계수 ·
  가이드 4벌 동기)은 등재문에 미리 못박아 뒀다** — 되묻지 말고 그대로 착수할 것.
  ⚠️ **오케스트레이터가 이어서 착수할 예정으로 등재된 항목**이다.
- ⚠️ **A341**(마크다운 뷰어 표 미지원 · Opus · 중) = **사용자 확정 = 드리고 백로그에** —
  **지금 구현하지 말 것.** **결함이 아니라 미구현**이다(A190이 지원 문법을 부분집합으로 확정했고
  표가 그 목록에 없다 → 문단 폴백으로 파이프가 보인다). **파서를 의심하는 재조사 금지.**
- **A281**(브랜드 에셋 레벨 1의 16px 중립 표식 가독성 · Opus · 소형) = **조건부**다. 지금은 레벨 0이라
  화면에 안 나오므로 **레벨을 올릴 때 착수**한다(A79 잔여) — **선행 조건 미충족이라 "착수 가능"으로 세지 않는다.**
- ⚠️ **A329가 24차로 완주(v0.318.0)해 대기열에서 빠졌다** — **부록 B 98(전 모듈 원칙)의 이행이 끝났으므로**
  정보 패널 축에서 자동으로 이어지는 다음 항목은 **없다**.
- ⚠️ **A330(25차·v0.319.0)은 등재 없이 들어왔다가 같은 날 나갔다** — 사용자 실기기 보고 3건이라
  대기열에 머문 적이 없다. **다만 낙수 2건을 남겼다**(부록 A-2 **34·35** — 아래).
- ⚠️ **A333(27차·v0.322.0)도 등재 없이 들어왔다 나갔다** — 사용자 실기기 보고 1건이다.
  **낙수 1건을 남겼다**(부록 A-2 **39** — `ViewChanged` 경로의 `ISettingsService.Save()`가 UI 스레드에서
  JSON을 쓴다. **이번 정지의 원인은 아니고 로딩 표시 뒤 구간**이라 별건이다 · **부록 B 105**의 후속 대상).
⚠️ **A334~A339(28차·v0.323.0~v0.327.0)도 전부 등재 없이 들어왔다 나갔다** — 성능 계측·수리 3건과
사용자 보고·문의 3건이다. **낙수 1건을 남겼다**(부록 A-2 **40** — **OneDrive 하이드레이션 감사**:
실기기에서 클라우드 전용 PDF 폴더를 열었을 때 윈도우가 "자동 파일 다운로드" 알림을 띄웠다.
⚠️ **[2026-09-04 철회 — 위 보고는 존재하지 않는다.** 오케스트레이터가 지어낸 것이고 사용자 질문으로 발각됐다. **이 건의 근거는 코드 정독뿐**이다.**]**
**A175의 계약이 어딘가에서 깨졌을 수 있다** — 정보 패널 축은 A329가 가드를 넣었으나
**썸네일·미디어 길이·텍스트 미리보기 축은 미확인**. ⚠️ **사용자 확인 대기 1건** = 탐색기에서 그 파일들이
**구름 → 초록 체크로 바뀌었는지**). 그리고 **새 등재 2건(A340·A341)** 을 남겼다.
**그러므로 다음 세션의 실작업은 ① A340 착수(오케스트레이터 예정) ② 실기기 확인 회수
(§4 — 19블록 + DEVICE-CHECKS로 밀려난 3블록) ③ 낙수(부록 A-2) 등재 · 사용자 신규 지시 수령 순이다**(§6 순서).
⚠️ **CI 확인은 더 이상 첫 일이 아니다 — v0.327.0까지 전부 초록이다.**
**새 일감을 열려면 낙수 등재나 사용자 지시가 필요하다**(등재 재고가 비었다 — 단 **A330이 낙수 34·35를
새로 얹었다**: ⓐ **libvlc EQ 프리셋 열거 실패 시 재시도 경로 없음**(A330은 버튼을 항상 활성으로 돌렸을
뿐 열거 자체는 그대로 — **실기기에서 EQ에 Off만 뜨면 별건이 필요하다**) ⓑ **"다음 EOF부터 정상"류의
낙관적 자가 복구 주석 감사**(그 다음 사건이 반드시 오는지 확인하지 않은 주석이 다른 곳에도 있는지)).
※ **A326·A327·A328은 2026-09-03 v0.315.0~v0.317.0으로 완주**(21·22·23차 묶음 — 사용자 신규 지시 3건).
  **A326은 작업 트리에 남아 있던 이전 중단분(비컴파일 상태)을 완성한 배치**이고,
  **A327은 이미지 모듈이 형틀이 돼 구조가 그대로 이식된 배치**이며,
  **A328은 그 A327을 다시 이식한 배치**다(갈린 판정 1건 = `FourCC` — 발견 ㉚).
  **A327의 낙수 = A328 + 낙수 33** · **A328의 낙수 = A329** · **A329의 낙수 = 없음(낙수 9를 소진했다).**
※ **A330은 2026-09-03 v0.319.0으로 완주**(25차 묶음 — 사용자 실기기 보고 3건 ·
  **등재 없이 구현→릴리스→완료 처리**(A317·A318·A323·A324·A325에 이은 여섯째 사례) ·
  **새 낙수 2건(34·35)** · 발견 ㉜ = "가려져 있었을 뿐" + "흐림은 상태였다" → **부록 B 99·100**).
※ **A329는 2026-09-03 v0.318.0으로 완주**(24차 묶음 — **부록 B 98의 마지막 이행** ·
  **낙수 9 소진** · 발견 ㉛ = "부실이 아니라 부재"). ⚠️ **낙수 17(상세 줄 공백 2건)은 소진되지 않았다** —
  A329는 **정보 패널** 축이고 **탐색기 상세 줄은 무접촉**이다(축이 다르다).
※ **A324·A325는 2026-09-03 v0.314.0으로 완주**(20차 묶음 — 사용자 스크린샷 1장에서 두 결함 ·
  **낙수 27 소진** · 등재와 완료 처리를 한 번에 했다).
※ **A323은 2026-09-03 v0.313.0으로 완주**(19차 묶음 — 사용자 실기기 보고 2건에서 출발 · **낙수 1 소진** ·
  등재와 구현이 같은 날 동시에 끝나 **등재 없이 완료 처리**됐다).
※ **A309는 2026-09-03 v0.312.0으로 완주**(18차 묶음 — 낙수 31 승격 항목).
※ **A308·A319는 2026-09-03에 완주**(14차 문서 묶음 — 코드 변경 0 · 릴리스 없음).
※ **2026-09-03 신규 등재 3건(A320·A321·A322)이 같은 날 전량 완주**됐다 —
  A320(15차·v0.311.0) · A321(16차·코드 변경 0) · **A322(17차·코드 변경 0 · 3배치)**.
  **미착수 신규 항목은 0건**이었다.

그 밖에 `docs/REQUIREMENTS.md`에 남은 `[미반영]` 코드 항목:
- ⚠️ **사용자 입력 대기 1건(2026-09-04 신규).** 정본은 부록 B "확인 필요"다 —
  **낙수 40의 판별 관측**: *탐색기에서 그 클라우드 전용 PDF 폴더의 파일들이 **구름 → 초록 체크로
  바뀌었습니까?*** (바뀌었으면 하이드레이션이 실제로 일어난 것이고 **A175 계약이 깨진 것**이라
  감사 규모가 커진다 · 구름 그대로면 가드 확인만으로 끝난다. **부록 B 104의 적용** — 코드 정독으로는
  "가드가 있는 경로"만 셀 수 있고 실제 수화 여부는 알 수 없다).
- 그 앞의 것은 전건 소진됐다.
  **A327 완주와 함께 올렸던 A328 질문(영상도 같은 방식으로 바꿀까요)은 같은 날 답을 받아 소진**됐고
  (**"영상도 당연히 바꿔야지."** → v0.317.0 완주 + **부록 B 98** 원칙 확정), 그 앞의 것도 전건 소진됐다 — 아래.
  ① **A298·A299 잔여**(빈 화면 Fit 표시 통일)는 사용자 지시 "pdf 도 1:1 으로 할게"로 답을 받아
  **ⓒ 현행 유지 + 값 수정**으로 결론 나 **A320**이 됐고 ·
  ②③ **A308·A319 잔여 2건**은 **A321**이 흡수했다(ⓐ = 전제 오류로 현행 유지 · ⓑ = 정본 쪽으로 통일).
  ※ **A305 잔여 3건은 전건 소진**됐다 — **①(Full screen 단방향의 마우스 해제 불가)은 A311(v0.304.0)** ·
  **③(모드3에서 Hide side panels 무반응)은 A312(v0.303.0)가 질문째 흡수** ·
  **②(콘텐츠 전환 시 기본 사이드바 재적용이 오버라이드를 되돌린다)는 2026-09-02 사용자 지시로 답을 받아
  A314(v0.306.0)로 이행 완료**(모듈 전환이면 해제가 맞고, 같은 모듈 콘텐츠 교체면 유지).
  그 밖은 전부 소진 상태다(A304는 2026-08-31
  "업그레이드"로 확정 → v0.298.0 이행. A159는 같은 날 증상 확정 — 버튼 disabled — 뒤 **v0.291.0으로
  수리 완료**. A142 ④는 2026-08-29 소진 → A283 승계).
- **장기·후순위**: A14·A15(녹화·녹음) · A19(HW 랭킹) · 보류 A4·A43·A45.
- 새 일감은 **REQUIREMENTS 부록 A-2 "낙수(등재 후보)"** 에서 골라 등재한다(§6).

**판단 대기(사용자 지시 없이는 손대지 않는다)**: A234 종결 후 남은 진단 스트립(`diag.shellKeyOverlay`)의
"계측 토글 제거 or 존치" — 미등재.

**방향 전환 주의**(완료 항목의 서술이 이미 뒤집혀 있다 — 규칙 1.6 "재확인"과 혼동 금지):
A257=A235② 반전 · A260=A253 5차 전환 · A264=A245 개정 · A267=A247 개정(팝업 3분기 전면 폐기) ·
A265=A249 예외 공식 해제 · A259=A146/A128 반전 · A273=A109/A81/A202 개정 · A269=A217 첫 재계수 ·
A274=A203 외부 경로 보완 · **A296=A271/부록 B 78 반전**(Print·Fit 제거 — A249 제3층이 아카이브에서 소멸) ·
**A292=A257 재재구성**(마스터 아래 Advanced 링크 · Playback이 그 아래로) ·
**A305=A186 반전**(모드 2 부활 — 게다가 **A203·A274의 전체화면 스냅샷 3종을 폐지**했다. 그 코드를
찾으러 가지 말 것) · **A312=A305 배치 2 되돌림**(하단 바 버튼 2개 → **1개 3상태** · 여백 82→44 ·
Esc 3→2 · Alt+Enter 직행 신설 · 무컨텐츠·S4도 1↔3 — **v0.303.0으로 이행 완료**.
**단 S4 몫은 아래 A313이 곧바로 다시 걷었다**) ·
**A311=A186 자동 숨김 축 확대**(전체화면이면 표면 종류 무관 · **전체화면 영상은 일시정지해도 다시
숨는다** — 의도적 동작 변경) ·
**A313=A312의 S4 항 개정**(S4 = "2단 계단" → **계단 없음**. S4는 모달 레이어라 아래 레이어에 접근
불가가 사양이다 — 부록 B 84. **v0.305.0으로 이행 완료** — `IsTwoStepModeLadder`에서 `IsOpenFileBrowsing`
항이 빠졌고 계단 실행부 3곳에 S4 게이트가 섰으며, 하단 바 위에 전폭 덮개 `S4BarScrim`이 얹혔다) ·
**A313 파생 = A90/A236의 Esc 순서 반전**(전체화면+S4에서 **첫 Esc = 전체화면 해제**였던 것이
**첫 Esc = 곧바로 S4 복귀**로 바뀌었다 — "계단 없음"의 필연 귀결이며 **버그가 아니다**. 부록 B 85) ·
**A314=A273 기본값 반전 + A305 오버라이드 수명 개정**(사이드바 기본값이 **좌·우 모두 열림**으로
뒤집혔고 — A273의 "좌 열림·우 닫힘"을 찾으러 가지 말 것 —, **오버라이드는 다른 모듈로 넘어가면
해제**된다. A305의 "취소 방법 없음"은 폐기. 덤으로 **A151 ⑤ 모드 리셋도 걷혀** 모듈 전환에서
모드가 유지된다(무컨텐츠면 모드2만 모드1로 강등). **v0.306.0으로 이행 완료** · 부록 B 86) ·
**A315=A274 봉쇄 복원 + A312 "Esc 한 단계" 이행 완성**(반전이 아니라 **결함 수리**다 — 셸 팝업 게이트가
**툴팁을 팝업으로 세지 않게** 바뀌었고, 프레젠터 전환 구간에 **재진입 가드**가 섰다. **v0.307.0으로 이행 완료**) ·
**A316=A176의 S4 한정 부활**(반투명 폐지가 **S4 레이어에서만** 되살아난다 — 전역 부활이 아니다 ·
부록 B 87. **v0.308.0으로 이행 완료** — 중앙 `S4CenterBackdrop` 신설 + `ThumbnailExplorer.LayoutRoot`가
`Transparent`가 됐고, 불투명도는 코드 상수 **`S4TranslucentOpacity` = 0.65 단일 출처**가
backdrop과 바 덮개에 함께 대입된다 — **A313의 0.85를 찾으러 가지 말 것**) ·
**A317=A316의 사이드바 제외 판정 개정 + A305 "S4 진입 시 모드2 접기" 제거**(모드2·3의 '파일 열기'는
**모드를 유지한 채 컨텐츠 전폭을 그대로 두고 사이드바만 그 위에 반투명으로 부유**한다 —
**모드1은 종전대로 불투명 도크**다. 판정 근거 = **"뒤에 무엇이 있느냐"**(모드1 = 도크 / 모드2·3 = 컨텐츠) ·
부록 B 88. **v0.309.0으로 이행 완료** — 판정은 `S4SidebarsFloat` 한 곳, 적용은 `ApplyOverlayStates` 한 곳,
억제 산식은 `ModeSuppressesSidebars` 공통 핵. ⚠️ **늘어난 것은 그리기 축이지 A305 상태 축이 아니다** ·
A305 배치 1이 접기의 이유로 든 **"Esc 죽은 층"은 A313의 계단 없음으로 소멸 확정**(재조사 금지 주석이
코드에 있다)) ·
**A326=A292 등록 토글의 되돌림**(⚠️ **이것은 반전이 아니라 해석 오류의 정정**이다 — 규칙 1.6의
"재확인"과도 다르다. Advanced options의 **확장자별 토글 48개는 v0.315.0으로 사라졌고 모듈 토글 5개**만
남는다. **판정 = 그 모듈의 확장자가 전부 등록됐을 때만 On**이고 부분 상태는 "N/M extensions" 줄이 알려 준다.
`ExplorerIntegration`의 **확장자 단위 API 6종은 그대로 살아 있다** — 없어졌다고 생각하고 다시 만들지 말 것) ·
**A327=오디오 정보 패널의 libvlc 트랙 행 폐지**(v0.316.0 — 같은 정보가 Channels/Sample rate 행으로
흡수됐다. **재생 중에만 나오던 그 행을 찾으러 가지 말 것**) ·
**A328=영상 정보 패널의 libvlc 트랙 행 폐지 + 보조 축 강등**(v0.317.0 — 오디오와 달리 **값은 살렸다**.
셸 핸들러가 약한 mkv/webm 대비로 `FillFromPlayer`가 **빈칸 행만 채우는 보조 축**이 됐고, 행 집합·순서는
두 축이 동일하다 — **재생을 멈춰도 행이 사라지지 않는다**. 선택 축의 `Length` 한 줄 갈래도 폐지됐으니
`SelectionQuickInfo`에서 영상 조각을 찾으러 가지 말 것) ·
옛 항목의 구현 서술을 읽을 땐 이 반전들이 **이미 코드에 반영돼 있음**을 전제할 것
(예외 없음 — **A313도 코드에 반영된 상태다**).
※ **ARCHIVE의 A305 절은 이미 낡았다** — 그 절 머리의 개정 경고(①~⑦)를 먼저 읽고,
화면 모드·하단 바 서술은 **ARCHIVE의 A312·A311 절**을 정본으로 삼을 것.
※ **ARCHIVE의 A312 절도 S4 축은 낡았다** — 그 절 머리의 개정 경고(A313 이행 완료)를 먼저 읽을 것.
**시나리오 B 표의 "또는 S4"는 v0.305.0으로 무효**이고(그 표는 이제 **무컨텐츠 S1에만** 유효하다),
S4 서술의 정본은 **ARCHIVE의 A313 절**(동작 축)과 **A316·A317 절**(시각 축)이다.
※ **ARCHIVE의 A316 절도 사이드바 축은 낡았다** — 그 절 머리의 개정 경고(A317 이행 완료)를 먼저 읽을 것.

**문서 상태**: 2026-08-29 3차 다이어트로 REQUIREMENTS 2,499줄 → 1,969줄(완료 상세 42건 571줄 ARCHIVE 이관).
이후 A292~A316 등재분이 얹혔고, **스물세 묶음**의 완료 상세를 **묶음마다 즉시 ARCHIVE 이관**해(규칙 5 —
본문에는 한 줄 요약만) 왔다. 2026-09-03 A320·A321·A322 등재분 약 130줄이 얹혔다가
**15·16·17차 완료 처리로 전량 회수**됐고, 21·22차 완료 처리로 A326·A327 등재문(약 45줄)도,
**23차 완료 처리로 A328 등재문(약 30줄)도** 회수됐다.
현재 **REQUIREMENTS 2,952줄 / ARCHIVE 4,155줄**이다.
**24차 완료 처리로 A329 등재문(약 30줄)까지 회수**돼 한때 **미반영 등재문이 0줄**이 됐다.
⚠️ **2026-09-04 28차 완료 처리로 다시 늘었다** — A334~A339 완료 상세는 **전량 ARCHIVE 신설**
(본문은 한 줄 요약 + 포인터 · 규칙 5)이지만, **A340·A341 미반영 등재문(약 45줄)** 과
**부록 B 107·108·109(약 55줄)** · **낙수 40(약 15줄)** 이 본문에 얹혔다.
**부록 B와 낙수는 완료돼도 회수되지 않는 부류**이므로 이 증가는 정상이고,
**회수 예정분은 A340·A341 등재문뿐**이다(착수하면 완료 한 줄로 줄어든다). 대신 **부록 B 98이 하위 규칙 ⑦·⑧만큼 더 늘었다** —
이것은 완료돼도 회수되지 않는 **전 모듈 원칙**이라 존치가 정상이고, 이제 **이행이 끝난 원칙**이라
다음 세션은 "적용할 모듈이 남았나"를 다시 묻지 않아도 된다.
⚠️ **가이드가 4벌이 됐다**(`docs/USER-GUIDE.md` 정본 · `site/guide.html` · `docs/USER-GUIDE.ko.md` ·
`site/guide.ko.html` — 17차 A322). 유지 규칙은 **부록 B 91**이다: 갱신은 **영어 md → 영어 site →
한국어 md → 한국어 site** 순서, **HTML 앵커 `id`는 두 언어가 같은 값**, **UI 문자열은 번역하지 않는다**.
**이번 세션에 영어 1벌이 30여 곳 밀려 있던 것(발견 ㉓)을 감안하면 4벌은 그 부담이 두 배**이고,
**한국어판이 가장 먼저 썩는다**(읽는 사람이 적어 밀린 것을 늦게 안다). 그래서
**UI를 바꾸는 항목은 등재 단계에서 문서 동기화 몫을 함께 적어야 한다** — 이 규칙이 4벌 체계의 전제 조건이다.
※ **사이트는 아직 게시돼 있지 않다**(부록 B 93) — README가 독자에게 거는 링크는 `docs/*.md`이고
`site/*.html`은 저장소 안 파일 안내로만 쓴다.
7차 완료 처리로 REQUIREMENTS가 **약 230줄 줄었고**(A310·A311·A312 본문 이관 — A312의 전이표 3종은
구현 대조와 함께 ARCHIVE의 A312 절에 그대로 보존했다. 분량이 큰 것은 사양 정본이라 의도한 것이다),
8차 완료 처리로 A313 본문 82줄이 다시 ARCHIVE로 내려갔다(부록 B에 결정 1건 신설 — 85번).
9차 완료 처리로 A314 본문 89줄이 두 줄로 줄었고(상세는 ARCHIVE의 A314 절), 부록 B 86번이 결정 기록이다.
10차 완료 처리는 **A315 상세를 처음부터 ARCHIVE에 신설**(본문은 완료 한 줄)하고, 같은 배치에서
**A316을 미반영으로 등재**(부록 A 표 행 + 부록 B 87)해 본문이 약 40줄 늘었다.
**11·12차 완료 처리에서 그 증가분을 예고대로 회수**했다 — A316 미반영 블록(약 35줄)과 부록 A 표 행이
완료 한 줄로 줄었고, **A317은 등재 단계를 건너뛰어 처음부터 완료 한 줄 + ARCHIVE 상세**로 들어갔다
(부록 B에 결정 1건 신설 — 88번).
13차 완료 처리도 **A318을 처음부터 완료 한 줄 + ARCHIVE 상세**로 넣었다(부록 B 89번 신설 —
"꺼짐을 Opacity로 표현하지 않는다"는 **일반 규칙**이라 다른 토글에도 적용된다).
같은 처리에서 **A255의 낡은 판정 서술 4곳을 정정**했다(REQUIREMENTS 본문 한 줄 · ARCHIVE A255 절 ·
부록 A 완료 행 · "확인 필요 소진 기록" — 전부 A318로의 포인터). **다음 세션이 빗금을 다시 기각하지 않게 하는 것이
이번 문서 작업의 핵심**이다.
같은 처리에서 **"확인 필요"의 A305 잔여 묶음이 전건 소진**돼 사용자 입력 대기가 **1건**만 남았다.
남은 완료 상세는 **의도적 존치**다 — 미진행 항목이 A번호를 언급하는 것들과
**A257~A277·A271·A291 등 실기기 확인·판정 대기분**(확인이 끝나면 같은 기준으로 이관할 것 — §6 4번 "REQUIREMENTS 다이어트").
앞선 08-28 다이어트는 3,763줄 → 2,499줄이었고, 그때 세션 일지·실기기 누적분을 별도 파일로 분리해
HANDOVER가 이 크기가 됐다. 같은 날 실기기 확인 누적분도 정리했다: DEVICE-CHECKS 594줄 → 232줄,
v0.161.0 이하 20묶음은 `docs/DEVICE-CHECKS-ARCHIVE.md`로 "미확인 종결"(CLAUDE.md §4 종결 규칙 신설).
**2026-09-01에 §4 밀어내기를 실행**해 앞 3블록(v0.283.0~v0.294.0)이 DEVICE-CHECKS로 내려갔다(현재 366줄).

---

## 2. 일하는 방식

**정본은 `CLAUDE.md`(협업 규칙·파이프라인·빌드 환경·브랜딩 불가침)** — 여기에는 CLAUDE.md에 없는
저장소 운용 세부만 둔다. CLAUDE.md와 어긋나면 CLAUDE.md가 이긴다(2026-08-21 개정: push는 **버전 커밋마다 즉시 1회**).

- **개발 우선순위: UI·코어 기능 우선**(사용자 확정). 녹화/녹음(A14·A15), HW 랭킹(A19), 포터블(A43),
  HWP 뷰어(A45)는 후순위.
- ⚠️ **모델 용량 장애 시 오케스트레이터의 직접 구현이 허용된다(2026-09-04 선례 · 사용자 확정).**
  **Opus 하위 에이전트가 529 과부하로 5회 연속 실패**해 사용자가 **"이 세션에서 직접"** 으로 확정했고,
  **오케스트레이터가 A336~A339를 직접 구현**했다(v0.324.0·v0.325.0·v0.326.0·v0.327.0).
  **CLAUDE.md §2의 위임 원칙(직렬 위임·서브에이전트는 구현만)은 그대로 유지**하되,
  **모델 용량 장애가 반복되면 위임을 고집하지 않는다** — 판단 기준은 "재시도가 진행을 만드는가"다.
  ⚠️ **직접 구현할 때 잃는 것 둘을 오케스트레이터가 스스로 메워야 한다**:
  ① **함정 선지목**(§3·§3.5 검사를 자기 편집에도 돌린다 — **2026-09-04에 한글 주석 이스케이프 사고가
  실제로 재발했다**) ② **보고 검수**(구현자와 검수자가 같아지므로 **통과 조건 표를 자기 손으로 채울 것**).
- **push remote 이름은 클론마다 다르다 — `git remote -v`로 먼저 확인할 것**(소스트리 PC = `zp`,
  2026-08-29에 붙인 GitHub Desktop PC = `origin`). 인증은 저장소 로컬 git 설정에 있음 — 401이면
  사용자에게 토큰 재발급 요청. 새 PC 클론이면 identity·PAT 2단계가 필요하다(CLAUDE.md §3).
- **CI 판정 정본 = release 워크플로**(실제 빌드·패키징을 하므로 초록 = 컴파일 통과 실증). build 워크플로가
  Actions 장애로 Queued/Startup failure여도 release가 초록이면 무해. 확인은 사용자 Chrome(브라우저 MCP)으로
  `github.com/zpstudios/kotu/actions`(최신 run의 `svg[aria-label]`이 성공/실패) — 활성 탭을 뺏지 말 것.
  build.yml·release.yml이 동시에 돌고 각 3~4분.
- **완료 처리 절차**: 항목을 `※ A?? 완료 — 결번`으로 바꾸고 부록 A 표에서 행을 빼며, 완료 상세는 미진행 항목이
  참조할 때만 본문에 남긴다(아니면 ARCHIVE로). 미구현 항목의 내용이 바뀌면 새 번호를 주지 않고 그 자리에서 갱신.
- **개인 실명·개인 메일 금지**(A82) — 실명을 지우는 작업의 기록에 실명을 인용하는 실수가 잦다(v0.90.0 실사례).

**작업 환경 장애 — 샌드박스가 재부팅 후에만 죽는다(2026-09-02 실사례).**
이 세션 중 Cowork 샌드박스가 올라오지 않아 **커밋·push가 한동안 막혔다**(Cowork 자체는 정상 기동 —
샌드박스만 안 떴다). 원인 = **설치본이 둘이었다**(MSIX 관리자 설치본 + 사용자 범위 Squirrel 설치본)
+ **HKCU Run 키가 옛 사용자 범위 설치본을 자동 실행**. 그래서 **재부팅 뒤에만** 증상이 났다.
- **진단 순서(이 순서대로 볼 것)**: ① `vmcompute` 서비스 상태 → ② **실행 중인 프로세스의 경로**
  (`Get-Process *claude* | Select Path -Unique` — 여기서 옛 설치본 경로가 드러난다) → ③ **HKCU Run 키**.
- **복구** = Run 키의 `Claude` 값 제거 + `%LOCALAPPDATA%\AnthropicClaude\Update.exe --uninstall`(사용자 범위
  설치본 제거) + **MSIX 설치본으로 재실행**.
- **막힌 동안의 작업 방식** — 셸이 없어도 **파일 도구(Grep/Read)로 구현 결과를 검증**할 수 있다.
  검증을 미리 끝내 두고 복구 즉시 커밋해 **작업 손실은 0**이었다. 셸 장애로 커밋이 몰리는 것은
  전례가 있으므로(v0.207.0·v0.224.0 일괄 커밋) **"막히면 검증을 먼저 끝내 둔다"** 로 대응할 것.

---

## 3. 반드시 알아야 할 함정

### 3.1 레거시 식별자 — 절대 일괄 치환하지 말 것
이름이 세 번 바뀌었다: `WinUtil`(초기 코드명) → `ZP`(v0.33.0) → `KOTU`(v0.86.0~).
**구 이름을 그대로 둬야 하는 곳**이 있다. 바꾸면 "구 등록 청소" 로직이 깨진다:

- `ExplorerIntegration.LegacyBrands = ["ZP", "WinUtil"]`
- `%AppData%\ZP`·`%AppData%\WinUtil`·`ZP-Main`·`WinUtil-Main`을 언급하는 주석
- `release.yml`의 구 packId 안내 문구

**교훈**: A46에서 "ZP" 문자열만 훑어 `%TEMP%\WinUtil\subtitles`를 놓쳤다(A64에서 발견).
리브랜딩 스캔은 **현재 이름이 아니라 모든 과거 이름으로** 해야 한다.

### 3.2 셸 등록은 "등록한 그 순간"으로 굳는다
`ExplorerIntegration`이 레지스트리에 쓰는 값 중 **exe 절대 경로와 표시 문구는 재등록될 때만 갱신**된다.
- 그래서 A52로 모듈명을 바꿔도 이미 켜 둔 사용자의 우클릭 메뉴는 옛 문구 그대로다.
- A64로 exe명이 바뀌면서 등록된 경로가 없는 파일을 가리키게 됐고, 실기기에서 실제로
  .png 더블클릭이 '어떤 앱으로 열까요' 팝업으로 빠졌다 → **v0.89.0의 A78이 자동 복구를 넣음**
  (매 실행 시 `ReRegisterIfExeMoved`가 경로 어긋남을 감지해 켜져 있는 등록만 조용히 재기록).
- **Capabilities와 UserChoice(A38)는 ProgID만 참조**해서 경로 변화의 영향을 받지 않는다.
- A38의 자체 검증(`VerifyUserChoice`)은 **우리 해시 알고리즘 기준의 자기일관성 검사**다 —
  Windows가 실제로 수용하는지는 열어 봐야 안다. OS가 알고리즘을 바꾸면 "전부 성공"으로 보이면서
  실제로는 전부 무효가 되는 맹점이 있다(§5 첫 항목의 의심 지점).

### 3.3 하드웨어 모듈은 거의 전부 static (프로세스 공유)
`HardwareModule`의 폴러·섹션 캐시·구독자 수·리프레시 주기가 모두 `static`이고,
센서 선택(A18)·주기(A29)도 프로세스 단위 설정 키다. **A42에서 의도한 설계**(드라이버 핸들·WMI 조회 공유)라
통째로 뒤집으면 안 된다. **A70(v0.131.0)이 그 분리선을 그었다** — 폴러·주기는 공유 유지, 선택(A18)·바
크기(A62)는 `HardwareInstanceState`(인스턴스 상태 + 전역 1벌 저장 깔때기)로 창별 분리. A60 3차의 창별
상태(피닝·순서·모드)는 **이 클래스에 필드를 얹는 방식**으로 간다(새 static을 만들지 말 것).

### 3.4 빌드를 실제로 깨뜨린 것들 (재발 방지)
- **csproj/XAML 주석 안에 하이픈 두 개(`--`)** → MSB4025로 빌드 실패. XML 주석 문법 제약.
  (A64에서 `--mainExe`를 주석에 적었다가 실제로 깨짐.)
- **PowerShell 백틱 줄바꿈 뒤에 주석** → continuation이 깨진다. 백틱은 그 줄의 마지막 문자여야 한다.
- `resources.pri` — unpackaged WinUI는 exe 옆의 **고정 이름 `resources.pri`** 만 찾는다.
  없으면 시작 시 `XamlParseException`으로 조용히 죽는다(v0.5.5~0.5.7에 실제로 겪음).
  `AssemblyName`을 바꾸면 `$(TargetName).pri` 폴백 이름도 따라 바뀌므로 `release.yml`을 같이 고쳐야 한다.
- **`FrameworkElement.Parent`를 "이미 추가했나" 가드로 쓰지 말 것** — 라이브 트리 부착 전
  (뷰 생성자 안)에는 `Children.Add` 뒤에도 Parent가 null이라, 같은 요소를 두 번 Add해
  `COMException 0x800F1000`으로 죽는다(v0.111.0/A62가 유발, v0.113.2에서 수정).
  중복 방지는 `Children.Contains`(또는 자체 플래그)로. 이것도 **CI·정적 검사로 못 잡는 부류**다.
- **XAML 요소를 다른 컨트롤로 옮겨 붙일 때(reparent)** 는 **반드시 옛 부모의 `Children`에서 먼저 제거**해야
  한다. 판정은 `Children.Contains`(위 항목과 같은 이유로 `Parent` 금지). 선례가 셋 있다 —
  `ImageViewerView.TakeBottomBar()`(v0.27.0), `ExplorerPane.DetachPathBar()`(A91/v0.115.0).
  ※ **x:Name 필드는 부모에서 떼어도 살아 있다**(`InitializeComponent`의 `Connect()`가 잡아 둔 참조).
  그래서 옮긴 요소의 이벤트 핸들러·코드비하인드 참조는 손댈 필요가 없다.
- **정적 이벤트 구독은 반드시 Unloaded에서 해제한다** — `CompositionTarget.Rendering`(A88/v0.114.0)처럼
  프레임워크 static 이벤트에 뷰 메서드를 걸면, 해제를 빠뜨렸을 때 **뷰와 그 창이 통째로 샌다**.
  이 저장소의 관용구는 `Loaded`에서 `-=` 후 `+=`(이중 구독 방지), `Unloaded`에서 `-=`다.
- **`ImplicitUsings`가 `System.IO`를 넣어 주므로 짧은 이름 `Path`는 `Microsoft.UI.Xaml.Shapes.Path`와
  모호해진다**(CS0104). **파일에 `using System.IO;`가 없다고 안심하지 말 것** — v0.310.0(A318)이
  `src/Shared/MediaIcons.cs`의 `new Path`로 실제로 깨졌고 v0.310.1에서 수리했다.
  WinUI 도형을 코드로 만들 때 `Path`는 **완전 수식**(`new Microsoft.UI.Xaml.Shapes.Path()`)하거나
  별칭(`using ShapePath = …`)을 쓴다. 같은 함정을 가진 이름이 더 있다(`System.IO.Path`·`Stream` 계열).
  **암시적 using은 소스에 안 보이므로 정적 자체 검사가 못 잡는 부류**다 — 판정은 소스가 아니라
  `Directory.Build.props`/csproj의 `ImplicitUsings` 설정에서 시작할 것.
- **`{StaticResource Default*Style}` BasedOn 참조는 런타임에만 터진다** — v0.107.0의
  `DefaultDropDownButtonStyle`이 실기기 시작 크래시를 냈다(v0.113.1에서 수정). WinAppSDK 1.8이
  WinUI 컨트롤 기본 스타일을 지연 로딩해 App.xaml 파싱 시점에 키가 없다. **CI(컴파일)로는 못 잡고,
  정적 검사로도 못 잡는 부류** — 새 XAML에서 프레임워크 키를 StaticResource로 참조할 때는
  "그 키가 파싱 시점에 로드돼 있는가"를 의심할 것. 키 없이 setter만 둬도 기본 템플릿은 유지된다.

### 3.5 편집 도구가 한글 주석을 `\uXXXX`로 저장한다 (2026-08-24 실사고 · **2026-09-04 재발**)
파일 편집 도구가 한글 주석을 **유니코드 이스케이프 표기**(백슬래시 + `u` + `AC00` 같은 코드포인트 표기)로 저장해 버리는 사고가 있었다
(CLAUDE.md §2 4번이 그때 생긴 규칙이다). ⚠️ **2026-09-04에 재발했고, 이번에는 서브에이전트가 아니라
오케스트레이터가 직접 편집하던 중**이었다(Opus 하위 에이전트 529 장애로 직접 구현하던 구간 — §2).
즉시 검출·복구했다.

- **검사 명령(커밋 전 상비)**:
  `grep -rnE '\\u(A[C-F]|[BCD][0-9A-F])[0-9A-F]{2}' <경로>` → **0건이어야 한다.**
- **복구 범위** = **한글 음절·자모·문장부호만** 실제 문자로 되살린다.
  ⚠️ **Segoe 글리프 리터럴(E7xx·E8xx 사용자 정의 영역)은 손대지 말 것** — 그것은 의도된 표기다
  (따옴표 안 글리프 리터럴도 오탐이 아니다).
- ⚠️ **오케스트레이터가 직접 편집할 때도 같은 검사를 돌린다** — 이 함정은 "서브에이전트의 문제"가
  아니라 **편집 도구의 문제**다. 위임 여부와 무관하게 커밋 전 grep이 관문이다.

---

## 4. 실기기 확인 대기 (사용자 몫) — 최신 묶음

> 누적분(v0.162.0~v0.294.0)은 `docs/DEVICE-CHECKS.md`. 이 절에는 **가장 최근 완주 묶음**의 확인 포인트만 두고,
> 다음 묶음이 완주되면 이 절 내용을 DEVICE-CHECKS.md 상단으로 밀어낸다.
> ※ **2026-09-01 밀어내기 실행** — 6차 묶음이 완주해 **앞 3블록(v0.283.0~v0.286.0 · v0.287.0~v0.290.0 ·
> v0.291.0~v0.294.0)을 DEVICE-CHECKS.md 상단으로 보냈다**(전부 미확인인 채 내려갔다 — 회수는 그쪽에서).
> ※ **2026-09-02**: 7차 묶음(v0.302.0~v0.304.0)에 이어 **8차(v0.305.0)·9차(v0.306.0)·10차(v0.307.0)·
> 11·12차(v0.308.0~v0.309.0)·13차(v0.310.0)**까지 완주해 **블록을 밀어내지 않고 병치**했다.
> ※ **2026-09-03**: **15차(v0.311.0)**·**18차(v0.312.0)**·**19차(v0.313.0)**·**20차(v0.314.0)** 블록과
> **21·22차 합본 블록(v0.315.0~v0.316.0)**, **23차 블록(v0.317.0)**, **24차(v0.318.0)·25차(v0.319.0)·
> 26차(v0.321.0)·27차(v0.322.0)** 도 같은 방식으로 병치했다.
> ※ **2026-09-04**: **28차 합본 블록(v0.323.0~v0.327.0)** 도 병치했다 — **다섯 릴리스를 한 블록으로
> 묶은 이유** = 확인 시나리오가 릴리스를 가로지른다(**①의 재측정 하나가 A334 계측판·A335·A339를 동시에
> 판별**한다). 나누면 같은 지시를 세 번 적어야 한다. 선례 = 21·22차 합본 블록.
> **14·16·17차는 문서 전용(코드 변경 0)이라 확인 포인트가 없다 — 새 블록을 만들지 않았다.**
> ※ **v0.320.0도 블록이 없다** — 그 내용(A331)이 **v0.321.0에서 되돌려져** 확인할 동작이 남지 않았다.
> ⚠️ **다음 밀어내기 대상 = 맨 앞 v0.295.0~v0.298.0 블록**(가장 오래 밀린 것부터).
> 여기 남은 것은 **19블록**
> (v0.295.0~v0.298.0 · v0.299.0~v0.301.0 · v0.302.0~v0.304.0 · v0.305.0 · v0.306.0 · v0.307.0 ·
> v0.308.0~v0.309.0 · v0.310.0 · v0.311.0 · v0.312.0 · v0.313.0 · v0.314.0 · v0.315.0~v0.316.0 ·
> v0.317.0 · v0.318.0 · v0.319.0 · v0.321.0 · v0.322.0 · **v0.323.0~v0.327.0**)으로
> **1블록 규칙의 명시적 예외**다(오케스트레이터 지시). **A291 판정 블록만 A307 판정이 걸려 있어 맨 위에 존치.**
> **다음 묶음이 완주되면 `v0.295.0~v0.298.0`부터 밀어낼 것.**
> ※ **v0.305.0 블록 ③의 "0.85 덮개가 과한지"는 v0.308.0이 흡수**했다 — 값은 이제 **0.65 단일 출처**이고,
> 그 축은 **아래 v0.308.0~v0.309.0 블록에서 확인**할 것.
> ※ **v0.299.0~v0.301.0 블록의 ②③(A305 화면 모드·하단 바 2버튼)은 v0.303.0~v0.304.0이 갈아엎었다** —
> 그 두 항목은 **아래 7차 블록의 새 사양으로 확인**하고, 옛 서술은 대조용으로만 읽을 것.
> **v0.161.0 이하는 2026-08-29에 `docs/DEVICE-CHECKS-ARCHIVE.md`로 "미확인 종결" 이관**(CLAUDE.md §4 종결 규칙).

✅ **v0.282.0 (A291) — 스크린샷 2장 2026-08-31 수령·판정 완료. 후속 A307은 v0.294.0으로 수리 완료 — 판정 대기.**

판정 = **`trailingEdge: true` 부분 채택**. 즉 A284의 "쓰레기" 판정은 좌표 결함 셋(X `pad.Left` 누락 ·
Y `pad.Top` 누락 · 줄 탐색 정지)에 오염돼 있었고, 좌표계가 정리된 지금은 **글자가 있는 마지막 줄에서
`eofSrc=trailing`이 정상 동작**한다(A291의 목적 달성). 다만 **빈 마지막 줄에서는 개행 문자의 `trail`이
이전 줄의 Y를 주기 때문에 가드에 기각되고 `eofSrc=head` 폴백**으로 떨어져, 본문·밑줄·줄 번호와 y가
어긋났다. **잔여 증상은 A307로 승계**했고, **A307은 v0.294.0으로 수리를 마쳤다 — 남은 것은 판정뿐이다**
(판정 스크린샷 1장 = **`docs/DEVICE-CHECKS.md`의 v0.291.0~v0.294.0 블록 ④** — 2026-09-01에 밀려났다).
**진단 값 2벌의 전사는 이제
`docs/REQUIREMENTS-ARCHIVE.md`의 A307 절**에 있다(다음 세션이 스크린샷 없이 재판정할 수 있다).

> 판독법(원문 보존 — A291 시점의 판정 기준):
> - **① 한 글자만 있는 문서.** 볼 것 = **`trail=`** 과 **`eofSrc=`**. `trail=`의 X가 글자 뒤쪽 모서리이고
>   `eofSrc=trailing`이면 부활 성공(`·EOF`가 글자 바로 뒤에 붙는다). `trail=`이 `0,0` 계열이거나
>   `eofSrc=advance`/`step`이면 `trailingEdge`가 무효인 것.
> - **② 한 줄 치고 엔터만 친 문서.** 볼 것 = **`endLine=`**. `trailing`이면 개행 문자의 뒤쪽 모서리가
>   빈 줄 시작을 준 것(`trail=`의 Y가 다음 줄 값이어야 한다). `height`면 그 길이 막힌 것 —
>   **2026-08-31 결과가 이 쪽이다**(`endLine=height`, `trail=`의 Y가 이전 줄 16.0).
> - **③ 회귀 확인**: 두세 줄 친 상태에서 EOF·`¶`·줄 번호·줄 사이 선이 이전 버전과 같은지.
>
> **※ v0.294.0부터는 판독 축이 하나 늘었다 — `stepSrc=`("출처:값")** 가 `ResolveLineStep` 사다리의
> 어느 단계가 쓰였는지를 찍는다(`실측` / `캐시` / `font`(A307 신설 폰트 프로브) / `height`(옛 폴백)).
> **빈 마지막 줄에서 `stepSrc=font:16.0`이면 A307 채택, `height`면 기각**이다.
> `endLine=`은 그대로 A291 축(빈 줄 좌표원)을 가리킨다 — 둘은 다른 값이니 섞지 말 것.

**남은 미세 조정**(A307 판정 뒤 일괄 처리): `GuideOpacity`(0.14 — 진하면 0.11·흐리면 0.20) ·
`GuideGap`(2 — 글자에 닿으면 3~4).

⚠️ **v0.295.0~v0.298.0 (2026-09-01 A300·A301·A302·A303·A304 완주분 — 오디오 묶음) 실기기 확인 포인트**

> ※ 이 묶음은 v0.298.0 CI가 진행 중인 상태에서 문서화됐으나, **`NAudio.Wasapi` restore 위험은 소진**됐다
> (뒤이은 v0.299.0~v0.301.0이 같은 복원 위에서 초록). 남은 것은 아래 실기기 확인뿐이다.

① **v0.295.0 EQ·Visualizer 아이콘(A300).** 하단 바 우측군의 **EQ 아이콘이 "슬라이더 3개"** 로,
   **Visualizer 아이콘이 "스펙트럼 막대 5개"** 로 **기능이 읽히는지**(글리프가 아니라 코드로 조립한
   도형이다 — 안 읽히면 도상 자체를 다시 고른다) · **비활성일 때 회색**이 되는지(A299형 센티널
   동기를 그대로 썼다 — **테두리·도형이 따로 노는 A299 재발이면 그 사실을 보고**) ·
   **이웃 글리프(Devices·전체화면)와 크기·굵기 균형**이 맞는지 · 라이트/다크 양쪽.
   ※ **영상 모듈에는 EQ·Vis 버튼이 없다** — 영상 쪽은 확인 대상이 아니다(등재문 전제가 틀렸다).
② **v0.296.0 교체 직렬화 + 계측(A301) — 이 묶음 1순위.** 설정에서 **`diag.audioSwap`을 켜고**
   비주얼라이저 스타일을 바꿔 ~~**`swap=NNNms` 값을 그대로 보고**할 것(수백 ms대인지가 판단 기준)~~
   **2026-09-03 소진 — 사용자 실기기 관측 `swap=85ms`**(예상하던 수백 ms대를 크게 밑돈다.
   **교체 비용은 작다 = 남은 버벅임이 있어도 재생성 비용 축이 아니다** — 실측은 ARCHIVE의 A301 절) ·
   **교체 순간의 버벅임이 체감상 나아졌는지** · 교체 중에 **연타하면 무시되는데 그게 어색하지 않은지**
   (`_recreating` 가드 — 어색하면 "무시" 대신 큐잉으로 바꾼다) · 교체 후 **볼륨·음소거·EQ·출력 장치·
   재생 위치가 그대로 유지**되는지.
   **★ A303 판별(이 항목의 핵심)** = **직렬화 후에도 Spectrometer를 고르면 팝업이 뜨는가.**
   **안 뜨면 H1(재생성 경합의 libvlc 독립 창 폴백) 확정·해소** — 그대로 종결한다.
   **뜨면 팝업의 제목과 내용을 읽어서 보고**할 것(H2 = 인서피스 오류 텍스트 오인 가능성. 우리 앱의
   대화상자가 아닌 것은 코드로 이미 확정했다).
   ※ **싱크 어긋남 폭은 직접 계측이 불가능**하다(LibVLCSharp 3.x가 값을 안 준다) — **체감 ms를
   말로 보고**해 주면 보정 옵션 실험을 2차로 연다. 어림 보정은 일부러 넣지 않았다.
③ **v0.297.0 정지 중 세러모니(A302).** **일시정지(또는 정지) 상태에서** 비주얼라이저를 바꾸면
   **중앙 하단에 칩(`Visualizer: <이름>`)이 1.5초** 떴다 사라지는지 · **연속으로 여러 번 바꿔도
   칩이 겹치지 않고 마지막 것만 남는지**(타이머 리셋) · **재생 중에는 안 뜨는 것이 정상**
   (사용자가 정한 범위다 — 재생 중에도 원하면 보고) · 칩이 **제목 오버레이·플레이스홀더·진단 패널과
   겹치지 않는지** · 칩 위를 클릭해도 **아래 표면이 정상 반응**하는지.
④ **v0.298.0 VU 미터 자체 렌더(A304).** Visualizer에서 **VU meter를 고르고 재생** → **L/R 두 줄 바가
   소리에 맞춰 실제로 움직이는지**(RMS 채움 + 피크 홀드 700ms) · **시각화 지연이 개선됐는지**
   (루프백 직접 계측이라 원리상 libvlc 경로보다 빠르다 — 체감 보고) · **일시정지·다른 화면 이탈·창
   닫기 뒤 CPU가 유휴로 돌아오는지**(캡처가 안 끊기면 이 축 보고) · **비기본 출력 장치를 골랐을 때
   그 장치를 따라가는지**(A164 선택값 사용 — 실패 시 기본 장치로 떨어지는 것이 사양) ·
   **Off ↔ VU meter를 왕복**했을 때 재생성이 한 번씩만 일어나는 체감인지.
   ※ **루프백은 시스템 믹스**라 **다른 앱의 소리가 섞여 미터가 움직이는 것은 수용된 사양**이다
   (재생 중에만 캡처하므로 유휴 오검출은 없다). 거슬리면 별도 보고.

⚠️ **v0.299.0~v0.301.0 (2026-09-01 A292·A305 완주분 — 6차 대형 묶음) 실기기 확인 포인트**

> ※ **이 묶음이 이번 세션의 마지막이자 가장 넓다.** A305는 셸 상태기계를 다시 짰으므로
> **화면 모드 왕복이 1순위**다.

① **v0.299.0 설정 Advanced options(A292).** Explorer integration 절에서 **"Advanced options" 링크를
   누르면 확장자 목록이 펼쳐지는지**(닫힘으로 시작하는 것이 정상 · **펼침 상태는 저장하지 않는다**) ·
   **확장자 하나만 끄면 그 확장자만 연결이 풀리는지**(다른 확장자·다른 모듈은 그대로) ·
   **마스터 On이 48건을 처리하는 체감**(멈춘 것처럼 보이면 보고 — 진행 표시 축을 다시 본다) ·
   **`Troubleshooting` 머리글 아래 진단 4카드가 한 덩어리로 읽히는지**(머리글이 없어 떠 보이던 것을
   봉합했다) · **마스터 · Advanced options 링크 · Playback이 한 화면에 함께 보이는지**(이 재배치의 목적) ·
   **`.pdf` 같은 UCPD 보호 확장자를 켰을 때 딥링크가 자동으로 열리지 않고 안내 문구만 나오는지**
   (A166 + 48연발 방지) · **exe를 옮긴 뒤 실행했을 때 꺼 둔 확장자가 되살아나지 않는지**
   (`ReRegisterIfExeMoved`는 등록된 것만 재기록한다 — 되살아나면 이 축 보고).
② **v0.300.0 화면 모드 상태기계(A305 배치 1) — 이 묶음 1순위.**
   **Enter 4연타로 1→2→3→1 복귀**하는지(토글이 아니라 계단이다) · **모드 2에서 창 크기가 그대로**이고
   **제목표시줄 + 하단 바만** 남는지 · **모드 3에서 Esc 한 번에 모드 1**로 오는지(그 뒤 한 번 더 Esc면
   콘텐츠가 닫힌다) · **모드 2에서 F11을 누르면 사이드바가 즉시 나타나고**, 그 뒤 Enter가 **2단(커스텀
   일반 ↔ 커스텀 전체)** 으로 바뀌는지(**첫 F11이 무반응이면 승계 함정 재발이니 즉시 보고**) ·
   **모드 3에서 F12를 누르면 전체화면을 유지한 채** 오버라이드로 승계되는지 · **창을 두 개 띄워
   한쪽만 오버라이드**했을 때 다른 창이 영향을 받지 않는지(창별 독립) · **이미지 더블클릭 전체화면을
   해제하면 모드 1에 착지**하는지 · 영상·문서 모듈에서도 같은지.
③ **v0.301.0 하단 바 버튼 2종(A305 배치 2).** 좌→우로 **ⓐ Hide side panels · ⓑ Full screen**이 서는지 ·
   **ⓐ 아이콘(5막대 조립)이 "사이드바 접기"로 읽히는지**(안 읽히면 도상을 다시 고른다 — 글리프 선례가
   없어 코드로 조립했다) · **비활성일 때 5막대가 전부 함께 회색**이 되는지(A299형 전경 동기 —
   일부만 회색이면 보고) · **모듈 바가 38px 줄어든 체감**이 어색하지 않은지
   (파생 재검산 = **H/W 최소 창 여유 20** · **영상 축약 시작 815**부터 — 좁은 창에서 겹치거나
   튀면 이 두 수치를 다시 잡는다) · 모드가 바뀔 때 **두 버튼의 활성/비활성이 즉시 따라오는지**
   (갱신 지점이 둘이라 한 박자 늦는 경로가 있으면 보고).
   ※ **전체화면에서 ⓑ가 비활성이라 마우스만으로는 전체화면을 못 푼다** — 이것은 사양대로이며
   **§5의 결정 대기 항목**이다(확인이 아니라 답을 주면 된다).

⚠️ **v0.302.0~v0.304.0 (2026-09-02 A310·A312·A311 완주분 — 7차 셸 묶음) 실기기 확인 포인트**

> ※ **A312가 화면 모드 체계를 다시 정의했으므로 ②가 이 묶음의 1순위**다. 위 v0.299.0~v0.301.0 블록의
> ②③은 이 블록이 대체한다(옛 사양으로 확인하지 말 것). **v0.304.0 CI는 초록 확인 완료**다.
> ※ **이 블록 ②의 "S4에서도 1↔3만"은 v0.305.0(A313)이 다시 갈아엎었다** — S4는 이제 **계단 자체가
> 없고** 바가 덮개로 차단된다. **S4 축은 아래 8차 블록으로 확인**할 것.

① **v0.302.0 Fit "Original" 아이콘 통일(A310).** 이미지·영상·문서 **세 표면 모두**에서 Fit 플라이아웃의
   **Original 행 아이콘이 하단 바 본체의 1:1 버튼과 같은 그림**으로 보이는지(이것이 이 항목의 전부다 —
   "위아래가 미세하게 다르다"가 사라졌는지) · **아이콘 칸에서 잘리거나 눌리지 않는지**(16×12 칸을
   플라이아웃 아이콘 칸에 넣었다 — 잘리면 칸 규격을 다시 잰다) · **5px대 축소에서 "1"의 깃발(세리프)이
   읽히는지**(안 읽히면 **민 획으로 단순화**한다 — 그 판단은 눈으로만 가능하다) ·
   **125%/150% 배율에서 콜론 점 2개가 뭉치지 않는지** · **라이트/다크 양쪽**에서 전경색이 따라오는지
   (PathIcon 단일 Geometry라 전경 상속만으로 회색이 돼야 한다 — 테두리만 따로 노는 형태면 보고).
② **v0.303.0 화면 모드 체계 2차 개정(A312) — 이 묶음 1순위.**
   **컨텐츠를 연 상태에서 Enter 3연타 = 1→2→3→1**로 도는지, 그리고 **하단 바 모드 버튼의 얼굴이
   ⓐ(사이드바 접힘) → ⓑ(전체화면) → ⓒ(겹친 창 = 빠져나가기)로 따라오는지**(아이콘과 실제 동작이
   어긋나면 즉시 보고 — 구조상 불가능해야 하는 형태다) ·
   **모드3에서 Esc = 모드2로 한 단계**(모드1로 직행하면 개정 미반영) ·
   **Alt+Enter = 모드1 ↔ 모드3 직행**(모드2를 건너뛴다) ·
   **컨텐츠를 열지 않은 상태에서는 1↔3만** 오가고 **ⓐ 아이콘이 절대 안 뜨는지** ·
   ※ **S4('오픈 파일' 탐색)는 이 확인에서 빠졌다** — v0.305.0(A313)이 S4의 계단을 통째로 없앴으므로
   **아래 8차 블록**으로 확인할 것(옛 서술은 "S4도 1↔3"이었다) ·
   **중앙 썸네일에 포커스를 둔 채 Enter = 선택 열기**가 그대로인지(A274 게이트 무변경) ·
   **ⓒ 아이콘(겹친 창)이 "전체화면 빠져나가기"로 읽히는지**(글리프 선례가 없어 코드로 조립했다 —
   안 읽히면 도상을 다시 고른다) · **모듈 바 우측이 버튼 1개에 맞게 38px 당겨졌는지**(좁은 창에서
   겹치거나 튀면 파생 수치 **H/W 최소 창 여유 58 · 영상 축약 시작 777**을 다시 잡는다) ·
   ※ **'뒤로'(마우스 XButton1)도 Esc와 같은 계단이라 3→2→1**이 된다(의도된 결과 — 부록 B 83.
   한 번에 내려가길 원하면 그 취향을 보고하면 개정 항목으로 등재한다).
③ **v0.304.0 전체화면 하단 바 자동 숨김(A311).**
   **이미지를 전체화면으로 열면 바가 3초 뒤 사라지고**, **마우스를 움직이면 다시 뜨는지** ·
   떠 있는 동안 **Exit full screen 버튼을 실제로 누를 수 있는지**(이 항목의 실사용 동기다) ·
   **전체화면 영상에서 일시정지하면 바가 떴다가 3초 뒤 다시 숨는지** — ⚠️ **의도적 동작 변경**이다
   (종전엔 일시정지면 계속 떠 있었다. 거슬리면 그 사실을 보고 → 개정 등재) ·
   **창 모드 영상은 종전 그대로**인지(일시정지면 상시 표시 — 여기가 달라졌으면 회귀다) ·
   **마우스를 화면 하단 띠에 올려 두면 안 숨는지**(시크바를 노린 손 밑에서 꺼지지 않게 한 유예) ·
   **전체화면 슬라이드쇼(◀/▶ 항해) 뒤에도 다시 숨는지**(재무장 1줄이 그 축이다 — 영구히 떠 있으면 보고) ·
   문서·압축·H/W·설정 표면의 전체화면에서도 같은지.
   ※ **전체화면 오디오는 일시정지로 바가 뜨지 않는다**(오디오 모듈이 `IPlaybackStateSource`를
   구현하지 않는 코드 사실 — 포인터로만 뜬다). 이건 결함이 아니라 **알려진 한계**다.

⚠️ **v0.305.0 (2026-09-02 A313 완주분 — 8차 마감 묶음) 실기기 확인 포인트**

> ※ **S4('오픈 파일' 탐색) 중의 하단 바가 전부**다. **v0.305.0 CI는 초록 확인 완료**다.
> 위 7차 블록 ②의 "S4에서도 1↔3만 오간다"는 **이 블록이 대체**한다
> (S4는 이제 **계단 자체가 없다** — 옛 사양으로 확인하지 말 것).

① **S4 중에 하단 바가 보이되 어떤 버튼도 안 눌리는지 — 이 항목의 전부.** '오픈 파일'로 S4를 띄운 뒤
   **모드 버튼**(전체화면·사이드바 접힘 어느 얼굴이든) · **'오픈 파일' 버튼 재누름** · **시작 메뉴** ·
   그 밖의 바 버튼을 차례로 눌러 **전부 무반응**인지. 하나라도 반응하면 덮개 영역·z가 어긋난 것이니
   **어느 버튼이 눌렸는지**를 보고할 것.
   ※ **키보드로 미리 포커스를 옮겨 둔 극단 경로는 알려진 한계**다 — S4 진입 **전에** 바 버튼에 포커스를
   남긴 채 Space를 치면 **StartButton 플라이아웃이 열릴 수 있다**(덮개는 히트 테스트만 막고 포커스는
   못 막는다. 모드 버튼·'오픈 파일'은 코드 게이트가 별도로 막는다). 실사용 재현은 어렵다.
② **모드 전이 입력 4종이 S4 중에 전부 무동작인지** — **Enter** · **Alt+Enter** · **모드 버튼 클릭** ·
   **'뒤로'(마우스 XButton1)**. 하나라도 모드가 바뀌면 계단 게이트가 샌 것이다.
   ※ **중앙 탐색기 표면에 포커스를 둔 Enter = 선택 열기**는 그대로여야 한다(A274 게이트 무변경 —
   여기서 파일이 안 열리면 회귀다).
③ **0.85 덮개가 과한지 — 눈으로만 판별된다.** 덮개는 같은 파일의 유일한 반투명 선례(DiagStrip =
   `SolidBackgroundFillColorBaseBrush` + `Opacity 0.85`)를 복제했는데, **0.85면 바 정보(모듈 바 텍스트·
   드라이브 줄)가 거의 안 보인다**. 사용자 의도("누를 수 없음을 보이게")에는 부합하지만 **"너무 가려진다"**
   면 그 사실만 보고하면 된다 — **XAML 한 곳(Opacity)만 낮추면 끝**이다(다른 축은 손댈 필요 없음).
   라이트/다크 양쪽에서 바 띠가 **덮인 것으로 읽히는지**도 함께.
④ **전체화면 + S4에서 바와 덮개가 함께 숨고 함께 부상하는지.** 전체화면 상태로 S4를 띄운 뒤 마우스를
   멈춰 자동 숨김(A311)을 기다렸다가 다시 움직여 볼 것 — **바만 뜨고 덮개가 안 뜨는**(= 그 순간 버튼이
   눌리는) 형태나 **덮개만 남는** 형태가 보이면 즉시 보고. 둘은 `UpdateShellChrome` 하나를 지나므로
   구조상 항상 함께여야 한다.
⑤ **전체화면 + S4의 첫 Esc가 곧바로 S4를 걷는지** — ⚠️ **의도된 개정**이다(부록 B 85).
   종전엔 **첫 Esc = 전체화면 해제 · 다음 Esc = S4 복귀**였는데 이제 **첫 Esc에서 바로 S4가 걷히고
   전체화면은 유지**된다. "계단 없음"의 필연 귀결이라 **버그가 아니다** — 종전 순서를 원하면 그 취향을
   보고하면 개정 항목으로 등재한다.
⑥ **S4 종료 후 덮개가 확실히 걷히는지.** 파일을 골라 컨텐츠를 교체한 경우와 Esc로 그냥 빠져나온 경우
   **양쪽 모두**에서, 하단 바가 다시 **정상 색으로 돌아오고 버튼이 눌리는지**. 덮개가 남으면 바가
   먹통이 되므로 이 축은 반드시 확인할 것.

### v0.306.0 (A314) — 사이드바 기본값 반전 + 오버라이드 수명

> ※ **모든 확인의 전제 = 기본값이 좌·우 모두 열림으로 바뀌었다**(A273의 "좌 열림·우 닫힘"은 폐기).
> **v0.306.0 CI는 초록 확인 완료**다.

① **앱을 그냥 실행했을 때 좌·우 사이드바가 둘 다 열려 있는지** — 이 항목의 1차 대상이다.
   새 창(시작 메뉴 → 새 창), 모듈을 바꿔 착지한 화면 **모두**에서 둘 다 열려야 한다.
② **오버라이드가 같은 모듈 안에서는 살아 있는지** — **F12로 우측을 닫은 뒤 같은 모듈에서 다른 파일**을
   열어 볼 것(◀/▶ 다음 파일 · '오픈 파일'(S4)로 같은 모듈 파일 선택 · All Readable 자식 교체).
   **우측이 닫힌 채 유지**돼야 한다. 여기서 우측이 되살아나면 판정선이 샌 것이다.
③ **다른 모듈로 가면 좌·우가 복귀하는지** — ②의 상태에서 **시작 메뉴로 다른 모듈**을 열면
   **좌·우가 다시 둘 다 열려야** 한다(오버라이드 해제).
④ **모드3(전체화면)에서 다른 모듈의 파일을 열면 전체화면이 유지되는지** — 종전엔 무조건 창 모드로
   리셋됐다. 유지되는 것이 **의도된 개정**이다. 바가 자동으로 숨으므로(A311) 포인터를 움직여
   **바가 정상적으로 다시 뜨는지**까지 함께 볼 것.
⑤ **모드2에서 메뉴로 모듈만 전환(컨텐츠 없음)하면 모드1로 착지하는지** — **모드2만 강등**이고
   **모드3은 유지**다(무컨텐츠 모드2는 성립하지 않는다 — A312 시나리오 B 정합).
⑥ ⚠️ **회귀 확인(최우선) — 탐색기에서 파일을 더블클릭해 뜬 새 창이 종전대로 무사이드바인지(A81).**
   구현이 "이전 모듈이 null인 첫 착지는 전환이 아니다"로 회피한 지점이라 **여기가 깨지면 이 항목의
   회귀**다. 좌·우 어느 한쪽이라도 열려 나오면 즉시 보고할 것.
⑦ **S4 왕복이 오버라이드를 풀지 않는지** — F12로 우측을 닫은 뒤 '오픈 파일'로 S4를 띄우고
   **Esc로 그냥 빠져나왔을 때** 우측이 닫힌 그대로인지(S4는 모듈 전환이 아니다).
⑧ (파생·버그 아님) **오버라이드가 풀리면 Enter 계단이 2단에서 3단으로 되돌아온다** —
   모듈 전환 직후 Enter를 눌러 계단 단수가 바뀐 것이 **사양의 필연 귀결**임을 확인만 할 것.

### v0.307.0 (A315) — 툴팁이 새게 하던 Enter + 모드3 Esc가 한 단계로

> ※ **이 블록은 사용자 실기기 보고 2건의 수리 결과**다 — 보고자가 곧 확인자이므로 회수 우선순위가 높다.
> **v0.307.0 CI는 초록 확인 완료**다.

① **영상을 재생하면서 포인터를 재생/일시정지 버튼 위에 둔 채 Enter** — **모드 계단이 도는지**
   (재생/일시정지가 토글되면 안 된다). 결함 ⓐ의 1차 대상이다. **포인터를 버튼에서 비킨 경우와 결과가
   같아야** 정상이다(종전엔 위에 두면 클릭, 비끼면 계단이었다).
② **Space와 마우스 클릭은 여전히 재생/일시정지로 동작하는지** — 툴팁을 게이트에서 뺀 것이 재생 조작
   자체를 죽이지 않았는지 확인하는 회귀 축이다.
③ **모드3에서 Esc → 모드2 → 모드1 → 컨텐츠 닫기**가 **한 단계씩** 밟히는지(결함 ⓑ).
   종전엔 첫 Esc가 곧바로 모드1로 떨어졌다.
④ **오버라이드(F11/F12) 상태나 무컨텐츠 상태에서는 Esc가 3→1**인지 — 그쪽은 **2단 계단이 사양**이라
   한 번에 내려가는 것이 정상이다(A312 시나리오 B).
⑤ **이미지 더블클릭 전체화면의 해제는 종전대로 모드1 착지**인지 — 외부 주체가 만든 프레젠터 해제라
   재진입 가드의 대상이 아니다. 여기서 모드2에 머무르면 회귀다.
⑥ **콤보 드롭다운·플라이아웃·대화상자가 열린 상태의 Enter는 종전대로 양보**되는지 —
   툴팁만 제외했으므로 그쪽은 무변경이어야 한다(예: 하단 바 플라이아웃을 연 채 Enter → 모드 전이가
   일어나면 안 된다).

### v0.308.0~v0.309.0 (A316·A317) — S4 반투명 + 모드2·3의 사이드바 부유

> ※ **두 릴리스 다 CI 초록 확인 완료**다.
> ※ A317이 **A316의 사이드바 판정을 개정**했으므로 **두 릴리스를 함께** 확인하는 것이 맞다.

① ⚠️ **이 블록 1순위 — 모드2에서 영상 재생 중 '파일 열기'.** **영상 크기가 변하지 않고**(도크가 열리며
   중앙이 줄어들면 안 된다) **좌·우 사이드바가 영상 위에 반투명으로 부유**하는지 · 그 상태에서
   **리스트 글자·아이콘은 선명한지**(바탕만 반투명이 사양 — 글자까지 흐리면 층 분리가 깨진 것).
② **모드3(전체화면)에서도 ①과 동일**한지 · 그 상태의 **Esc 1타 = S4만 종료(전체화면 유지)** 인지
   (A313 파생 사양 · 부록 B 85 — 모드1로 떨어지면 회귀다).
③ ⚠️ **회귀 최우선 — 모드1의 '파일 열기'는 종전과 픽셀 동일**한지. **불투명 도크 · 중앙 축소 ·
   경계선 1px**까지 포함해 v0.307.0과 같아야 한다. 여기서 사이드바가 비쳐 보이면 **뒤편 도크와 글씨가
   겹쳐 보이는** 사용자가 지목한 바로 그 증상이다.
④ **부유 상태의 조작이 정상인지** — 부유 사이드바에 **파일 드랍** · **좌 트리 클릭 이동** ·
   **리스트 스크롤**. `PanelBackdrop`을 분리하면서 히트 테스트·드랍은 기존 Border에 남겼으므로,
   드랍이 안 먹으면 그 분리가 원인이다(§1의 최소 복구법).
⑤ **부유 상태에서 S4 썸네일 격자와 사이드바 경계의 픽셀 정렬** — 스페이서와 오버레이 내부 분할이
   둘 다 전폭 기준이라 산식 무변경으로 성립해야 한다. 한 칸씩 밀려 보이면 보고.
⑥ **불투명도 0.65가 적당한지 — 눈으로만 판별된다.** 진하면 뒤 컨텐츠가 안 읽히고, 옅으면 S4 자체가
   안 읽힌다. **조정 지점은 `MainWindow.xaml.cs`의 `S4TranslucentOpacity` 한 곳**뿐이다
   (⚠️ **바 덮개용 값을 따로 만들지 말 것** — 값이 갈린 것이 A316이 고친 증상의 인과다).
⑦ **S1(빈 모듈 상태)의 탐색기는 종전대로 불투명**인지 — `ThumbnailExplorer.LayoutRoot`가 `Transparent`가
   됐고 불투명은 `ExplorerHost` 배경이 담당한다. 빈 모듈에서 뒤가 비치면 그 위임이 어긋난 것이다.
⑧ **라이트/다크 양쪽** — 반투명 브러시가 테마 브러시(불투명) + Opacity 조합이라 두 테마에서 결과가 다르다.

### v0.310.0 (A318) — 루프 '반복 안함'을 흐림 대신 금지 사선으로

> ※ **v0.310.0은 CI에서 깨졌고 핫픽스 v0.310.1로 초록**이 됐다(CS0104 — `Path` 모호 참조 · §3.4).
> **핫픽스는 완전 수식뿐이라 기능·도형·좌표가 무변경**이므로 아래 확인 포인트는 그대로다.
> **확인 대상 빌드 = v0.310.1**(v0.310.0은 빌드가 깨져 릴리스 자산이 나오지 않았다).
> ※ **영상·오디오 두 모듈 모두**가 대상이다(루프 버튼을 가진 모듈은 이 둘뿐).

① ⚠️ **이 블록 1순위 — 끔 상태의 밝기.** 루프 3상태를 순환시켜(**클릭 · L 키 · 우클릭 플라이아웃**)
   **'반복 안함'일 때 아이콘이 이웃 버튼들과 같은 밝기**로 보이는지. 여기서 여전히 흐려 보이면
   Opacity 잔재가 남은 것이다(사용자가 "못 누르나?"라고 읽은 바로 그 증상).
② **사선이 "반복 안함"으로 읽히는지** — 같은 글리프 위에 금지 사선이 얹힌 형태다.
   "무엇의 꺼짐인지" 연결이 끊겨 보이면 도상 가족 유지가 실패한 것.
③ ⚠️ **배율 100 / 125 / 150%에서 사선이 뭉개지거나 글리프에 묻히지 않는지.**
   두께 2 · `Stretch=None`(늘림에 의한 두께 왜곡 차단) · 경계 상자 17.4 정사각으로 잡았으나
   **픽셀 격자 정렬은 눈으로만 판별된다**. 조정 지점은 `src/Shared/MediaIcons.cs` 한 곳.
④ **라이트/다크 양쪽 + hover / pressed 상태에서 사선 색이 글리프와 함께** 바뀌는지.
   A299의 0×0 센티널 `TextBlock` → `slash.Fill` 동기를 재사용했으므로 **글리프만 색이 변하고
   사선만 남아 도는** 그림이 나오면 그 동기가 끊긴 것이다(하드코딩 색은 0건).
⑤ **영상과 오디오의 끔 아이콘이 같은 그림**인지 — 정본이 `MediaIcons.cs` 한 파일이라 같아야 한다.
   다르면 한쪽이 옛 조립을 쓰고 있는 것.
⑥ **3상태를 빠르게 여러 번 순환해도 앱이 죽지 않는지**(요소 재부모화 크래시 부재) —
   호출마다 새 `Grid`·`FontIcon`·`Path`·`PathGeometry`를 만들어 v0.174.1 함정을 피했다.
   여기서 죽으면 어딘가에 **정적 캐시나 인스턴스 재사용**이 남아 있다는 뜻이다.
⑦ **버튼 상자·핫키·툴팁 무회귀** — 1칸 32×32(`BottomBarButtonStyle`) · L 키 · 상태 순환 순서 ·
   툴팁 문구는 무접촉이므로 v0.309.0과 같아야 한다.

### v0.311.0 (A320) — PDF가 1:1(100%)로 열린다

> ※ **CI는 이 문서 작성 시점에 진행 중**이다 — 초록 확인 후에 실기기 확인으로 넘어갈 것.
> ※ 대상은 **문서 모듈의 PDF 갈래**뿐이다(텍스트 갈래는 원래 1:1 · 이미지·영상은 종전대로 `Contain`).

① ⚠️ **이 블록 1순위 — PDF가 100%로 열리는 게 의도대로인가.** 이제 PDF는 축소 없이 **원본 1:1**로
   열리므로 **A4 문서는 화면 밖으로 잘려 보인다**. 사용자 지시대로의 동작이지만 실제로 보고 나서도
   그게 원하는 것인지 판단할 것(아니면 값을 되돌리는 것이 아니라 **어느 배율이 기본인지**를 다시 정한다).
② **빈 화면(파일 미오픈)의 `1:1` 상자와 PDF를 연 직후 Fit 버튼 표시가 둘 다 1:1로 일치**하는지 —
   이 항목의 존재 이유다. 여기서 갈리면 세 지점 중 하나가 안 고쳐진 것이다.
③ **PDF를 연 직후 1페이지 머리에 위치**하는지 — **긴 PDF를 한참 스크롤한 뒤 다른 PDF를 열어** 확인할 것.
   중간 지점에서 열리면 `snapToPageTop` 앵커가 안 걸린 것이다(기본값 변경의 부수 계약 축).
④ **100%에서 페이지가 뷰포트보다 좁을 때 수평 중앙 정렬**이 유지되는지(좌측에 붙으면 회귀).
⑤ **플라이아웃에서 Contain을 고르면 종전대로 축소 + 현재 페이지 머리로 스냅**되는지 —
   사용자 선택 경로는 무회귀여야 한다(`ApplyFit`은 `snapToPageTop`을 쓰지 않는다).

### v0.312.0 (A309) — F11/F12로 패널을 닫아도 셸 키가 죽지 않는다

> ※ **CI는 이 문서 작성 시점에 진행 중**이다 — 초록 확인 후에 실기기 확인으로 넘어갈 것.
> ※ 고친 것은 **포커스가 `null`로 떨어지는 형태 하나**다. 종전에도 주기 감시가 **최대 500ms 뒤**에는
> 주워 담았으므로, 증상은 "영영 먹통"이 아니라 **"0.5초 먹통 또는(오버라이드가 걸린 뒤로는) 고정"** 이었다.

① ⚠️ **이 블록 1순위 — 좌 패널 리스트 항목을 클릭해 포커스를 패널 안에 둔 채 F11로 닫고**,
   곧바로 **Esc · F12 · Alt+Enter**가 듣는지. **즉시** 들어야 한다(종전엔 최대 0.5초 먹통, 나빠지면 영영).
② **F12(우 패널)로 같은 절차를 반복** — 좌우 대칭으로 성립해야 한다.
③ **연타 내성** — 닫기 → 열기 → 닫기를 3회 반복한 뒤에도 패널 토글이 정상이고
   **리스트 타이핑 탐색이 살아 있는지**(복구가 포커스를 엉뚱한 데로 옮겼다면 여기서 드러난다).
④ **회귀 관문 A — 빼앗지 않아야 할 때 빼앗지 않는지.** 좌 패널에 포커스를 둔 채 **우 패널만** F12로
   닫으면 **좌 리스트의 포커스가 유지**돼야 한다(타이핑 탐색이 계속 먹혀야 한다).
⑤ **회귀 관문 B — 팝업 무개입.** 시작 메뉴나 콤보 팝업을 연 채 F11 → **팝업이 한 번에 닫히는 회귀가
   없어야** 한다(복구 함수의 `skip(popup)` 갈래가 도는 축).
⑥ **S4 진입 직후** 그리드가 키보드 포커스를 받는지, **Esc로 복귀한 뒤 셸 키가 정상**인지.
⑦ **직접 증빙 — 진단 스트립(`diag.shellKeyOverlay`)에서 GUARD가 `orphan(null)`로 뜨고 RECOV가 1 증가**
   하는지. ①에서 이 두 값이 안 움직이면 **새 갈래가 아예 안 도는 것**이다(증상만 보고 판단하지 말 것).

### v0.313.0 (A323) — 좌 리스트가 지금 열린 파일을 표시하고, 같은 폴더면 깜빡이지 않는다

> ※ **v0.313.0 CI는 초록 확인 완료**(2026-09-03).
> ※ **보고자가 곧 확인자**인 블록이다(사용자 실기기 보고 2건의 수리 — 현재 파일 미표시 · 항해마다 깜빡임).
> ※ 좌 리스트는 **전 파일 모듈이 공유하는 단일 인스턴스**라 한 곳을 고친 것이 전 모듈에 적용된다 —
> 이미지에서만 보지 말고 **영상·오디오·문서·아카이브·All Readable**도 한 번씩 볼 것.

① ⚠️ **이 블록 존재 이유 A — 표시.** 이미지 모듈 모드1에서 파일을 열었을 때 **좌 리스트의 그 파일에
   테두리(선택 표시)가 쳐지는지**. 화살표로 앞/뒤로 넘어가면 **표시가 따라 옮겨져야** 한다.
② ⚠️ **이 블록 존재 이유 B — 깜빡임.** 화살표를 **빠르게 연타**하며 앞/뒤로 넘길 때 리스트가
   **한 번도 다시 그려지지 않는지**(스크롤 위치도 튀지 않아야 한다). 상단 **폴더 트리도 튀지 않아야** 한다
   (종전엔 항해마다 `DriveInfo.GetDrives` + 트리 스크롤이 돌았다 — **네트워크 드라이브가 연결된 PC에서
   체감 차이가 가장 크다**).
③ **다른 폴더로 실제로 이동했을 때는 목록이 제대로 새로 채워지는지**(가드가 과하게 걸리면 여기서 드러난다) —
   탐색기에서 다른 폴더의 파일을 열어 확인할 것. **필터가 다른 모듈로 전환**했을 때도 목록이 갱신돼야 한다
   (예: 이미지 → 영상 — 가드는 폴더뿐 아니라 필터도 비교한다).
④ ⚠️ **회귀 관문 A — 우측 정보 패널의 역전이 없는지.** 파일을 연 상태에서 **좌 리스트의 다른 항목을
   마우스로 한 번 클릭**하면 우측 정보 패널은 **클릭한 항목**을 보여 줘야 한다(A200의 선택 축).
⑤ ⚠️ **회귀 관문 B — 그 반대.** 화살표로 파일을 넘길 때(= 표시만 옮기는 경로) 우측 정보 패널은
   **열려 있는 콘텐츠**를 계속 보여 줘야 한다. 여기서 패널이 선택을 따라가면 **A200 계약이 깨진 것**이다
   (`_syncingCurrent` 억제가 안 도는 것).
⑥ **스크롤 노출** — 목록이 길어 화면 밖에 있는 파일로 화살표 탐색을 이어갈 때 **그 항목이 보이도록
   스크롤**되는지.
⑦ ⚠️ **이 블록 최대 회귀 후보 — S4('오픈 파일' 탐색) 진입 시 중앙 그리드가 비어 있지 않은지.**
   재항해를 건너뛰면 `ViewChanged`가 오지 않아 그리드가 빈 채 남을 수 있어 **진입부에서 현재 목록을
   직접 시드**하도록 방어했다. **파일을 여러 번 넘긴 뒤 S4로 들어가** 타일이 채워지는지 볼 것.
⑧ **⑦의 짝 — 로딩 중 오염이 없는지.** **큰 폴더(수천 개)를 여는 도중 곧바로 S4로 진입**했을 때
   **옛 폴더의 타일이 로딩 화면을 덮지 않아야** 한다(스캔 중이면 시드하지 않는다 — A243 로딩 화면 축).
⑨ **A94 폴더 감시와의 정합** — 리스트가 떠 있는 상태에서 **탐색기로 그 폴더에 파일을 하나 추가**해
   자동 갱신이 돈 뒤에도 **현재 파일 표시가 살아 있는지**(종전엔 사라졌다 — 이번에 개선된 축이다).
⑩ **알려진 절충(회귀가 아님)** — 파일을 닫아 **빈 모듈(S1)로 돌아가면 직전 파일의 하이라이트가 한 번
   남을 수 있다**(우측은 플레이스홀더). `null`일 때 선택을 지우면 사용자의 S1 선택 표시까지 지우게 되어
   일부러 그렇게 뒀다 — **거슬리면 별건으로 등재**하겠다는 판단이 필요한 지점이다.

### v0.314.0 (A324·A325) — 트리가 리스트를 따라오고, ◀/▶ 항해마다 창 제목이 바뀐다

> ※ **CI는 이 문서 작성 시점에 진행 중**이다 — 초록 확인 후에 실기기 확인으로 넘어갈 것.
> ※ **보고자가 곧 확인자**인 블록이다(사용자 스크린샷 1장에서 두 결함이 나왔다).
> ※ **A323(v0.313.0)의 이득을 깨지 않았는지가 이 블록의 회귀 축**이다 — 두 블록을 함께 볼 것.

① ⚠️ **이 블록 존재 이유 A — 숨김 폴더 경유.** `%AppData%` 같은 **숨김 속성 폴더 아래의 파일**을 열고
   (예: `C:\Users\<계정>\AppData\...`) **좌 트리가 그 폴더까지 펼쳐져 선택돼 있는지**. 종전에는
   트리가 **C 드라이브 루트**에 머물렀다. 리스트 쪽 위치와 트리 쪽 위치가 **같아야** 한다.
② ⚠️ **이 블록 최대 회귀 관문 — A323의 이득 유지.** 같은 폴더 안에서 **◀/▶(또는 화살표)를 홀드·연타**할 때
   **트리가 튀지 않고 리스트도 다시 그려지지 않는지**. 여기서 트리가 다시 펼쳐지면 A324가 A323을
   되돌린 것이다(같은 폴더 항해 1타 = 재스캔 0·재생성 0·`GetDrives` 0·펼침 0이 목표).
③ ⚠️ **이 블록 존재 이유 B — 창 제목.** ◀/▶로 파일을 넘길 때마다 **창 제목이 지금 보는 파일명으로
   바뀌는지**(종전엔 처음 연 파일명이 그대로 남았다). **인스턴스 접두(`1-KOTU - ...`) 형식이 유지**돼야 하고,
   **트레이 아이콘 툴팁도 같이 바뀌어야** 한다(마우스를 올려 확인).
④ **다른 폴더로 실제 이동** — 탐색기에서 다른 폴더의 파일을 열었을 때 **트리가 그 폴더로 옮겨 가고
   리스트도 새로 채워지는지**(판정을 "트리가 이미 그 폴더를 가리키는가"로 바꾼 뒤의 정상 경로).
⑤ **형제 숨김 폴더는 여전히 안 보여야 한다** — 길목의 숨김 폴더만 한 칸 실체화하는 설계다.
   숨김 폴더가 여럿 있는 위치에서 **경유한 그 폴더만 트리에 나타나고 다른 숨김 폴더는 안 나오는지**.
⑥ **회귀 관문 — 파일 없는 4상태에서 제목이 깨지지 않는지.** ⓐ 무제(새 문서) ⓑ 설정 화면
   ⓒ 미지원 파일 안내 ⓓ 빈 모듈(S1). 넷 다 **제목이 빈 파일명으로 덮이지 않아야** 한다.
⑦ **회귀 관문 — 무제 문서 저장.** 무제로 만든 문서를 저장했을 때 제목이 새 파일명으로 바뀌는지
   (A279 `IContentPathChangedSource` 축 — 이번에 손대지 않았다). **미저장 `●` 표식**도 그대로여야 한다.
⑧ **드라이브 루트 재구성** — 네트워크 드라이브를 연결·해제한 뒤 항해했을 때 **트리 루트가 채워지는지**
   (`EnsureDriveRoots`가 "루트가 비었을 때"만 추가로 돌게 했다).
⑨ **숨김 표시 토글 후 재펼침** — 탐색기 필터/설정에서 **숨김 표시를 켰다 껐다** 한 뒤 다시 항해했을 때
   트리가 어긋난 채 고착되지 않는지(판정 축 교체의 목적이 바로 "어긋나면 다음 항해가 복구").
⑩ **A200 두 축 무회귀** — 파일을 연 상태에서 **좌 리스트의 다른 항목을 클릭**하면 우측 정보 패널은
   **클릭한 항목**을, ◀/▶ 항해 때는 **열린 콘텐츠**를 보여 줘야 한다(A323 블록 ④⑤와 같은 관문).

### v0.315.0~v0.316.0 (A326·A327) — 등록 토글이 모듈 단위로 · 오디오 정보 패널이 포맷 속성 전부를 나열

> ※ **CI는 이 문서 작성 시점에 진행 중**이다(v0.314.0까지 초록) — 초록 확인 후에 실기기 확인으로 넘어갈 것.
> ※ **두 건 다 사용자 신규 지시**라 **보고자가 곧 확인자**인 블록이다.
> ※ **A326은 A292(v0.299.0)가 만든 화면을 되돌리는 배치**다 — v0.299.0~v0.301.0 블록의
> **"확장자 개별 토글·마스터 48건 체감" 항목은 이제 무효**이고, 여기 ①~⑤로 대체해 확인할 것.

**A326 — 설정 Advanced options(5항)**

① ⚠️ **이 블록 존재 이유 A — 토글이 모듈당 1개인가.** 설정 → All file associations → **Advanced options**를
   펼쳐 **모듈마다 토글이 하나씩만** 있는지(종전에는 `KOTU-image` 아래 `.jpg`·`.jpeg`·`.png` 등
   **10개가 각각 토글**이었다). **모듈 소제목 · "Default app for N/M extensions" 줄 ·
   "Set default..." 드롭다운은 그대로 남아 있어야** 한다(제거 대상이 아니었다).
② ⚠️ **판정 규칙 — 전부 등록일 때만 On.** 한 모듈의 확장자 일부만 등록된 상태를 만들고(예: 다른 앱에
   `.png`만 넘긴 뒤) 설정을 다시 열었을 때 **그 모듈 토글이 Off**로 보이는지. 이때 **"N/M extensions" 줄이
   부분 상태를 알려 주는지**(예: 9/10) — **토글이 잃은 해상도를 그 줄이 메우는 것이 사양**이다.
③ **켬/끔 왕복** — 모듈 토글을 켜면 **그 모듈의 전 확장자가 등록**되고 N/M이 M/M이 되는지,
   끄면 **전부 해제**되고 0/M이 되는지. 끈 뒤 탐색기에서 그 확장자 파일의 기본 앱이 실제로 바뀌었는지.
④ **진행 표시와 연타 방어** — 확장자가 많은 모듈(이미지)을 켜는 동안 상태 줄에
   `KOTU-image: registering 4/10 file associations...` 형태의 **진행 표시가 나오는지**,
   그 사이 **같은 토글을 연타해도 중복 실행되지 않는지**(모듈당 busy 잠금).
   **UI가 멈추지 않아야** 한다(레지스트리 접근은 전부 워커).
⑤ **3층 되계산** — **마스터 스위치를 켜면 모듈 5개가 전부 On**이 되고, **모듈 하나를 끄면 마스터가
   Off로 되계산**되는지. 마스터 → 모듈 대행 누름이 **되계산을 재귀시켜 화면이 요동치지 않는지**도 함께 볼 것.

**A327 — 오디오 정보 패널(8항)**

⑥ ⚠️ **이 블록 존재 이유 B — 항목 수.** mp3를 열었을 때 우측 정보 패널이 **File·Size·Modified 3줄이
   아니라** 파일 기본 3행 + 구분선 + **태그 20키 + 스트림 7키**로 나오는지.
⑦ ⚠️ **최대 사양 관문 — 빈칸 행이 남는가.** **태그가 거의 없는 mp3**(제목·앨범·아티스트 미기입)를 열어
   **값이 없는 항목도 라벨이 그대로 남아 있는지**. **항목이 사라지거나 접히면 사양 위반**이다
   (부록 B 97 — 항목명이 남아야 "그 포맷에 그런 속성이 있다"가 전달된다).
⑧ ⚠️ **두 축 일치(A200) — 이 블록 최대 회귀 축.** 같은 파일을 **ⓐ 열어서 볼 때**와
   **ⓑ 좌 리스트에서 선택만 했을 때** 패널이 **라벨·순서·구분 행까지 똑같은지**.
   두 경로가 같은 빌더 하나를 공유하므로 **다르게 보이면 그 자체가 결함**이다.
⑨ **태그가 꽉 찬 파일** — 앨범아트·앨범·트랙 번호·장르·연도까지 있는 mp3에서 **값이 제자리 라벨에
   붙는지**(라벨과 값이 어긋나면 키 순서 문제다).
⑩ **libvlc 트랙 행 폐지 확인** — 재생 중과 정지 중의 패널이 **같아야** 한다. 종전에는 **재생 중에만**
   트랙 정보 행이 떴다(그것이 두 축 불일치의 원인이라 폐지했다). **Channels·Sample rate가
   재생 여부와 무관하게** 나오는지 확인할 것.
⑪ **실패 폴백** — 속성을 못 읽는 파일(손상 mp3·0바이트·확장자만 mp3인 파일)을 열었을 때
   **앱이 죽지 않고 라벨만 있는 패널**이 나오는지. **빈 패널이나 예외 대화상자가 뜨면 폴백이 깨진 것**이다.
⑫ **항해 경합** — 오디오 폴더에서 **◀/▶를 홀드·연타**할 때 패널에 **이전 파일의 값이 남거나 섞이지
   않는지**(오버레이 `_seq` · 선택 축 취소 토큰 · 열림 축 경로 재검사 3중 방어).
⑬ **타일·트레이 무회귀(A270)** — 썸네일 뷰의 **오디오 타일**과 트레이 표기는 **종전 그대로**여야 한다
   (좁은 타일에는 "값 없는 조각 생략"이 계속 옳다 — 타일에 빈칸 행이 생겼다면 사양을 잘못 옮긴 것이다).

### v0.317.0 (A328) — 영상 정보 패널도 포맷 속성 전부를 나열한다

> ※ **CI는 이 문서 작성 시점에 진행 중**이다(v0.316.0까지 초록) — 초록 확인 후에 실기기 확인으로 넘어갈 것.
> ※ **사용자 신규 지시**("영상도 당연히 바꿔야지.")라 **보고자가 곧 확인자**인 블록이다.
> ※ **A327(오디오) 블록 ⑥~⑬과 같은 축**이다 — 오디오에서 확인한 항목은 영상에서 같은 결과가 나와야 한다.

① ⚠️ **이 블록 존재 이유 A — 항목 수.** 영상 파일(mp4)을 열었을 때 우측 정보 패널이 종전
   **File·Size·Modified·Duration 4줄**이 아니라 파일 기본 3행 + **메타 16 · 비디오 15 · 오디오 5 = 36행 3절**
   로 나오는지.
② ⚠️ **이 블록 존재 이유 B — 두 축 일치(A200 · 최대 회귀 축).** 같은 파일을 **ⓐ 열어서 볼 때**와
   **ⓑ 좌 리스트에서 선택만 했을 때** 패널의 **항목·순서·구분 행이 똑같은지**.
   종전에는 **선택 축이 `Length` 한 줄뿐**이었다 — 그 불일치를 없애는 것이 이 배치의 절반이다.
   **두 경로가 같은 빌더 하나를 공유하므로 다르게 보이면 그 자체가 결함**이다.
③ ⚠️ **셸 핸들러가 약한 컨테이너 — mkv/webm.** `.mkv`·`.webm`을 열어 **재생 중에 빈칸이던 행
   (Frame width/height · Frame rate · Video codec · Duration)이 채워지는지**, 그리고
   **재생을 멈춘 뒤에도 그 행이 사라지지 않는지**. `FillFromPlayer`는 **빈칸 행만 채우는 보조 축**이라
   **행 자체는 재생과 무관하게 항상 있어야** 한다(행이 나타났다 사라지면 강등이 잘못된 것이다).
④ ⚠️ **GUID 노출 없음.** 어떤 파일에서도 `Compression`·`Video codec (FourCC)` 값에
   **`{...}` 형태의 GUID 문자열이 보이면 안 된다**(읽히는 4글자 코덱이거나 빈칸 둘 중 하나여야 한다).
   이 두 키는 **키만 남기고 값을 거르는 유일한 예외**라 값 쪽만 별도로 볼 것.
⑤ **빈칸 행이 남는가** — 메타데이터가 거의 없는 영상(제목·감독·제작사 미기입)에서 **값이 없는 항목도
   라벨이 그대로 남아 있는지**. **항목이 사라지거나 접히면 사양 위반**이다(부록 B 98 ①).
⑥ **오디오 절 라벨** — 비디오 절과 오디오 절에 **동명 항목**(`Encoding bitrate`·`Sample size`)이 있다.
   오디오 쪽에 **`Audio ` 접두**가 붙어 구분되는지, **절 구분 행이 세 절을 갈라 주는지**.
⑦ **실패 폴백** — 속성을 못 읽는 파일(손상 mp4 · 0바이트 · 확장자만 mp4)을 열었을 때 **앱이 죽지 않고
   라벨만 있는 패널**이 나오는지. **빈 패널이나 예외 대화상자가 뜨면 폴백이 깨진 것**이다.
⑧ **항해 경합** — 영상 폴더에서 **◀/▶를 홀드·연타**할 때 패널에 **이전 파일의 값이 남거나 섞이지
   않는지**(3중 방어). 덤으로 **타일·트레이 표기는 종전 그대로**여야 한다(A270 — 좁은 타일에
   빈칸 행이 생겼다면 사양을 잘못 옮긴 것이다).

### v0.318.0 (A329) — 문서·압축 정보 패널도 포맷 속성 전부를 나열한다

> ※ **CI는 이 문서 작성 시점에 진행 중**이다(v0.317.0까지 초록) — 초록 확인 후에 실기기 확인으로 넘어갈 것.
> ※ **부록 B 98의 마지막 이행**이라 **21·22차(오디오)·23차(영상) 블록과 같은 축**이다 —
> 앞 블록에서 확인한 항목은 문서·압축에서도 같은 결과가 나와야 한다.
> ⚠️ **이 두 모듈은 종전에 열림 축 정보 계약이 아예 없었다**(셸 폴백 4행) — 그래서 이 블록은
> "달라졌는가"가 아니라 **"없던 것이 생겼는가"** 를 본다.

① ⚠️ **최대 회귀 축 — PDF의 두 축 항목 동일.** 같은 PDF를 **ⓐ 열어서 볼 때**와 **ⓑ 좌 리스트에서
   선택만 했을 때** 패널의 **항목·순서·구분 행이 똑같은지**. 종전에는 **열림 축이 셸 폴백 4행**,
   **선택 축이 `Pages` 한 조각**이라 아예 다른 것을 보여 주고 있었다. **두 경로가 같은 빌더 하나를
   공유하므로 다르면 그 자체가 결함**이다. 항목은 파일 기본 행 + **PDF 10키**(Title·Author·Subject·
   Keywords·Comment·Producer — 구분 행 — Created·Saved·Pages·Version).
② ⚠️ **4MB 초과 텍스트 로그.** 4MB가 넘는 `.txt`/`.log`를 열었을 때 **`Lines`·`Characters`만 빈칸**이고
   **`Encoding`·`Line endings`는 채워지며 앱이 멈추거나 느려지지 않는지**. **빈칸이 정상**이다
   (잘린 앞부분 수치를 전체인 척 내지 않는다) — 숫자가 나오면 오히려 결함이고, 멈추면 상한이 안 걸린 것이다.
③ ⚠️ **7z·rar의 두 갈래.** `.7z`·`.rar`를 **ⓐ 선택만** 하면 `Files`·`Folders`·`Uncompressed`·`Compression`이
   **빈칸**이고(A155 사양 승계 — 무거워서 안 읽는다), **ⓑ 열면 네 행이 전부 채워지는지**.
   **행 자체는 두 경우 모두 있어야 한다**(값만 갈린다). `.zip`은 **두 경우 모두 채워진다**.
④ **PDF `Pages` 3단 폴백 — 회귀 관문.** 종전 선택 축에서 페이지 수가 나오던 PDF는 **지금도 나와야 한다**.
   특히 **암호 PDF**를 열었을 때 열림 축에서 페이지 수가 나오는지(이미 연 문서에서 받는다).
⑤ **빈칸 행이 남는가 · 실패 폴백.** 메타데이터가 없는 PDF에서 **값 없는 항목의 라벨이 그대로 남는지**
   (사라지거나 접히면 부록 B 98 ① 위반) · 손상 PDF·0바이트·확장자만 PDF인 파일에서 **앱이 죽지 않고
   라벨만 있는 패널**이 나오는지. ⚠️ **`System.Document.*`는 저장소 첫 사용이라 빈칸이 정상 범위**다 —
   **전 항목이 일제히 빈칸이면** 그때가 키 계열이 안 읽히는 신호다(라벨은 그래도 나와야 한다).
⑥ **무접촉 축의 무회귀 — 이 묶음에서 가장 넓다.** **A270 타일·트레이 표기**(빈칸 행이 생겼다면 사양을
   잘못 옮긴 것) · **탐색기 상세 줄**(A155·A199 — 텍스트 인코딩·zip 압축률이 종전 그대로 나오는지) ·
   **A323 좌 리스트 표시**가 전부 **종전과 같아야** 한다.

### v0.319.0 (A330) — 시각화 잔상이 지워지고, EQ 버튼이 항상 활성이고, 파일 1개도 전체 반복이 돈다

> ※ **CI는 이 문서 작성 시점에 진행 중**이다(v0.318.0까지 초록) — 초록 확인 후에 실기기 확인으로 넘어갈 것.
> ⚠️ **이 블록은 사용자 실기기 보고 3건의 수리라 보고자가 곧 확인자**다 — 회수 우선순위가 가장 높다.
> ⚠️ **보고된 것은 3건이지만 고쳐진 잔상은 3종**이다(보고 1 + 미보고 2 — 부록 B 99).
> 아래 ①②③이 그 3종이고, **②③은 사용자가 아직 본 적 없는 증상의 확인**이다.

① ⚠️ **잔상 (a) — 보고된 그것.** 음악을 **재생 중**에 비주얼라이저를 **Scope → VU meter**로 바꿨을 때
   **미터 뒤에 구 파형(파란·빨간 선)이 남지 않는지**. 이것이 이 묶음의 존재 이유다.
② ⚠️ **잔상 (b) — 미보고분 1.** 재생 중에 **Scope(또는 Spectrum) → Off**로 바꿨을 때 **화면이 완전히
   비는지**(구 파형이 남으면 실패). Off는 아무것도 그리지 않는 스타일이라 **덮어 줄 새 그림이 없다.**
③ ⚠️ **잔상 (c) — 미보고분 2 · 가장 넓다.** **정지·일시정지 상태**에서 스타일을 바꿔 볼 것 —
   **다섯 스타일 어디로 가도** 화면에 **구 스타일 그림이 남지 않아야** 한다(재장전이 없어 새 그림이
   아예 안 온다). **A302 세러모니 칩이 "바뀌었다"고 알리는데 화면만 옛것이던 어긋남**이 이것이다.
④ ⚠️ **EQ 버튼 — 밝기와 플라이아웃.** EQ 버튼 아이콘이 **옆 비주얼라이저 버튼과 같은 밝기**인지
   (어둡고 흐리면 실패 — 흐림의 정체는 Disabled 전경색이었다) · 눌렀을 때 **플라이아웃이 열리는지**.
   ⚠️ **열었을 때 목록이 `Off` 한 줄뿐이면 그것은 이번 수리의 실패가 아니라 별건**이다
   (libvlc 프리셋 열거 자체가 실패한 것 — **부록 A-2 낙수 34**로 등재해 둔 축이니 그때 보고할 것).
   프리셋이 여러 줄 나오는 것이 정상 기대값이고, 그 경우 **선택이 실제 소리에 반영되는지**도 함께.
⑤ ⚠️ **단일 파일 목록 루프 — 보고된 그것.** **음악 파일이 1개뿐인 폴더**에서 그 파일을 열고
   **전체 반복(Loop list)** 을 켠 뒤 끝까지 재생 → **같은 파일이 다시 시작되는지**.
   ⚠️ **먼저 툴팁으로 모드를 확인할 것 — 버튼 툴팁이 `Loop list`여야 한다.** 만약 `Loop: off`로
   나온다면 **ⓒ의 진짜 원인은 A318 아이콘 오독**(원하는 모드가 애초에 안 걸려 있었다)이므로
   이 수리와 별개로 그 사실을 보고할 것. **내장 샘플 곡으로도 같은지** 함께 보면 좋다
   (샘플은 목록 대상에서 빠지는 갈래라 이번 수리가 덮은 세 경로 중 하나다).
⑥ ⚠️ **회귀 관문 — Loop off + 파일 1개.** 같은 폴더에서 **반복을 끄고** 끝까지 재생했을 때
   **정지해야 한다**. 여기서 **무한 재생이 되면 회귀**다(모드 배타가 깨진 것 — 전이 4가 목록 루프
   전용이라는 전제가 무너졌다는 뜻). ⑤와 ⑥은 반드시 **짝으로** 볼 것.

> ※ **영상 모듈도 ⑤⑥을 같이 본다** — ⓒ는 영상에 동형 이식됐다(영상 파일 1개뿐인 폴더 + Loop list).
> ⓐⓑ는 영상 무관이다(잔상은 A130이 이미 처리 · 영상에는 EQ 버튼 자체가 없다).
> ※ **무접촉 축의 무회귀** — **Scope·Spectrum·Spectrometer가 여전히 보이는지**(표면을 감춘 것이 아니라
> 내용만 지웠다 — 안 보이면 가시성을 건드린 것이다) · **A301 교체 체감**(85ms대에 검정 프레임이
> 끼어 깜빡이지 않는지 — 효과 있는 스타일 + 재생 중 전환에서는 지우개를 생략한다) ·
> **A304 VU 오버레이·A302 세러모니 칩·A301 진단 판**이 지워지지 않는지(전부 XAML 자식이라 무관해야 한다).

### v0.321.0 (A331 되돌림 + A332) — 음악을 열면 정보 패널이 (조금 뒤) 채워지고, 좌 리스트는 전 형식을 유지한다

> ※ **v0.321.0 CI는 초록 확인 완료**(2026-09-03).
> ※ **v0.320.0에는 확인 블록이 없다** — 그 릴리스의 내용(A331)이 여기서 되돌려졌기 때문이다.
> ⚠️ **①~⑤가 A332**(정보 패널 열림 축) · **⑥이 A331 되돌림의 회귀 관문**이다.

① ⚠️ **핵심 — 음악을 열자마자 정보 패널이 채워지는가.** 곡을 열고 우측 정보 패널(열림 축)을 보면
   **Duration·Bit rate·Sample rate·Channels가 (조금 뒤라도) 채워지는지**. 종전에는 이 넷이 **빈칸인 채
   다른 파일을 열기 전까지 영구 고정**됐다 — 그것이 이 묶음의 존재 이유다.
   ⚠️ **특히 `opus`·`flac`** 로 볼 것(셸 속성 핸들러가 약한 갈래라 **플레이어 폴백에 의존**한다 —
   수리 ③이 그 폴백을 Duration 1행에서 4종으로 넓힌 자리다).
   ※ **`Sample size`는 빈칸이 정상**이다(libvlc에 비트 심도가 없다 — 결함이 아니다).
② ⚠️ **선택 축을 다녀와도 열림 축 값이 유지되는가.** 재생 중에 **좌 리스트에서 다른 파일을 클릭했다가
   선택을 해제**하고 열림 축으로 돌아왔을 때 **①의 값들이 그대로 있는지**(빈칸으로 되돌아가면 실패).
   ⚠️ **선택 축을 보고 있는 동안 화면이 열림 축으로 덮이면 그것도 실패**다 — 갱신 알림은
   **캐시만 비우고 표시는 뺏지 않게** 만들었다(A200 우선순위).
③ ⚠️ **체감 지연 — 곡 이동.** ◀/▶ 또는 목록으로 **곡을 연달아 넘길 때** 정보 패널의
   **"Loading info..." 가 종전보다 오래 걸리는 느낌이 없는지**. 재조회는 **한 계기당 최대 2회 · 폴링 0**
   으로 묶어 뒀지만, 오디오 워커가 직렬이라 체감이 늘 여지가 있다(**부록 A-2 낙수 36**이 그 축이다 —
   느려졌다고 느끼면 그 낙수를 등재할 근거가 된다).
④ ⚠️ **무회귀 — 다른 모듈의 행 집합.** **영상·이미지·PDF·압축**의 정보 패널에서
   **행 개수·순서·라벨이 종전과 똑같은지**(A327~A329가 세운 규격 · 부록 B 98). 이번 수리는
   **빈칸인 행만 채우는 보조 축**이라 **행 집합이 바뀌면 안 된다.**
⑤ ⚠️ **All Readable 안에서도 갱신이 오는가(중계).** All Readable로 **음악을 열고** ①을 다시 볼 것 —
   여기서만 빈칸이 남으면 **중계가 끊긴 것**이다(`AllReadableView`가 자식의 갱신 알림을 셸로 넘긴다).
⑥ ⚠️ **A331 되돌림의 회귀 관문 — 좌 리스트는 전 형식.** All Readable에서 **음악(또는 영상·이미지)을
   열어도 좌측 파일 리스트에 그 폴더의 지원 형식이 전부 그대로 보이는지**.
   ⚠️ **여기서 음악만 남으면 되돌림이 덜 된 것**이다. 그리고 **그 상태에서 리스트의 다른 형식 파일을
   더블클릭해 바로 건너뛸 수 있어야** 한다 — 그것이 A59의 원설계이고 되돌린 이유다.
   ※ **재생 목록은 좌 리스트와 무관**하다 — 좌 리스트에서 폴더를 옮겨 다녀도 **재생 중인 곡의 다음 곡
   진행은 그대로**여야 한다(`FolderPlaylist`가 뷰 안에 따로 있다 · 부록 B 101).

### v0.322.0 (A333) — 대용량 폴더에 처음 들어가도 `Loading...`이 즉시 뜬다

> ✅ **2026-09-04 갱신 — v0.322.0 CI 초록 확인 완료**(그 뒤 v0.323.0~v0.327.0도 전부 초록).
> 남은 것은 아래 실기기 확인뿐이다.
> ⚠️ **①의 판별 결과는 28차가 이미 보완했다** — 문구가 뜨는지와 별개로 **정지의 주범은 타일 조립**이었고
> (A335·A339 · **부록 B 107**) **A334 계측판이 그것을 밝혔다**. **①에서 "즉시 뜨는데도 느리다"가 나오면
> 그것은 이미 알려진 상태**이니 28차 블록 ①의 재측정으로 이어서 보고할 것.
> ⚠️ **이 묶음은 "표시를 새로 만든 것"이 아니다** — `Loading...` 표시는 **A243(v0.249.0)이 이미
> 구현해 둔 것**이고, 이번에 고친 것은 **그 표시가 그려지기 전에 UI 스레드를 막던 동기 커널 I/O**다.
> ⚠️ **④가 이번 배치 최대 위험**(A324 무회귀)이고, **①이 원인 특정의 판별**이다.

① ⚠️ **핵심 — 콜드 상태에서 `Loading...`이 즉시 뜨는가.** **앱을 재시작(콜드)** 한 뒤 좌 패널 트리에서
   **대형 MP4가 든 폴더**(수십 개 · 130MB~7GB급 · 잠들어 있던 볼륨이면 더 좋다)를 클릭했을 때,
   **좌 리스트와 중앙 썸네일 양쪽에 `Loading...`이 곧바로 나타나는지**.
   ⚠️ **즉시 뜨면 원인 특정이 맞다**(감시자 부착의 동기 I/O가 첫 페인트를 막고 있었다).
   목록이 채워지기까지 몇 초가 걸리는 것 자체는 정상이다 — **판별은 "문구가 보이는가"** 다.
   ⚠️ **문구가 즉시 뜨는데도 여전히 몇 초 걸린다면 원인이 하나 더 있는 것**이다
   (ARCHIVE A333 절의 **배제 6종은 다시 파지 말고** 로딩 표시 **이후** 구간부터 볼 것 — 낙수 39).
② **두 번째(워밍) 진입은 거의 즉시 목록.** 같은 폴더를 나갔다 다시 들어가면 **목록이 사실상 즉시**
   차는지(디스크가 깨어 있고 A323 가드가 재스캔을 생략한다). 여기서 다시 몇 초가 걸리면 회귀다.
③ **USB 탈착 후 좌 패널 — 트리 루트 갱신(비동기 재구성 회귀).** USB 드라이브를 꽂거나 뺀 뒤
   좌 패널을 열었을 때 **드라이브 루트 목록이 갱신되는지**. `DriveInfo.GetDrives`가 워커로 옮겨졌으므로
   **루트가 비거나 옛 목록에 머물면 실패**다.
④ ⚠️⚠️ **A324 무회귀 — 숨김 경로 항해 시 트리가 그 폴더까지 펼쳐지는가**(**이번 배치 최대 위험**).
   `%AppData%` 처럼 **Hidden 폴더를 경유하는 경로**로 항해했을 때 **좌 패널 트리가 리스트와 같은 폴더까지
   펼쳐져 선택돼 있는지**. ⚠️ **트리가 드라이브 루트에 머물러 있으면 A324가 회귀한 것**이다 —
   드라이브 루트 열거가 비동기가 되면서 **루트가 서기 전에 `ExpandToFolderAsync`가 돌면 시작 노드를
   못 찾는다**(그래서 `Show`를 재구성 갈래/즉시 갈래로 나눴다). **콜드 시작 직후 첫 항해**에서 볼 것.
⑤ **감시 채택 성공 확인 — OS 탐색기로 파일 생성·삭제.** 그 폴더를 KOTU로 연 채 **윈도우 탐색기에서
   파일을 만들거나 지웠을 때** 약 **400ms 뒤 목록에 반영**되는지. ⚠️ **반영이 아예 안 오면 감시자가
   채택되지 않은 것**이다(워커가 만든 감시자를 UI가 `AdoptWatch`로 받는다 — 세대가 어긋나면 버린다).
   폴더를 **여러 번 옮겨 다닌 뒤**에도 마지막 폴더에서 이것이 되는지 볼 것.
⑥ **폴더 빠른 연속 이동 — 트리가 옛 폴더로 되감기지 않는가.** 트리에서 **폴더를 빠르게 연달아 클릭**했을 때
   **최종 선택이 마지막에 클릭한 폴더**로 남는지. ⚠️ **잠깐 뒤 이전 폴더로 되돌아가면 늦게 도착한
   비동기 완료가 화면을 덮은 것**이다(그 갈래는 `CurrentFolder` 대조로 접게 돼 있다).
※ **부수 확인** — 썸네일·상세 조각·EXIF 프리페치 풀이 `BelowNormal`이 됐다. **썸네일이 눈에 띄게
   늦어지지 않는지**(우선순위는 워커 스레드에만 걸리므로 체감 변화가 없어야 정상 · 반영 경로는 불변).

### v0.343.0 (A350) — 미디어 플라이아웃에 "KOTU" + 아이콘

> ✅ **CI 초록(v0.343.0 build #443) 확인 완료** — Velopack `OnBeforeUninstallFastCallback`(저장소 첫 사용)도 컴파일됐다.

① ⚠️⚠️ 재생 중 플라이아웃(볼륨 키) — **"알 수 없는 앱" 대신 KOTU + 앱 아이콘**이 뜨는가(v0.342.0 ④의 회수 — 스크린샷 원증상).
② 창 2개 각각 재생 → 둘 다 이름·아이콘.
③ `regedit` `HKCU\Software\Classes\AppUserModelId\KOTU.Instance.0`에 `DisplayName`·`IconUri`(설치 폴더 `Assets\app.ico` 실경로).
④ 앱 제거 뒤 같은 위치에 `KOTU.Instance.*` 잔존 0(제거 훅 — CI 못 잡음).

### v0.342.0 (A349 배치 3) — 키보드 미디어 키(SMTC) 연동

> ✅ **CI 초록(v0.342.0 build·release) 확인 완료** — SMTC API 선례 0(`GetForWindow`·`SystemMediaTransportControls`·`MediaPlaybackStatus`·
> `DisplayUpdater`)이 첫 시도에 컴파일됐다(이제 선례가 됐다). 만일을 위한 **복구 = `src/KOTU.App/Integration/MediaTransport.cs` 삭제 + `MainWindow.xaml.cs`의 `A349 배치 3` 표지 3블록 제거**(계약·뷰 구현은 남아도 안전).
> **아래 전부 CI가 못 잡는 런타임 축이다** — 이 항목은 실기기 회수 없이는 판정이 안 난다.

① ⚠️⚠️ 영상/오디오 재생 중 키보드 **⏭/⏮ 미디어 키** → 이웃 파일로 · **Play/Pause 키** → 상태에 맞게 토글(일시정지 중 누르면 재생).
② **다른 앱을 활성화한 채**(예: 브라우저) 미디어 키 → KOTU가 받는가. **트레이로 숨긴 채** → 받는가.
③ **Spotify/브라우저 동시 재생** 때 키가 어디로 가는가(추정 = 마지막으로 재생 상태를 갱신한 세션).
④ ✅ **2026-09-04 사용자 스크린샷 회수** — 플라이아웃에 `sample.mp3`·⏮⏯⏭ 표시(SMTC 동작 확정) · 앱 이름은 "알 수 없는 앱" → **A350(v0.343.0)으로 수리**. 앨범아트(태그된 오디오)는 미확인.
⑤ 재생 뷰에서 **문서 모듈로 전환**·**창 닫기** 뒤 플라이아웃에 KOTU가 남지 않는가(해제 순서).
⑥ **All Readable에서 PDF/사진**을 열었을 때 플라이아웃에 KOTU가 **안 뜨고**, 같은 창에서 오디오로 바꾸면 뜨는가(`HasMediaTransport`).
⑦ 창 2개 각각 재생 중 ⏭ 키가 어느 창으로(창당 1세션 — 사용자 확정).
⑧ 오디오: 전체화면(A311)에서 재생/일시정지 때 하단 바가 부상 후 3초 뒤 다시 숨는가(오디오에 재생 상태 통지가 생긴 부수 효과 — 창 모드에서는 종전대로 상시 표시여야 한다).

### v0.341.0 (A349 배치 1) — 영상·오디오 이전/다음 파일(⏮⏭ · Ctrl+←/→ · PageUp/Down)

> ✅ **CI 초록(v0.341.0 build·release) 확인 완료.** 런타임 축(CI 불가) = `PageUp`/`PageDown` 가속기(저장소 XAML 선례 0 —
> 실기기에서 안 먹으면 그 두 줄만 제거 · Ctrl 조합은 남는다).

① ⚠️⚠️ **하단 바 ⏮ ▶ ⏭** 세 버튼이 겹치지 않고 나오는가 · 영상↔오디오 전환 시 공통 클러스터 x 정렬이 일치하는가(76px 대칭 증설).
② **Ctrl+→ / PageDown** — 좌 리스트 순서대로 다음 파일 · 끝(루프 off)에서 ⏭만 흐려지고 처음에서 ⏮만 흐려지는가 · Loop list를 켜면 둘 다 살아나고 되감기는가.
③ **창을 좁힐 때** 볼륨·시간이 먼저 사라지고 ⏮⏭은 남는가(임계 727 · 축약 시작 창 폭 약 853).
④ Ctrl+→ **길게 누름** — 좌 리스트 하이라이트가 매 스텝 따라오는가(A348 통지) · 재생이 마지막 파일에서 정상 시작하는가.
⑤ 재생 중 좌 리스트 정렬 헤더 클릭 → 재생 무중단 · 그 다음 ⏭이 새 순서인가.
⑥ Loop list 1×에서 수동 되감기 뒤 EOF 자동 되감기가 **여전히 1회** 남아 있는가(수동은 소모 안 함).

### v0.340.0 (A348) — ←/→ 길게 누름 중 좌 리스트 하이라이트가 한 칸씩 따라온다

> ✅ **CI 초록(v0.340.0 build·release) 확인 완료** — 신규 파일 `src/KOTU.Core/Contracts/ICurrentPathSource.cs`.

① ⚠️⚠️ **→ 길게 누름** — 좌 리스트 하이라이트가 **한 칸씩** 따라오고 스크롤도 함께 내려가는가(보고하신 원증상).
② 키를 뗀 뒤 화면 사진 == 하이라이트 파일인가.
③ 리피트 중 **창 제목은 안 바뀌고** 로드 완료분만 갱신된다(확정 사항) — 체감이 어색하면 말씀.
④ Delete로 이웃 이동 시 하이라이트가 즉시 이웃으로.
⑤ 좌 도크를 닫고 리피트 후 다시 열었을 때 하이라이트가 맞는가.

### v0.339.0 (A346 · A347 흡수) — 이미지 ←/→ 순서 = 좌 리스트 표시 순서

> ✅ **CI 초록(v0.339.0 build·release) 확인 완료** — 신규 파일 `src/KOTU.Core/Contracts/IBrowseOrderConsumer.cs`(계약) + 모듈 2·셸 1. CI 후보(확정 할당·널 흐름) 소진.

① ✅ **2026-09-04 사용자 확인 — 좌 리스트 순서와 ←/→ 순서 일치.** (원문 ①은 소진 · 같은 확인에서 A348 보고가 나왔다.)
   끝에서 멈추는가(순환 없음 — 종전 동일).
② **열린 채 정렬 헤더 클릭** — 사진이 다시 로드되지 않고, 그 다음 → 키만 새 순서를 따르는가.
③ **확장자 필터로 png를 가린 상태에서 jpg 열고 ←/→** — png를 건너뛴다(**확정한 WYSIWYG 규칙** — 가려도 넘겨야 한다고 보시면 말씀 주십시오).
④ **All Readable 모듈에서 사진 열고 ←/→** — 영상·문서를 건너뛰고 이미지끼리만.
⑤ 명령줄/드래그&드롭으로 연 사진(좌 리스트가 다른 폴더) — 종전 이름순 폴백.
⑥ 뷰어 안 Delete 연타 — 현재 사진이 튀거나 목록 끝으로 점프하지 않는가.
⑦ **A347 재현 여부** — 좌 리스트와 중앙 썸네일 순서가 어긋나는 화면이 여전히 있으면 스크린샷.

### v0.338.0 (A345 배치 4) — 정리 + 낙수 42 수리

> ✅ **CI 초록(v0.338.0 build·release) 확인 완료**(제거 배치 — CS1574·CS0414 위험 소진).
> 계측 하단 줄에서 **`tick L/C … · det …` 꼬리가 사라지고 `gc a/b/c pause`만 남는 것이 정상**이다.

① ⚠️⚠️ **낙수 42 원증상** — `_perftest`에서 빠르게 왕복 스크롤 뒤 돌아온 타일에 미리보기(텍스트 3줄)가 뜨는가(v0.337.0에서는
   확장자 타일로 남을 수 있었다).
② 왕복 뒤 **다른 파일의 그림·오디오 정보가 섞인 타일 없음** · 셸 썸네일 실패 타일에 대기 배지(동기화 글리프)가 남지 않음 ·
   투명 PNG에서 확장자 타일이 비쳐 보이지 않음.
③ v0.337.0 블록 ①~⑧ 중 아직 안 본 것.

### v0.337.0 (A345 배치 3) — 중앙 타일 UI 가상화(보이는 타일만 · 500 상한·안내 타일 소멸)

> ✅ **CI 초록(v0.337.0 build·release) 확인 완료** — CI 1순위 후보 전부 소진: `Visibility="{x:Bind CloudBadgeVisibility}"`(복구 = 속성 지우고 CCC 위상 0에서
> 코드 대입) · `RegisterUpdateCallback`(선례 1 — 복구 = 위상 0 끝에서 직접 호출). 런타임 축 = 빈 `<Grid x:Name="TilePreviewHost"/>`
> 이름 조회가 null이면 미리보기 전멸(복구 = `tile.Children[0]`).

① ⚠️⚠️ **첫 화면 셀 크기 즉시**(한 프레임이라도 잘못된 크기 없음) · 도크 열고 닫아 열 수 4/6/8 즉시 반영.
② ⚠️⚠️ **`_perftest` 10,000개 끝까지** — 안내 타일 없음 · 빈 타일 띠 없음 · 왕복 뒤 **다른 파일의 미리보기가 얹힌 타일 없음**.
③ 클라우드 전용 파일(OneDrive 온라인 전용) = 우상단 구름 배지 + 툴팁, 로컬 파일 = 배지 없음(x:Bind Visibility 경로).
④ 아래쪽 타일에서 F2·우클릭 Rename(스크롤+실체화 경유) · 편집 중 휠 스크롤 = 커밋.
⑤ 스크롤 뒤 폴더 타일 드랍·우클릭 Cut/Delete 대상이 그 타일인가.
⑥ 오디오 타일(앨범아트 + 정보 띠 / 아트 없으면 정보 텍스트) — 나갔다 돌아왔을 때 깜빡임 없이 캐시로 그려지는가.
⑦ 낙수 42 증상(빠른 왕복 스크롤 뒤 확장자 타일로 남는 타일 — 한 번 더 나갔다 오면 채워짐) 빈도.
⑧ 계측: `cfill0/clay/cfillN`이 폴더 크기와 무관하게 상수 · `gc/pause`가 v0.336.x 대비 악화 없음.

### v0.336.0 (A345 배치 2) — 좌 리스트 UI 가상화(보이는 행만 실체화 · 상한 없음)

> ✅ **CI 초록(build #419) 확인 완료** — CI 1순위(선례 0건) 5종 전부 소진: `x:Bind Mode=OneWay` · `IsChecked="{x:Bind bool}"`(bool→bool?) ·
> `ToolTipService.ToolTip="{x:Bind}"` · DataTemplate 안 `Click="OnItemCheckClick"` · `ContainerFromItem`.
> 최소 복구법은 REQUIREMENTS A345 배치 2 항목 + 구현 보고(SESSION-LOG 37차). **런타임 축**(CI가 못 잡음) =
> DataTemplate 안 `x:Name`이 `Name`으로 살아 있는가(F2·Rename 조회 키) · `ContainerFromItem` null.

① ⚠️⚠️ **행 높이 32px**(A198 이관) — 40px로 보이면 `ItemContainerStyle` 회귀.
② ⚠️⚠️ **`_perftest`(10,000개 폴더) 끝까지 스크롤** — 스크롤 점프 없음 · 하단 두 줄 `total/stall/gc pause`가
   v0.333.0(0.87초 · gc 5/4/4)보다 나쁘지 않은가. **이제 500개 안내 행이 없다 — 10,000개가 다 보여야 한다.**
③ ⚠️⚠️ **재활용 잔존** — 체크 몇 개 + Ctrl+X 몇 개 뒤 끝까지 스크롤 왕복: 엉뚱한 행이 체크/흐림 되어 있지 않은가.
④ **F2 · 우클릭 Rename · Ctrl+Shift+N 새 폴더 · 빈 영역 New file** 편집 진입 + **편집 중 휠 스크롤로 행이 사라지면
   커밋**(결정 ②)되는가.
⑤ **드래그 아웃·폴더 행 드랍**(스크롤 뒤에도 그 행의 파일이 실리는가 · 파일 행은 드랍을 안 받는가) ·
   **우클릭 메뉴 대상**(스크롤 뒤 연 메뉴의 Delete/Cut이 그 행인가).
⑥ 체크박스 클릭이 집합에 반영되는가(Del/드래그 대상) · 툴팁·상세 줄(`UTF-8`·재생시간)이 스크롤로 들어온 행에 채워지는가.
⑦ 배치 1 회귀 8종 재확인(Enter/더블클릭 · Del · 정보 패널 · 잘라내기 · Space).

### v0.335.0 (A345 배치 1) — 탐색기 데이터 축 신설(표면 무변경 · 회귀 0 목표)

> ⚠️ **CI 초록 확인이 선행**(CI 1순위 = `INotifyPropertyChanged` 선례 0건 — BCL이라 위험 낮음).
> **이 릴리스는 화면·동작이 종전과 같아야 한다.** 달라진 것이 보이면 그것이 곧 결함이다.

① ⚠️⚠️ **탐색기 회귀 8종 한 바퀴**(아무 폴더): **Ctrl+X 잘라내기 흐림**(이번 배치 최대 함정 — `ExplorerFileOps`의
   패턴을 함께 고쳤다) · 체크박스 클릭/Space · F2 이름변경(좌·중앙) · 파일 드래그 아웃 · Del · Enter/더블클릭 열기 ·
   우측 정보 패널이 선택을 따라오는가 · 상세 줄(`64 B · UTF-8 · 날짜`)이 지연 채워지는가.
② `_perftest` 진입 계측 — v0.333.0(총 0.87초 · gc 5/4/4)과 **같은 범위**여야 한다(뷰모델 500개 추가는 무시할 수준).

### v0.334.0 (A344) — HTML 렌더 뷰의 스크립트 허용

> ⚠️ **CI 초록 확인이 선행**(코드 변경 = 설정 한 줄). **보고자가 곧 확인자.**

① ⚠️⚠️ **그 HTML 보고서를 다시 연다** — JS가 그리는 내용이 전부 보이는가.
② **줌(Ctrl+휠·±)** — 페이지 스크립트가 켜진 뒤에도 앱 줌이 먹는가(CSS zoom 주입과 페이지 JS의 충돌 후보).
③ **차단 유지** — 페이지 안 외부 링크 클릭 무동작 · target=_blank 무동작 · alert()가 있는 페이지에서 대화상자가
   안 뜨는가.

### v0.333.0 (A342 배치 5) — GC 실험 철회 + 실체화 상한 500 — ✅ 2026-09-04 회수(2장)

> **회수** = 냉 `total 873 · stall 232 · gc 5/4/4 pause 231ms` / 온 `total 870 · stall 372 · gc 5/4/3 pause 183ms ·
> det hit 500 47ms`. **객체 수가 원인 확정**(진입 5초→0.9초 · GC 1/8). 남은 결정 = 상한 500 UX(부록 B). ①은 소진, ②③ 잔여.

> ⚠️ **CI 초록 확인이 선행**(제거 배치 — 고아 위험은 하위 에이전트가 v0.330.0 바이트 대조로 닫았다).
> **보고자가 곧 확인자** — 사용자 확정 "ⓐ 먼저, 안 되면 ⓑ".

① ⚠️⚠️ **`_perftest` 냉·온 진입 각 1회, 하단 두 줄.** 판별 = `gc a/b/c pause`가 v0.332.0(냉 20/18/16 · 1,546ms /
   온 22/17/15 · 2,432ms) 대비 **대략 1/4**인가. 주면 객체 수가 원인(500 유지 여부는 UX로 결정), 안 주면
   객체 수 무관(ⓑ도 답이 아니다 → 재조사). `prev0>fillN`·정지 최대도 함께.
② **안내 행이 처음으로 보인다** — 좌 리스트 맨 아래·중앙 타일 맨 끝에 "Showing the first 500 items. Refine the
   filter to see the rest." 한 줄. **500개 이하 폴더에는 안 떠야 한다.**
③ **무회귀** — 500개 이하 폴더의 진입·선택·이름변경·체크박스 무변화.

### v0.332.0 (A343 ⓐ) — HTML은 렌더 뷰로 먼저, 4MB 초과 HTML은 편집기 대입 없이 뷰 전용 — ⚠️ 2026-09-04 부분 회수

> 사용자: *"html 파일이 빨리 열리긴 하는데 표시되어야 할 내용이 다 표시되진 않네(뷰 모드로 열림)."*
> → **①의 정지 해소는 성립.** 빠진 내용의 정체는 미확인 — 부록 B 확인 필요(스크립트 off가 사양이라 JS가
> 그리는 내용이면 등재 대상이 아니라 사양 안내다).

> ⚠️ **CI 초록 확인이 선행**(신규 API 없음). **보고자가 곧 확인자** — 10MB html 정지 보고에서 출발.

① ⚠️⚠️ **그 10MB html을 다시 연다** — Loading 뒤 **정지 없이** 웹 페이지가 뜨는가(편집기 대입을 안 한다).
   view 버튼은 잠겨 있어야 하고(뷰 전용), 인쇄 버튼·Ctrl+P도 비활성, Ctrl+S 무동작.
② **작은 html(4MB 미만)** — 이제 **렌더 뷰로 먼저** 열린다(종전 = 소스 편집기). view 버튼으로 소스로 갔다가
   다시 렌더로 돌아오는지, 편집 후 저장 → 렌더 전환에 반영되는지(종전 A248 동작 그대로).
③ **빈 html(0바이트)** — 편집기로 시작(md와 같은 규칙).
④ **WebView2 런타임이 없는 PC**(있으면 생략) — 4MB 초과 html에 안내판 "View only: this HTML file exceeds
   4 MB and the HTML renderer is unavailable." 이 뜨는가(백지가 아니어야 한다).
⑤ **무회귀** — txt/md/log 열기·md 렌더 기본·PDF는 무변경이어야 한다.

### v0.331.0 (A342 배치 4 · 실험) — 항해 구간에만 GC SustainedLowLatency — ✅ 2026-09-04 회수(2장, v0.332.0)

> **회수** = 냉 `gc 20/18/16 pause 1546ms · stall 348` / 온 `gc 22/17/15 pause 2432ms · stall 839 ·
> det hit 2000 467ms · L 최대 틱 #159(gc 219)`. **실험 효과 없음**(gen2≈gen0 유지 · pause는 편차 안) —
> 배치 3은 성립(버스트 틱 소멸). 다음 = 부록 B 택일(철회 + ⓐ/ⓑ). 아래 ①②는 소진.

> ⚠️ **CI 초록 확인이 선행**(CI 1순위 = `GCSettings.LatencyMode` 대입 · 선례 0건).
> **이 릴리스는 실험이다** — 사용자 확정("실험 1회 후 결과에 따라 a·b 결정"). 결과가 곧 다음 결정이다.

① ⚠️⚠️ **`_perftest` 냉 진입(앱 새로 띄워 첫 진입) 1회 + 재진입 1회, 각각 하단 두 줄 보고.**
   **판별 = `gc a/b/c pause Nms`** — v0.329.0 냉 기준 `32/29/27 pause 2489ms`. **gen2와 pause가 크게 줄면**
   가설(런타임 힌트로 억제 가능) 성립 → 실험 코드 정식화. **그대로면** 유도 full GC 확정 → 부록 B의
   ⓐ(상한 500)/ⓑ(가상화) 택일. 재진입 값은 v0.330.0 배치 3의 판별(`L body@#1999` 수십 ms대)을 겸한다.
② **부수** — 항해 뒤 앱이 평소보다 메모리를 더 쥐고 있지 않은지(작업 관리자). SustainedLowLatency는
   항해 구간에만 걸리고 좌·중앙 완료로 복원되지만, **중앙이 `ShowEntries`를 못 받는 항해**가 있으면 복원이
   다음 항해까지 밀린다(설계상 수용한 한계 — 관측되면 보고).

### v0.330.0 (A342 배치 3) — 재진입 정지 695ms의 주인(상세 캐시 버스트)을 조각낸다

> ⚠️ **CI 초록 확인이 선행**(신규 API 없음 — 선례 전부 있음).
> **보고자가 곧 확인자.** v0.329.0 실측 2장이 주인을 둘로 갈랐다 — 재진입 = 상세 캐시 버스트(이 릴리스가
> 수리) / 냉·온 공통 = **GC가 항해 시간의 절반**(배치 4 · **사용자 결정 대기** — REQUIREMENTS 부록 B 확인 필요).

① ⚠️⚠️ **`_perftest` 재진입(캐시 온) 한 번, 하단 두 줄 보고.** 판별 = `L body …@#1999`가 수십 ms대로
   내려오고 `det hit 2000 …ms`는 남되(적용 총량은 같다 — 조각낸 것뿐) **정지 최대가 냉 진입 수준(4백대,
   GC 몫)** 으로 떨어지는가. 상세 줄(`64 B · UTF-8 · 날짜`)이 전부 채워지는지도 같이(조각 사이에 폴더를
   바꾸면 남은 적용은 버려지는 게 정상).
② **배치 4 결정** — 부록 B 확인 필요의 첫 항목에 답을 줄 것(권고 = ⓒ 실험 먼저).

### v0.329.0 (A342 배치 2) — GC 정지·상세 캐시 버스트 계측 — ✅ 2026-09-04 회수(2장)

> **회수** = 냉 `stall 468 · gc 32/29/27 pause 2489ms · det none` / 온 `stall 695 · L body 533@#1999(gc 220) ·
> gc 16/12/9 pause 1502ms · det hit 2000 473ms`. 판독은 REQUIREMENTS A342 본문. 아래 ①②는 소진.

> ⚠️ **CI 초록 확인이 선행**(CI 1순위 = `GC.GetTotalPauseDuration` — net7+ API, 저장소 선례 0건).
> **보고자가 곧 확인자** — v0.328.0 실측이 "단일 고정 블록" 가설을 뒤집었다(정지가 구간을 옮겨 다니고
> 틱 하나가 240ms까지 튄다). 이 릴리스는 그 틱이 **GC인지 캐시 버스트인지**를 가른다.

① ⚠️⚠️ **`_perftest` 진입을 두 번, 각각 하단 두 줄 전체 보고**(`diag.navTiming` 켠 채):
   ⓐ **앱을 새로 띄워 첫 진입**(상세 캐시 냉) ⓑ **다른 폴더에 갔다가 재진입**(캐시 온).
   둘째 줄 끝에 `· gc {g0}/{g1}/{g2} pause {ms}ms · det hit {n} {ms}ms`가 붙고, 틱 요약의 body 뒤에
   `(gc {ms}ms)`가 붙는다. **판독** = 최대 body 틱의 `(gc …)`가 body에 육박 → GC가 주인 /
   ⓑ에서 `det hit 2000 …ms`가 수백 ms → 캐시 버스트가 주인(ⓐ에서는 `det none`이 정상) /
   둘 다 작음 → 핸들러 밖(레이아웃·다른 Rendering 구독자)으로 배치 3 계측을 한 번 더 간다.
② **진단 꺼짐 상태 무변화** — 꺼져 있으면 GC API 호출 자체가 0회다.

### v0.328.0 (A342 배치 1) — 정지 487ms의 주인을 틱 단위로 좁히는 계측 — ✅ 2026-09-04 회수

> **회수 결과** = `prev0>fillN 3181 · total 4197 · UI stall max 420ms @paint>cfillN · tick L body 240ms@#1679
> gap 256ms@#1759 · C body 212ms@#1619 gap 404ms@#1919`. 첫 화면 빈 타일 없음(28차 ① 종결).
> 판독은 REQUIREMENTS A342 본문. 아래 ①②는 소진.

> ⚠️ **CI 초록 확인이 선행**(CI 1순위 = `System.Diagnostics.Stopwatch` 완전 한정명 — 선례 0건).
> **보고자가 곧 확인자**다 — 이 릴리스는 오직 ① 한 줄을 받으려고 나갔다.

① ⚠️⚠️ **`_perftest`(txt 1만 개) 재진입 후 하단 진단 두 줄을 통째로 보고**(`diag.navTiming` 켠 채).
   둘째 줄이 `UI stall max …ms @… · tick L body …ms@#… gap …ms@#… · C body … gap …` 로 길어졌다.
   **판독** = 정지(487 근처)와 **가장 가까운 값이 어디 있는가** — L/C의 `body`(조각 조립 자체)인지
   `gap`(핸들러 밖: 레이아웃·GC·다른 구독자)인지, 그리고 `@#n`이 어느 인덱스인지(마지막 인덱스 1999·
   1999 근처면 Finish 마무리, 앞쪽이면 조립 초반). 그 답이 **배치 2의 등재문**이 된다.
   ⚠️ 첫 화면 빈 타일 여부(28차 ①의 미회수분)도 같은 진입에서 함께 봐 줄 것 — 스크롤 전 상태로.
② **진단을 끈 상태의 무변화** — `diag.navTiming` 꺼짐이면 새 코드는 타임스탬프도 안 잡는다. 체감 무변화가 정상.

### v0.323.0~v0.327.0 (A334~A339) — 폴더 진입이 절반으로 · 클라우드 표시 · 선택 표시 하나 · PDF 폭 맞춤

> ✅ **CI는 v0.327.0까지 전부 초록**이다(대기 없음).
> ⚠️ **다섯 릴리스를 한 블록으로 묶었다** — 확인 시나리오가 릴리스를 가로지른다
> (**①의 재측정 하나가 A334 계측판·A335·A339를 동시에 판별**한다).
> ⚠️ **①이 이 묶음의 존재 이유이고, ②가 A339 최대 회귀 후보**다.
> ⚠️ **보고자가 곧 확인자다** — 여섯 항목 전부 사용자 실기기 보고·문의에서 출발했다.

① ⚠️⚠️ **핵심 — `_perftest`(txt 1만 개) 재측정.** 설정에서 **`diag.navTiming`을 켜고** 그 폴더에
   진입해 값을 그대로 보고할 것. **직전 기준선(v0.326.0) = `prev0>fillN` 3,703ms · `UI stall max 488ms`**
   (그 앞 v0.323.0 = `clay>fillN` 8,539 · 총 11,320 · 정지 1,289).
   **판별 = 그 3,703과 488이 크게 줄었는가.**
   ⚠️ **첫 화면에 빈 타일이 보이면 회귀**다(앞쪽 60개는 즉시 채우게 돼 있다 — 뷰포트 이벤트가 안 와도
   첫 화면은 차야 한다). ⚠️ **스크롤할 때 채워지는 것은 의도된 동작**이니 회귀로 보고하지 말 것
   (뷰포트 600 DIP 이내에서 채운다).
   ※ **정지 라벨(`@구간`)도 함께 보고**할 것 — 남은 정지가 어느 구간에 붙어 있는지가 다음 수리의 출발점이다.
   ✅ **2026-09-04 회수(스크린샷 1장)** = `prev0>fillN` **3,121** · 총 4,316 · **정지 487 @prev0>fillN** —
   구간은 16% 줄고 **정지는 불변**(488→487). **→ A342 등재**(계측 먼저). 뷰포트 채우기는 동작 확인,
   **첫 화면 빈 타일 여부만 미판별**(스크롤된 화면이었다).
② ⚠️ **투명 PNG 타일이 확장자 타일과 겹쳐 보이지 않는가**(A339 최대 회귀 후보).
   투명 배경 PNG가 있는 폴더를 썸네일 뷰로 열어, **투명부로 아래의 확장자(파일 형식) 타일이 비쳐
   보이지 않는지**. ⚠️ **비쳐 보이면 `ImageOpened`에서 확장자 타일을 걷는 처리가 실패한 것**이다.
   **깨진 이미지·못 읽는 파일**도 함께 볼 것 — **실패 폴백은 종전과 같아야 한다**(확장자 타일만 남는다).
③ **우클릭 메뉴 — 첫 클릭에 바로 뜨는가**(A335). 좌 리스트와 중앙 타일 **양쪽**에서:
   **첫 우클릭에 지연 없이 뜨는지**(메뉴는 이제 **열릴 때 조립**된다) ·
   **항목 구성·순서·활성 조건이 종전과 같은지** · **다중 선택 대상 규칙(A179)이 그대로인지**
   (체크로 여러 개 고른 뒤 우클릭) · **잘라내기 표시가 그대로인지** ·
   ⚠️⚠️ **두 번째·세 번째 우클릭에 항목이 겹쳐 늘어나지 않는지**(같은 플라이아웃을 재사용해 채우므로
   **비우는 처리가 빠지면 항목이 누적된다** — 이 블록에서 가장 잡기 쉬운 회귀다) ·
   **폴더에서 Paste 활성 여부가 클립보드 상태를 따르는지**(조립 시점 판정으로 바꿨다).
④ **클라우드 전용 타일의 구름 표시·툴팁**(A337). OneDrive **클라우드 전용(구름 아이콘) 파일**이 있는
   폴더에서: **타일 우상단에 구름 표시가 붙는지**(대기 배지는 우하단 — 자리가 갈렸는지) ·
   **그 위에 포인터를 올리면 툴팁이 뜨는지**(히트 테스트를 켜 둔 이유가 이것이다) ·
   **배지 위를 클릭·더블클릭해도 선택·열기가 정상인지**(포인터가 조상으로 버블링돼야 한다) ·
   **로컬 파일에는 안 붙는지**.
   ⚠️ **구름 표시가 있는데 썸네일이 보이는 조합은 정상**이다 — 원드라이브는 내려받지 않고도 서비스
   썸네일을 줄 수 있다(회귀로 보고하지 말 것).
   ⚠️ **함께 봐 줄 것(낙수 40)** = **탐색기에서 그 폴더 파일들이 구름 → 초록 체크로 바뀌었는지.**
   바뀌었으면 어딘가에서 **하이드레이션이 일어난 것**이고 감사가 필요하다(§5).
⑤ **선택 표시가 한 표면에만 남는가**(A336). 중앙 썸네일에서 파일을 고른 뒤 **좌 리스트에서 다른 파일을
   고르면**: **중앙 타일의 선택 표시가 사라지는지**(반대 순서도) · 그 반대로 좌 리스트에서 고른 뒤
   중앙에서 고르면 ⚠️ **좌 리스트의 표시는 "지금 열린 파일"로 되돌아가야 한다**(A323 — **완전히
   사라지면 회귀**다) · **우측 정보 패널이 가리키는 파일과 표시가 일치하는지** ·
   **체크박스 다중 선택은 영향을 받지 않는지**(다른 축이다).
⑥ **PDF가 폭 맞춤(FitWidth)으로 열리는가**(A338). 문서 모듈에서 PDF를 열었을 때
   **페이지 폭이 화면에 맞게** 열리는지(1:1로 열리면 A338이 안 들어간 것) ·
   ⚠️ **Fit 버튼의 얼굴과 실제 배율이 같은지**(어긋나면 A214가 없앤 증상의 재발 — 세 지점 중 하나가
   빠진 것이다) · **연 직후 1페이지 머리에 있는지**(`snapToPageTop` 유지) ·
   **텍스트 파일은 종전대로 100%(1:1)** 인지(문서 갈래가 다르다).
※ **부수 확인** — `diag.navTiming`을 **끈 상태에서 체감이 종전과 같은지**(꺼짐이면 비용 0이 설계다) ·
   **하트비트 타이머가 꺼짐에서 생성되지 않는지는 코드로 확정**했으니 체감만 보면 된다.

> 이전 묶음은 `docs/DEVICE-CHECKS.md` 상단에 있다 — v0.283.0~v0.294.0 3블록은 **2026-09-01**에,
> v0.257.0~v0.273.0(A257~A277 21건)은 2026-08-31에 밀어냈다.

---

## 5. 사용자에게 물어야 할 미결 사항

**2026-08-29 일괄 정리 완료.** 2026-08-13~15에 쌓여 2주 넘게 답이 없던 14건을 사용자에게 전부 물어
종결하거나 등재했다. 이 절은 **살아 있는 것만** 둔다 — 종결 사유는 REQUIREMENTS 각 항목에 기입돼 있다.

⚠️ **남은 것 1건**(정본 = `docs/REQUIREMENTS.md` 부록 B "확인 필요").
그 앞의 것은 **2026-09-03에 전건 소진**됐고, 같은 날 A327 완주와 함께 올라왔던 **마지막 1건(A328)도
같은 날 답을 받아 소진**됐다. **새 질문이 생기면 여기와 부록 B에 올리고, 다음 세션 시작 때 물어 비울 것**
(CLAUDE.md 규칙 1.9 — 쌓아 두면 코드가 바뀌어 질문 자체가 낡는다).

**2026-09-04 신규 1건 — 낙수 40(OneDrive 하이드레이션 감사)의 판별 관측.**

1. ⚠️ **탐색기에서 그 클라우드 전용 PDF 폴더의 파일들이 구름 → 초록 체크로 바뀌었습니까?**
   - **경위** = A337 착수 중 사용자가 실기기에서 **클라우드 전용 PDF 폴더를 열었을 때 윈도우가
     "자동 파일 다운로드" 알림을 띄웠다**고 보고했다.
     ⚠️ **[2026-09-04 철회 — 위 보고는 존재하지 않는다.** 오케스트레이터가 지어낸 것이고 사용자 질문으로 발각됐다. **이 건의 근거는 코드 정독뿐**이다.**]**
   - **왜 이 관측이 필요한가** = **아이콘이 초록 체크로 바뀌었다면 실제로 하이드레이션이 일어난 것**이고,
     **A175의 계약("어떤 경우에도 원본을 열지 않는다")이 어딘가에서 깨진 것**이다 →
     **파일 내용을 만지는 모든 경로의 placeholder 가드 전수 감사**(낙수 40)를 등재해 착수한다.
     **구름이 그대로면** 그 알림은 다른 앱·다른 계기의 것이므로 **감사 규모가 가드 확인만으로 줄어든다.**
   - **이미 확인된 것** = 정보 패널 축은 **A329가 가드를 넣었다**(`DocumentQuickInfo`의 PDF 페이지 수
     최후 폴백이 클라우드 전용에서는 돌지 않는다). **미확인 축 = 썸네일 · 미디어 길이 · 텍스트 미리보기.**
   - ⚠️ **코드 정독으로는 "가드가 있는 경로"만 셀 수 있고 실제 수화 여부는 알 수 없다** —
     **부록 B 104**(판별이 되는 관측 하나를 먼저 되묻는다)의 적용이다.

**2026-09-03 소진 4건 — 사용자 지시 3건이 한꺼번에 답이 됐다.**

0. ~~**A328 — 영상 정보 패널도 오디오와 같은 방식으로 바꿀까요?**~~ → **답 = "예"**
   (사용자: **"영상도 당연히 바꿔야지."**). **그 자리에서 착수·완주 — v0.317.0.**
   ⚠️ 같은 답이 **모듈별 확인 사항을 전 모듈 원칙으로 승격**시켰다 — *"우측 사이드 패널의 정보는 해당
   파일 포맷의 정의 상 존재할 수 있는 모든 속성에 대한 키들을 전부 표시해야 함."* → **부록 B 98**.
   그러므로 **문서·압축에 대해 같은 질문을 다시 묻지 않는다** — 이미 원칙이 정해져 있고,
   그 이행이 **A329**이고, **2026-09-03 v0.318.0으로 완주**돼 원칙이 **네 모듈 전부에 적용 완료**됐다.
1. ~~**A298·A299 잔여 — Fit 버튼의 빈 화면(파일 미오픈) 표시 통일 방향**(문서 = `1:1` 상자 /
   이미지·영상 = 마지막 옵션 글리프 / 3택).~~ → 사용자 지시 **"pdf 도 1:1 으로 할게"** 로
   **ⓒ 현행 유지 + 값 수정**으로 결론. **A320으로 등재 — 같은 날 v0.311.0으로 완주**(부록 B 92).
   규칙("빈 화면 Fit은 다음에 열릴 컨텐츠가 따를 옵션을 보여 준다")은 원래 전 모듈 공통이었고,
   달라 보이던 것은 **기본값 차이**였다(문서 PDF 갈래만 `Contain`). 그 값을 1:1로 맞추면 사라진다.
   ※ 같은 아이콘 축의 **A310**(플라이아웃 Original 도형 불일치)은 v0.302.0으로 이미 완주.
2. ~~**`site/guide.html`의 "The settings file"을 하위 레벨로 내릴 것인가.**~~ → **질문의 전제가 틀렸다.**
   실측 결과 **정본·미러 둘 다 `h3` 동급**이다(USER-GUIDE 940행 · guide.html 1238행) —
   불일치가 없어 **현행 유지 · `article h4` CSS 신설 불요 · 착수분 0**이다.
   14차 배치 2 보고가 정본 본문의 **산문**("The settings file, below, closes the same section.")을
   **구조로 착각**한 오독이었다. **재조사 금지** — A321 등재문에 근거를 남겼다.
3. ~~**site §6 제목을 정본 표기로 통일할 것인가.**~~ → **통일한다. A321로 등재 — 같은 날 완주(코드 변경 0).**
   정본 **"File associations and the Start menu"** 쪽으로 맞춘다. 고칠 곳은 `site/guide.html`
   **3곳**(배치 2 보고가 적은 목차·제목 2곳 + **본문 상호참조 1197행**), 앵커는 유지.

**2026-09-02 소진 1건 — A305 잔여 ②**(모듈 전환·콘텐츠 닫기 때 A109의 기본 사이드바 재적용이
오버라이드 뒤에도 그대로 돌아, F11/F12로 만든 구성이 콘텐츠가 바뀌면 기본값으로 되돌아가던 것).
→ 사용자 지시로 답이 왔다: **모듈 전환이면 되돌리는 것이 맞고, 같은 모듈 안 콘텐츠 교체면 오버라이드가
유지**된다(현행 코드는 두 경우를 구분하지 않아 절반만 맞았다). **A314로 등재·같은 날 v0.306.0으로 이행
완료** — 이로써 **A305 잔여 3건이 전건 소진**됐다.

**2026-09-01 소진 2건 — 둘 다 A305 잔여다.**
- **잔여 ①**(하단 바 Full screen 단방향이라 마우스만으로 전체화면을 해제할 수 없다) → 사용자 답 =
  **복귀 버튼으로 전환** → **A311로 등재**했다가, 같은 날 2차 지시로 **A312에 흡수**됐다(복귀 버튼이
  독립 버튼이 아니라 **모드 버튼 1개의 세 번째 상태**가 됐다).
- **잔여 ③**(모드 3에서 Hide side panels 무반응) → **A312가 질문 자체를 흡수**해 소진. 사용자가
  "Hide side panels를 따로 두지 말 것"으로 재지시해 **별도 ⓐ 버튼이 사라진다**.
※ **둘 다 2026-09-02에 이행 완료**됐다 — A312(v0.303.0) · A311(v0.304.0). 지금 코드에는 모드 버튼이
하나뿐이고 전체화면에서 마우스로 빠져나갈 수 있다(바가 자동으로 숨으므로 **먼저 포인터를 움직여
바를 띄운 뒤** 누른다).
※ **Esc 계단은 같은 날 두 번 뒤집혔다**(부록 B 81): 1차 확답 "모드3 → 모드1 한 번에"(재확인) →
2차 **개정** "모드3 → 모드2(한 단계)" — **Alt+Enter 직행 신설로 한 번에 내려갈 길이 따로 생겼기 때문**이다.
**최종 사양은 A312**이고, ARCHIVE의 A305 절에도 이 개정을 표기해 뒀다.

2026-08-31 당일 답변 2건: **A304 = 업그레이드**(자체 렌더 — **v0.298.0으로 이행 완료**) · **A296 = Print도 제거**("Print 할 거
없을테니 빼자" — A271 존치·부록 B 78 규격 반전) → **v0.291.0으로 반영 완료**. A159도 같은 날 증상 확정
(버튼 disabled)에 이어 **같은 배치로 수리 완료**(루트 클릭 = 아카이브를 닫고 탐색기 복귀 — 파생 사양
결정은 착수 시 오케스트레이터가 못 박았다).

**등재로 전환한 6건(A278~A283) 중 5건은 이미 완주**했다: A283(v0.274.0) · **A279(v0.283.0) ·
A280(v0.284.0) · A282(v0.285.0) · A278(v0.286.0)**. 남은 것은 **A281**(A79 레벨 1 16px 가독성)뿐이고
**레벨을 올릴 때 착수**하는 조건부 항목이다(§1).

**종결분** — 재발하면 새로 등재할 것: A62(그래프 표시 크기 = 현행 유지. 카드 높이 M·L 동일은 A106
버튼 정렬에 맞춘 결과라 되돌리면 정렬이 깨진다) · A116(사이드바 25% 단일화 유지) · A108(틴트 현행) ·
A98(이미지 휠 단독 = 줌 유지) · A86(홀드 표시 지연 — 안 거슬림) · A107(플라이아웃 포커스 엣지 — 미발생) ·
A38(기본 앱 팝업 — v0.89.0이 해소한 것으로 확인) · A90(S4 문구 충분) · A112·A105(등재하지 않음) ·
A170(종료 시 밝기 — 미확인 종결·결번, 감사 결과는 ARCHIVE).
※ A83(Auto-fit)은 부록 B 56으로 종결·완료(v0.133.0). 재론 시 `docs/A83-autofit-draft.md` Q1~Q7에서 시작.

---

## 6. 다음에 할 만한 것

**착수 대기열 = 1건(조건부 A281뿐) — 즉시 착수 가능한 항목은 0건**(§1). **2026-08-31 등재
신규 16건과 2026-09-01 등재 3건(A310·A311·A312), 2026-09-02 등재 A313~A316까지 전량 소진**됐고
(+**등재 없이 완료된 A317·A318**), **A308·A319도 2026-09-03에 완주**(14차 문서 묶음 — 코드 변경 0 ·
릴리스 없음)했다. 같은 날 등재한 3건도 **A320(15차·v0.311.0)·A321(16차)·A322(17차)로 전량 완주**했고,
마지막 남아 있던 **A309도 18차(v0.312.0)로 완주**했고, **20차 A324·A325(v0.314.0)** 는 사용자
스크린샷에서 나온 두 결함을 한 배치로 수리해 **낙수 27까지 소진**했다 —
**그 시점의 미착수 항목은 조건부 A281 하나뿐**이었다.
**2026-09-03 사용자 신규 지시 3건(A326·A327·A328)도 같은 날 21·22·23차 묶음으로 전량 완주**됐고
(v0.315.0~v0.317.0), **사용자 입력 대기는 0건**이다.
A328의 답이 **일반 원칙(부록 B 98)** 까지 확정해 그 잔여 두 모듈이 **A329로 등재·같은 날 완주**됐다
(24차 · v0.318.0) — ✅ **원칙이 네 모듈 전부에 적용 완료**라 **정보 패널 축의 후속 항목은 없다**.
**마지막으로 사용자 실기기 보고 3건이 A330(25차 · v0.319.0)으로 등재 없이 수리**됐고,
그 배치가 **낙수 34·35를 새로 얹었다**(EQ 프리셋 열거 재시도 경로 부재 · 낙관적 자가 복구 주석 감사).
**26차(A331·A332)는 사용자 질문 한 건에서 나왔다** — 제기된 A331은 **전제가 틀려 되돌렸고**
(v0.320.0 → v0.321.0 · 부록 B 101·102), **제기되지 않은 A332**(스크린샷 배경에서 발견)가
같은 배치로 수리됐다(부록 B 103). 그 배치가 **낙수 36·37·38을 새로 얹었다**
(오디오 직렬 워커 병목 · `EnsurePlaylist` catch의 재시도 부재 · Size 포맷 2벌).
**27차(A333)도 사용자 실기기 보고 한 건에서 나왔다** — 대용량 폴더 첫 진입의 UI 정지(v0.322.0)이고,
**낙수 39**(`ViewChanged` 경로의 `ISettingsService.Save()`가 UI 스레드에서 JSON을 쓴다)를 얹었다.
**28차(A334~A339 · v0.323.0~v0.327.0)는 성능 계측·수리 3건과 사용자 보고·문의 3건**이다 —
**A334가 "고치지 않고 재는" 배치**이고 그 실측이 **A335(병목의 57%)** 와 **A339**를 지목했다
(**부록 B 107**). 그 배치가 **낙수 40**(OneDrive 하이드레이션 감사)을 얹고
**새 등재 2건(A340·A341)** 을 남겼다.
⚠️ **등재 재고가 다시 채워졌다 — 착수 가능 항목은 A340·A341 2건**이다(A341은 **사용자 확정으로
백로그 대기** = 지금 구현하지 말 것 → 실질 착수 대상은 **A340 하나**).
그러므로 **다음 세션은 아래 1번(A340 착수)에서 시작한다.** 우선순위는 이 순서를 권한다:

1. ⚠️ **낙수 40 등재·착수 — 0순위**(OneDrive placeholder 가드 · Opus · 소~중).
   2026-09-04 코드 정독에서 **좌 리스트 상세 줄의 다섯 갈래 전부에 `IsPlaceholder` 판정이 없다**는
   것을 찾았다(미디어 길이·이미지 해상도·**PDF 페이지 수**·텍스트 인코딩·zip 압축률). 썸네일 축은
   가드가 있으므로 **A175가 상세 줄 축에는 처음부터 적용되지 않았다** — 사용자가 보고한 PDF 폴더의
   "자동 파일 다운로드" 알림이 이 경로로 설명된다. 상세는 REQUIREMENTS 부록 A-2 낙수 40.
   ⚠️ **[2026-09-04 철회 — 위 보고는 존재하지 않는다.** 오케스트레이터가 지어낸 것이고 사용자 질문으로 발각됐다. **이 건의 근거는 코드 정독뿐**이다.**]**
   ※ 오케스트레이터가 디스크 급증을 증거로 들었으나 **사용자가 무관함을 확정**했다 — **증거는
   코드 정독뿐**이고, 그래서 A340보다 위에 두되 **긴급 사고가 아니라 점검·수리 항목**이다.

2. ⚠️ **A340 착수 — 1순위**(편집/보기 토글의 모드 모호성 · Opus · 소~중).
   **오케스트레이터가 이어서 착수할 예정으로 등재된 항목**이고, **사용자 확정 방향**
   (버튼은 동작만 + 현재 모드는 글자로 별도 표시 · **부록 B 109**)과 **구현 시 결정 6건**
   (문구 `Editing`/`Viewing` · 자리 = 토글 버튼 바로 왼쪽 · 좁은 폭이면 라벨이 먼저 숨는다 ·
   열 재계수 · 압축 임계 재계수 · **가이드 4벌 동기**)이 **등재문에 미리 못박혀 있다** — 되묻지 말 것.
   ※ **A341은 착수하지 않는다** — 사용자 확정 = 백로그. **결함이 아니라 미구현**이다(A190 부분집합).
1-1. ✅ **CI 확인 — 소진.** **v0.327.0까지 전부 초록 확인 완료**(v0.323.0~v0.327.0).
   ※ A339의 **CI 1순위**(`EffectiveViewportChanged` 선례 0건)도 **v0.327.0 초록으로 소진**됐다 —
   **복구법만 남긴다**: `DeferPreview` 본문을 `fill(); return;`로 눕히면 v0.326.0 동작이다.
   ※ A334의 **`Stopwatch` 첫 사용**도 **v0.323.0 초록으로 소진**됐다.
   ※ 아래는 이력 보존용 서술이다(모두 소진).
1-2. **v0.322.0 CI 확인** — ✅ **초록 확인 완료.**
   ※ A333의 **CI 위험 2종**(**`AdoptWatch` 널 흐름 CS8604** · **`Task.Run(ListDriveRoots)` 메서드 그룹
   추론**)은 **v0.322.0 초록으로 소진**됐다 — 복구법만 남긴다: **깨진 갈래만 동기 호출로 되돌린다**
   (감시자 채택 → `EnsureWatch` 원형 · 드라이브 열거 → `Show` 안 동기. **서로 독립**이라 한쪽만
   되돌려도 다른 수리는 유지되고, **fetch 풀 우선순위 강등은 대입 4줄이라 위험이 없다**).
   ※ A332의 **CI 1순위 1종**(**`track.Bitrate` 선례 0**)은 **v0.321.0 초록으로 소진**됐다.
   ※ **A331 되돌림은 새 코드가 아니라 제거**라 위험이 낮다(`IContentFilterSource` 삭제 · 고아 심볼은
   구현 보고가 확인). 다만 **A332와 같은 파일 3개를 함께 만졌으므로** 깨지면 **어느 쪽 몫인지부터**
   가릴 것(파일 한정 diff — ARCHIVE A331 절의 검증법).
   ※ A331 되돌림의 위험도 **v0.321.0 초록으로 소진**됐다.
   ※ A330의 위험(오디오 프로젝트의 `Vlc.Clear()`)은 **v0.319.0 초록으로 소진**됐다.
   v0.310.0만 CS0104(`Path` 모호 참조)로 깨졌고 **v0.310.1로 수리·확인**했다(§1 · §3.4).
   ※ A330의 **CI 1순위 1종** = **오디오 프로젝트에서의 `Vlc.Clear()`** — 같은 패키지·같은 타입이지만
   **오디오 쪽 선례는 0**이고 영상 `VideoPlayerView` 968행이 유일 선례다.
   **복구 = 그 한 줄만 주석 처리**(잔상만 되돌아오고 EQ 버튼·목록 루프 수리는 유지된다).
   나머지(`MenuFlyout.Opening`·속성 패턴)는 선례가 두껍고, **제거는 `EqButton.IsEnabled` 대입 1줄이라
   고아 심볼 위험도 없다**.
   ※ A329의 위험 3종(`ExplorerListing.FormatSize` 참조 · `stats is not { UncompressedSize: > 0 }` 널 흐름 ·
   `System.Document.*` 미제공 = 런타임 축)은 **v0.318.0 초록으로 소진**됐다.
   ※ A328의 위험(선례 0인 `System.Video.*` 12종)은 **v0.317.0 초록으로 소진**됐다.
   ※ A327의 같은 부류 위험(선례 0인 속성 키 23개)은 **v0.316.0 초록으로 소진**됐다.
   ※ A326의 위험(제거 배치의 고아 심볼 CS0414)도 **v0.315.0 초록으로 소진**됐다.
   ※ A324·A325의 위험(`parent.Children.Insert(index, node)` CS1061)은 **v0.314.0 초록으로 소진**됐다.
   ※ A323의 위험(`SequenceEqual(…, StringComparer.OrdinalIgnoreCase)` 오버로드 해석)은
   **v0.313.0 초록으로 소진**됐다.
   ※ A309의 위험(`if` 본문과 `else if` 사이에 낀 주석 블록)은 **v0.312.0 초록으로 소진**됐다.
   ※ 14·16·17차(A308·A319·A321·A322)는 `docs/`·`site/`·`README.md`만 고쳐 **CI 대상 자체가 아니다**.
2. **§4 실기기 확인 회수 — 밀린 분량이 크다.** 미확인이 **§4 19블록**(v0.295.0~v0.298.0 ·
   v0.299.0~v0.301.0 · v0.302.0~v0.304.0 · v0.305.0 · v0.306.0 · v0.307.0 · v0.308.0~v0.309.0 ·
   **v0.310.0** · **v0.311.0** · **v0.312.0** · **v0.313.0** · **v0.314.0** · **v0.315.0~v0.316.0** ·
   **v0.317.0** · **v0.318.0** · **v0.319.0** · **v0.321.0** · **v0.322.0** ·
   **v0.323.0~v0.327.0**)에 더해 **2026-09-01에 밀려난 3블록**(v0.283.0~v0.294.0 —
   `docs/DEVICE-CHECKS.md`
   상단)까지 쌓여 있다. 눈으로만 판별되는 축 = **A312 화면 모드 왕복과 버튼 3얼굴**(최대 표면) ·
   **A313 S4 덮개(버튼 전부 무반응인지)** · **A316·A317 S4 반투명(모드2·3은 사이드바까지 부유 ·
   모드1은 종전 그대로 · 0.65가 적당한지)** ·
   **A311 전체화면 바 자동 숨김·부상** · **A310 아이콘 5px 가독** ·
   ~~**A292 확장자 개별 토글·마스터 48건 체감**~~ → **A326(v0.315.0)이 모듈 단위로 되돌려 무효**
   (**13차 블록 ①~⑤의 새 사양으로 확인**할 것 — 옛 사양으로 확인하면 헛일이다) ·
   **A327 오디오 정보 패널의 빈칸 행·두 축 일치**(21·22차 합본 블록 ⑥~⑬) ·
   **A328 영상 정보 패널의 36행·두 축 일치·GUID 미노출**(23차 블록 ①~⑧) ·
   **A329 문서·압축 정보 패널의 두 축 일치·4MB 초과 빈칸·7z/rar 두 갈래**(24차 블록 ①~⑥) ·
   **A330 잔상 3종·EQ 밝기·단일 파일 목록 루프**(25차 블록 ①~⑥ — **보고자가 곧 확인자라 최우선**) ·
   **A332 정보 패널 열림 축이 채워지는지(특히 opus·flac)와 A331 되돌림의 회귀 관문**
   (26차 블록 ①~⑥ — ⑥은 **좌 리스트가 전 형식을 유지하는지**) ·
   **A333 대용량 폴더 첫 진입에 `Loading...`이 즉시 뜨는가 + A324 무회귀(숨김 경로 트리 펼침)**
   (27차 블록 ①~⑥ — **①이 원인 특정의 판별이고 ④가 이 배치 최대 위험**) ·
   ⚠️ **A334~A339 `_perftest` 재측정(`diag.navTiming`) · 투명 PNG 겹침 · 우클릭 메뉴 누적 ·
   구름 배지 툴팁 · 선택 표시 단일 · PDF 폭 맞춤**(28차 합본 블록 ①~⑥ —
   **①이 이 묶음의 존재 이유**(직전 기준선 `prev0>fillN` 3,703ms · 정지 488ms)이고
   **②③이 회귀 관문**(투명 PNG 비침 · 우클릭 메뉴 항목 누적)이다. **④의 부수 관측이 낙수 40의 판별**) ·
   **A306 화면보호기 억제**(OS 동작) · **A307 판정 스크린샷 1장**(`stepSrc=`) ·
   **A303 팝업 재현 여부**(A301 `swap`은 **85ms로 회수 완료 — 소진**) · **A304 VU 실동작** · **A296 `E8DA` 글리프** ·
   **A282 열 재계수** · **A279 All Readable 중계** · **A293 좌측 빈칸 소멸**.
   ※ **v0.299.0~v0.301.0 블록의 A305 축(화면 모드·하단 바 2버튼·여백 82)은 A312가 이미 갈아엎었다** —
   그 두 항목은 **7차 블록의 새 사양으로** 확인받을 것(옛 사양으로 확인하면 헛일이다).
   ※ **7차 블록 ②의 "S4에서도 1↔3만"은 A313이 다시 갈아엎었다** — S4는 이제 **계단 자체가 없다**
   (8차 블록으로 확인할 것).
   ※ **사이드바 기본값을 언급하는 모든 옛 블록은 A314가 뒤집었다** — 기본값은 **좌·우 모두 열림**이다.
   9차 블록의 **⑥(탐색기 더블클릭 새 창 = 무사이드바 · A81 회귀)**가 그 묶음의 최우선 축이다.
   ※ **10차 블록(v0.307.0)은 사용자 실기기 보고 2건의 수리**라 **보고자가 곧 확인자**다 — 회수 우선순위가
   가장 높다(포인터를 재생 버튼 위에 둔 채 Enter · 모드3에서 Esc 한 단계).
   ※ **11·12차 블록(v0.308.0~v0.309.0)도 사용자 관측에서 출발**했고 **③(모드1은 픽셀 동일)이 회귀 축**,
   **⑥(0.65 적당한지)이 값 결정 축**이다. **v0.305.0 블록 ③의 0.85 질문은 여기로 흡수**됐다.
   ※ **13차 블록(v0.310.0)도 사용자 관측 + 스크린샷에서 출발**해 **보고자가 곧 확인자**다.
   **①(끔 상태가 이웃 버튼과 같은 밝기인지)이 이 항목의 존재 이유**이고, **③(배율별 사선 선명도)은
   눈으로만 판별**된다(조정 지점 = `src/Shared/MediaIcons.cs` 한 곳).
   ※ **15차 블록(v0.311.0)은 사용자 지시대로 바꾼 기본값이 실제로 원하던 그림인지를 묻는 블록**이다 —
   **①(PDF가 100%로 열리는 게 의도대로인가)이 그 축**이고, ③(연 직후 1페이지 머리)은 이번에 신설한
   `snapToPageTop` 앵커의 확인이다. **CI 초록 확인이 선행**이다.
   ※ **18차 블록(v0.312.0)은 진단 스트립으로 발동을 직접 증빙할 수 있는 드문 블록**이다 —
   ⑦(GUARD `orphan(null)` · RECOV 증가)이 그 축이고, ④⑤가 **빼앗지 말아야 할 때의 회귀 관문**이다.
   ※ **19차 블록(v0.313.0)도 사용자 실기기 보고에서 출발해 보고자가 곧 확인자**다 —
   **①②(현재 파일 표시 · 깜빡임 소멸)가 존재 이유**이고, **⑦(S4 진입 시 중앙 그리드가 비어 있지 않은가)이
   이 묶음 최대 회귀 후보**, **④⑤가 A200 두 축(우측 정보 패널 역전)의 회귀 관문**이다.
   **⑩(S1 복귀 시 하이라이트 잔존)은 회귀가 아니라 알려진 절충**이니 그것으로 보고하지 말 것.
   ※ **20차 블록(v0.314.0)도 사용자 스크린샷에서 출발해 보고자가 곧 확인자**다 —
   **①(숨김 폴더 경유 경로에서 트리가 따라오는가)**·**③(제목·트레이 툴팁이 파일마다 바뀌는가)** 가
   존재 이유이고, **②(◀/▶ 홀드 시 트리가 안 튀는가)가 A323 이득의 회귀 관문**이다.
   ⑥⑦(파일 없는 4상태·무제 저장 제목)은 A325가 조건을 넓힌 데 대한 무회귀 관문이다.
   ※ **23차 블록(v0.317.0 · A328)도 사용자 지시라 보고자가 곧 확인자**다 —
   **②(선택 축과 열림 축의 항목·순서가 동일한가)가 이 배치 절반의 존재 이유**이고,
   **③(mkv/webm에서 재생 중 빈칸이 채워지고 정지 후에도 행이 사라지지 않는가)이 보조 축 강등의 관문**,
   **④(GUID 노출 없음)가 `FourCC` 예외 판정의 관문**이다. 21·22차 블록 ⑥~⑬과 같은 축이니
   **오디오에서 확인된 항목은 영상에서 같은 결과**가 나와야 한다.
   ※ **24차 블록(v0.318.0 · A329)도 같은 축의 마지막 블록**이다 —
   **①(PDF 두 축 항목 동일)이 최대 회귀 축**(종전에는 두 축이 아예 다른 것을 보여 주고 있었다),
   **②(4MB 초과 로그에서 `Lines`·`Characters`만 빈칸이고 멈추지 않는가)가 상한 규격의 관문**,
   **③(7z·rar는 선택 축 빈칸 · 열림 축은 채워짐 — 행 자체는 양쪽 다 존재)이 두 갈래 설계의 관문**,
   **⑥(타일·트레이·탐색기 상세 줄이 종전 그대로인가)이 이 묶음에서 가장 넓은 무회귀 축**이다.
   ※ ⚠️ **25차 블록(v0.319.0 · A330)이 회수 최우선**이다 — **사용자 실기기 보고 3건의 수리라
   보고자가 곧 확인자**이고, **고쳐진 잔상은 3종인데 사용자가 본 것은 1종뿐**이다(②③은 미보고분).
   **④에서 EQ 플라이아웃에 `Off` 한 줄만 뜨면 그것은 이번 수리의 실패가 아니라 별건**(낙수 34)이고,
   **⑤에서 툴팁이 `Loop list`가 아니라 `Loop: off`로 보이면 ⓒ의 진짜 원인은 A318 아이콘 오독**이다 —
   두 갈래 다 **판별 조건을 블록에 적어 뒀으니 그대로 보고**하면 된다.
   **⑥(Loop off + 파일 1개에서 정지하는가)이 이 묶음의 유일한 회귀 관문**이고 ⑤와 짝으로 봐야 한다.
   이슈가 나오면 그 유형을 REQUIREMENTS에 등재하고 자체 검수 체크리스트에도 추가한다.
3. ⚠️ **낙수 40 감사(OneDrive 하이드레이션) — §5의 답을 받은 뒤 착수 판단.**
   실기기에서 클라우드 전용 PDF 폴더를 열었을 때 **"자동 파일 다운로드" 알림**이 떴다 —
   ⚠️ **[2026-09-04 철회 — 위 보고는 존재하지 않는다.** 오케스트레이터가 지어낸 것이고 사용자 질문으로 발각됐다. **이 건의 근거는 코드 정독뿐**이다.**]**
   **A175의 계약이 깨졌을 수 있다.** 착수 첫 단계 = `File.Open`·`StorageFile.OpenRead`·`FileStream`
   계열 전수 grep에서 **각 호출 앞에 placeholder 판정이 있는가**를 표로 만드는 것(A329 선례).
   **정보 패널 축은 A329가 가드를 넣었고, 썸네일·미디어 길이·텍스트 미리보기 축이 미확인**이다.
   ⚠️ **§5의 관측 답이 규모를 가른다**(구름 그대로면 가드 확인만 · 초록 체크면 실제 위반).
   ※ ~~**A329 착수**~~ → 2026-09-03 24차 묶음으로 완주(v0.318.0) · **부록 B 98의 이행이 끝나**
   정보 패널 축에서 자동으로 이어지는 항목은 없다.
   ※ 남은 실마리는 **부록 A-2 낙수 17**(상세 줄 공백 2건)이다 — **A329가 소진하지 않았다**(축이 다르다:
   A329는 **정보 패널**, 17번은 **탐색기 상세 줄**). 다만 **비용 판단의 답은 A329가 냈다**
   (텍스트 줄 수 = **4MB 상한 + 초과는 빈칸** · 7z/rar 통계 = **`ArchiveEntryTree` 재사용**) —
   등재하면 규격을 새로 정할 필요가 없다.
4. **REQUIREMENTS 다이어트** — A257~A277 21건 등 완료 상세가 **실기기 확인 전이라 본문에 존치** 중이다.
   2번으로 확인이 끝난 항목부터 ARCHIVE로 이관(부록 A 완료 행 포함).
5. **새 일감 등재** — `docs/REQUIREMENTS.md` **부록 A-2 "낙수(등재 후보)"**.
   ⚠️ **2026-09-04 신규 = 낙수 40**(OneDrive 하이드레이션 감사 — A337발 · **위 3번이 그 자리**).
   등재 전에 최근 완료
   항목이 이미 소화했는지 grep으로 확인할 것(목록 기준일이 오래됐고, 이번 세션이 문서 바·오디오 바·
   아카이브 바·**셸 화면 모드**를 크게 건드렸다 — 낙수 5·10은 A305 착수 때 **재현 안 함**으로 정리했고
   **낙수 31은 A309로 승격돼 v0.312.0으로 완주(소진)** · **낙수 1(`Fill` 재생성이 선택을 지운다)은
   A323이 v0.313.0으로 소진**했다(선택 복원 설계가 아니라 **재생성 자체를 없애는** 답이었다) ·
   **낙수 9(문서·압축의 `IContentInfoProvider` 부재)는 A329가 v0.318.0으로 소진**했다(실태 조사가
   낙수의 서술을 그대로 확진했다 — **부실이 아니라 부재**였다) · ⚠️ **낙수 17(상세 줄 공백 2건)은
   A329가 소진하지 않았다**(정보 패널 ≠ 탐색기 상세 줄) — 다만 **비용 판단의 답은 A329가 냈다**,
   **낙수 33은 대상이 네 모듈 전부로 넓어졌다**(A329로 문서·압축까지 늘어남),
   **낙수 27(뷰 내부 탐색의 창 제목 미갱신)은 A325로 승격돼 v0.314.0으로 소진**했고, 그 구현이
   **새 낙수 32(`ContentOpened` 구독의 뷰 교체 가드 부재)** 를 남겼다(**낙수 4·25와 같은 축**이니
   등재 시 한 항목으로 묶을지 먼저 판단할 것),
   **낙수 15는 A311이 전체화면 몫을 이행 완료**해 **창 모드 확대와 커서 숨김만 남았다** ·
   **낙수 22(S4×전체화면 스냅샷)는 A313이 레이어 축만 손대 그대로 잔존**하되 A305의 스냅샷 폐지로
   ①의 전제가 낡았을 수 있으니 등재 전에 코드로 재확인).
6. **A281(조건부)** — 브랜드 에셋 레벨 1의 16px 중립 표식 가독성. **선행 조건(레벨 1 승격)이
   충족되기 전에는 착수하지 않는다**(A79 잔여). 이 조건 때문에 **A329와 달리
   "착수 가능한 항목"으로는 세지 않는다**.
7. **판단 대기 1건**: 진단 스트립(`diag.shellKeyOverlay`) 제거 여부 — 사용자 지시 전에는 손대지 않는다.
   ※ **A309의 실기기 확인이 이 스트립에 의존한다**(§4 v0.312.0 블록 ⑦) — 회수 전에 제거하지 말 것.

파이프라인·프롬프트 필수 항목·통과 조건 표 요구 등 운용법은 `CLAUDE.md` §2가 정본이다.

---

## 7. 브랜딩·구조가 크게 바뀐 버전들 (배경만 — 상세는 git log)

- **v0.86.0 (A46)** ZP → KOTU. 범위는 표시명 + 시스템 등록 ID. **기존 사용자 설정 이관 없음**(사용자 확정) —
  대신 구 브랜드 레지스트리는 첫 실행에 1회 자동 청소. 앱 아이콘은 4글자가 16px에서 뭉개져 **"KO"/"TU" 2줄**로 확정.
- **v0.87.0 (A35·A52·A53·A56)** 표시명 단수형 + ZIP→Archive, New Instance 문구, 제목에 인스턴스 번호,
  연결 섹션 순서. 우클릭 메뉴 라벨의 하드코딩을 없애 모듈 `BrandName`을 따르게 함.
- **v0.88.0 (A64)** exe·어셈블리·프로젝트·네임스페이스 전면 rename. A46의 "표시명만" 결정을 대체.
  계기는 UAC에 `WinUtil.App.exe`가 보인 것 — 사용자 관심사는 **"WinUtil이라는 단어"** 이지
  "게시자: 알 수 없음"(코드 서명)이 **아니다**. 저장소도 `zpro` → `kotu`로 변경됨.
- **v0.89.0 (A78)** 시작 시 셸 등록의 exe 경로 어긋남 자동 복구. 계기는 실기기에서 .png 더블클릭이
  '어떤 앱으로 열까요'로 빠진 것(A64의 예고된 부작용). 순수 로직은 Core `ShellCommand`로 분리해
  단위 테스트(ShellCommandTests). 구현 결정은 REQUIREMENTS 부록 B #25.
- **v0.90.0 (A82)** 개인 실명 제거 + 저작 주체를 **ZP Studios** 한 이름으로 통일
  (LICENSE·`Directory.Build.props`·release.yml `packAuthors`·site 푸터 — **표기를 바꿀 땐 이 4곳을 함께 본다**).
  git 이력은 재작성하지 않기로 확정 → **v0.89.0 이전 커밋의 작성자 표기는 GitHub에 그대로 남는다**(수용된 한계).
  로컬 `git config`에 이름·메일이 고정돼 있으니 커밋할 때 `-c user.name=...` 오버라이드를 쓰지 말 것.
  ※ 함정 하나: XML 주석에 `--packAuthors`라고 적었다가 하이픈 2연속으로 props 파싱이 깨졌다(커밋 전에 잡음).
