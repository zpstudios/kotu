# KOTU 인수인계 문서

> 세션이 바뀌어도 이어서 일할 수 있게 하는 문서. **이 문서 + `docs/REQUIREMENTS.md` 두 개면 충분**해야 한다.
> 저장소가 이미 기록하는 것(코드 구조는 `ARCHITECTURE.md`, 빌드는 `docs/BUILD.md`, 변경 이력은 git log)은
> 여기에 중복해 적지 않는다. 여기 적는 건 **코드를 읽어도 알 수 없는 것** — 결정의 배경, 함정, 대기 중인 확인.
>
> 최종 갱신: 2026-08-25 (v0.238.0 시점 — 미반영 코드 항목 0. §1 "2026-08-25" 블록 필독)

---

## 1. 지금 상태 한 줄

앱 이름 **KOTU (King Of The Util)**, 저장소 **zpstudios/kotu**, 실행 파일 **KOTU.exe**, 최신 **v0.238.0**
(push 완료. 태그 확인 현황은 §1 2026-08-25 블록 말미).

**2026-08-25 세션 — 실기기 피드백 2차 8건(A226~A233) 등재 + 전량 완주: v0.231.0~v0.238.0 (8릴리스)**
전부 서브에이전트 위임 정상(권장 모델 지정 — Opus 5·Fable 3). 사용자 질문("편집기 성능 최근에
고친 게 있나") = A177(v0.189.0)·A193(v0.207.0)이라 답변.
- **v0.231.0 [A229]**(Opus) 문서 줌 범위 20~500%(구 50~300) — 서술 전수 동기·무영향 증빙 4건.
- **v0.232.0 [A232]**(Opus) 시작 메뉴 "Min to tray" + 글리프 E896(구 E921 원시 문자 제거) —
  트레이 우클릭 메뉴는 전체 표기 유지(의도된 비대칭).
- **v0.233.0 [A231]**(Opus) Fit 100% 아이콘 3차 = 텍스트 "100%"(플라이아웃 항목 아이콘 제거·
  본체 TextBlock 9px — PathIcon·BuildActualSizeIconGeometry 전량 제거·v0.174.1 함정 축 소멸).
- **v0.234.0 [A230]**(Opus) 빈 상태(썸네일 탐색기) = Fit 조절기 숨김(3모듈·버튼 개별 Collapse —
  HotkeySupport 게이트가 버튼 Visibility를 봐서 A·F 키 동반 차단).
- **v0.235.0 [A227]**(Opus) 파일 연결 마스터 스위치 — 각 토글 IsOn 세팅으로 기존 A77 흐름 재사용·
  전용 `_suppressMasterToggle` 축(기존 suppress와 겹치면 등록이 죽는다).
- **v0.236.0 [A226]**(Fable) **F11/F12 3차 근본 수리** — A212의 대증(썸네일 정착) 불충분 판명 후
  **RootLayout.PreviewKeyDown 터널링 승격**(WinUI 내장 Handled 선점 원천 봉쇄·버블 분기 제거·
  게이트 ① 폐기) + 클릭 시점 포커스 고아 방어(PointerPressed 관찰 → RecoverChromeFocusOrphan).
  한계 명기: 팝업 트리(플라이아웃 열림) 포커스는 종전처럼 못 받는다.
- **v0.237.0 [A228]**(Fable) 새 인스턴스 포그라운드 — 리다이렉터 redirect 직전
  `AllowSetForegroundWindow(AppInstance.ProcessId)` + BringToFront 말미 SFW·실패 시만 FlashWindowEx.
  신규 P/Invoke = `Integration/ForegroundActivation.cs`(셸 격리·전부 try/catch).
- **v0.238.0 [A233]**(Fable) 썸네일 텍스트 파일 내용 프리뷰 — 확장자 타일 즉시 + 워커 지연
  교체(A175 구조)·ModuleWorkerPool 3+세마포어(A194)·seq 이중 대조·4KB/12줄·BOM→UTF-8→CP949.
  KOTU.App에 System.Text.Encoding.CodePages 직접 참조 추가(모듈 3곳과 같은 8.0.0).
- 가이드 2종 배지 "As of v0.238.0" — A227~A233 사용자 가시 변화 반영(마스터 스위치·Min to tray·
  100% 표기·빈 상태 Fit 부재·줌 범위·텍스트 썸네일).
- **태그 확인 현황**: **v0.238.0까지 전부 발행 확인 완료(세션 내 검증)** — CI 1순위 후보였던
  A228 신규 P/Invoke·AppInstance.ProcessId, A233 CodePages 참조 모두 컴파일 통과 실증.
  다음 세션의 태그 확인 부채 없음.

**2026-08-24 세션 — 사용자 피드백 14건(A212~A225) 등재 + 전량 완주: v0.224.0~v0.230.0 (7릴리스)**
- **선행 소화**: 전 세션 셸 2차 장애로 커밋 대기였던 v0.223.0(A211 4+5/5)을 §1(4) 절차대로
  커밋·push. v0.215.0~v0.223.0 태그 발행 일괄 확인(전부 CI 초록 — A211 5배치·A210 포함).
- **등재**: A212~A225 14건(권장 모델 병기·코드 사실 앵커·확인 필요 3건). ⚠️ A224 등재문의
  ".html 문서 확장자 실존"은 오인이었다(UCPD 목록 참조 착오) — 구현에서 신규 추가로 정정.
- **⚠️ 운용 장애 2종 동시 발생(세션 전반부)**: ① 서브에이전트 API 529 연속(4회+ — 재시도 무효)
  ② 샌드박스 셸 재무응답(echo 불통). 대응 = 오케스트레이터(Fable)가 파일 도구만으로 직접 구현
  (Opus 몫 8건 — 상향이라 무리스크)·복구 후 **8건 일괄 커밋 v0.224.0**(파일 겹침 — v0.207.0 전례.
  버전별 태그는 224부터 재개). 후반부에 둘 다 복구 → Fable 몫 5건 + 가이드 동기화는 서브에이전트
  위임 정상 수행(권장 모델 지정).
- **v0.224.0 [8건 일괄]**: A213 썸네일 4/6/8(`8−2×도크` — A168 동형) · A215 편집 표시 토글 2종
  (가이드·¶ — 바 중앙 star 2개 구조·document.* 키 2개·EditorDecor 2축) · A216 영상 루프 버튼
  c0 · A218 최소화 트레이 철회 + 명시 "Minimize to tray" 2곳(시작 메뉴 최하단 + 트레이 우클릭 —
  A69/A185 반전·WindowMinSize SC_MINIMIZE 기계 제거) · A220 인티그레이션 카드 해체(여백 구분) ·
  A221 Windows display scale 전면 제거(A48 반전·DisplayScale.cs 삭제) · A222 always-new-instance
  폐지(A24 반전 — WindowManager 설정 주입도 제거) · A223 문서 바 좌측 순서(Open 신설 —
  **IOpenFileRequestSource 계약 신설**(셸 위임 = 미저장 가드 경유)·All Readable 중계·Print 이동).
- **v0.225.0 [A212]** F11/F12 불통 — 감사(선소비 0 증빙) → 원인 = 썸네일 빈 공간 클릭 포커스
  고아(A209의 클릭 시점 무방비) → LayoutRoot 구독 승격 + FocusGrid 정착(S1·S4).
- **v0.226.0 [A224]** 편집/뷰 전 형식(_viewMode 축·잠금 뷰·.html/.htm 확장자 신규·md 렌더 종속·
  인쇄 갈래 무손상). **v0.227.0 [A225]** 렌더 뷰 Ctrl+휠 줌(ZoomFactor+역수 랩 폭 — PdfPane A188
  관용구·줌 축 공유). **v0.228.0 [A214]** 문서 Fit 텍스트 활성(Fit height만 비활성 —
  MenuFlyoutItem x:Name 저장소 최초·CI 통과 실증). **v0.229.0 [A217]** AV 바 공통 클러스터 정렬
  (오디오 루프 c0 + 스페이서 26 — 폭 등식 426·오디오 축약 임계 640 신설). **v0.230.0 [A219]**
  문서 외부 열기 특칙(트레이 숨김 창 불가침 = 전 모듈·문서 콘텐츠 열림 = 새 인스턴스).
- **가이드 2종 v0.230.0 동기 완료**(A213~A225 사용자 가시 변화 전 반영·전 서술 코드 앵커 검증·
  배지 "As of v0.230.0").
- **운용 메모(도구 함정 — 서브에이전트 지시에 반영할 것)**: 파일 Edit 도구가 한글 주석을
  `\uXXXX` 이스케이프로 저장하는 사고 1건 발생(v0.224.0에 유입 → v0.226.0에서 복원). 이후 배치
  프롬프트에 "주석은 실제 한글 문자로(이스케이프 저장 금지)" 명시 + 커밋 전
  `grep -E '\\u(A[C-F]|[BCD][0-9A-F])[0-9A-F]{2}'` 검사를 상비 절차로.
- **낙수(등재 후보) 추가**: ㉮ 대용량 문서(A177 장식 오프)에서는 A215 토글이 보여도 시각 무동작
  (SetDecorVisibility가 _disabled 무시 — 비활성화가 친절) ㉯ 렌더 뷰 줌 시 판 패딩(24/16/68)이
  배율 비례로 커지는 체감 ㉰ USER-GUIDE §2.9의 "100% = 1:1 아이콘" 서술이 A184 이전 것(가이드
  기존 부채 — 도형 서술로 갱신 후보) ㉱ HTML 진짜 렌더(WebView2 vs 자체 — 확인 필요 절 답 대기).

**2026-08-21 세션 — 착수 대기열 11건 완주(A196·A201·A202는 전 세션 v0.204.0 완료 확인 → 잔여 8건
7배치) + A205 등재 + 운용 규칙 개정. 오케스트레이터(Fable) + Fable 서브에이전트 직렬.**
- **규칙 1.3 개정(사용자)**: push는 커밋마다 즉시(버전별 태그·릴리스 — 종전 "묶음 끝 1회" 폐지).
  CLAUDE.md 반영·커밋 완료.
- **v0.205.0 [A203]** 전체화면 진입 시 패널 닫기(스냅샷은 닫기 전) — **커밋·push·릴리스 완료**.
- **v0.206.0 [A198+A199]** 좌 리스트 행 압축(숨은 하한 3종 명시 0) + 상세 줄 재정리(속성 1조각·
  절대 날짜·`DocumentQuickInfo` 신설)·Info 헤더 제거 — **커밋·push·릴리스 완료**.
- **⚠️ 샌드박스 셸이 세션 중반 약 1시간 완전 무응답**(bash 10회 연속 타임아웃 — echo도 불통.
  세션 말미에 자연 복구). 그동안 배치 3~7은 파일 도구만으로 구현·검수를 계속했고, 복구 직후
  **v0.207.0 [A204+A200+A193+A194+A192] 5건 일괄 커밋·push 완료**(파일이 배치 간 겹쳐
  (ExplorerPane.xaml.cs = 3배치 공동 수정) 배치별 분리 커밋이 불가했다 — 규칙 1.3의 "커밋마다
  push"는 이 장애 구간에 한해 예외). 내역: A204 정렬 안정성(stable sort·입력 순서 = 2차 키) /
  A200 선택 기반 우측 정보 + EXIF 16행(`ImageQuickInfo`·`SelectionQuickInfo` 신설) / A193
  마크다운 분할 조립(AppendRange + 틱 루프) / A194 병렬화(`ModuleWorkerPool` 신설·게이트 3) +
  이웃 선읽기(Task째 캐시) / A192 증분 생성 + 상한 2000(가상화 재작성 기각 — 계약 6종 무손상).
  상세는 REQUIREMENTS 각 ※ 항목.
- **운용 메모**: push는 `zp` 원격(HTTPS PAT — SSH origin은 샌드박스 차단), push 후
  `.git/HEAD.lock`·`maintenance.lock` 잔존 시 rm(파일 삭제 권한 도구 선행). **CI 확인 미수행** —
  v0.205.0·v0.206.0·v0.207.0 세 릴리스의 build·release 결과 확인이 다음 세션 몫(v0.207.0은
  5배치 몫이라 실패 시 원인 분리에 REQUIREMENTS 각 항목의 "CI 위험 후보" 서술 참조).
- **A205 등재**(사용자 지시 "설정 모듈은 좌우 사이드바 안 떠야 해" — A196 부분 반전·Opus·확인 1건
  부록 B 상단). ~~착수 대기열은 이제 A205(확인 대기) 하나다~~ → 아래 2차분에서 소화.

**2026-08-21 (2) — "나머지 작업들도 진행" 지시로 2차 완주: v0.208.0~v0.214.0 (7배치 + 조사·설계 4건)**
사용자 재확인: "오케스트레이션만 담당" — 구현 전부 서브에이전트 위임(권장 모델 지정). 셸은 복구됨.
- **CI 확인**: `git ls-remote --tags`로 v0.205.0~v0.207.0 태그 발행 확인 = 세 릴리스 CI 초록
  (5건 일괄 v0.207.0 포함 전부 컴파일 통과). 이후 v0.208.0~v0.214.0도 버전별 커밋·push 완료
  (규칙 1.3 개정대로 — 각각 태그·릴리스 자동. **결과 미확인은 이 7개** — 다음 세션이 태그 확인).
- **확인 소진**: A205 범위 = 전면 배제 / 감사 낙수 ①②③ = 전부 현행 유지(부록 B 반영) /
  선행군 택일 = 부록 B 76(A11 착수·A47(b)·A48 ①+③·A43 보류).
- **v0.208.0 [A205]**(Opus) 설정 사이드바 전면 배제 — IsSettingsView 게이트 한 곳(산식 3+1곳 경유
  정합)·미지원 안내/무제 편입분 유지.
- **v0.209.0 [A41]**(Fable) UI 배율 라이브 조절 — Ctrl+±·Ctrl+넘패드* 리셋·Ctrl+휠(하단 바·빈
  셸 한정). 설정 콤보 Changed 역구독 동기.
- **조사·설계 4건(코드 0)**: `docs/A11-playlist-design.md` · `A47-sensor-access-research.md`
  (**WinRing0 전제 소멸 — LHM 0.9.6 = PawnIO** 중대 발견) · `A48-display-scale-research.md` ·
  `A43-portable-research.md`(보류 — 재개 시 단일 exe vs 무Velopack zip 택일부터).
- **v0.210.0~v0.212.0 [A11]** 재생 목록 루프 3배치(Core FolderPlaylist(Opus) → 영상(Fable) →
  오디오 동형(Opus)) — 사양은 부록 B 76, 상세는 REQUIREMENTS ※ A11.
- **v0.213.0 [A47]**(Fable) PawnIO 침묵 실패 수리 + WMI 클럭 근사. NVMe 온도 = 등재 후보로 보류.
- **v0.214.0 [A48]**(Fable) 비공식 DisplayConfigSetDeviceInfo(type -3/-4) + 딥링크 폴백 —
  **컴파일 통과 ≠ 동작인 부류라 실기기 확인이 본질**(§4).
- 이로써 **미반영 코드 항목 = 0**. 잔여 = 사용자 입력 대기 3건(A142 ④·A159·A170) + 보류 2건
  (A4·A43) + 후순위군(A14·A15·A19·A45) + 낙수(등재 후보) 목록.

**2026-08-21 (3) — 실기기 보고·재지시 3차분: v0.215.0~v0.217.0 (3배치) + 재확인 1건**
- **v0.215.0 [A206]**(Opus) 업데이트 자동 확인 = 설정 화면 체류 중에만 — 업데이트 체크 주제
  5차 정책 반전(A114 상시 2분 폐지). 참조 카운트 결박·진입 1회 체크 Loaded 이동·창 닫기 누수 보정.
- **부록 B 77 [A189 재확인]**: "문서 모듈 빈 파일 만들기" 재요청 → **v0.199.0에 이미 구현**
  (하단 바 New text file — 콘텐츠 열려 있으면 비활성) 확인·기록. 신규 작업 0.
- **v0.216.0 [A207]**(Fable) 빈 상태 ▶ = 테스트 클립/샘플 곡 회귀 수리 — 두 기능·자산 실존,
  원인 = `_player` null 가드가 분기보다 앞(초기화 미완 구간 침묵). 가드 순서 이동(신규 0줄).
  **오디오 샘플도 원래 있었음**(sample.mp3 — 사용자 요청분 충족).
- **v0.217.0 [A208]**(Fable) 문서 편집기 줄번호 미표시 — 2단(감사 → 사용자 판별 답변("소형
  .txt·줄번호만 없음·가이드·¶ 정상") → 정밀 추적): 원인 = 패딩 예약이 런타임에 안 실린 상태에서
  10줄+ 파일이면 거터만 침묵 숨김(수치 정합 — 실기기에선 처음부터 안 보였을 개연성). 수리 =
  클립 좌변에 붙여서라도 그리기(본문 침범 직전만 숨김 — 폴백 구조 보존).
- 태그: v0.208.0~v0.214.0 발행 확인 완료(전부 CI 초록). v0.215.0~v0.218.0은 다음 확인 몫.
- **v0.218.0 [A209]**(Fable) 전체화면 키 전멸(사용자 "심각" 보고 — 이미지 더블클릭 진입 후
  Esc·Enter·Alt+Enter 무반응·더블클릭만 생존) — **A135 한계 갈래 실증·해소**: 바 Collapse 시
  포커스 고아 → KeyDown 미발화가 원인. `RecoverChromeFocusOrphan`(UpdateShellChrome 말미 —
  셸·외부·A186 자동 숨김 공용 지점, 고아일 때만 모듈 뷰 재포커스). 낙수 30·31 동반.

**2026-08-21 (4) — A210 릴리스 경합 + A211 인쇄 기능 전체: v0.219.0~v0.223.0 (6배치 + 조사 1)**
- **v0.219.0 [A210]**(Opus) release.yml 멱등 경합 수리(concurrency 직렬화) — 사용자 보고("빌드
  성공·릴리스 실패")로 발견. **push에 토큰 workflow 스코프 필요**(사용자가 권한 추가 — 운용 메모).
  태그 발행 실검증 완료.
- **A211 인쇄**(사용자 지시 "필요한 모듈 모두" → 범위 확인: 이미지·문서·PDF 우선): 조사
  (`docs/A211-print-research.md` — A축 WinRT 확정) → 설계 확정 4건(부록 B 78) → 배치 5개 직렬:
  v0.220.0 공통 PrintHost+계약+Ctrl+P / v0.221.0 이미지(All Readable 중계 포함) / v0.222.0 PDF
  (지연 렌더·페이지 범위) / **v0.223.0 텍스트+마크다운 통합(4+5/5 — 세션 후반 셸 2차 장애(소켓
  EACCES)로 분리 커밋 불가·같은 파일 공동 수정)**. 상세 = REQUIREMENTS ※ A211.
- **⚠️ 셸 2차 장애**: v0.223.0 배치 완료 시점부터 bash 불통(srt-mux 소켓 EACCES — 1차 장애와
  다른 유형). 복구 시 커밋 순서: ① 코드+가이드 = `v0.223.0 [A211 4+5/5] 문서 텍스트+마크다운
  인쇄 …` ② docs(REQUIREMENTS·HANDOVER — 이 갱신) 별도 커밋 ③ push 각각(A210 덕에 같은 버전
  후속 push는 skip 초록). **props는 이미 0.223.0**(배치 4가 올림 — 배치 5는 무변경 지시 준수).
- 태그 확인 몫: v0.215.0~v0.223.0(이 중 219까지는 발행 확인 완료 — 220~223이 다음 확인).
**2026-08-19 밤~08-20: 착수 대기열 18건 + 세션 중 추가 1건(A191) = A173~A191 전부 완주**
(v0.185.0~v0.201.0 17배치 + A178 감사 1건). 앞선 2026-08-19 3개 세션(A136~A172 등재 →
Opus 몫 20건 v0.162.0~v0.174.0 + 핫픽스 v0.174.1 + Fable 확정 사양군 11배치 v0.175.0~v0.184.0)에
이은 소진이다. **이로써 사양이 확정돼 바로 착수할 수 있는 코드 항목은 0건이다.**
~~push 대기 8커밋~~ → **push 완료·v0.201.0 태그·릴리스 발행 확인(2026-08-20). A173 델타 1회차 검증
성공 — v0.201.0 자산에 `KOTU-0.201.0-delta.nupkg` 560KB(풀 152MB 대비). 2회차(소용량 다운로드 체감)는
사용자 실기기 몫.**
**2026-08-20 (2): 실기기 피드백 2차 A197~A204 등재(반전 3건 — 부록 B 75) 후 Opus 2건만 직렬 완주:
v0.202.0 [A195] 설정 레지스트리 워커 이관(A178 ④ — A38 경로와 같은 워커 큐 직렬화·초기값도 이관,
근거 = 같은 카드의 RefreshDefaultsAsync 선례와의 비대칭 해소) · v0.203.0 [A197] 토글 제목 왼쪽
(On/Off 라벨 제거·스피너 우측 끝 — Toggled·워커 배선 diff 0). 오케스트레이션 세션 = Fable 확정
(CLAUDE.md 규칙 1.4). ~~남은 착수 대기열 = 전부 Fable: A192·A193·A194·A196·A198~A204(11건)~~ →
**2026-08-21 세션에서 전량 완주**(권장 순서 그대로 — 위 2026-08-21 블록).**
저작 주체는 **ZP Studios**(A82/v0.90.0) — 개인 실명·개인 메일은 어디에도 쓰지 않는다. 커밋도 이 이름으로 남는다.
**실기기 확인이 v0.85.0부터 v0.201.0까지 밀려 있다**(§4 — 최우선은 종전대로 v0.161.0(A135) 포커스 고아
가설 검증. 기각되면 재조사 새 항목 등재).
**미반영 잔여 = ① A142 ④(¶/EOF 위치 — 사용자 스크린샷 대기) ② A159(압축 뒤로가기 — 증상 기억 안 남,
보류 유지·재현 시 루트/폴더 구분 보고, 부록 B 74) ③ A170(종료 시 밝기 — 감사·판정 완료, 실기기 판별
5종 관찰 대기) + 조사·설계 선행군(A41·A47·A48·A43·A11) + 후순위군(A4·A14·A15·A19·A45).
A115 ④는 부록 B 74로 보류 종결 → A115 완료·결번. Win+M = 트레이 수용(부록 B 74 — 추가 작업 없음).**
즉 **셋 다 사용자 입력이 다음 단계**이고, 나머지는 조사·설계 선행군뿐이다. 새로 일감을 만들려면
**낙수(등재 후보) 목록**(아래 · A178 감사분 4건 포함)에서 골라 등재하는 것이 다음 수순이다.
**2026-08-14 세션: 운용 규칙 2건 추가(권장 모델 병기 = 규칙 5 · 다이어트 일괄 적용 22건 이관) +
사용자 실기기 보고 2건 등재(A99 열기 아이콘 중복 — 방향 확정 대기 / A100 트레이 미표시) +
A100 1차(v0.126.0)·A85(v0.127.0) 완료 — 상세는 아래 세션 절.**
**2026-08-13 7회전(Fable ②)으로 A98 완료 + A94 1·2차(v0.123.0~v0.125.0) — 휠 줌 재정의와 자체 탐색기
안전 조작(다중선택·이동/복사·클립보드·이름변경·새 폴더·휴지통)까지. A94는 후속(영구 삭제·권한 등)이
사용자 결정 대기로 남아 미완료 유지 — 진행 단일 원본 = `docs/A94-matrix.md`.**
**A83은 2026-08-14 사양 확정으로 보류 해제** — Fit 4옵션(100%·Contain·Fit width·Fit height)
이미지·PDF·영상 공통(조건 적응형·초안 표 프로세스 폐기, `docs/A83-autofit-draft.md` 폐기됨).
**2026-08-13 사용자 일괄 답변(부록 B 41~53)으로 사용자 답 대기 항목 소진** — 신규 A88~A98 등재,
A86 키 동작 표까지 합의 완료(`docs/A86-keymap.md`).
같은 날 REQUIREMENTS **다이어트**: 참조 끊긴 완료 상세 20블록을 `docs/REQUIREMENTS-ARCHIVE.md`로
분리(본문 헤더에 다이어트 규칙 명문화 — 앞으로 완료 처리 때도 같은 기준). **2차 다이어트(완료 상세
전부 이관)는 사용자 보류 중.**

**2026-08-16 세션 — v0.145.0 실기기 보고 6건 + 과거 프롬프트 재대조: A120~A126 등재·A123 즉시 결번**
보고 처리(문답 = 부록 B 61): A120 문서 본문 중앙 컬럼(900+Center — A115 장식 레이어 좌표계 함정
병기) · A121 PDF 키보드 스크롤(↑↓/PgUp/Dn/Home/End — 에디터 불간섭 함정 병기) · A122 H/W 그래프
채널명·y축 라벨 겹침(A119로 타일이 넓어져 축 라벨 표시 조건 충족 → 드러남) · A123 작업표시줄
클릭→창·아이콘 소실 = **A69 사양 재확인 종결**(코드 0·결번 — 사용자 "판단 보류·현상 유지") ·
A124 Restart as admin 후 창 세트 복원(Fable — 전 창 종료는 프로세스 단위라 불가피, 복원은 설계
가능) · 탐색기 더블클릭 새 창 = 설정 "Always open files in a new instance" ON 확인으로 종결(등재
불요). **사용자 제공 2026-08-14 프롬프트 전문 재대조(부록 B 62): 37개 서술 중 누락 2건 발견** —
A125 Check now 버튼 제거(이 주제 다섯 번째 변경 — 자동 확인 ①②는 유지) · A126 창 아이콘
글리프-링 겹침(inset 합성). GitHub 커미터 표기 질문 답변: Claude 명의 5개(v0.80.0~v0.84.0,
2026-08-09)는 이력 재작성 없이 변경 불가 — A82 "재작성 안 함" 결정 준용, 현 로컬 config로 증가
없음. **착수 가능 부활: Opus 소형 5건(A120·A121·A122·A125·A126) + Fable 1건(A124).**

**2026-08-16 세션 (12회전 계속) — 위 등재 6건을 같은 날 4배치로 완주: v0.146.0~v0.149.0 (opus 3·fable 1)**
- v0.146.0 **A120+A121** 문서 중앙 컬럼 = **MaxWidth 900만 추가**(Center 명시 금지 — TextBox가
  콘텐츠 폭으로 접혀 컬럼이 무너진다. Stretch의 constrained fallback이 중앙 배치를 만든다),
  DecorLayer도 같은 제약(A115 좌표계 일치 — 오프셋 보정 금지, 두 요소 주석에 상호 의존 명기).
  PDF 키 스크롤 = DocumentView **PreviewKeyDown 터널링**(저장소 최초 API — 실패 시 KeyDown 1줄
  교체가 복구법) + 4중 게이트(PDF 모드/OriginalSource 텍스트/ShouldPassThrough/수정키) +
  **오토리피트 허용**(연속 이동 목적 — A86 3종 세트와 반대, 주석 명기). 상수 2개 = PdfPane 상단.
- v0.147.0 **A122** 비-바 표면 한정 header.SizeChanged 실측으로 y축 라벨을 헤더 아래로(같은 값
  0.5px 가드 조기 반환 — A119 정사각형 재계산과 되먹임 없음 정독 증빙). ※ 등재문의
  "CreateSensorGraph"는 실존하지 않는 이름 — 실제는 `MakeGraph`(코드 따름 보고).
- v0.148.0 **A125+A126** Check now 버튼 제거(빈 buttonRow까지 걷어 헛간격 정리 — "You are on the
  latest version."은 두 자동 경로가 공용이라 존치), 본체 inset 합성 = `Graphics.TranslateTransform`
  (저장소 최초 — 복구는 두 줄 삭제) + 축소 크기 반올림(.ico 실프레임 선택 유리). **트레이 링
  경로(TrayIcon.SetIcon)도 같은 Compose라 함께 inset** — 창·트레이 한 모습(의도로 수용).
- v0.149.0 **A124** Core `RestartSession` 정적 훅(Writer/Discarder — 모듈은 Core만 의존) + 셸
  `RestartSessionFile`(%AppData% JSON·UTC 2분 유효·**읽고 유효성 판정 전 즉시 삭제**). 복원 =
  기존 Create→OpenFile/OpenModuleById 경로 재사용(슬롯·AUMID·트레이 자동 배정), 루프는 단일 UI
  스레드 동기 완결(빈 창 ConfirmDiscardAsync = 완료 태스크). **runas 경로는 현행대로 미저장
  가드를 타지 않는다**(묻지 않고 종료 — 유지). UAC 취소 = 세션 파일 되지움. 사라진 파일 경로는
  그 모듈 빈 컨텍스트로 후퇴(모듈 ID까지 무효면 건너뜀).

**2026-08-16 세션 (14회전) — 오후 실기기 보고 등재분 6배치 완주: v0.154.0~v0.159.0 (opus 3·fable 3)**
같은 날 오후 보고 A127~A133 등재(부록 B 63·64 문답) 후 "진행하자"로 전부 소화. 요지:
- v0.154.0 **A127+A128** y축 라벨 "max" 접두 + 하단 바 축 라벨 숨김(AxisMinWidthNow 배수 연쇄 정리).
- v0.155.0 **A133** 힌트 문구 다크 반투명 판(A12 칩 규격·세 벌 전부·요소 불투명도 0.6→1 개정 —
  판 알파가 반투명 담당·페이드 대상 Border 승격).
- v0.156.0 **A129** 편차 판정 = **스왑체인 아크릴 폴백(ⓑ) 확정·상태 버그 기각**(분기 전무 증빙) →
  비디오·**오디오**(둘 다 libvlc) 한정 반투명 단색 폴백(OverlayBackdrop 단일 헬퍼·ApplyOverlayStates
  재푸시로 원복 구조 보장). **§4 "오래 밀린 것"의 v0.31.0 확인(아크릴×스왑체인)은 이 판정으로 소화** —
  블러 없는 투과가 이제 사양. 실기기 확인은 §4 v0.156.0.
- v0.157.0 **A130** 판별 = Ended는 재표시 펌프 정지+Scale 불달(libvlc 소스 대조)로 **진짜 리핏 불가** →
  Ended 중 크기 변경 시 잔상 Clear(검정 정리 — 최대화·스냅·F11 공통). **근본 리핏은 A11(루프)이
  EOF를 재정의할 때 함께 설계**(승계 주석). 트레이드오프: EOF 후 리사이즈하면 마지막 프레임 소실.
- v0.158.0 **A131** 원인 = 더블클릭 판정 2종 모두 컨테이너 수명 결박 + 압축 시작 폴더의 감시(5차)
  재스캔 빈발(**모듈 분기 아님 — 구조×데이터**) → 원시 눌림 쌍 폴백(경로 키·500ms·관찰 전용,
  Handled 불변·A85 억제가 이중 발화 흡수). 낙수(등재 후보): ShowModule의 ContentOpened 구독만
  stale-view 가드(ReferenceEquals) 부재 — 늦게 발화하는 뷰가 교체 후 _currentFilePath 오염 가능.
- v0.159.0 **A132** 가이드·site 최신화 — v0.140.0~v0.158.0 누적분(26서술 grep 검증·기존 2건 정정·
  확신 부족 7건 제외 목록 보존), **site/index.html v0.89.0 표기 해소**(§5 묵은 건 ③ 소화),
  A86-keymap Enter 다중 주석. 가이드 "As of v0.159.0".

**2026-08-16 세션 마감 — 추가 실기기 보고 2건 등재(미착수): A134·A135**
- **A134 · Opus (소형·원인 특정 완료)**: 좌 패널 리스트 폴더 진입 시 상단 트리 미동기 —
  `ExpandToFolderAsync`(완비)가 Show() 시에만 호출, `ViewChanged` 배선 1곳 누락. 루프 가드
  (트리 클릭 → 리스트 항해 → 재동기 순환)·같은 폴더 조기 반환 함정 본문 병기.
- **A135 · Fable (중형·감사형)**: 콘텐츠 열림 상태에서 F1/F2가 안 듣는 모듈 존재(비디오·이미지·
  PDF 등 보고) — 모듈 × 상태 × 포커스 감사표가 통과 조건, 원인 4후보(HasPanelContext 게이트 /
  모듈 선소비 / 스왑체인 포커스 미도달 / F2 이름변경 과잉 양보) 본문 병기.
~~다음 세션 1순위 = 이 두 건~~ → **2026-08-18 세션에서 둘 다 소화(아래 세션 절)**.
그 외 남은 미반영 = 조사·설계 선행군(A41·A47·A48·A43·A11)·후순위군(A4·A14·A15·A19·A45)·
A115 ④ 잔여. 등재 후보 낙수 = ShowModule ContentOpened stale-view 가드(14회전 A131 발견).

**2026-08-18 세션 — A134·A135 완주: v0.160.0·v0.161.0 (opus 1·fable 2배치, push 1회, CI build·release #143 초록 — v0.161.0만 태그·릴리스, 핫픽스 0)**
- v0.160.0 **A134** 좌 패널 트리-리스트 항해 동기 — `FileListOverlay.NavigateList`의 ViewChanged
  람다에 `SyncTreeToFolder` 1줄 배선(가드 2중: `!IsOpen` / 트리 선택 경로 동일 조기 반환).
  루프 판정 = 트리 항해 트리거가 `OnTreeItemInvoked`(ItemInvoked) 단독·SelectionChanged 구독
  0건이라 무해. **구현 중 발견: 같은 폴더 재통지는 감시(A94 5차)만이 아니라 정렬(A5)·필터(A7)
  변경도** — 가드 ②가 셋 다 거른다(없으면 ScrollTreeTo 반복으로 트리가 튄다).
- v0.161.0 **A135** F1/F2 전 모듈 감사 + 방어 수리 — **단일 원본 = `docs/A135-audit.md`**.
  감사 판정: 게이트(HasPanelContext)·모듈 선소비·F2 과잉 양보 전부 부존재(키 경로가 셸 단일
  핸들러). 최정합 가설 = **포커스 고아**(패널 안 포커스인 채 닫히면 Hide()가 포커스 무처리 →
  셸 KeyDown 미발화. 비디오·이미지·PDF만 중앙 클릭 복구 불가 — 보고 패턴 정합). 수리 =
  `ApplyOverlayStates` 말미 한 블록(4표면 `!IsOpen && IsFocusWithin` → 모듈 뷰 재포커스,
  S4 종료 관용구 재사용·새 훅/상태 0). 부수 효과: 이름변경 편집 중 패널 닫힘이 LostFocus
  커밋으로 정상 종료. **한계: WinUI가 포커스를 null로 떨어뜨리는 갈래면 증상 잔존 — 그 경우
  가설 기각·새 항목 등재**(실기기 확인 §4 v0.161.0).

**2026-08-18 세션 (2) — 실기기 피드백 1차 등재만 수행(코드 변경 0): A136~A147 신설 (부록 B 65)**
사용자가 "이번 피드백의 약 50%"라고 예고한 1차분. 등재 전 코드 사실 확인을 서브에이전트 4개로 돌려
**앵커·함정을 항목마다 박아 뒀다** — 확인 과정에서 사용자 전제가 코드와 달랐던 것 3건:
① 제목은 지금 창이 1개면 번호가 **아예 없다**(`KOTU`) — 사용자가 본 `1KOTU`는 창 2개 이상 상태.
② 창 아이콘의 인스턴스 번호는 **A102에서 렌더 코드째 제거**돼 지금 없다 — A137은 부활(부분 반전)이다.
③ 문서 모듈 Fit은 "없는" 게 아니라 **PDF 전용**(텍스트 모드 Collapsed = A49 결정).
사용자 질문("타이틀바·작업표시줄 아이콘도 트레이처럼 실시간 표시가 되나") = **가능** —
`WindowIcon.Apply`가 16px(ICON_SMALL)·32px(ICON_BIG)을 **따로 합성해 보낸다**(코드 근거 A137 본문).
주제 묶음: 번호 정책 상시화(A136·A141) / 아이콘 3면 역할 분담(A137·A138) / 아이콘 렌더 규격·색
(A139·A140) / 편집기 5종(A142) / Fit 조절기(A143·A144) / 공통 요소 전수조사(A145) / H/W 바 기간 표기
(A146 — **A128 부분 반전**) / Alt+숫자 폐지(A147 — A32→A107에 이은 세 번째 변경).
**착수 전 확인 필요 11건이 REQUIREMENTS 부록 B "확인 필요"에 모여 있다**(A138 텍스트 모드 표시·A140
H/W 링 예외·A142 ¶/EOF 재현 조건·A146 표기 위치·A147 Alt+0 유지 여부 등). 확인 없이도 착수 가능한
소형 = A141·A143·A139(Opus).

**2026-08-18 세션 (3) — 피드백 잔여분 등재: A148~A172 (25건, 부록 B 66 · 코드 변경 0)**
서브에이전트 5개로 영역별(이미지·비디오 뷰 / 전체화면·키 / 탐색기 좌 패널·압축 / H·W·트레이·설정·
업데이트 / 오디오·PDF·문서 폭) 코드 사실을 먼저 확인하고 등재했다. **이번 묶음의 핵심은 신규 기능이
아니라 "확정 사양 3건의 반전"** — 착수 순서를 잘못 잡으면 서로를 무효화한다:
- **A151(전체화면 3단: Enter 순환 · Alt+Enter · Esc · 버튼 2개)이 A86의 "Enter = 좌/우 패널 일괄
  토글"을 대체**한다. 일괄 토글의 거취 미정(확인 필요). 부수로 **전체화면 진입점이 지금 모듈·화면마다
  8벌**이라 셸 통합이 실제 작업량이고, "자체 바만 숨기는 모드"는 **부존재**(단 `MainWindow.xaml.cs:249-255`가
  이미 바 숨김 기계를 갖고 있어 프레젠터를 안 바꾸고 그 두 줄만 쓰면 성립).
- **A158(F1/F2 → F11/F12)은 A151 이후에만 가능**(F11이 현행 전체화면 키). A118에 이은 세 번째 키 변경.
- **A168(H/W 센터 4/8열)은 A119의 2/3/4열을 대체**. 도크 1개일 때 값 미정.
등재 중 **"이미 되어 있음" 3건**을 코드로 확인해 항목 본문에 박아 뒀다(사용자 기대와 어긋날 지점):
전체화면에서 패널을 막는 **게이트는 코드에 없고**(A153 — 증상 재현 확인이 착수 조건) · All Readable은
파일을 열면 **자식 모듈 바를 통째로 쓰며**(A165) · 이미지 하단 바에는 해상도·확장자·용량·순번이
**이미 다 있다**(A149 — 없는 건 줌 레벨뿐). 반대로 **없는 것도 확정**: 드래그 팬(A148 — 코드 주석의
"팬"은 ScrollViewer 스크롤 뜻이라 오독 주의) · 창 backdrop 코드 자체(A170 — `SystemBackdrop`류 0건,
"Dispose 누락" 가설 기각) · 오디오 EQ/장치 API 선례(A163·A164) · 월페이퍼 선례(A161).
**확인 필요가 2차분에서 14건 추가**(부록 B "확인 필요" 절 상단). 확인 없이 바로 착수 가능한 소형 =
A148·A156·A160·A162·A167·A172 + 1차분 A139·A141·A143.

**2026-08-18 세션 (4) — 확인 필요 일괄 답변 반영(부록 B 67): 13건 확정 · A165 결번 · 코드 변경 0**
사용자 답변으로 **A151(전체화면 3단)의 사양이 표로 확정**됐다 — 모드1(하단 바 있음) → 모드2(하단 바
**와 좌/우 패널** 없음) → 모드3(전체화면) → 모드1의 **한 방향 Enter 순환**, 3→1은 하단 바만 복귀
(패널 복원 없음), **1→2 전이가 패널을 닫는 역할을 겸하고 Enter는 패널을 열지 않는다**.
이로써 **A86의 "Enter = 좌/우 일괄 토글"은 폐지 확정**(같은 주제 세 번째 지시)이고 **F11은 전체화면
매핑에서 빠진다**(A158 선행 조건 충족). 패널 조작은 **모드 무관·불투명/반투명 무관**하게 F11/F12로
가능해야 하며, 전이 체계(더블=불투명·홀드=반투명·장홀드=피닝·단타=닫기)는 A107/A118 그대로 재확인.
그 밖 확정: 제목 하이픈 2개 수용(A136) · 아이콘 번호는 전면 채움(A137) · 문서 Fit **존치**(A145 —
A49 대체, 텍스트는 `1/1`) · 트레이 텍스트 문서도 `1/1`(A138) · **H/W는 새 색 규칙 적용 제외**(A140 —
전용 색·링 없음 유지) · Alt+0 폐지·Alt+\` 유지(A147) · H/W 센터 **4/6/8열**(A168 = `8 - 2×도크 수`) ·
이미지 하단 바 **좌측 정렬 연속 배치**(A149) · A165는 코드 설명 수용으로 **종결·결번**.
**정정 1건**: A146의 '기간' = 그래프 좌→우가 담는 **히스토리 시간 범위**(하단 바 = 최근 5분).
→ 여기서 새 쟁점 — 링 버퍼 600 고정이라 **주기가 짧으면 5분을 못 담는다**(50ms=30초). "항상 5분"으로
갈지(버퍼 확대) "표기만 실제 범위"로 갈지 확인 대기.
**A151 잔여 확정 6건**(모드2의 타이틀바 / 모드3 = Alt+Enter 결과 동일성 / "직전 상태" 범위 / Enter 원
기능 우선 범위 — **문서 모듈은 Enter가 줄바꿈이라 모드 순환 불가** / 모드 리셋 시점 / 창별·비저장)은
**같은 날 부록 B 68로 전부 확정**(아래).

**2026-08-19 — A151 사양 완결 + A146 링 버퍼 확대 확정(부록 B 68 · 코드 변경 0)**
- **A151 = 이제 사양이 닫혔다**: 모드2는 **타이틀바 유지**(하단 바 + 좌/우 패널만 사라짐) · **모드3 =
  Alt+Enter 결과와 동일**(Alt+Enter는 상황 무관 직행 + 복귀 지점 기억) · Esc·Alt+Enter의 복귀 스냅샷에
  **좌/우 패널 구성까지 포함**(닫힘·반투명·피닝·불투명 4상태를 좌우 각각) · **Enter 순환 제외 = 문서
  편집 가능 상태 + 탐색기 표면 포커스**(현행 양보 코드가 그대로 규칙) · **영상은 전용 액셀러레이터를
  제거해 순환 대상으로** · 리셋 = 모듈 전환·파일 열기·새 창 · **창별 상태, 저장 안 함**.
- **A146**: 사용자 조건부 승인으로 **링 버퍼 600 → 6000 고정**(주기 무관 항상 5분). 비용 실측 근거를
  항목 본문에 표로 박아 뒀다 — 메모리 약 620KB(`SensorFrame`이 record = 참조형·약 104B) · `History()`
  복사 48KB/회 · **다운샘플 상한 150은 불변이라 그리기 비용은 그대로**. GC가 문제 되면 `History()`의
  매회 새 배열(`SensorService.cs:120`)을 구간 전달로 바꾸는 것이 1순위 최적화(호출처 2곳).
- **파생 미결 1건**: 문서 모듈은 편집 중 Enter가 줄바꿈이고 모드2에서는 하단 바 버튼도 사라져
  **모드2 탈출 경로가 Alt+Enter뿐**이다 → **Esc가 모드2 → 모드1도 담당할지** 확인 대기(제안: 담당).

**2026-08-19 — Opus 실개발 1차: v0.162.0~v0.165.0 (4배치, push 1회 — v0.165.0만 태그·릴리스)**
오케스트레이터 + Opus 서브에이전트 직렬. **A136·A141 / A147 / A139·A140 / A146·A168·A172 = 8건 완료·결번.**
- v0.162.0 **A136+A141** 제목 `1-KOTU`(ASCII 하이픈·창 1개여도 표시 — `UpdateInstanceNumbers`의 개수
  조건 폐지) + 하단 바 번호 배지 제거. **낙수 정리**: 배지가 `InstanceIcon.ColorFor`/9색 `Palette`/`Rgb`의
  유일 소비처여서 함께 제거(남기면 CS0414). 번호 소비처는 이제 **창 제목 하나뿐**.
- v0.163.0 **A147** Alt+숫자·Alt+0 폐지(Alt+\`만 남음). **서브에이전트가 내 전제를 코드로 반증**:
  `BuildStartMenu`는 `AddModuleItem` 순서를 하드코딩하고 `ModuleShortcuts` 배열을 읽지 않는다 →
  배열 소비처가 등록·툴팁 둘뿐이라 통째 삭제가 정답이었다(남기면 CS0414). **Alt 골격
  (`_altComboUsed`·단독 up 소비)은 무손상** — Alt+\`가 같은 경로를 탄다.
- v0.164.0 **A139+A140** `EdgeUnit(size) = Max(1, Round(size/16))` 단일 식으로 두께·반경 통일
  (16px 1 / 32px 2), 트레이 유휴 = **accent 원색 불투명 채움 + 흰 글자**. 하드웨어는 규칙 밖 —
  판정은 **`Branding.IdleFill`(moduleId 기준)** 신설로 했다(`IconRing`의 null은 하드웨어와 중립을
  구분하지 못한다 = 이 배치의 함정). ⚠️ **파생**: A126 inset이 줄어 32px 본체가 24 → **28px**로
  커졌는데 28은 실존 `.ico` 프레임이 아니다(GDI 보간) — 흐리면 실존 크기로 스냅할지 판단(§4).
- v0.165.0 **A146+A168+A172** 링 버퍼 600 → **6000**(주기 무관 5분 — 좌 10s·센터 30s 창은 무변화 검증) ·
  기간 표기는 **그래프 표면이 아니라 바 레이아웃의 `SpanText` 1개**(전체화면에서 `PlaceSensorGrid`가
  SensorGrid를 통째로 옮기므로 BarGrid에 두면 전체화면에서만 사라진다 — star 칸 마지막 열로 넣고
  폭은 `LongGraphsWidth`로 회계, 맥박 임계 676 → **716**, 좁은 창 2단 축약 신설) ·
  센터 열 `8 - 2×clamp(도크,0,2)` = **4/6/8** · 우 패널 폰트 제목 18→14·라벨/값 11 명시·라벨 열 120→96.
- **미완**: 배치5(A148+A149 이미지 팬·하단 바)는 **Opus 서브에이전트 호출이 API 529로 두 번 연속
  실패**해 착수하지 못했다(작업 트리는 깨끗). 재시도부터 시작하면 된다.
- **CI 확인이 남았다** — push는 됐고(v0.165.0만 태그 대상) build·release 결과는 미확인.

~~남은 Opus 착수 가능분 · 다음 세션 배치 계획~~ → **2026-08-19 (2) 세션에서 배치 5~12를 전부 완주**(아래).

**2026-08-19 (2) — Opus 잔여 완주: v0.166.0~v0.174.0 (8배치 + 핫픽스 1, push 3회)**
사용자 지시 "배치5부터 재개 · 서브에이전트 최대 활용 · 반드시 직렬" + "opus 쓸 것만 우선".
**A148·A149 / A143 / A156·A157 / A160 / A154 / A162·A167 / A169 / A171 / A161 = 12건 완료·결번.**
이로써 **Opus 몫은 전부 소진**됐고 남은 미반영은 **전부 Fable 몫 + 조사·설계 선행군**이다.
- v0.166.0 **A148+A149** 이미지·PDF 드래그 팬(좌버튼·마우스 전용·임계 4px·스크롤 여지 0이면 캡처 안 함,
  `DoubleTapped`가 UserControl **루트**라 층이 달라 **팬 종료 후 400ms 억제 창**으로 막는다) +
  하단 바 좌측 한 덩어리 재구성·`InfoText` → **`ZoomText`** 개명(칸 번호 불변).
- v0.167.0 **A143** Fit 100% 아이콘 = App.xaml `PathGeometry` 1벌 + 참조 6곳.
  ⚠️ **v0.169.1 핫픽스로 이어짐**(아래).
- v0.168.0 **A156+A157** 좌 리스트 2줄 + 체크박스·Space. 본체는 **인덱스 계약 3지점을 이름 기반으로
  이관**한 선행 리팩터(어긋나면 예외 없이 F2 무반응·길이 미표시라 QA에서만 잡힌다).
  회귀 수리 동반: 영상·오디오 Space 액셀러레이터에 `ShouldPassThrough` 가드 추가.
- v0.169.0 **A160** 숨김/시스템 표시 토글 — 판정 단일 원본 `ExplorerListing.ShouldShow`로
  **트리의 인라인 복제 제거**(리스트·트리 불일치가 이 항목의 최악 회귀).
- **v0.169.1 [A143 핫픽스] — 이 세션 유일한 CI 실패의 수리.**
  `PathGeometry.Figures`에 path 미니언어 **문자열**을 넣어 `XamlCompiler error WMC0055`.
  **WinUI는 WPF와 다르다** — `Data="M ..."`처럼 **Geometry 타입 속성**에 거는 문자열 변환만 성립하고
  `Figures`(PathFigureCollection)에는 안 된다. 수리 = `PathFigure` + `LineSegment` **요소 구문**.
  ⚠️ **교훈: WPF에서 되는 XAML 문법이 WinUI에서 된다고 가정하지 말 것**(저장소에 실제로 쓰이는 형태만 복제).
  같은 push의 나머지(A148·A149·A156·A157·A160)는 **전부 컴파일 통과** — 이 한 건만 빌드를 깼다.
- v0.170.0 **A154** 경계 버튼 하단 이동 + 핀/순간 2종. 함정 5개를 **구현 전 정독으로 잡아** 전부 처리:
  ① 루트 개입 핸들러가 방금 연 peek을 같은 이벤트에서 도로 닫는다(스택을 예외에 추가)
  ② `Button`이 `PointerPressed`를 삼킨다(`AddHandler(handledEventsToo)` 필수)
  ③ peek이 열리면 경계선 x가 25%로 옮겨가 버튼이 커서 밑에서 빠져나간다(peek 중 근접 판정 제외)
  ④ `UpdateEdgeButtons`가 Margin을 통째로 덮어쓴다(하단 여백 정본은 코드 상수)
  ⑤ 뗌 유실 = 오버레이 영구 열림(캡처 + Released/CaptureLost 공용 핸들러 + `CancelHoldCore` 결박).
- v0.171.0 **A162+A167** 설정 문구 축약 4건 + Learn more 링크 · `(A38)` 노출 제거 ·
  업데이트 재확인 카운트다운(`NextCheckAt` + 1초 타이머). ⚠️ **Learn more 목적지가 `site/guide.html`이
  아니다** — **그 사이트는 어디에도 게시돼 있지 않다**(Pages 워크플로 0건). GitHub 렌더링본
  `docs/USER-GUIDE.md`를 연다. **앵커 이름을 두 문서에 같게 맞춰 뒀으므로 사이트가 게시되면
  `Branding.UserGuideUrl` 한 줄만 바꾸면 된다.**
- v0.172.0 **A169** 트레이 글씨 채널 색 복원(A101에서 소실). 계약은 **`uint?` ARGB** —
  `KOTU.Core.csproj`가 `net8.0` + Abstractions 2개뿐이라 **`Windows.UI.Color`는 애초에 컴파일 불가**.
  ⚠️ 최대 함정 = **재합성 스킵 키에 색 축 추가**(안 넣으면 "62% CPU → 62% GPU"가 갱신 안 된다).
  등재문 정정: 모듈 `_trayKey`는 채널 ID를 이미 포함해 **원래도 안 깨졌다** — 깨진 곳은 셸 `ComposeKey` 하나.
- v0.173.0 **A171** 편집기 폭 설정(700/900/1200/Unlimited=0). **문서 모듈 최초의 `ISettingsService`
  주입·최초의 `document.*` 키**. 불가침 3제약(두 요소 같은 값 한 지점 · `Center` 금지 · `Left` 금지) 무손상.
  **즉시 전파는 일부러 안 만들었다**(모듈 전환 시 뷰가 새로 만들어져 반영 — 설정 문구가 그 사실을 알린다).
- v0.174.0 **A161** 이미지 우클릭 "Set as desktop background". **모듈 DllImport 0건 규약 유지** —
  Core 훅(`DesktopWallpaperHook`, A124 패턴이되 **결과를 돌려준다**) → App `DesktopWallpaper`.
  변환은 **항상 PNG**(`%AppData%\KOTU\wallpaper.png` 고정) · Fill 고정 · 다중 모니터 별도 처리 없음 ·
  변환/파일/레지스트리/P-Invoke 전부 **뷰 워커**에서. **CI 1순위 위험 = `SystemParametersInfoW` 시그니처.**
- **가이드 동반 갱신**: 배지가 `As of v0.171.0`으로 올라간 김에 **v0.166.0~v0.171.0의 사용자 가시 변화를
  두 문서(USER-GUIDE.md·site/guide.html)에 반영**했다(이미지 팬·하단 바·PDF 팬·Space 양보·리스트 2줄·
  체크박스·숨김 토글·경계 버튼 2종). 전 서술 코드 검증 + 슬러그·앵커 기계 검증.

~~남은 몫 = 전부 Fable~~ → **2026-08-19 (3) 세션에서 확정 사양군 11배치로 전부 소화**(아래).
~~잔여는 A142 ④ · A159 · A170 + 선행군·후순위군·A115 ④~~ → **2026-08-19 (4)~08-20 세션에서
A173~A191까지 완주**(아래). 잔여는 그대로 A142 ④ · A159 · A170(전부 사용자 입력 대기) +
조사·설계 선행군(A41·A47·A48·A43·A11) + 후순위군(A4·A14·A15·A19·A45) + A115 ④.

**낙수(등재 후보) — 이 목록이 정본이다**
1. `Fill` 전량 재생성이 **재스캔 후 선택을 지운다**(체크는 A179/v0.190.0의 체크 집합 분리 + 복원·prune으로
   해소됐고 **선택 복원만 잔여**. 선례 = `ApplyCutMark`의 경로 집합 재적용).
2. `ImageFolderNavigator`의 ←/→ 탐색은 속성 필터가 없어 **숨김 이미지에 도달한다**(A160 이전부터).
3. A108 안내 문구 판이 **세로 중앙에 남았다**(A154로 버튼만 내려감 — 세 표면 정렬 동반 이동이 후속 후보).
4. `ShowModule`의 ContentOpened 구독에 **stale-view 가드 부재**(A131 발견).
5. **영상 하단 바 압축 임계에 A151 몫 −38 미반영**(A144가 660 → 640까지만 내림. 주석 명기 — **602가 후보**).
   ※ A186(v0.188.0)의 모드 2단화로 전제가 바뀌었으니 재계수부터 할 것.
6. **드라이브 줄(A22) 미구현 3모듈**(영상·오디오·H/W — A145 잔여 결정 ⓑ, 사용자 결정 대기).
7. **페이지·탐색 버튼화**(A145 잔여 결정 ⓒ, 사용자 결정 대기).
8. **오디오 EQ 밴드 슬라이더**(A163 1차는 libvlc 내장 프리셋만).
9. **문서·압축 모듈의 `IContentInfoProvider` 구현**(현재 셸 폴백 — 문서 = 페이지 수·인코딩 /
   압축 = 엔트리 수·압축률. A150 ⓒ).
10. ~~H/W 센서 그리드가 A151 모드2에서 바와 함께 사라진다~~ / ~~A61 접힘 중 모드2 진입 = 빈 창~~ —
    **둘 다 A186(v0.188.0) 모드2 폐지로 전제 소멸**(모드가 창/전체화면 2단이 됐다). 재현되면 새로 등재.

**A178 전역 감사(2026-08-20)가 새로 올린 등재 후보 4건 — 위반이 아니라 개선 여지**
11. **탐색기 목록/타일이 컨테이너를 전량 수동 생성**한다 → 가상화·증분 생성·최대 건수 상한(대량 폴더 1순위).
12. **마크다운 렌더 트리 분할 조립**(A190 렌더러 — 지금은 한 번에 조립).
13. **탐색기 썸네일·상세 조각의 상한 병렬화**(+ 이미지 이웃 선읽기 — 현 ModuleWorker는 뷰당 직렬).
14. **설정 컨텍스트 메뉴 등록의 워커 이관**(소형).

**세션 중 배치 보고에서 올라온 등재 후보(2026-08-20)**
15. 영상 바 **자동 숨김 시 마우스 커서도 숨기기** + **오디오 모듈로 자동 숨김 확대**(A186발).
16. 리스트 **전체 체크 해제 제스처 부재**(A179발).
17. 상세 줄 공백 2건 — **비PDF 텍스트의 줄 수**(비용 판단 필요) · **7z/rar 압축률**(A180발).
18. **우측 인포 패널의 드롭 규칙**(A187이 별건으로 남긴 판단 — 모듈 기준 라우팅과 정합시킬지).
19. **USER-GUIDE의 Explorer integration 절 서술이 낡았다**(A182발).
20. 마크다운 **렌더 모드 줌 미적용** · 편집 중 **실시간 미리보기** 부재 · 렌더 뷰 **키보드 스크롤**(A190발).
21. 이미지 **하단 바 메타·우측 정보 패널의 해상도 미스왑 표기**(회전 미반영 — 트레이만 A191로 고쳐졌다).

**세션 중 배치 보고에서 올라온 등재 후보(2026-08-21)**
22. **S4×전체화면 스냅샷 정합 2건**(A203발 판정 보고): ① S4+전체화면 중 파일 열기 = 양 스냅샷 폐기로
    진입 전 도크 구성 소실(종전 = S4 강제 Docked 잔존 — 동작 변화·해석 여지) ② 역순 진입(전체화면 →
    S4)의 스냅샷 겹침은 A203 이전부터의 기존 함정.
23. **이미지 뷰어 파일 변경 감지 부재**(A194발): FileSystemWatcher·재로드 경로가 원래 없음 — 이웃
    선읽기 캐시의 낡음은 양옆 왕복 구간으로만 제한됨. 감지 신설 시 캐시 무효화 결박 필요.
24. **USER-GUIDE·site 정렬 옵션 서술에 Type 누락**(A204발 — A155 이전 서술 잔존, 기존 문서 부채).
25. **탐색기 Unloaded 후 늦은 스캔 continuation이 조립 루프를 잠깐 돌릴 수 있음**(A192발 — 자기
    종결형이라 영구 누수는 없음·종전 동기 Fill과 동급. 신경 쓰이면 Unloaded 플래그 가드 한 줄).
26. **비관리자 NVMe SSD 온도**(A47발 보류 — IOCTL_STORAGE_QUERY_PROPERTY + Temperature. 비관리자
    성립이 "추정" 수준이라 실기기 선검증(A47 조사 §4.3 ㉰) 후 Core 훅 경유로 등재).
27. **뷰 내부 탐색(A11 목록 진행·이미지 ←/→)의 창 제목 미갱신**(A11 설계 §5.1 — 기존 거동 답습).
28. **A11 수동 이전/다음 키**(PgUp/PgDn 후보 — B 76에서 "추후 별도"로 확정된 몫).
29. **센서 승격 수집 서비스 상주(A47 (a)안)** — 수요 확인 시(전 센서 무승격이 필요해지면) 별도 등재.
30. **바 버튼 클릭 직후 Enter = 버튼 재클릭**(A209발 실재 확인): ButtonBase가 Enter를 invoke로
    소비(원 기능 우선 양보 규칙의 정상 동작) — 예: Rotate 클릭 후 Enter는 전체화면이 아니라 재회전.
    UX로 거슬리면 등재(바 클릭 후 포커스를 모듈 뷰로 되돌리는 방향).
31. **패널 닫기(F11)에서 포커스가 null로 떨어지는 형태**(A209발 잔여): A135 블록은 IsFocusWithin
    (비-null 전제) 판정이라 null 형태를 못 잡고, 패널 닫기는 UpdateShellChrome을 안 지나 A209
    방어 밖 — 실기기에서 "패널 닫은 직후 키 전멸"이 재현되면 이 갈래로 등재.

**2026-08-19 — 잔여 확인 15건 일괄 확정(부록 B 69): 확인 대기 사실상 소진 · 코드 변경 0**
사용자가 14번(오디오 장치 범위)만 뒤집고 나머지는 전부 제안대로 확정 —
A137(두 자리는 폰트 축소·용량은 저장 시 1회 갱신) · A138(`999+`) · A142 ⑤(`GuideGap = 2`) ·
A143(본체 `1:1`도 아이콘 교체) · A146(기간 표기 = 긴 그래프 우측 끝 공통 1개) · A150(GPS 기본 숨김 +
라벨·값 구조화) · A154(버튼 2개 · 도크 토글은 핀 버튼 겸임) · A155(정렬 방향 토글 도입 · **필터 A7 존치** ·
모듈별 대체 정보 = 영상 길이+해상도/오디오 길이/문서 페이지/압축 압축률) · A157(체크 = 선택 집합 하나) ·
A166(안전한 경로 없으면 폴백 UX 개선으로 종결 승인) · A171(설정 콤보 700/900/1200/무제한).
**A164만 확대**: 입력·출력 **둘 다** — 다만 "입력 선택이 무엇을 하는가"(녹음 준비 저장 / 모니터링 /
Windows 기본 장치 변경)는 미정으로 남겼다.
**남은 확인 필요는 4건뿐**: ① 문서 모듈 모드2 탈출(Esc 확장) ② A164 입력 동작 정의 ③ A159 증상 구분
④ A142 ④ 스크린샷 대기. **즉 A136~A172 대부분이 착수 가능 상태**이며, 큰 배치는
**A151 → A158 → A152·A153** 순서가 고정이다.
→ ①②는 같은 날 **부록 B 70**으로 확정(①Esc가 모드2→1 담당 · ②ⓒ = Windows 기본 입력 장치 변경),
③④는 보류. 진행 범위 = **확정 사양군만**으로 지시됨.

**2026-08-19 (3) — 핫픽스 v0.174.1 + Fable 확정 사양군 11배치 완주: v0.175.0~v0.184.0**
오케스트레이터 + Fable 서브에이전트 직렬(부록 B 70의 진행 범위 지시 그대로). **결번 15건**
(A151 / A158 / A152·A153 / A137·A138 / A144·A145 / A150 / A155 / A163·A164 / A166) +
A142 부분 완료(①②③⑤) + A170 감사·판정. **v0.184.0이 태그 대상**.
- **v0.174.1 [A143 핫픽스2] — 실기기 크래시 수리(이미지·문서·영상 모듈 진입 즉시 다운).**
  원인 = **App.xaml의 공유 `PathGeometry`를 여러 `PathIcon.Data`에 걸었다** — **WinUI `Geometry`는
  부모가 하나뿐**이라 두 번째 사용처에서 런타임 `XamlParseException`. **CI는 컴파일만 하므로
  이 부류를 원리적으로 못 잡는다.** 수리 = 사용처별 인스턴스(XAML 인라인 `Data` 문자열 +
  코드 `BuildActualSizeIconGeometry()` 빌더, 6곳) + App.xaml 리소스 제거 + 금지 사유 주석.
  ⚠️ **교훈은 CLAUDE.md §3에 반영됨** — 리소스로 공유해도 되는 것은 Brush·Style까지고,
  `Geometry`·`UIElement`는 안 된다. 태그·릴리스 발행까지 확인 완료.
- v0.175.0 **A151** 전체화면 3단(모드1 창+바 → 모드2 바·패널 숨김(타이틀바 유지) → 모드3 전체화면)
  **한 방향 Enter 순환** · Alt+Enter = 어디서든 모드3 직행 + 복귀 스냅샷(모드 + 좌/우 패널 구성) ·
  Esc = 모드3→스냅샷 / **모드2→모드1**(부록 B 70) · 셸 모드 버튼 2개. **전체화면 진입점 8벌을 셸로
  통합**(F11·모듈 Esc·영상 Enter 액셀러레이터 제거)하고 **A86 Enter 일괄 토글을 폐지**
  (`BatchToggleOverlays`·`_lastBatchStates` 제거). **바 가시성 단일 원본 = `UpdateShellChrome`**,
  경계 버튼 여백은 바 가시성 연동(`EdgeButtonsBottomInset` 52/8 — A154가 남긴 훅 소진).
  모드 리셋 = 모듈 전환·파일 열기·새 창, 창별 비저장.
- v0.176.0 **A158** 패널 키 F1/F2 → **F11/F12**(A107→A118→A158, 세 번째 변경). 코드는 키 정본
  2줄 + 힌트 2줄이 전부. **F2 = 탐색기 이름변경은 유지**(충돌 소멸). 문서 5종(A86-keymap·
  USER-GUIDE `As of v0.176.0`·site/guide.html·A135-audit·A94-matrix)에 **A151 3단 서술까지 함께 현행화**.
- v0.177.0 **A152+A153** 행 분할(`BottomBarRow`) **폐지** → 콘텐츠는 항상 창 전체, 하단 바는 겹치는
  오버레이. 그래서 **전 모드 뷰포트가 동일**하고 모드 전환 출렁임이 사라졌다. 세 Fit 계는 표면
  자기완결이라 **모듈 무수정**, 보정은 스크롤형 3표면(문서 에디터·PDF 리스트·탐색기)에 끝 여백 +44만.
  A153은 **패널 경로 `_viewMode` 게이트 0건 감사표**로 보증(수리 대상 버그 없음).
- v0.178.0 **A137+A138** 창 아이콘 2종 분리 = 16px 인스턴스 번호(전면 채움+흰 숫자, 1도 표시) /
  32px 확장자·용량 2줄(셸이 경로로 직접 계산). **A104 캐시 유계 전제를 지키려고 무캐시 즉시 합성 +
  지연 회수**로 갔다(창 아이콘 키에 파일 축 금지). 트레이 문서 = 우상→좌하 대각 페이지 표시
  (텍스트 `1/1`, 4자리+ = `999+`), `PageChanged`→`TrayStatusChanged` 배선.
  **`ComposeKey`에 대각 축·창 아이콘 키 신설**(A169 교훈).
- v0.179.0 **A142 ①②③⑤** 스크롤 장식 렌더 **50ms 코얼레싱**(+최종 즉시 렌더) · `Text` COM 마샬링을
  **편집당 1회 스냅샷 공유** · `FontFamily=Consolas`(**TextBox 유지 판정**) · 행 번호 거터 =
  **DecorLayer 음수 x**(레이아웃 불변이라 A115/A120/A171 좌표계 3자 계약 무손상, 폭이 없으면 통째 숨김) ·
  `GuideGap=2` + 병합을 gap 적용 후 좌표로 재계산. **④(¶/EOF 위치)만 스크린샷 대기로 잔여**.
- v0.180.0 **A144+A145** SplitButton 84×32 3곳 → **본체 Button 32 + DropDownButton 32**
  (SplitButton 내부 분할은 통제 불가라는 판정 유지) · 문서 모듈 Fit = **A49 Collapsed 분기 대체**,
  전 모드 상시 표시하되 텍스트/빈 화면은 `1/1` 비활성(A/F 키는 `IsEnabled` 게이트 통과) ·
  전수조사 대조표 완성. 영상 압축 임계 660 → 640. ⚠️ **A151 몫 −38은 미반영(주석 명기, 602 후보)**.
- v0.181.0 **A150** `IContentInfoProvider`를 **`IReadOnlyList<ContentInfoItem(Label, Value)>`** 로 교체
  (소비처 1곳·구현 4곳 + 셸 폴백 동시 이식, 병행 메서드 없음). 렌더는 H/W 우 패널 관용구(라벨 96·폰트 11)
  + `Separator` 그룹. 이미지 **EXIF +6키**, **GPS는 수집 자체를 안 한다**. 영상은 Resolution/Frame rate/
  Video codec/Audio로 분해. 문서·압축은 폴백 유지(등재 후보).
- v0.182.0 **A155** 좌 리스트 컬럼 헤더 6종(Name/Type/Size/Created/Modified/Info) · 클릭 정렬 ·
  재클릭 방향 토글(`explorer.sortDesc` 신설, 구 설정은 종전 고정 방향 폴백 = 마이그레이션 0) ·
  `SortKey.Type` 신설(`Entry` 무변경) · 모듈별 Info는 **A6 지연 로드**로(zip 압축률 = `ArchiveQuickInfo`
  신설, 7z·rar 생략). `Info`는 지연 로드 값이라 **정렬 비대상**. 테스트 4건 추가.
- v0.183.0 **A163+A164** 하단 바 c7 EQ·c8 Devices 2칸 신설(자막 버튼+Flyout 규격, 축약 임계 불요) ·
  EQ = **libvlc 내장 프리셋만**(`audio.equalizer` = 이름, 밴드 슬라이더는 등재 후보) ·
  출력 = `AudioOutputDeviceEnum`/`SetOutputDevice`(`audio.outputDevice` = **장치 ID**, `Playing`이
  최초 유효점이라 무재시작 이동) · **입력 = Windows 기본 입력 장치 변경**(부록 B 70 ⓒ — Core 훅 +
  App 격리 `DefaultAudioInput`, 비공개 COM `IPolicyConfig`의 `SetDefaultEndpoint`만, 전부 try/catch +
  안내 폴백). WinRT `DeviceInformation`/`MediaDevice` **저장소 최초 사용**(A14·A15가 참조할 선례).
- v0.184.0 **A166** **판정 = 우회 불가.** UCPD.sys(커널 필터)가 보호 확장자 9종의 `UserChoice` 쓰기를
  **MS 서명 프로세스에만** 허용 — 무력화 외 경로가 없어 부록 B 69 승인대로 **폴백 UX로 종결**.
  `UcpdProtectedExtensions`/`IsProtectedExtension` 신설 → 보호 확장자는 강행 시도 자체를 생략하고
  전부 보호면 딥링크 자동 오픈 억제 + `Windows requires confirmation` 안내 + Set default
  (`SHOpenWithDialog`) 유도. **§5 ② 관찰 소화**: 빈 "기본 앱" 페이지는 **등록 결함이 아니라 OS 인덱싱
  아티팩트**이고 이번 변경이 노출 자체를 우회한다.
- **A170 — 코드 변경 0(감사·판정만, 미결번).** 앱에 밝기에 관여하는 자산이 **없다**:
  `SystemBackdrop`류 0건이고, 인앱 `AcrylicBrush`는 **HostBackdrop이 아니다**(WinUI 3에서 제거된 API —
  창 안만 흐리며 DWM 전역 자원과 무관, 문헌 확정). 트레이 `NIM_DELETE` 가설도 기각.
  남은 후보 = ①CABC(1순위) ②DWM 재합성 ③MPO(비디오 한정) ④LHM 해제(낮음) — 전부 앱 밖이라
  **실기기 판별 절차 5종을 사용자 관찰 대기로 존치**했다(§4 신규 블록 끝 · REQUIREMENTS 본문 A170).
- **운용 메모**: 서브에이전트 호출이 **API 오류로 3회 중단**됐다(A150 2회·A163 1회). 매번 작업 트리가
  깨끗한 지점에서 끊겨 **같은 배치를 이어받아 재호출**하는 것으로 전부 완주했다 — 중단 시 롤백하지 말고
  마지막 커밋 기준으로 재개할 것.

**2026-08-19 (4)~08-20 — 착수 대기열 전량 완주: v0.185.0~v0.201.0 (17배치 + A178 감사) · A173~A191 전부 결번**
오케스트레이터 + 서브에이전트 직렬(항목별 권장 모델 지정). **push 대기 8커밋(v0.196.0~v0.201.0 + docs 2건),
v0.201.0이 태그 대상 · CI 결과 미확인.** 이번 묶음으로 **확정 사양 항목이 0건**이 됐다.
- v0.185.0 **A173** 업데이트 델타 — `vpk pack` **앞에** `vpk download github --token`(직전 릴리스 취득)을
  넣어 델타를 동반 생성하고, pack 후 **직전 버전 nupkg는 업로드에서 제외**(구 풀 재첨부 방지). 실패는
  **경고 + 정리 후 풀만 발행으로 조용히 후퇴**(`continue-on-error` 3중 — 릴리스가 막히지 않는 것이 우선).
  **클라이언트 무수정** — 체인 >10 or 크기 합 > 풀이면 Velopack이 자동 풀 폴백(1.2.0 소스 확인).
  ⚠️ **CI가 아니라 릴리스 2회 실측이 통과 조건**이다(§4).
- v0.186.0 **A174** 탐색기 위치 유지 = **전역 `explorer.lastFolder` 단순화**(모듈별 `lastFolder.{id}` 소비처
  제거·구 키는 잔존). 빈 모듈 전환은 폴더 유지 + 필터만 교체(빈 목록 허용), 브라우징 항해도 전역 키 갱신
  (`ViewChanged` 단일 배선 + **폴더 실변경 가드** — 정렬·필터·감시 재발화로 인한 저장 0회).
  **세션 폴더를 전역보다 우선**한 것이 구현 중 판단 — 안 그러면 창끼리 서로의 위치를 되밟는다.
- v0.187.0 **A175** 하이드레이션 방지 — `IsCloudPlaceholder`(Offline·RecallOnDataAccess·RecallOnOpen) →
  `Entry.IsPlaceholder`, **열거가 이미 읽는 Attributes 재사용이라 추가 IO 0**. placeholder면 썸네일은
  원본 풀디코드 금지(`ReturnOnlyIfCached` + 확장자 타일), 지연 로드 생략(**캐시에 안 남겨** 로컬화 후 재조회),
  명시적 열기는 허용. ⚠️ **등재문의 원흉 지목이 틀렸다** — ExplorerPane 경로는 휴면이고 **실원흉은
  `ThumbnailExplorer`의 원본 디코드**였다(실측으로 정정).
- v0.188.0 **A176+A186** 이 묶음 최대 배치(24파일 **+733/−1085**). 반투명 오버레이 축을 통째로 걷어내
  **불투명 사이드바 + 단타 토글**만 남기고(`OverlayBackdrop`·`OverlayMode` 파일 삭제 = A129 스왑체인 폴백
  기계까지), 모드는 **창/전체화면 2단**으로 축소해 Enter를 Alt+Enter와 한 함수로 수렴시켰다(원 기능 우선
  3종 유지). 영상 바 자동 숨김은 **`IPlaybackStateSource` 신설**(모듈 → 셸 재생 상태 신호가 없어서 만든
  최소 계약, All Readable 중계 포함) · 재생 중 무입력 3s. 파생 수치 동반 수정 = `EdgeButtonsHeight` 66→32 ·
  `ModuleBarHost` 우측 82→44 · 경계 버튼 여백은 **바 실표시 연동**. 문서 4종 As of v0.188.0.
- v0.189.0 **A177** 기동 블로킹 = **로딩 프레임 보장 후 지연 대입**(`CompositionTarget.Rendering` 2틱 +
  `Loading {name}...`, **1MB 초과에만**) + 대입 전 조작 가드(편집 불가·저장 무동작·**닫기는 허용**) +
  대용량 장식 자동 오프(A115 `Disable` 재사용 — 렌더 패스당 수백 회 rect 호출이 0). **기준 텍스트 워커
  선계산은 불채택** — `:352` 더티 오탐 방어 계약이 깨진다(판단 근거를 보고에 남겼다).
- v0.190.0 **A179** 체크 집합 분리(A157 반전) — `_checkedPaths` 단일 원본, 선택 거울 철거, 작업 집합은
  **체크 ≥1이면 체크(현재 리스트 교집합) / 0이면 선택**. **재스캔 생존**(Fill 복원 + prune)으로 기존 낙수의
  체크 쪽이 자연 해소됐다(선택 복원은 잔여).
- v0.191.0 **A180** 상세 줄 보강 — `InfoKind.Image` 신설(기존 지연 로드 관용구를 타서 **A175 placeholder
  skip이 자동 적용**), 날짜 = Created `yy-MM-dd` / Modified 순수 상대 표기(단위 내림 · **갱신 트리거 미신설**,
  오차 허용), 툴팁을 `yyyy-MM-dd HH:mm` 정본으로 승격. 전수 조사 공백 2건은 등재 후보로만 보고.
- v0.192.0 **A181** 편집기 화면 폭 디폴트 + 줌 — MaxWidth 900·폭 설정 콤보·상수 3종·`ApplyEditorMaxWidth`
  철거(**고아 0**), `_settings`는 **`document.zoom`으로 재사용**해 CS0414를 피했다. 줌 = FontSize 배율
  (50~300%·10% 스텝) + **장식·거터 `SetScale` 동배율**(A115 좌표계 계약이 이 배치의 최대 함정), 거터 자리는
  `EditorBox` 좌측 패딩 예약 + **자리 부족 시 거터만 숨는 폴백**.
- v0.193.0 **A182** Learn more 앱 내 펼침(A162 부분 반전) — 링크 4곳 → 토글 + 상세 TextBlock, 음수 Margin
  상쇄는 묶음 StackPanel로 보존. `GuideLink` 삭제 · **`UserGuideUrl`은 존치**(사이트 게시 훅).
- v0.194.0 **A183** 설정 인티그레이션 = 모듈당 카드(HardwareView 카드 관용구 복제) + **토글 우측 끝**.
  등재 시 판정대로 **실원인은 스위치 위치가 아니라 구획 컨테이너 부재**였고, 구분선은 카드 테두리가 대체.
- v0.195.0 **A184** Fit 100% 아이콘 = **네 모서리 꺾쇠 프레임 + 중앙 4x4**(글자 `1:1` 폐기), 16px 좌표계
  5도형을 **6곳 동시 교체**(v0.174.1 공유 Geometry 금지 구조 유지). **Segoe 재탐색 = E700~E8FF 전수에 적합
  글리프 없음 · E900~ 구간은 문서 접근 한계로 미확인**(A111 판정 유지 — 이 미확인 구간은 보고로 남겼다).
- v0.196.0 **A185** 트레이 최소화 = **SC_MINIMIZE 관찰**(개입 없음 — 기각 주석의 근거 2개를 "관찰뿐"으로
  재평가한 기록 포함). 기록은 **체인 호출 앞**(DefWindowProc 중첩 Changed 선행 대비), `ConsumeUserMinimize`는
  1회 소비 + 500ms lazy 타임아웃 + NCDESTROY 정리. Win+D류는 일반 최소화 유지. ⚠️ **Win+M은 문헌상
  SC_MINIMIZE 유력**이라 트레이로 갈 수 있다 — 실기기 결과에 따라 사양 재판단.
- v0.197.0 **A187** 드롭 라우팅 완결 — 조건을 `_currentFilePath` → **`IsFileModule` 게이트**로.
  **지시의 "조건 제거"를 코드 판정으로 치환한 사례**다: 무조건 제거하면 H/W·설정·빈 셸에 드롭했을 때
  "갓 띄운 창 재사용" 의도와 충돌하는 회귀가 난다. 실거동 변화는 **빈 파일 모듈에 다른 종류 드롭 = 새 창** 하나.
- v0.198.0 **A188** PDF 중앙 정렬 수리 — **원인 판정 = Contain 배율(<1)에서 ZoomMode ScrollViewer가 줌
  콘텐츠를 좌상단에 앵커해 `HorizontalContentAlignment=Center`가 퇴화**(등재 시의 "세터가 있는데 왜" 역설
  해소, 재현은 열자마자 항상). 수리 = `ItemsPanelRoot.MinWidth = 뷰포트/배율`(4지점) + `ApplyFitAt`의 낡은
  `ExtentWidth` 동반 해소. **텍스트 전폭은 버그가 아니라 A181 사양**이라 버그는 PDF만이라고 판정했다.
- v0.199.0 **A189** 새 텍스트 파일 — 하단 바 New text file → **무제 문서**(`_untitled` 축 신설로 `_path=null`
  3상태 구분 · 제목 `KOTU - Untitled` · 셸 통지 = **`IUntitledContentSource` 소형 계약 신설**), 저장은 A113
  Save as 재사용 + **무제 첫 저장의 제목 갱신 수리**. 탐색기 빈 영역 New file은 New folder 관용구 복제라
  감시 보류·즉시 이름변경이 자동으로 붙는다.
- v0.200.0 **A190** 마크다운 편집 + 뷰어 — **자체 최소 렌더러**(`MarkdownParser` 워커 파싱 +
  `MarkdownRenderer` UI 조립, **NuGet 0 · RichTextBlock 미사용**). 열면 렌더가 기본이고 하단 바 토글로 편집.
  지원 밖·예외는 **전부 원문 폴백**(다운 금지), 대용량(A177 임계)·잘림은 렌더 생략. 상태 전이표를 주석에
  수록했다(`ResetRenderState` 5지점).
- v0.201.0 **A191** 이미지 트레이 = 해상도 2줄(A137 ② 중복 해소). `TrayFormat.Pixels` 신설(실측 픽셀 —
  Resolution 등급 축약과 구분). **5자리 산정**: 폰트 축소 하한 5px에서도 16px 폭에 5자리가 안 들어가므로
  **≥10000은 `12k` 축약**(A138 `999+` 상한 관례와 동형). 값은 **표시 기준**(EXIF+R 회전 스왑 —
  `RotationSwapsAxes` 단일 원본을 `UpdateFit`과 공유). 하단 바·정보 패널의 미스왑 표기는 별건 보고.
- **A178 — 코드 변경 0(전역 감사·명문화, 결번).** UI 스레드 점유 감사표 작성 + **아키텍처 경계 위반 0건**
  (셸↔모듈·Core↔뷰 의존 방향)으로 **수리 대상이 없었다**. 원칙은 **`ARCHITECTURE.md` §11**로 명문화(A42
  스레드 모델의 상위 원칙). 1호 실사례였던 `EditorBox.Text`는 A177에서 이미 해소. **개선 여지 4건은 낙수
  목록 11~14번**으로 올렸다.
- **운용 메모**: 이번 묶음은 서브에이전트 중단 없이 완주했다. 지시-코드 불일치가 **2건**(A187 조건 제거 ·
  A175 원흉 지목) 나왔고 둘 다 **코드를 따르고 보고**하는 규칙대로 처리했다 — 프롬프트 9번 항목이 실제로
  값을 한 사례로 기록해 둔다.

**2026-08-16 세션 (13회전) — A94 후속 "가능한 전부" 4배치 완주 → A94 완료·결번: v0.150.0~v0.153.0 (fable 2·opus 2)**
사용자 확정: "윈도우 탐색기 동등 수준을 가능하면 다". 스코프 제외 5건은 매트릭스 머리에 영구 기록
(셸 확장 메뉴·Alt 바로가기 = COM 대형·별도 등재 후보 / 드래그 배지 / UAC 자동 재시도 / 진행률 대화상자).
- v0.150.0 **3차** 이름 충돌 대화상자(Replace/Skip/Keep both·**폴더 Replace=병합**·all 체크·Esc
  취소=남은 작업 중단·수행분 유지) + 진행 문구(3개 이상·100ms 스로틀). 워커→UI = TCS await만
  (UI .Wait 금지), **ContentDialog 창당 1개 세마포어**(A113 실사례 대응 — XamlRoot 단위).
- v0.151.0 **4차** Shift+Del 영구 삭제(확인창 기본 Cancel — Del과 같은 루프·옵션만 다름) +
  잘라내기 반투명 0.5(전역 1벌 HashSet·Esc/새 복사/소진 해제 — **Clipboard.ContentChanged 선례
  없어 미구독**: 외부 붙여넣기 후 표시 잔존 한계 명기) + 접근 거부(0x80070005) 구분 안내 →
  **Restart as admin 제안**(runas 흐름을 `AdminRelaunch`로 공용화 — HardwareView는 Core 훅
  `AdminRelaunchHook` 경유로 동작 불변, A124 창 세트 복원과 자동 결합).
- v0.152.0 **5차** 폴더 감시 — ExplorerPane(단일 원본) FileSystemWatcher, 디바운스 400ms·
  전체 재스캔 재사용(증분 없음)·**이름변경 편집 중 보류**(EditEnded로 소화)·폴더 소실 = 존재하는
  상위로 이동·실패 전부 조용히 무감시. 재대상 = dispose+재생성. 재진입은 기존 `_loadSeq`가 방어.
  ⚠️ FileSystemWatcher·Renamed 반공변 구독 = 저장소 최초(복구법 확보 — BCL 표준이라 저위험).
- v0.153.0 **6차** 빈 영역 우클릭(New folder·Paste·Refresh — ContextFlyout 중첩 규칙로 이중
  발화 차단, **전제 = IconGrid/ListPane `Background=Transparent` 추가**) + 항목 메뉴 Cut/Copy/
  폴더 Paste + **다중 일괄 열기**(첫 파일 = A24 규칙·나머지 새 인스턴스·폴더 제외·상한 10+안내) +
  **Preferred DropEffect 왕복**(쓰기 2/5·읽기 = 이동 비트 판정 — OS 탐색기 잘라내기 정합).
  낙수: ThumbnailExplorer/FileListOverlay `SelectedFilePath`가 질의 API로만 남음(주석 명기).
  A86-keymap의 Enter 다중 확장 미언급은 후속 정리 후보.

**2026-08-15 세션 (11회전 계속) — 사용자 신규 지시 2건 등재→확정→구현: v0.144.0·v0.145.0 (fable 2배치)**
사용자 실기기 피드백("Alt+Z/X 문제 많음"·H/W 정사각형)을 A118·A119로 등재, 질문 6건 답변으로 사양
확정(부록 B 59·60 — 개념 재확인: 사이드바=불투명·밀어냄/오버레이=반투명·덮음, 내용 동일·전 모듈 공통).
- v0.144.0 **A118** 좌/우 패널 키 = **F1/F2**(Alt+Z/X 폐지, 전이·2연타·동시 규칙 승계). 분기는
  텍스트 입력 가드보다 앞(편집 중 동작이 목적). **F2 = 탐색기 이름변경(A94)과 원 기능 우선 공존**
  (표면 Handled 시 셸 양보 — 기존 Handled 실증). Alt 부기(`_altComboUsed` 등)는 Alt+`·Alt+0~7용으로
  존치. 키 상수 2개 한 곳(LeftPanelKey/RightPanelKey — 시험적 변경 되돌리기 지점). 힌트·키맵·
  USER-GUIDE(As of v0.144.0)·A34 표 연쇄 갱신.
- v0.145.0 **A119** 정보 모듈 사이드바 편입 — Core `ISidePanelProvider`(뷰가 좌/우 콘텐츠 제공,
  object 반환·뷰 소유·셸 호스트 유일 부모) + 셸 `SidePanelHost`(반투명/불투명·힌트를 파일 오버레이와
  동일 규칙로 재현 — **A92 힌트 타이밍 장치가 세 벌**이 됨, 상호 경고 주석). 게이트 전수 =
  `HasPanelContext` 하나로 통일(F1/F2 소비·전이·경계 버튼·CurrentShellState → Enter 성립). 진입
  기본 양쪽 사이드바는 수리 0건(A109 경로가 원래 부르고 있었고 표시 층 게이트만 막고 있었다).
  `ISidebarAwareView` = 도크 수(0/1/2) 계약 개정 → 센터 **2/3/4열**(오버레이는 계수 안 함 — A93
  해석), 타일·좌 그래프 **정사각형**(실폭 파생·조기 반환으로 레이아웃 사이클 방어·세로 스크롤).
  ApplyOverlayStates가 상태 변경 단일 종착점이라 재푸시 누락 구조적으로 없음. 모듈 전환 3경로에서
  `ClearModulePanels`(잔존 방지). 하단 바·맥박 임계 676 무변경(센터 폭과 무관 — 재계수 결과).
  CI 1순위 위험 = 코드 전용 컨트롤의 XAML 선언(복구법 확보).

**2026-08-15 세션 (11회전) — 잔여 착수 가능분 완주: A110 감사 + v0.140.0~v0.143.0, fable 서브에이전트 4배치**
사용자 확정 "순서대로 전부"(A110 → A112 → A113+A115 → A104·A105). push 1회(몰아 push — v0.143.0만 태그, 사용자 의도).
- **A110**(코드 감사 — 코드 변경 0·결번): 오버레이/사이드바/셸 키 도메인 **실버그 0건**. 상태 소유
  감사표 30행 — 창별 상태 전부 인스턴스 필드(A107 `_altComboUsed`는 Deactivated 리셋), 전역은 전부
  의도됨(A70 1벌·A55/A89·`explorer.sort`·아이콘 캐시 — 키에 렌더 입력 전부 포함). **스레드 모델
  확정: 모든 창 = 단일 UI 스레드**(WindowManager.Create 1지점). 오버레이 열림·S4를 설정에 저장 안
  함 = 사양 확인. 낙수: 비활성 창 경계 버튼 표시 잔존(코스메틱 — 방어선은 Deactivated에
  `HideEdgeButtons()` 한 줄, 실기기에서 거슬리면 등재).
- v0.140.0 **A112** 마우스 뒤로가기(XButton1)+Browser Back 키 = '뒤로' 분배 `TryNavigateBack` —
  한 번에 한 층: ① 전체화면 해제(A90 Esc 순서를 셸이 직접 시행 — 전체화면 검사가 S4보다 앞) ②
  S4 복귀 ③ 콘텐츠 닫기 = **ShowModule(빈 컨텍스트) 재사용**(A37 가드·A59 해체·제목/트레이 복귀
  전부 기존 경로 몫, defaultSidebars=false라 **사이드바 보존**) ④ 없음=무동작·무소비. 신규 API 2건
  선례 없음(PointerUpdateKind·VirtualKey.GoBack — CI 통과 여부 확인 대상이었고 복구법 확보).
- v0.141.0 **A113** 문서 정합 런타임 체크 ⓐ~ⓓ(사용자 승인 사양 그대로 — 강행 금지·선택권):
  저장 순서 = ⓓ 외부 변경(Overwrite/Cancel·기본 Cancel) → ⓑ 라운드트립 손실 예고(로드 시 1회
  판정) → CP949 기존 경로 → 쓰기 → ⓐ 재검증(Retry/**Save as...**(FileSavePicker 신규)/Cancel).
  저장 성공 시 재기준화(누락 시 ⓓ 오탐). **ⓒ 더티 = 기준 텍스트 비교**(길이 즉시 + 250ms 디바운스
  — undo 원복이면 ● 꺼짐) + 보류 판정 settle(저장·닫기 직전 확정 — 자체 발견 구멍 2건 수리).
  한계: Save as... 후 창 제목 미갱신(셸 OnContentOpened가 제목 안 만짐 — 후속 후보).
- v0.142.0 **A115** 에디터 라인 가이드 + 비가시 문자(¶·EOF) — **`GetRectFromCharacterIndex` 실측**
  (LineHeight 강제 아님 — TextBox에 그 속성이 없다), 뷰포트 한정 렌더 + 내부 ScrollViewer 훅.
  **실패 시 장식만 조용히 오프**(템플릿 의존 = v0.113.1급 부류 — CI로 못 잡는다). 불투명도 상수
  0.08/0.25 한 곳. **④ 커서 형광 녹색 = 불가 판정**(WinUI 캐럿 색 공개 API 없음 — microsoft-ui-xaml
  #9005 not planned. RichEditBox도 동일 한계라 이관 실익 없음) — 항목 잔여로 존치(§5).
- v0.143.0 **A104+A105** — A104 = **상한 부재 증빙**(제거 코드 0: 슬롯 int 단조·캐시 전부 모듈 축
  유계·제목 번호 자릿수 무제한. 배지 10+ 숨김·9색 순환은 사양 유지). A105 ① 창별 AUMID
  `KOTU.Instance.{슬롯}` — **저장소 최초 COM 인터페이스 interop**(`TaskbarIdentity.cs` 격리 신설,
  실패 전부 조용히 무시·최소 복구 = 호출 1곳 주석). 시퀀스 = A100 슬롯(표시 번호는 재배정되므로
  부적격). 수용 트레이드오프: pin·점프리스트 인스턴스 단위 분리, Velopack 바로가기 그룹 연결
  느슨. ② 창 32px 아이콘 하단 모듈 3자(IdleTrayLabel 단일 출처·다크 판+Lighten 0.30·캐시 키에
  3자 명시·**16px 생략**(폰트 4.8px < A54 실증 하한)).

**2026-08-15 세션 (10회전 계속) — 합의 순서 전체 완주: v0.131.0~v0.139.0, 9배치(fable 4·opus 4 + A70), CI 전부 초록·핫픽스 0**
사용자 지시 "다음 오케스트레이션 세션으로 넘어가야 하는 상황 전까지 계속" — A70 뒤로 8배치를 이어서 완주했다.
push 4회(v0.131 / v0.132+133 / v0.134+135 / v0.136~138 / v0.139 — 몰아 push 시 마지막만 태그, 사용자 의도).
- v0.132.0 **A106** 버튼 32×32·시작 메뉴 항목 36(+패딩 8) — 파생 재계수: BarFixedWidth 364·영상 임계 698·
  배지 24,6·ModuleBarHost 82·1칸 Padding 4→2(아이콘 잘림 방지 — 32에서 필수였다).
- v0.133.0 **A83+A111** Fit 4옵션(100%·Contain·Fit width/height) 상태형 SplitButton 3모듈 통일,
  1:1 별도 버튼 제거(컬럼 재번호), **Contain=축소만 — 실동작 변경은 영상뿐**(이미지·PDF는 원래 축소만),
  A/F 키 재배선(A=100% 적용·본체 표시도 갱신), 영상 임계 698→660.
- v0.134.0 **A107** 전역 키 Alt 조합(Alt+`·Alt+0~7·Alt+Z/X) — 전이 유지·"열림 1회=닫기"는 재검토 결과
  **유지**·Z+X 동시 신설·단독 키 폐지. **Alt 처리 = down 소비 없이 "우리 조합 발화 시 Alt 단독 up만
  조건 소비"**(A58 방식 개작 — down 소비는 시스템 조합(Alt+Tab 등)을 죽여 채택 불가). `OverlayHints.cs`
  신설(A92 문구 단일 출처). keymap 개정.
- v0.135.0 **A108+A116** 용어 확정(Sidebar=불투명/Overlay=반투명 — UI 문구·주석·keymap, 식별자는 유지),
  틴트 ×0.6(App.xaml OverlayAcrylicBrush, 다크·라이트 쌍), 힌트 위치 = 경계 버튼 옆.
  **A116 실원인 = 리사이즈 버그가 아니라 상태별 25/30 이원값**(콘텐츠 상태 30% — A57 사양) →
  `SidebarPercent = 25` 전 상태 단일화. ⚠️ **구 사양(콘텐츠 30%) 대체가 의도에 맞는지 사용자 확인 대상**
  (되돌리기 = MainWindow.xaml.cs 상수 1곳).
- v0.136.0 **A109+A114+A117** 모듈 전환 = 좌·우 사이드바 기본(파일 인자 시작은 무사이드바 유지) /
  업데이트 4차 개정(2분 주기 + 설정 진입 1회 + Check now, 조용히 설정 화면 반영만 — 토스트 코드 미복원) /
  탐색기 정렬 Date created(Entry 레코드 확장·`explorer.sort` "created").
- v0.137.0 **A101** SensorTray(A18 상시 아이콘)·TraySensors **삭제**, HardwareView가 ITrayStatusProvider
  구현(창별 아이콘 = 그 창 선택값 2줄·유휴 INF 폴백·ComposeKey 선비교). 낙수 3건: ① 채널 색 트레이
  소실(TrayStatus 계약에 색 없음 — 확장은 후속 후보) ② 선택 1개 = `값/—` 표기(계약상 2줄 고정)
  ③ "앱 실행 중 상시 표시" 소멸(사양 변경 확정분). 고아 연쇄 정리(SubscribeSensors·ShowHardware 등).
- v0.138.0 **A60 3차**(A71·A72 흡수) H/W 3구획: 좌 큰 그래프(10s·선택 2)/센터 10타일(30s·클릭 선택·
  드래그 순서 — `hardware.channelOrder` 전역 1벌)/우 스펙 텍스트, 하단 바 = 선택 긴 그래프 2개(각 152px·
  최대 5분 = min(300s, 링600×주기))가 카드 10개 대체, v0.42.0 대시보드 폐기, `ISidebarAwareView` 계약
  신설(셸이 4/8열 신호 푸시). ⚠️ **현행 셸 정책상 H/W엔 사이드바가 안 떠 상시 8열**(4열 배선은 완료 —
  정책 바뀌면 자동). 맥박 임계 = 676.
- v0.139.0 **A87** 사용자 가이드 — `docs/USER-GUIDE.md`(영어·As of v0.139.0·스크린샷 없음) +
  `site/guide.html`(index 룩 복제) + 내비·README 링크. 전 서술을 코드 grep으로 검증(사실 검증표),
  확신 부족 8건은 의도적으로 뺐다. **발견: site/index.html이 아직 v0.89.0을 광고**(후속 등재 후보, §5).

**2026-08-15 세션 (10회전 전반) — A70 완료(v0.131.0), fable 서브에이전트 1배치, CI·릴리스 초록**
- v0.131.0 **A70** 하드웨어 모듈 다중 인스턴스 — 신규 `HardwareInstanceState.cs` = **인스턴스 상태 +
  전역 1벌 스토어 + 저장 깔때기 단일 소스**(뷰가 `CreateForView()`로 소유). 인스턴스별 = 센서
  선택(A18)·바 크기(A62) / 공유 유지 = 폴러·섹션 캐시·**리프레시 주기**(폴러가 하나 — 항목의
  "구현 시 확인 필요"였던 인스턴스별 주기는 **공유 유지로 확정**, 폴러 분할 비용 대비 이득 없음).
  저장 키 불변(마이그레이션 0)·모든 변경 즉시 커밋(닫힘 훅 불필요)·**창 간 동기화 이벤트 폐지**
  (`BarScaleChanged` 삭제, `TraySensors`는 SensorTray용 전역 파사드로 축소 — Selected volatile
  스냅샷 규약 유지). 트레이 메뉴 Clear = **전역 1벌만** 비움(열린 창 선택 유지 — 다음 토글로 부활).
  배치 최대 함정이던 **뷰의 배율 static 헬퍼 연쇄**(AxisMinWidthNow·MinCardWidthNow·UpdateCard·
  RenderSparkline)는 인스턴스 승격으로 해소(배율 static 사본 금지 — 몰래 공유 재발 방지).
  Shift+N(구 Ctrl+N) 새 창 흐름은 하드웨어 모듈에서도 성립함을 코드로 확인(REQUIREMENTS 완료 서술).
  **A60 3차·A101이 이 구조에 얹힌다**(훅은 클래스 헤더 주석에 명시 — 선행 조건 충족).

**2026-08-14 세션 (9회전, Fable 오케스트레이터) — A100 ②·A99·A102+A103 완료(v0.128.0~v0.130.0)**
- **운용 방식 전환(사용자 확정)**: 실개발은 모델별 새 세션 대신 **오케스트레이터가 서브에이전트
  호출의 model 파라미터로 권장 모델을 명시 지정해 위임**(A99 = fable, A102+A103 = opus로 실증).
  CLAUDE.md 규칙 1.4·§2 개정. 진행 순서 합의: A99 → A102+A103 → A70 → A106·A83+A111 →
  A107·A108+A116 → A60+A101 → A87(소형 A114·A117·A109는 틈에). push는 묶음 끝 1회.
- v0.129.0 **A99** 모듈 열기 버튼 제거(6표면) — O 핫키 소멸(부록 B 58), S4 버튼 핫키 미배정,
  Grid.Column 재번호(컬럼 0 제거 — 검산 스크립트로 증빙), Video 폭 임계 760→718(42px 감산),
  A34 키 맵 표 O 행 제거. 다른 열기 진입로 없음 확인.
- v0.130.0 **A102+A103** — 배지 렌더 제거(창·트레이), 링 = `Branding.IconRing` 모듈 액센트
  단일 소스(항상 표시 · 중립·정보(H/W)는 무링), 트레이 값 글자 ×0.85(`TrayStatusIcon.FontScale` 한 상수),
  InstanceIcon 캐시 키에 링 ARGB. 제목 = `2KOTU - 파일명`(조립 지점 8곳 → `FileTitle` 헬퍼),
  em-dash→하이픈 21곳(제외: 값 없음 글리프 `—`·MissionStatement — splash.png와 한 벌).
  **구현 시 결정: 설정 화면 제목도 `KOTU`만**(구 `KOTU Settings` — 되돌리려면 MainWindow 한 줄).
- v0.128.0 **A100 ②** IsPromoted 자동 승격 — `Integration/TrayPromotion.cs` 신설: NIM_ADD 후 약
  1/4/10초 3회 `Control Panel\NotifyIconSettings` 스캔, 자기 exe 항목(ProcessPath 파일명 **접미사**
  매칭 — 하드코딩 없음) `IsPromoted`(DWORD 명시)=1. 이미 1이면 안 씀, 실패 전부 조용히 무시,
  항목별 개별 try/catch, RegistryKey는 using. 호출 4곳 = 생성자·TaskbarCreated × TrayIcon/SensorTray.
  **함정 회피 핵심: SensorTray는 첫 NimAdd 전이에서만 호출** — Render가 200ms 폴링이라 NimModify
  경로에서 부르면 스캔 태스크가 증식한다(firstAdd 캡처). 이로써 **A100 완료·결번** — ①의 "슬롯당
  1회 수동 켜기"까지 소멸(② 실패 시 그 수동 1회로 후퇴할 뿐). 실기기 확인은 §4 v0.128.0.

**2026-08-14 세션 (8회전, Fable) — A100 1차 직접 구현 + A85 서브에이전트 1배치, push 2회**
- 규칙: **요구사항 등재 시 권장 모델(Fable/Opus) 병기**(운용 규칙 5 — 기존 16건 소급) · **다이어트
  일괄 적용**(22건 ARCHIVE 이관, 1231→873줄 — 남긴 완료 상세는 전부 미진행건 참조 근거를 헤더에 명기).
- v0.126.0 **A100 1차** — 트레이 아이콘 식별 결정화: 원인 = 콜백 창 클래스명 랜덤 GUID →
  Win11 NotifyIconSettings 항목이 실행·창마다 증식(새 항목 기본 '끔'). 클래스명·uID를 프로세스 내
  단조 증가 슬롯(`KOTUTrayWnd_{n}`·uID 100+n, SensorTray는 고정 이름·uID 2)으로 교체.
  guidItem은 exe 경로 결박(dev/설치본 병행 실패)으로 배제. Velopack `current` 고정 경로 확인.
  **②(IsPromoted 자동 쓰기)는 리스크 평가 등재·사용자 결정 대기**(① 실기기 검증 후 판단 권장).
- v0.127.0 **A85** — 원인은 클릭 쌍(ItemClick 2회) 판정: 두 번째 클릭이 더블탭 제스처로 소비돼
  열기 요청이 침묵 소실("같은-모듈 조기 반환" 가설은 기각 — ShowModule 조기 반환 없음).
  DoubleTapped 열기 신설 + Activate 종착점 + 이중 발화 억제, 좌 리스트 단발 Enter 구멍 수리.
  서브에이전트 대조표 작성 중 Enter 구멍을 스스로 발견 — "대조표 요구" 장치가 또 값을 했다.
- A99(하단 바 '열기' 아이콘 2개 중복) 등재 — **방향 사용자 확정 대기**(아이콘 차별화 vs 통합).
- 같은 날 오후 **실기기 피드백 34건 일괄 등재 → A101~A117 신설 + A60 3차 교체(A71·A72 흡수 결번)**.
  굵직한 것: 전역 키 Alt 조합 전환(A107 — keymap 개정 필요·대형), H/W 3구획 재구성(A60 3차),
  사이드바/오버레이 개념 확정(A108), 업데이트 4차 반전(A114 — 2분 주기), H/W 트레이 이원화
  해소(A101). **같은 날 사용자 일괄 답변(부록 B 55)으로 확인 필요 대부분 소진** — A99(S4만 유지)·
  A100 ②(IsPromoted 확정·착수 가능)·A101(A18 축 폐지)·A60(A17 대체·승계)·A105(인스턴스별 정정)·
  A107(전이 유지)·A109·A117 확정. 이어진 답변(부록 B 56~58)으로 **확인 필요 완전 소진**:
  A113 ⓐ~ⓓ 승인(경고 = 선택권 제공) · A83 = Fit 4옵션 공통·Contain 축소만(보류 해제, Opus로 변경) ·
  A114 = (b) 설정 화면 반영만 · A87 = "vX.Y.Z 기준" 표기 · A107 문자 핫키 = **전부 유지**(잠정 폐지
  번복 — O만 A99로 소멸). **전 항목 착수 가능.**

**2026-08-13 세션 (7회전, Fable ②) — §6 배치 제안의 "Fable ②"(A98→A94)를 3배치로 완주(A94는 1·2차 분할), CI 확인·핫픽스 0**
서브에이전트 직렬 파이프라인. 착수 전 사용자 확정 2건: **배치 범위 = A98→A94** / **A94 = 안전 조작 우선**
(영구 삭제·권한 상승은 후속 등재로 미룸).
- v0.123.0 **A98** 휠 줌 재정의 — 이미지·PDF Ctrl+휠 복귀(A84 Shift+휠 폐기), **사진은 휠 단독으로도 줌**
  (구 휠 이전/다음 탐색(v0.41.0)은 사양상 대체 — 탐색은 ←/→·하단 바 버튼 존속), **영상 줌 신규** =
  A30 libvlc `MediaPlayer.Scale` 위에 `VideoFitMode.Manual` 추가(자동 맞춤 유효 배율을 뷰포트·트랙
  크기로 직접 계산해 출발, 노치당 ×1.1·0.1~8, Fit 추종 해제, 휠 단독=볼륨 유지, A13 칩 `Zoom n%`).
  keymap 휠 2행 반영 완료. 새 미디어 로드 리셋은 기존 Fit 회귀+Playing 재적용이 자동 수행(추가 코드 0).
- v0.124.0 **A94 1차** 다중선택(리스트·썸네일 Extended·Ctrl+A, **트리는 Single**), 드래그 아웃
  (StorageItems — **`DragItemsStarting`이 아니라 컨테이너 `CanDrag`+`DragStarting` 데퍼럴 경로**:
  StorageItems 수집이 비동기라서), 좌·중 드랍 이동/복사(같은 볼륨=이동/다른 볼륨=복사·Ctrl/Shift 강제,
  **외부 소스는 경로를 몰라 기본=복사**, 충돌="이름 (2)" 자동 — 대화상자 없음), 클립보드 Ctrl+C/X/V
  (잘라내기=RequestedOperation Move·성공 시 1회성). 공용 로직 `ExplorerFileOps.cs` 신설.
  **`docs/A94-matrix.md`(탐색기 동작 대조표)가 A94 진행의 단일 원본.**
- v0.125.0 **A94 2차** F2 인라인 이름변경(`ExplorerRenameBox.cs` — TextBox **삽입**(reparent 아님),
  Enter/LostFocus 커밋·Esc 취소·확장자 제외 선택, 실패=원복·자동 "(2)" 없음), Ctrl+Shift+N 새 폴더
  (생성 후 곧장 편집 진입 — ExplorerPane은 `NavigateToAsync` 분리로 재스캔 완료 대기, 썸네일은
  `_pendingRenamePath` 예약), Del=휴지통(`StorageDeleteOption.Default` — ImageViewerView에 선례 있었음,
  Shift/Ctrl+Del은 비켜 감), 우클릭 메뉴 Rename·Delete(폴더에도 부착). 빈 영역 메뉴는 원래 없어 새 폴더는 키만.

**2026-08-13 세션 (6회전, Fable ①) — §6 배치 제안의 "Fable ①"(A93→A86→A90)을 3배치로 완주, CI 3/3 초록·핫픽스 0**
서브에이전트 직렬 파이프라인(항목당 1배치). 착수 전 **A83은 사용자 보류** — 초안 v1은
`docs/A83-autofit-draft.md`에 보관(재론 시 출발점, 확정 전 구현 금지).
- v0.120.0 **A93** 탐색기 3구획 25:50:25 + 중앙 **ThumbnailExplorer 신설**(열 수 4/8·타일 크기 가변,
  이미지 BitmapImage 실미리보기·그 외 모듈 액센트 타일). **좌 도크 리스트(ExplorerPane)가 폴더 상태의
  단일 원본** — `ViewChanged`로 썸네일에 흐르고, `FileListOverlay.NavigateList`(Show에서 분리)가 되돌이
  경로. 드랍: 인포=열기 / 콘텐츠=같은 종류 그 자리·다른 종류 새 인스턴스 / **좌·중=무동작(A94 대기)**.
  A63 흡수·A81 중앙 숨김 대체. ※ 등재문의 "중앙에 Drop here 문구"는 착오였음(실제 = ExplorerPane).
- v0.121.0 **A86** 오버레이 키 Z/X(A58 전이 유지 + **열림 상태 1회 = 닫기** 신설 — 2연타 판정은
  **"첫 탭 이전 상태(TapStartState)" 기준**으로 충돌 해소), 셸 6상태(S1~S4) 판정 도입, **Enter 일괄
  토글**(직전 구성 세션 복원·원 기능 우선 — 영상 전체화면·에디터 줄바꿈·탐색기 선택 열기), 경계 버튼
  (근접 ±48px 표시·불투명 도크 토글), Alt OS 메뉴 회피 제거, A92 문구 "press Z/X to close" 갱신.
  구현 사양 = `docs/A86-keymap.md`. 이미지·오디오의 Enter=전체화면 액셀러레이터는 제거(F11 유지).
- v0.122.0 **A90** '오픈 파일' 버튼(시작 메뉴 옆 — **ModuleBarHost.Margin 48→90** 파생 재계산) = S4
  탐색 모드. 진입=기존 구획 유지·없는 쪽만 반투명 추가·중앙 반투명 썸네일(S4 **전용 인스턴스** —
  S1 인스턴스와 공유하면 reparent 함정), Esc/재누름=진입 전 스냅샷 복귀(`_s4Restore` — A86
  `_lastBatchStates`와 별개), **콘텐츠 전환 시 자동 종료**(SetContentState/OnContentOpened 입구).
  S1=액센트 테두리 450ms 강조만(타이머 2단계 — Storyboard 불사용). 그리드 Enter=선택 열기 신설(S1에도 적용).
  **Esc 우선순위는 지시를 코드에 맞게 조정**: 모듈 액셀러레이터가 셸 루트 핸들러보다 먼저 돌아
  전체화면+S4 중첩 시 첫 Esc=전체화면 해제, 다음 Esc=S4 복귀.

**2026-08-13 세션 (5회전) — §6 배치 제안의 "Opus ① → Opus ②"를 6배치로 완주, CI 초록**
서브에이전트 직렬 파이프라인(오케스트레이터가 배치 정의·diff 검수·커밋·푸시·CI 확인).
착수 전 사용자 확정 3건: **A96 번호 = 기존 순서 그대로 한 칸씩 밀기** / **A79 = 0·1·2·3 4단계** /
**A79 지정 UI = settings.json 키만**(설정 화면 비노출).
- v0.114.0 **A88+A89** EKG 연속 스크롤(`CompositionTarget.Rendering` 렌더 루프를 데이터 주기와 분리 —
  `RenderPulse` 좌표식은 이미 `now` 기준이라 **호출 빈도만 올리면 흘러간다**) + 새 창이 **마지막에 닫은 창을
  그대로 승계**(A55 계단식 오프셋 폐기, **겹쳐도 비켜 주지 않는다**).
  ⚠️ `CompositionTarget.Rendering`은 **static 이벤트** — Unloaded 해제를 빠뜨리면 창이 통째로 샌다.
- v0.115.0 **A91+A92+A67** 좌 오버레이 주소·필터 줄을 패널 최상단으로(`ExplorerPane.DetachPathBar()`로
  **요소를 reparent** — 중복 부착 가드는 `Children.Contains`/null, **`Parent` 금지** §3.4) +
  도크·고정 안내 문구 **2.5초 표시 → 300ms 페이드아웃**(재시작은 "표시 상태 신규 진입 또는 문구 변경"일 때만 —
  매 SetState마다 되감으면 영영 안 사라진다) + sponsors.json URL 4건 기입.
- v0.116.0 **A96+A97** 시작 메뉴 **All Readable=1번**(나머지 2~7로 한 칸씩), 구분선 2개, 폭 124→**136**,
  항목 높이 44→**40**, 플라이아웃 밀착(`FlyoutPresenter` **Margin `0,0,0,-6`** — WinUI가 간격 오프셋을
  속성·리소스로 안 내놔 음수 여백이 유일한 지렛대다. **실기기 미세조정 지점**) +
  하단 바 버튼 40→**36**(2칸 폭 84 유지), 간격 **6 통일**, SplitButton 인라인 **3곳**(이미지·문서·**영상**),
  `MaxCardHeight` 40→36, **`BarFixedWidth` 458→380**(구 값은 근사치였다 — 요소를 직접 세어 정확한 합으로 교체).
- v0.117.0 **A95** 업데이트 **수동 체크 전환** — 주기 타이머(30초·10분)·AppNotification 토스트·
  오토체크 토글 전량 제거. **정책 3차 반전**(v0.17.0 금지 → v0.105.0 도입 → 재폐기).
- v0.118.0 **A54** 트레이 정비 — Core **`ITrayStatusProvider`** 계약(모듈이 값만 내주고 셸이 GDI+ 합성),
  **유휴 = 1줄·중앙·저채도 3자(IMG/VID/AUD/DOC/ARC/ALL/INF) / 열림 = 2줄·모듈 색**.
  수명 감사에서 **"Restart as admin"의 `Application.Exit()` 경로에 아이콘 잔존** 위반을 찾아
  `AppDomain.ProcessExit` 정리를 추가(A18 `SensorTray`의 검증된 방어선 복제).
  **트레이 아이콘의 원형 번호 배지는 제거**(2줄 텍스트를 덮는다) — **창 아이콘 배지는 존치**.
- v0.119.0 **A79** 브랜드 에셋 **레벨 0~3** 구조 — `BrandAssets.IsEnabled(BrandPoint)` **단일 매핑**,
  지점 코드에 레벨 비교 금지. `packaging/brand.py`에 **같은 표 한 벌 더**(생성 스크립트용, 두 표는 같아야 한다).
  ①② 발바닥은 **벡터로 그린다**(시트 래스터를 16px로 줄이면 뭉개진다 — A46 결정 8번이 "KO/TU 2줄"을
  고른 바로 그 이유), ③④⑤⑥은 시트 조각 PNG. **기본 레벨 0 = 현행과 픽셀 동일**(생성기 산출물 md5 대조로 확인).

**2026-08-13 세션 (핫픽스 2) — v0.113.2: 정보 모듈 열기 크래시 수정**
v0.113.1로 올린 두 번째 실기기(v0.106에서 업데이트)에서 `COMException 0x800F1000
(설치된 구성 요소가 감지되지 않았습니다)` — 사용자가 보내 준 startup-error.log 스택으로
`HardwareView..ctor → ApplyBarScale → LayoutSensorCards → UIElementCollection.Add`를 특정.
원인: **`FrameworkElement.Parent`는 라이브 트리 부착 전(생성자 안)에는 `Children.Add` 뒤에도
null**이라 중복 추가 가드로 쓸 수 없다. v0.111.0(A62)이 생성자에서 `LayoutSensorCards`를
두 번(BuildSensorCards 끝 + ApplyBarScale) 타게 만들면서 같은 카드가 두 번 Add돼 죽었다.
`SensorGrid.Children.Contains`로 멤버십을 직접 확인하게 수정. 0x800F1000은 WinUI의 범용
오류 코드라 메시지로는 원인을 알 수 없고 **스택(startup-error.log)이 필수**였다 — 같은 다이얼로그가
떠도 원인은 제각각일 수 있으니 반드시 로그부터 받을 것.

**2026-08-13 세션 (핫픽스 1) — v0.113.1: 실기기 시작 크래시 수정**
사용자가 최신 빌드로 업데이트 후 실행하자 시작 즉시 `XamlParseException: Cannot find a Resource
with the Name/Key DefaultDropDownButtonStyle`. 원인은 v0.107.0(A27)의 App.xaml
`BasedOn="{StaticResource DefaultDropDownButtonStyle}"` — **WinAppSDK 1.8이 WinUI 컨트롤의
키 있는 기본 스타일을 지연 로딩으로 쪼개서**(소스 파일명 `DropDownButton_perf2026.xaml`) App.xaml
파싱 시점 StaticResource 조회가 실기기에서만 실패한다. **CI는 컴파일만 하므로 이 부류를 못 잡는다**(§3.4).
DropDownButton·ComboBox 두 스타일의 BasedOn을 제거해 해결(기본 템플릿은 명시 스타일에 Template
setter가 없으면 유지됨 — SplitButton 인라인 예외와 같은 원리). Button·ToggleButton의 BasedOn은
파싱 순서상 실기기 해석 성공이 확인돼 유지. **v0.107.0~v0.113.0 실기기 빌드는 시작 자체가 안 되므로
§4의 해당 구간 확인은 반드시 v0.113.1 이상으로 할 것.**

**2026-08-13 세션 (4회전) — "확인 없이 돌릴 수 있는 것 전부" 5배치, CI 5/5 초록**
(사용자가 항목 선정까지 오케스트레이터에 위임했다. 부록 B 39):
- v0.109.0 **A36+A67** 설정 파일 직접 열기 + 스폰서 링크. A36은 **새 창**으로 연다(설정 화면을 잃지 않게).
  `.json`이 어느 모듈 확장자도 아니라 **라우팅 재정의 `.json → document`** 한 줄을 썼다 —
  부수 효과로 탐색기에서 여는 임의의 `.json`도 문서 에디터로 열린다. A67 매핑은 `Assets/sponsors.json`
  (②안). **URL은 전부 비어 있다 — 사용자가 채워야 클릭이 동작한다**(§5).
- v0.110.0 **A73+A74** 리프레시 주기 **50/200/500(기본)/1000/2000/5000ms** + 그래프 축 스케일 표기.
  구 저장값은 **올림 이관**(100→200, 300→500 — 내리면 부하가 몰래 늘어난다). A74는 **A17 셀 = 하단 바
  카드가 같은 객체**라(A60 전까지 메인 그래프가 없다) 그 카드에 붙었고, **그래프 폭 90px 미만이면 숨김**.
- v0.111.0 **A61+A62** 핀(A39) = **하단 바만 남기고 접힘** + 하단 바 크기 S/M/L.
  ⚠️ 접힘은 **A40의 최소 높이 540을 창별로 낮춰 끼워야** 성립한다(`WindowMinSize.SetMinHeightOverride`).
  접힘은 **"핀 ON && 전체화면 아님"으로 계산되는 파생 상태**고, **접힌 높이는 A55에 저장하지 않는다**.
- v0.112.0 **A34** 전 모듈 버튼 핫키 + 툴팁 `(키)` 표기. 공용 `src/Shared/HotkeySupport.cs`를 7개
  csproj가 **Compile Link로 공유**(KOTU.Core가 UI 비의존이라 어셈블리로 못 만든다).
  **Z·X는 A86 예약으로 비워 뒀다** — 키 맵 표는 REQUIREMENTS A34 완료 서술에 있고 **A87 단축키 장의 원본**이다.
- v0.113.0 **A59** All Readable 모듈 — **센터+하단 바만 자식 모듈 뷰로 교체하는 중첩 호스팅**, 번호 키 **7**
  (1~6은 근육기억이라 재배치 안 함). 오버레이는 호스트가 소유하고 필터는 전 모듈 확장자 합집합.
  ⚠️ **확장자 연결에서 제외**(`IModule.RegistersFileAssociations = false`) — 확장자가 전부 겹쳐
  ProgID·UserChoice를 서로 덮어쓰기 때문. 그래서 탐색기 더블클릭은 종전대로 전용 모듈로 간다.

**2026-08-12 세션 (3회전) — 사용자 확정 없이 돌릴 수 있는 항목 4배치**:
- v0.105.0 **A26+A76** 업데이트 백그라운드 체크 — 전역 싱글턴 `Integration/UpdateCoordinator.cs`
  (시작 30초 뒤 첫 체크 → 10분 간격), WinAppSDK `AppNotification` 토스트, 오토체크 토글·마지막 확인 시각.
  v0.17.0 "주기 체크 금지"·v0.27.0 "설정 화면 카운트다운 루프" 정책이 **둘 다 폐기**됐다.
- v0.106.0 **A77** 확장자 연결 토글 워커 이관 — `ProgressRing` + `Registering... (3/12)` /
  `Setting as default... (3/12)` 단계별 표시. 설정 화면 전용 `ModuleWorker` 1개(모듈별로 안 나눔 —
  Capabilities 키 하나를 공유해 동시 쓰기가 서로를 지운다).
- v0.107.0 **A27** 하단 바 버튼 규격 통일 — `App.xaml`에 전역 스타일 5종
  (`BottomBarButtonStyle` 40×40 / `BottomBarWideButtonStyle` 84×40 / Toggle·DropDown·ComboBox 파생),
  전 모듈 29곳 적용. **SplitButton(Fit) 3곳만 예외** — WinUI가 키 있는 기본 스타일을 안 내놔
  `BasedOn` 대상이 없어서 같은 값을 인라인으로 뒀다(규격을 바꾸면 App.xaml + 그 3곳을 같이 고쳐야 함).
- v0.108.0 **A22** 드라이브 정보 개편 — 공용 컨트롤 `App/Controls/DriveStrip`, Core `IDriveStripHost`로
  모듈이 슬롯만 내준다(A57 ②와 같은 주입 방식). WMI 종류 조회는 `System.Management` 참조가 이미 있는
  **하드웨어 모듈**(`PhysicalDiskKinds.cs`)에 두고 App이 델리게이트로 넘긴다 — 새 패키지 참조 0.
  표시는 **파일이 열려 있지 않을 때만**(v0.47.0의 반대), 넘치면 초당 30px 마퀴 루프.
- v0.108.1 CI 핫픽스 — `TextBlock`에는 `IsEnabled`가 없다(`Control`이 아님). CS1061.

**같은 날 사용자 확정**: A86에서 **Enter 연동 전면 철회**(부록 B 35 — 오전 지시를 오후에 되돌림),
A36 = settings.json 그대로 열고 표기만 수정(37), 3회전 배치 선정(38).
**신규 등재**: A85(탐색기 더블클릭 열기 실패) · A86(오버레이 키 Z/X) · A87(사용자용 조작법 문서).

**2026-08-12 세션에서 한 것** — 오케스트레이터 + 서브에이전트 직렬 파이프라인 2회전
(배치마다 커밋·푸시·CI 초록 확인, 총 14배치):
- **1회전(확정 소형)**: v0.91.0 A80(문서 우측 빈 공간 — 원인은 EditorBox MaxWidth 900+Left) /
  v0.92.0 A65+A50 / v0.93.0 A75+A51 / v0.94.0 A40(WM_GETMINMAXINFO `WindowMinSize.cs`) /
  v0.95.0 A55 / v0.96.0 A49 / v0.97.0 A66.
- **2회전(UI 개편)**: v0.98.0 A57①②③(오버레이 공용 컨트롤 `Overlays/` 추출 + 좌(리스트)/우(정보)
  스왑 + 모듈별 필터. **CI 1회 실패** — 리팩터링에서 삭제한 2연타 필드를 사용처가 참조,
  핫픽스로 복원. 이후 서브에이전트에 "삭제 심볼별 grep 증빙" 의무화) /
  v0.99.0 A57④(좌측 상단 25% 디스크 트리) / v0.100.0 A58(입력: Shift(우)/Alt(좌), 홀드=반투명·
  2초=고정·2연타=불투명 밀어내기/해제, 4상태 머신 + 다른 키·클릭·휠 개입 시 홀드 취소) /
  v0.101.0 A81(파일 시작=닫힘 / 모듈 실행=양쪽 불투명 도크, 빈 모듈 컨텍스트) /
  v0.102.0 A84(전역 Ctrl→Shift — Shift+N·Shift+휠 줌, **Ctrl+S만 예외**) /
  v0.103.0 A68(인스턴스 색 테두리+원형 번호 배지, 9색 순환, GDI+ 합성 `InstanceIcon.cs`) /
  v0.104.0 A69(최소화=트레이 숨김, `AltTabExclusion.cs`).
- 같은 날 사용자 확정: A81 형태=불투명 밀어내기 양쪽(부록 B 30), Ctrl→Shift **전역**+Ctrl+S 예외(31→A84),
  A68 팔레트=9색 순환(32), A54 보류(33). **A83 등재**(Auto-fit 조건 적응형 — 규칙 표 합의 필요).

---

## 2. 일하는 방식 (반드시 지킬 것)

1. **요청은 `docs/REQUIREMENTS.md`에 등재만 한다. 개발은 하지 않는다.**
   실제 개발은 사용자가 **"A?? 진행"** 이라고 지시할 때만 착수.
2. **A번호는 영구 ID** — 재정렬·재사용 금지. 완료하면 항목을 "※ 완료 — 결번"으로 바꾸고
   부록 A 표에서 행을 빼고, 결번 목록에 추가.
   ※ **미구현 항목의 내용이 바뀐 경우엔 새 번호를 주지 말고 그 자리에서 갱신**한다(A26·A40·A60이 그렇게 처리됨).
3. **커밋은 버전을 명기**한다: `v0.XX.0 [A??] 요약`. `Directory.Build.props`의 `<Version>`을 같이 올린다.
4. **push는 작업 묶음 끝에 1회.** master push만으로 CI가 태그·릴리스까지 자동 처리한다.
5. **개발 우선순위: UI·코어 기능 우선** (사용자 확정). 녹화/녹음(A14·A15), HW 랭킹(A19),
   포터블 배포(A43), HWP 뷰어(A45)는 후순위.
6. 세션 전환이 필요한 시점이면 **묻지 말고 이 문서를 갱신**한다.
7. **개인 실명·개인 메일은 코드·문서·커밋 어디에도 쓰지 않는다**(A82). 저작 주체는 `ZP Studios`,
   커밋 identity는 저장소 로컬 `git config`에 이미 있다 — `git -c user.name=...` 오버라이드를 쓰지 말 것.
   ※ 실명을 지우는 작업의 기록에 실명을 인용하는 실수를 하기 쉽다(v0.90.0에서 실제로 썼다가 지웠다).

### 검증 분업
- **Claude**: 코드 시뮬레이션으로 자체 검수 + CI 통과 확인까지.
- **사용자**: 실기기에서만 확인 가능한 것(레지스트리 반영, UAC, 트레이, 백신 반응 등)을 마일스톤 테스트로.
- 개발 환경에 **dotnet이 없다** — 로컬 빌드 불가. **CI가 유일한 컴파일러**다.
  그래서 push 전 정적 검사(네임스페이스·경로·XML 유효성 등)를 반드시 스스로 돌릴 것.

### 빌드·릴리스
- master에 push → `build.yml`(빌드+테스트)과 `release.yml`(publish+Velopack+태그+릴리스)이 동시에 돈다. 각 3~4분.
- CI 결과는 **Chrome MCP로 확인**한다. `github.com/zpstudios/kotu/actions/workflows/build.yml`에서
  최신 run의 `svg[aria-label]`을 읽으면 성공/실패를 알 수 있다.
- push는 remote 이름 **`zp`** (PAT가 URL에 박혀 있음). `git push zp master`.
- 샌드박스에서 git이 `.git/*.lock`을 못 지우는 경우가 잦다 → `rm -f .git/*.lock` 후 재시도.
  파일 삭제가 "Operation not permitted"면 `mcp__cowork__allow_cowork_file_delete`로 권한을 받는다.

---

## 3. 반드시 알아야 할 함정

### 3.1 레거시 식별자 — 절대 일괄 치환하지 말 것
이름이 세 번 바뀌었다: `WinUtil`(초기 코드명) → `ZP`(v0.33.0) → `KOTU`(v0.86.0~).
**구 이름을 그대로 둬야 하는 곳**이 있다. 바꾸면 "구 등록 청소" 로직이 깨진다:

- `ExplorerIntegration.LegacyBrands = ["ZP", "WinUtil"]`
- `%AppData%\ZP`·`%AppData%\WinUtil`·`ZP-Main`·`WinUtil-Main`을 언급하는 주석
- `release.yml`의 구 packId 안내 문구

**교훈**: A46에서 "ZP" 문자열만 훑어 `%TEMP%\WinUtil\subtitles`를 놓쳤다(A64에서 발견).
리브랜딩 스캔은 **현재 이름이 아니라 모든 과거 이름으로** 해야 한다.

### 3.2 셸 등록은 "등록한 그 순간"으로 굳는다
`ExplorerIntegration`이 레지스트리에 쓰는 값 중 **exe 절대 경로와 표시 문구는 재등록될 때만 갱신**된다.
- 그래서 A52로 모듈명을 바꿔도 이미 켜 둔 사용자의 우클릭 메뉴는 옛 문구 그대로다.
- A64로 exe명이 바뀌면서 등록된 경로가 없는 파일을 가리키게 됐고, 실기기에서 실제로
  .png 더블클릭이 '어떤 앱으로 열까요' 팝업으로 빠졌다 → **v0.89.0의 A78이 자동 복구를 넣음**
  (매 실행 시 `ReRegisterIfExeMoved`가 경로 어긋남을 감지해 켜져 있는 등록만 조용히 재기록).
- **Capabilities와 UserChoice(A38)는 ProgID만 참조**해서 경로 변화의 영향을 받지 않는다.
- A38의 자체 검증(`VerifyUserChoice`)은 **우리 해시 알고리즘 기준의 자기일관성 검사**다 —
  Windows가 실제로 수용하는지는 열어 봐야 안다. OS가 알고리즘을 바꾸면 "전부 성공"으로 보이면서
  실제로는 전부 무효가 되는 맹점이 있다(§5 첫 항목의 의심 지점).

### 3.3 하드웨어 모듈은 거의 전부 static (프로세스 공유)
`HardwareModule`의 폴러·섹션 캐시·구독자 수·리프레시 주기가 모두 `static`이고,
센서 선택(A18)·주기(A29)도 프로세스 단위 설정 키다. **A42에서 의도한 설계**(드라이버 핸들·WMI 조회 공유)라
통째로 뒤집으면 안 된다. **A70(v0.131.0)이 그 분리선을 그었다** — 폴러·주기는 공유 유지, 선택(A18)·바
크기(A62)는 `HardwareInstanceState`(인스턴스 상태 + 전역 1벌 저장 깔때기)로 창별 분리. A60 3차의 창별
상태(피닝·순서·모드)는 **이 클래스에 필드를 얹는 방식**으로 간다(새 static을 만들지 말 것).

### 3.4 빌드를 실제로 깨뜨린 것들 (재발 방지)
- **csproj/XAML 주석 안에 하이픈 두 개(`--`)** → MSB4025로 빌드 실패. XML 주석 문법 제약.
  (A64에서 `--mainExe`를 주석에 적었다가 실제로 깨짐.)
- **PowerShell 백틱 줄바꿈 뒤에 주석** → continuation이 깨진다. 백틱은 그 줄의 마지막 문자여야 한다.
- `resources.pri` — unpackaged WinUI는 exe 옆의 **고정 이름 `resources.pri`** 만 찾는다.
  없으면 시작 시 `XamlParseException`으로 조용히 죽는다(v0.5.5~0.5.7에 실제로 겪음).
  `AssemblyName`을 바꾸면 `$(TargetName).pri` 폴백 이름도 따라 바뀌므로 `release.yml`을 같이 고쳐야 한다.
- **`FrameworkElement.Parent`를 "이미 추가했나" 가드로 쓰지 말 것** — 라이브 트리 부착 전
  (뷰 생성자 안)에는 `Children.Add` 뒤에도 Parent가 null이라, 같은 요소를 두 번 Add해
  `COMException 0x800F1000`으로 죽는다(v0.111.0/A62가 유발, v0.113.2에서 수정).
  중복 방지는 `Children.Contains`(또는 자체 플래그)로. 이것도 **CI·정적 검사로 못 잡는 부류**다.
- **XAML 요소를 다른 컨트롤로 옮겨 붙일 때(reparent)** 는 **반드시 옛 부모의 `Children`에서 먼저 제거**해야
  한다. 판정은 `Children.Contains`(위 항목과 같은 이유로 `Parent` 금지). 선례가 셋 있다 —
  `ImageViewerView.TakeBottomBar()`(v0.27.0), `ExplorerPane.DetachPathBar()`(A91/v0.115.0).
  ※ **x:Name 필드는 부모에서 떼어도 살아 있다**(`InitializeComponent`의 `Connect()`가 잡아 둔 참조).
  그래서 옮긴 요소의 이벤트 핸들러·코드비하인드 참조는 손댈 필요가 없다.
- **정적 이벤트 구독은 반드시 Unloaded에서 해제한다** — `CompositionTarget.Rendering`(A88/v0.114.0)처럼
  프레임워크 static 이벤트에 뷰 메서드를 걸면, 해제를 빠뜨렸을 때 **뷰와 그 창이 통째로 샌다**.
  이 저장소의 관용구는 `Loaded`에서 `-=` 후 `+=`(이중 구독 방지), `Unloaded`에서 `-=`다.
- **`{StaticResource Default*Style}` BasedOn 참조는 런타임에만 터진다** — v0.107.0의
  `DefaultDropDownButtonStyle`이 실기기 시작 크래시를 냈다(v0.113.1에서 수정). WinAppSDK 1.8이
  WinUI 컨트롤 기본 스타일을 지연 로딩해 App.xaml 파싱 시점에 키가 없다. **CI(컴파일)로는 못 잡고,
  정적 검사로도 못 잡는 부류** — 새 XAML에서 프레임워크 키를 StaticResource로 참조할 때는
  "그 키가 파싱 시점에 로드돼 있는가"를 의심할 것. 키 없이 setter만 둬도 기본 템플릿은 유지된다.

---

## 4. 실기기 확인 대기 (사용자 몫) — **v0.85.0~v0.238.0 누적 (최신 항목이 §4 안 최상단)**

⚠️ **최우선 = v0.236.0(A226) F11/F12 재검**: 각 모듈에서 문제 영역(스크롤바·리스트·슬라이더·뷰어
표면 등)을 한 번 클릭한 뒤 F11/F12가 즉시 듣는지 — 세 번째 시도(터널링 승격)라 이번에도 잔존하면
재현 표면(어느 모듈·어느 영역·클릭 종류)을 꼭 함께 보고(팝업(플라이아웃·콤보) 열림 중이었는지도 —
그 갈래는 알려진 한계).

**v0.231.0~v0.238.0 (2026-08-25 A226~A233 완주분) 확인 포인트**
- **A228**: 다른 앱 포그라운드(또는 Win+D) 상태에서 탐색기 더블클릭 → 새 창이 **실제로 앞으로
  튀어나오는지**(본체) · 기존 창 재사용 열기도 전면 전환 · SFW가 막히는 환경(전체화면 게임 등)
  = 작업표시줄 점멸 폴백·정상 케이스엔 점멸 없음 · 트레이 좌클릭 복귀 회귀 없음.
- **A233**: 텍스트 파일 타일에 첫 줄들 프리뷰 표시 · 12줄이 타일 하단 캡션을 침범하지 않는지
  (좁은 8열 타일) · 수백 파일 폴더 재스캔 체감 지연 없음 · CP949(BOM 없는 레거시 한글) 정상.
- **A227**: 마스터 스위치 On → 5모듈 일괄 등록(그룹별 링 병렬 진행) · 부분 상태면 마스터 Off
  표기 · busy 그룹 스킵 후 수동 재시도 성립.
- **A231**: 본체 "100%" 텍스트(9px) 잘림 없음(125%/150% DPI) · 플라이아웃 "100%" 항목(아이콘
  없음)과 형제(아이콘 있음)의 정렬 혼재가 거슬리는지 — 거슬리면 보고(전 항목 아이콘 제거 vs
  투명 자리 중 택일 후속).
- **A232**: 시작 메뉴 "Min to tray" 잘림 해소 · E896 글리프 모양 적합성(부적합하면 보고).
- **A230**: 3모듈 빈 상태에서 Fit 부재 · 파일 열면 복원 · 영상 테스트 클립 재생 시 나타남 ·
  이미지 마지막 파일 Delete 시 사라짐 · 이미지 로드 실패 상태에선 남는다(의도 — 판단 요청).
- **A229**: 20%·500% 극단 배율의 시각 성립(2.8px 폰트·500% 렌더 뷰 스크롤 도달).

⚠️ 종전 최우선이던 v0.161.0(A135) 포커스 고아 가설 검증은 A209→A212→A226 계보로 사실상 소화 —
A226 재검이 그 최종 판정을 겸한다.

**v0.224.0~v0.230.0 (2026-08-24 A212~A225 완주분) 확인 포인트**
- **A212(핵심 — 보고 재현 그대로)**: 문서 모듈 디폴트 상태에서 가운데 썸네일 **빈 공간** 클릭 →
  F11/F12 생존. 잔존하면 즉시 보고(다음 방향 = RecoverChromeFocusOrphan 클릭 시점 훅 — 코드 주석).
  부수: 빈 공간 클릭 시 타일 포커스 사각형/미세 스크롤이 거슬리는지 · F2 이름변경 중 빈 공간
  클릭 = 커밋 성립.
- **A213**: 사이드바 하나만 닫았을 때 썸네일 6열(타일이 커지는지).
- **A215**: 편집 화면 바 가운데 토글 2개 — 가이드/¶ 즉시 on·off·재시작 후 기억. 대용량 파일에선
  토글이 시각 무동작(낙수 ㉮ — 거슬리면 보고).
- **A216·A217**: 영상·오디오 전환 시 재생~배속 컨트롤 x 위치 픽셀 일치(넓은 창·640 미만 좁은 창).
  빈 스페이서 칸 렌더 회계(26+6)가 전제 — 어긋나면 수치 보고. 오디오 좁은 창에서 볼륨·시간 숨김
  첫 발동.
- **A218**: 최소화 버튼 = 일반 최소화(작업표시줄 유지) · 시작 메뉴 최하단/트레이 우클릭
  "Minimize to tray" 동작 · 시작 메뉴 항목 글리프 렌더 확인(A232/v0.232.0에서 E896으로 교체 —
  위 2026-08-25 블록의 A232 포인트가 최신).
- **A219**: 문서 창 트레이 숨김 → 탐색기에서 .txt 더블클릭 = **새 창**(숨김 창 무반응·아이콘
  변화 0) · 문서 창에 문서 열림 → 더블클릭 = 새 창 / 빈 문서 창 = 재활용 · **타 모듈도 숨김 창이
  더는 튀어나오지 않는다**(A218 정합 — 의도 확인) · 파일 없이 재실행 = 숨김 창 복귀(현행 유지).
- **A223·A224**: 문서 바 순서(Open·New·Save·편집/뷰·Print) · Open 픽커 → 미저장 가드 경유 ·
  .html 더블클릭 = 문서 에디터 · 잠금 뷰(비md 뷰 모드)에서 입력·Tab 차단 + 복사·저장(더티 시)
  성립 · 잘림 파일 토글 비활성.
- **A225**: 렌더 뷰 200%에서 문서 끝까지 스크롤 도달(핵심 — extent 추종) · 줌 후 인쇄물 크기
  불변 · 렌더 판 핀치 무동작(의도).
- **A214**: 텍스트에서 Fit height만 회색 비활성 ↔ PDF 전환 시 4옵션 복원 · Fit width/Contain
  선택 = 100%와 같은 결과 + 본체 아이콘만 그 옵션 유지.
- **A220**: 인티그레이션 스위치 좌변이 절의 다른 요소와 정렬(카드 여백 소멸) + 그룹 구분 체감.

**v0.220.0~v0.223.0 (A211 인쇄 — 실기기 확인이 본질인 기능) 확인 포인트**
- **1순위: unpackaged에서 인쇄 대화상자 성립 자체**(조사 판정 "추정(강)" — Microsoft Print to
  PDF로 검증. 안 뜨면 A211 전면 재설계 대상이니 즉시 보고).
- 이미지: 가로/세로·회전 90/270 contain 잘림 0 · 작은 아이콘은 페이지까지 확대(사양 — 거슬리면
  재확정) · psd·GIF 정지 프레임 · 인쇄 대화상자 중 ←/→ 이동 시 미리보기 변화(낙수 후보).
- PDF: "2-5" 범위 입력 실출력 · 100+페이지 미리보기 반응성 · 암호 PDF 무재입력 · 300DPI 체감.
- 텍스트: 소형 .txt 마지막 줄·오른쪽 끝 잘림 0 · 한글 혼합(줄 간격 넉넉 = 사양) · 탭 4칸 그리드 ·
  1MB 초과 안내 1장 · 무제 작업명 "Untitled".
- 마크다운: **다크 테마 인쇄에서 글자 검정**(백지 회귀 — 최대 런타임 리스크) · 렌더 모습(헤딩·
  코드 블록 배경) 실인쇄 · 긴 코드 블록 줄 단위 분할 · 대화상자 중 모드 토글 = 세션 고정.

**v0.218.0 (A209 — 사용자 "심각" 보고 수리) 확인 포인트**
- **재현 절차 그대로**: 이미지 모듈에서 하단 바 아무 버튼 클릭 → 이미지 더블클릭(전체화면) →
  **Esc/Enter/Alt+Enter가 이제 먹히는지**(핵심). 해제 후 연속 키 입력 정상인지.
- 문서 에디터 편집 중 Alt+Enter 전체화면 → 캐럿·IME 유지(방어 미발동 검증).
- 영상 재생 중 바 자동 숨김(3s) 후 Space 등 키 생존(같은 방어가 커버).

**v0.215.0~v0.217.0 (2026-08-21 (3) 완주분) 확인 포인트**
- **v0.217.0 (A208) — 핵심**: 10줄 이상 소형 .txt에서 **줄번호가 이제 보이는지**. 그래도 안 보이면
  본문 왼쪽 여백 폭을 관찰: 넓으면(약 60px) 그리기 쪽 재분기 / 좁으면(24px) 예약 미반영 확진 —
  UpdateEditorPadding 런타임 전파가 다음 감사 대상.
- **v0.216.0 (A207)**: 빈 영상 모듈 ▶ = 테스트 클립 재생(첫 실행은 수 초 지연 허용) · 빈 오디오
  ▶ = 샘플 곡 · S1(탐색기가 중앙을 덮은 상태)에서도 재생이 시작되는지(가림 중 스왑체인 초기화
  실발화 — 안 오면 예비 수리안(ContentOpened 선발화) 등재).
- **v0.215.0 (A206)**: 설정 밖에서 업데이트 확인이 정말 안 도는지(2분 뒤 Last checked 무변화) ·
  설정 진입 즉시 1회 + 체류 중 2분 간격 · 창 2개 설정 중 한쪽 닫기에 타이머 유지.

**v0.208.0~v0.214.0 (2026-08-21 (2) 완주분) 확인 포인트**
- **v0.214.0 (A48) — 이 배치의 본질(컴파일 ≠ 동작)**: ① 콤보 150% 선택 시 실제 적용·표시 동기
  ② 멀티 모니터 창 이동 후 콤보 재조회가 옳은 모니터인지 ③ SET 실패 환경(원격 세션)에서 딥링크
  폴백 발동 ④ 적용 직후 GET 스테일 여부 ⑤ 드롭다운 재구성 글리치.
- **v0.213.0 (A47)**: PawnIO 없는 승격 머신에서 설치 안내·링크 표시 · 비관리자 클럭 근사값 타당성
  (터보 반영) · 첫 1~2초 클럭 빈 값 허용 여부.
- **v0.210.0~v0.212.0 (A11)**: 루프 글리프(E8EE/E8ED) 렌더 · EOF 리핏 시 검은 프레임/소리 끊김 체감 ·
  목록 진행 시 바 잠깐 복원 후 재숨김 체감 · 단일 파일 폴더 + 목록 루프 연속 재생 · 오디오 동일.
- **v0.209.0 (A41)**: 비US 키보드 레이아웃에서 VK_OEM_PLUS/MINUS 실측 · 하단 바 위 Ctrl+휠과
  슬라이더 간섭 체감.
- **v0.208.0 (A205)**: 패널 연 채 설정 진입 시 숨김·나가면 복원(모듈 전환 복귀는 A109
  defaultSidebars=true가 다시 여는 게 정상 — A205와 무관한 기존 규칙).

**v0.205.0~v0.207.0 (2026-08-21 완주분) 확인 포인트**
- **v0.205.0 (A203)**: 전체화면 진입 시 패널이 닫히고 복귀(Esc/Enter/Alt+Enter)가 진입 전 구성으로
  돌아오는지 · 전체화면 중 F11/F12가 여전히 듣는지.
- **v0.206.0 (A198·A199)**: **압축된 2줄 행이 트리 행과 동급 높이로 보이는지**(체크박스 히트 영역·
  글리프 잘림 — MinHeight 0의 육안 검증) · 상세 줄 새 순서(크기·속성·날짜 2종 절대 표기)와 문서
  인코딩 조각(UTF-8·CP949) 표시.
- **v0.207.0 (A200)**: 썸네일 **단일 클릭만으로 우측 인포에 EXIF 전체 키**(빈 값 포함)가 뜨는지 ·
  선택 후 더블클릭으로 열었을 때 열린 콘텐츠 정보로 제대로 넘어가는지 · **GridView 전량 교체 시
  선택 이벤트 발화 여부**(코드로 판별 불가 — 폴더 이동 직후 인포 잔상이 있으면 보고).
- **v0.207.0 (A204)**: 이름 오름 → 크기 클릭 시 같은 크기끼리 이름 순서 유지 체감.
- **v0.207.0 (A193)**: 수백 KB md 렌더 진입이 즉시(첫 화면)인지 · append 중 스크롤/토글 이상 없는지.
- **v0.207.0 (A194)**: 대형 폴더 썸네일·상세가 **여러 개 동시에** 채워지는지 · 이미지 ←/→ 이동이
  두 번째부터 즉답인지(이웃 선읽기).
- **v0.207.0 (A192)**: **수천 항목 폴더 진입 시 첫 화면 즉시 표시 + 프레임 분할 체감**(조각 80/60) ·
  2000 상한 안내 행/타일 렌더 모양 · 최소화 중 감시 재스캔 후 복원 시 조립 재개.
※ **v0.166.0~v0.174.0(Opus 잔여 완주분)은 §4에 블록이 없다** — 그 배치들의 확인 포인트는 각 항목
본문(REQUIREMENTS의 A143·A154·A162·A167·A169·A171·A161 "실기기 확인" 줄)에 적혀 있으니 거기서 볼 것.

**v0.185.0~v0.201.0 (2026-08-19 (4)~08-20 완주분) 확인 포인트**
- **v0.185.0 (A173)**: ① 릴리스 자산에 **`KOTU-*-delta.nupkg`가 생기는지**(도입 1회차 — 이 릴리스 자체는
  여전히 풀) ② **그 다음 업데이트가 소용량으로 받아지는지**(2회차). 두 번을 봐야 판정이 끝난다.
- **v0.187.0 (A175)**: 온라인 전용(OneDrive) 폴더를 항해할 때 **다운로드가 0건인지**(작업 표시줄 동기화
  아이콘·용량으로 확인) · 클라우드 썸네일이 뜨는지 아니면 확장자 타일로 떨어지는지 · **더블클릭은 정상적으로
  받아서 열리는지**(허용된 하이드레이션).
- **v0.188.0 (A176·A186)**: 영상 재생 중 **3s 자동 숨김/재표시 체감**(포인터 이동·하단 근접·일시정지) ·
  전체화면에서 바가 나타나는 상호작용 · **불투명 패널만 남은 상태의 테마 전환**(다크/라이트) ·
  **바가 숨은 동안 Alt+\` 메뉴**가 정상인지.
- **v0.189.0 (A177)**: 큰 HTML/텍스트를 열 때 **로딩 표시 → 본문 표시** 순서가 보이는지(창 프레임만 뜨고
  안이 안 그려지던 증상이 사라졌는지) · 로딩 중 닫기가 되는지.
- **v0.190.0 (A179)**: 체크와 선택이 **분리된 UX가 헷갈리지 않는지**(하이라이트 = 선택 / 체크 = 작업 집합) ·
  그리드(타일) 쪽 Space에 시각 피드백이 없는 휴면 경로가 거슬리는지.
- **v0.191.0 (A180)**: 사이드바 25% 폭에서 **새 상세 줄이 잘리지 않는지** · **이미지 해상도가 코덱별로
  채워지는지**(webp·heic 등). ~~상대 표기 체감~~ → A199(v0.206.0)가 절대 표기로 원복해 소멸.
- **v0.192.0 (A181)**: 줌 후 **장식·거터가 본문과 정확히 겹치는지**(동배율의 유일한 육안 검증) ·
  **A80 우측 검은 띠 회귀** 없는지 · 4MB 파일 줌 시 **재랩 체감** · 좌측 패딩 비대칭.
- **v0.193.0~v0.194.0 (A182·A183)**: 펼침 시 **위아래 간격이 어긋나지 않는지**(음수 Margin 상쇄) ·
  카드 렌더(다크/라이트) · 토글이 실제로 우측 끝에 붙는지.
- **v0.195.0 (A184)**: 새 Fit 아이콘의 **획 무게와 중앙 블록** — 무거우면 **획 3.6 예비안**으로 교체.
- **v0.196.0 (A185)**: ⚠️ **Win+D가 일반 최소화인지(핵심)** · 타이틀바 버튼·시스템 메뉴·작업표시줄 클릭은
  트레이로 가는지 · **Win+M 실동작**(트레이로 가면 사양 재판단 대상).
- **v0.197.0 (A187)**: **빈 파일 모듈 S1에 다른 종류를 드롭**했을 때 새 창이 뜨는지(하단 바 띠가 표적).
- **v0.198.0 (A188)**: 창이 넓을 때 **PDF가 정중앙**인지 · fit-width 경계에서 **줌 점프가 없는지**.
- **v0.199.0 (A189)**: New text file 흐름과 **무제 저장**(제목이 파일명으로 바뀌는지) ·
  다른 모듈의 탐색기에서 New file을 쓰는 UX.
- **v0.200.0 (A190)**: md 렌더 모양 — **TextHighlighter 오프셋** · **링크 클릭** · **화살표 키 스크롤**.
- **v0.201.0 (A191)**: 이미지 트레이의 **해상도 2줄 가독성** · **R 회전 시 값이 스왑되는지** ·
  5자리 축약(`12k`) 표시.

**v0.174.1~v0.184.0 (2026-08-19 (3) 핫픽스 + Fable 확정 사양군) 확인 포인트**
- **v0.174.1(선결)**: 이미지·문서·영상 모듈에 **들어가자마자 죽지 않는지**(이 핫픽스가 고친 크래시).
  여기서 막히면 아래 항목은 전부 확인 불가다.
- **v0.175.0 (A151)**: 셸 모드 버튼 2개의 **E73F 글리프 모양·규격** · **Alt+Enter가 실제로 듣는지**
  (안 들으면 `RegisterShortcuts`에 `Menu+Enter` 한 줄 추가가 복구법) · 모드2에서 경계 버튼 바닥 여백이
  8인지 · ⚠️ **A61 접힘 중 모드2로 들어가면 빈 창이 되는 엣지**(미처리 코너 — 재현되면 등재) ·
  ⚠️ **H/W 센서 그리드가 모드2에서 하단 바와 함께 사라진다**(전체화면 이전이 프레젠터 키였던 탓 —
  A151이 다루지 않은 공백).
- **v0.177.0 (A152·A153)**: 바 배경이 콘텐츠 위에 겹치면서 **2겹 렌더 톤이 어색하지 않은지**(특히 영상 위) ·
  모드1에서 경계 버튼이 **바 위 8px로 내려앉은** 위치 · 모드 전환 때 버튼이 **44px 이동**하는 체감 ·
  A108 안내 판 위치(모드1 기준 종전보다 **22px 아래**) · ⚠️ **미보정 스크롤 표면**(정보 오버레이·압축
  트리·설정·H/W·AllReadable)은 모드1에서 **끝 44px가 바에 덮인다** — 거슬리면 +44 보정 등재.
- **v0.178.0 (A137·A138)**: 16px 타이틀바 번호의 **크기 가독성**(`NumberFontScale 0.72`) ·
  트레이 대각 표시의 **글자 상자 폭 0.78**에서 `999+`가 잘리지 않는지 · **유휴 32px에서 `.ico` 본체가
  소실돼 보이는지**(전면 채움과의 상충).
- **v0.179.0 (A142 ①②③⑤)**: 행 번호 거터의 **숨김/표시 경계**(좁은 창·폭 Unlimited) ·
  **Consolas에서 한글이 섞인 줄의 높이** · **스크롤이 멈춘 직후 장식 위치**(50ms 코얼레싱의 최종 렌더) ·
  거터 번호의 세로 정렬.
- **v0.180.0 (A144·A145)**: 32px 드롭다운 버튼의 **화살표 셰브론 잘림·치우침** · 두 버튼 이음새의 1px 틈 ·
  문서 텍스트 모드에서 **비활성 Fit 버튼의 툴팁이 뜨는지** · 영상 바 **640 임계** 체감.
- **v0.181.0 (A150)**: 실사진에서 **EXIF 확장 6행이 실제로 채워지는지** · `Separator` 빈 행 여백 체감.
- **v0.182.0 (A155)**: 사이드바 25% 폭에서 **6개 헤더 라벨이 잘리는지** · 정렬 방향 셰브론(8px) 렌더 ·
  mkv 등에서 **해상도 속성이 존재하는지** · **PDF가 많은 폴더에서 상세(Info) 열이 채워지는 속도** ·
  툴팁의 Info 줄.
- **v0.183.0 (A163·A164)**: EQ(E9E9)·스피커(E7F5) **글리프 모양** · 서브메뉴 **라디오 체크 표시** ·
  출력에서 "System default"로 **재생 중 복귀**가 되는지 · ⚠️ **`IPolicyConfig` 실동작**
  (입력 기본 장치가 실제로 바뀌는지 — 비공개 COM이라 실기기가 유일한 판별 수단).
- **v0.184.0 (A166)**: `.pdf` 토글을 켰을 때 **딥링크 대신 안내 문구**가 뜨는지 ·
  **Set default(`SHOpenWithDialog`)로 `.pdf`를 실제로 KOTU에 지정할 수 있는지**.
- **A170(코드 변경 없음 — 관찰 절차)**: ① 다른 **다크 테마 앱**을 전체화면으로 띄웠다 껐을 때도 같은
  밝기 변화가 나는가 ② **CABC/적응형 밝기를 끄고** 재현되는가 ③ **이미지만 본 세션 vs 비디오를 재생한
  세션**에 차이가 있는가 ④ **H/W 모듈을 한 번도 안 띄운 세션**에서도 나는가 ⑤ 돋보기로 **실제 밝기
  변화인지 아이콘 재배열인지** 구분. → 어느 것이든 "앱 밖 원인"이면 A170은 코드 수정 없이 결번.

**v0.162.0~v0.165.0 (2026-08-19 Opus 1차) 확인 포인트**
- **v0.162.0**: 창이 하나일 때도 제목이 `1-KOTU`인지(작업표시줄·Alt+Tab), 파일 열면 `1-KOTU - 파일명`
  (하이픈 2개 = 사양) · 시작 메뉴 버튼 위 숫자 배지가 사라졌는지(다른 요소가 밀리지 않았는지).
- **v0.163.0**: Alt+1~7·Alt+0이 안 먹는지 · **Alt+\`(메뉴)는 되는지** · **Alt를 눌렀다 떼도 OS 창 메뉴
  모드로 빠지지 않는지**(Alt 단독 up 소비가 살아 있는지 = 이 배치의 회귀 1순위) · 시작 메뉴 툴팁에서
  키 표기만 사라졌는지.
- **v0.164.0**: 트레이 아이콘 테두리가 1px로 얇아지고 모서리 낭비가 줄었는지 · **콘텐츠 없을 때 아이콘이
  모듈 색으로 꽉 차고 흰 글자인지** · 열림 상태는 테두리만 모듈 색인지 · **하드웨어(INF)는 종전 모습
  그대로인지**(전용 색·링 없음) · ⚠️ **32px 창 아이콘(작업표시줄·Alt+Tab)이 흐려지지 않았는지**
  (본체 24 → 28px로 커져 실존 `.ico` 프레임을 벗어나 GDI 보간이 걸린다 — 흐리면 스냅 처리 등재).
- **v0.165.0**: 정보 모듈 센터 타일이 **사이드바 없음 8개 / 한쪽 6개 / 양쪽 4개**인지, 8열에서 채널명·
  값이 잘리지 않는지 · 하단 바 긴 그래프 오른쪽에 **기간(`5m`)** 이 보이는지, 주기를 50ms로 내려도
  `5m`인지(링 버퍼 확대 효과) · 좁은 창에서 기간 표기·맥박이 순서대로 숨는지 · 우측 스펙 패널 글씨가
  80%로 작아지고 라벨 열이 96px로 줄었는지 · **주기 50ms 장시간 구동 시 메모리·부하 체감**(링 6000).

한 번에 묶어서 확인하는 게 효율적이다.
⚠️ **v0.107.0~v0.113.0 빌드는 실기기에서 시작 자체가 안 된다**(A27 스타일 크래시, v0.113.1에서 수정).
⚠️ **v0.111.0~v0.113.1은 정보(하드웨어) 모듈을 열면 죽는다**(A62 중복 Add, v0.113.2에서 수정) —
아래 확인은 전부 **v0.113.2 이상**으로 할 것.
※ v0.90.0(A82)은 문자열·메타데이터만 바꿨다 — 실기기 확인 항목은 **exe 속성 창의 회사/저작권 표기 정도**로 가볍다.

**v0.85.0 (A38) — 기본 앱 강제 지정**
- 연결 토글을 켰을 때 확장자들이 실제로 KOTU 기본 앱이 되는지
- `.pdf` 등 보호 확장자가 UCPD에 막혀 A25 딥링크로 폴백되는지
- 백신 오탐이 없는지 / 토글을 끄면 깨끗이 해제되는지

**v0.86.0 (A46) — ZP → KOTU**
- 첫 실행 시 구 ZP 우클릭 메뉴·파일 아이콘이 자동으로 사라지는지
- 트레이·타이틀바 아이콘이 새 "KO/TU" 아이콘으로 뜨는지
- 구 ZP 설치본 위에 KOTU Setup을 설치했을 때 충돌이 없는지

**v0.87.0 (A52·A56)**
- 제목표시줄이 `[2] KOTU Document — 파일명` 형태로 나오는지
- 우클릭 메뉴가 "KOTU-archive"로 바뀌는지 — **토글을 껐다 켜야 바뀔 것**(§3.2)

**v0.88.0 (A64) — exe 이름 ⚠️ 이번 건이 가장 중요**
- "Restart as admin"의 UAC 대화상자에 **`KOTU.exe`** 로 표시되는지 (이게 A64의 목적)
- 자동 업데이트 후 시작 메뉴·작업표시줄 바로가기가 새 exe를 가리키는지
- 설치 폴더에 구 `WinUtil.App.exe`가 남는지
- **파일 더블클릭이 여전히 동작하는지** — 안 되면 연결 토글을 껐다 켜야 한다(→ A78이 해결할 문제)

**v0.89.0 (A78) — exe 경로 자동 재등록 ⚠️ 사용자의 현재 고장을 고치는 버전**
- 업데이트 후 **아무 조작 없이** 앱을 한 번 실행하면 .png 등 더블클릭이 KOTU로 열리는지
  (첫 실행의 백그라운드 재등록이 고쳐야 정상 — 토글 조작 불필요)
- 우클릭 "Extract here"·"Compress"가 동작하는지 (verb command도 재기록 대상)
- 재등록 후 설정 화면의 "Default app for n/m" 숫자가 유지되는지 (UserChoice는 안 건드림)

**v0.91.0~v0.97.0 (2026-08-12 배치) — 각 버전 확인 포인트**
- **v0.91.0 (A80)**: 900px보다 넓은 창에서 문서(텍스트) 열었을 때 우측 검은 띠가 사라졌는지.
- **v0.92.0 (A50·A65)**: 시작 메뉴에서 라벨 왼쪽 여백을 눌러도 선택되는지·오선택률, 플라이아웃이
  가장자리 밀착으로 어색하지 않은지, New Instance 항목이 빠졌어도 Ctrl+N 등은 되는지.
- **v0.93.0 (A75·A51)**: Copy 아이콘 버튼 외관, 주기 전환 시 EKG 스파이크가 1~2개로 즉시 반응하는지
  (주기 목록은 v0.110.0/A73에서 50~5000ms로 바뀌었으므로 양 끝인 50ms·5000ms로 확인).
- **v0.94.0 (A40)**: 창이 720×540 미만으로 안 줄어드는지(150% 배율 모니터 포함), 하단 바 두께 불변,
  좁은 창에서 비디오 볼륨/시간 텍스트·HW 맥박 그래프·뒤 순서 센서 카드가 **의도적으로 숨는 것**(버그 아님),
  전체화면 왕복 후에도 최소 크기가 사는지.
- **v0.95.0 (A55)**: 최대화로 닫고 재실행(최대화 복원 + Restore Down 위치), 모니터 분리 후 재실행(화면 안 보정),
  Ctrl+N 연속(+32px 계단), 전체화면으로 닫기(일반 크기·위치 저장).
- **v0.96.0 (A49)**: PDF 하단 바의 1:1·Fit 버튼(A30과 같은 UX), Ctrl+휠 줌 시 Fit 추종 해제,
  좁은 바에서 드라이브 텍스트 숨김.
- **v0.97.0 (A66)**: 오디오 모듈 빈 상태 ▶ = 내장 샘플(18초 멜로디) 재생, 샘플은 이어듣기 제외.

**v0.98.0~v0.104.0 (2026-08-12 UI 배치) — 각 버전 확인 포인트**
- **v0.98~99 (A57)**: 좌=파일 리스트/우=정보 배치, 리스트 상단 25% 디스크 트리(펼침·현재 폴더 자동 선택),
  모듈별 확장자 필터, 다크/라이트 전환 시 오버레이 아크릴(App.xaml로 이동).
- **v0.100.0 (A58)**: Shift(우)/Alt(좌) 홀드=반투명, 2초 홀드=키 떼도 유지, 2연타=불투명 밀어내기(메인 축소),
  고정 상태 2연타=해제. Shift+클릭·다른 키 개입 시 홀드 취소. **Shift를 잠깐 누를 때마다 오버레이가
  깜빡 보이는 게 거슬리는지 체감 확인** — 거슬리면 표시 지연(예: 150ms) 항목 등재 후보.
- **v0.101.0 (A81)**: 파일 더블클릭 시작=오버레이 없음 / 시작 메뉴에서 모듈 실행=양쪽 불투명 도크
  (빈 모듈이면 좌=모듈 시작 폴더, 우="No file open"), 중앙 탐색기는 좌 도크와 중복이라 숨음.
- **v0.102.0 (A84)**: Shift+N=새 창(에디터에서는 대문자 N 입력), Shift+휠=줌(이미지·PDF, 뷰어 위에서만),
  구 Ctrl+휠 줌 차단, **Ctrl+S 저장은 그대로**.
- **v0.103.0 (A68)**: 창 2개 이상일 때 창·트레이 아이콘에 인스턴스 색 테두리+원형 번호 배지(16px 가독성 확인),
  10번째 창부터 색 순환. 배지가 아이콘 우하단 kotu 표식을 덮는 트레이드오프 확인.
- **v0.104.0 (A69)**: 최소화하면 작업표시줄·Alt+Tab에서 사라지고 트레이 좌클릭으로 복귀.
  **Win+D도 전 창이 숨는다(사양대로지만 체감 확인)**, 최대화→최소화→복귀 시 최대화로 돌아가는지.

**v0.105.0~v0.108.1 (2026-08-12 3회전) — 각 버전 확인 포인트**
- **v0.105.0 (A26·A76)**: ~~토스트·오토체크 토글·주기 체크 확인~~ → **v0.117.0(A95)에서 전부 제거됐다.
  이 항목은 확인 불요.** 남은 건 마지막 확인 시각이 로컬 시각으로 맞는지 정도(A95 항목에서 함께 본다).
- **v0.106.0 (A77)**: 연결 토글을 켤 때 진행 링이 돌고 `Registering... (n/m)` → `Setting as default... (n/m)`
  으로 **카운터가 두 번 도는 것**(단계가 다름 — 버그 아님)이 이상하게 느껴지는지.
  작업 중 그 모듈 토글만 잠기는지, 끝나면 "Default app for n/m"이 즉시 갱신되는지.
  두 모듈을 연달아 누르면 두 번째가 `(0/m)`에서 대기하는 것(직렬 실행, 의도됨).
- **v0.107.0 (A27)**: ⚠️ **시작 메뉴 ☰ 버튼이 투명→기본 테두리로 바뀌었다** — 통일 목적이지만
  셸 첫인상이 달라지므로 눈으로 보고 판단. 배속 ComboBox(높이 32→40)의 텍스트 수직 중앙·드롭다운 위치,
  리프레시 주기 DropDownButton의 최장 표기가 84px에 들어가는지(v0.110.0/A73부터 "5000 ms" — 구
  "1000 ms"와 같은 7글자라 폭 조건은 동일), 720×540에서 영상·문서 바가 안 잘리는지.
- **v0.108.0 (A22)**: 파일을 안 연 상태에서만 드라이브 줄이 뜨는지(파일 열면 사라짐 — v0.47.0의 반대),
  SSD/NVMe/USB 종류가 맞게 나오는지(**WMI 매핑이 다중 파티션·RAID에서 어긋날 수 있음**,
  실패하면 Local/Removable 근사로 떨어진다), 막대 색(90% 이상 경고색), 넘칠 때 마퀴가 도는지·
  이음새가 보이는지, 압축 모듈에서 진행 문구("Reading archive...")와 자리를 주고받는 게 튀지 않는지.

**v0.109.0~v0.113.0 (2026-08-13 4회전) — 각 버전 확인 포인트**
- **v0.109.0 (A36·A67)**: 설정 연결 섹션 아래 "Open settings.json" → **새 창**에 KOTU-doc으로 열리고
  원래 설정 창이 남는지, 저장 후 재시작 반영. `%AppData%\KOTU` 삭제 상태에서 눌렀을 때 파일이 만들어지는지.
  탐색기에서 임의의 `.json` 더블클릭이 문서 에디터로 열리는지(신규 부수 효과).
  광고는 **URL을 채우기 전까지 클릭 무반응이 정상** — 채운 뒤 손가락 커서·툴팁·브라우저 열림·메뉴 닫힘 확인.
- **v0.110.0 (A73·A74)**: ~~50ms 실측 부하~~ → **2026-08-13 사용자 확인: 감당됨 — 목록 유지 확정**(부록 B 47).
  남은 확인: 기존 300 사용자가 업데이트 후 **500으로 올라오는지**.
  축 라벨(좌상단 `100°C` / 우하단 `60s`)이 36px 카드에서 읽히는지·겹쳐 지저분하지 않은지,
  창을 좁히면 라벨이 먼저 사라지는지, 50ms에서만 x가 `30s`로 바뀌는지(이력 링 600개 한계 — 버그 아님).
- **v0.111.0 (A61·A62)**: 핀을 켜면 **타이틀바+44px**만 남는지(100%·150% 배율 각각), 접힌 뒤 더 줄일 수
  없고 해제하면 540 하한이 돌아오는지, 최대화 상태에서 핀 ON→OFF가 최대화로 복귀하는지,
  **접힌 채 종료 후 재실행이 접기 전 크기로 열리는지**, 접힘↔전체화면 왕복 후 재접힘.
  ※ **알려진 한계**: 접힌 채 다른 배율 모니터로 옮기면 높이가 재계산되지 않는다(핀을 껐다 켜면 정상).
  A62는 L 단계에서 카드가 44px 안에 들어가는지·글씨 잘림, 720px 폭에서 카드 수 축소.
- **v0.112.0 (A34)**: ⚠️ **핫키가 새는지가 핵심** — 문서 에디터에서 `o·a·f·s·c`가 그대로 찍히는지,
  탐색기 리스트·폴더 트리 포커스에서 첫 글자 점프가 살아 있는지(반대로 숫자 모듈 전환은 계속 돼야 한다 —
  의도적 비통과). 키로 여는 플라이아웃(영상 S·C, 압축 C, 정보 I)이 실제로 열리는지.
  비활성 버튼 상태에서 키가 무동작인지. 툴팁 `(키)` 표기 확인.
- **v0.113.0 (A59)**: ⚠️ **자식 전환 시 소리·파일 핸들이 남지 않는지가 최대 위험** — 영상/음악 재생 중
  이미지로 전환하면 소리가 즉시 멎는지, 전환 후 그 파일을 삭제·이름변경할 수 있는지.
  libvlc VideoView가 **2겹 ContentControl 안에서 정상 렌더**되는지(검은 화면 여부).
  하단 바가 깨끗이 교체되는지(이전 자식 버튼 잔존 없음), 자식 핫키(이미지 O/R/A/F·영상 Space·문서 Ctrl+S)가
  그대로 듣는지, 미저장 문서 가드와 제목 ● 표시, 시작 메뉴에서 `7` 실행 시 A81 좌우 도크.
  **설정 연결 섹션에 All Readable 토글이 없어야 정상**이고 탐색기 더블클릭은 전용 모듈로 가야 한다.
  시작 메뉴에서 "All Readable" 표시명이 잘리면 축약안을 정해야 한다.

**v0.114.0~v0.119.0 (2026-08-13 5회전) — 각 버전 확인 포인트**
- **v0.114.0 (A88·A89)**: 맥박 스파이크가 **우→좌로 흐르는 게 보이는지**(50ms·5000ms 양 끝), 주기 전환 시
  점프 없이 창 길이만 바뀌는지. **고주사율(120/144Hz) 모니터에서 CPU/GPU 체감** — 이제 맥박이 보이는 동안
  UI 스레드가 주사율만큼 돈다. 정보 모듈을 여러 번 열고 닫아 **메모리가 늘지 않는지**(static 이벤트 해제).
  A89: 창1·창2 → 창2 닫고 새 창 → **정확히 창2 크기·위치로 열리는지**(+32px 계단 없음), **살아 있는 창과
  완전히 겹쳐도 그대로 두는 게 정상**. 모니터 분리 후 화면 밖 보정은 여전히 도는지.
- **v0.115.0 (A91·A92·A67)**: 좌 오버레이 최상단에 주소·필터 줄이 오는지, 오버레이를 여닫아도
  **`COMException 0x800F1000`이 안 나는지**(reparent 배치의 핵심), 그 줄의 Up·필터·정렬 버튼이 **떼어 붙인 뒤에도
  동작하는지**, **중앙 탐색기는 종전 자리에 그대로**인지. 안내 문구가 2.5초 뒤 사라지고 도크↔고정 전환 시
  다시 뜨는지(투명도 0으로 남지 않는지). ⚠️ **`SetTargetProperty("Opacity")` 페이드는 CI로 검증 불가** —
  안 돌면 문구가 안 사라진다. 그때는 페이드를 빼고 2.5초 뒤 바로 Collapsed로 내리면 된다.
  A67: 광고 카드에 손가락 커서·"KOTU releases" 툴팁·클릭 시 릴리스 페이지.
- **v0.116.0 (A96·A97)**: ⚠️ **플라이아웃 밀착 `-6`이 실제로 먹는지 — 이번 배치 최대 미검증 지점.**
  뒤 배경이 남으면 숫자를 키우고(-8·-10), 하단 바를 덮으면 줄인다(`MainWindow.xaml` 한 줄).
  번호 키 **1=All Readable … 7=정보, 0=설정**이 전부 맞는지(툴팁 표기 포함), 구분선 2개 위치,
  **폭 136에서 "All Readable" 라벨이 잘리는지**, 항목 높이 40에서 오선택률(A31의 44 터치 타깃을 깬 값이다),
  광고 카드 132×66 렌더. A97: 36px 버튼·간격 6이 좁은 창(720)에서 안 잘리는지, **맥박 표시 임계가
  1290→1212로 내려갔으니 그 폭 근처에서 카드가 뭉개지지 않는지**, 인스턴스 배지(28,4) 정렬.
  ※ **A62 L 단계 카드 높이가 M과 같아졌다**(상한 36 = 기준 36) — 의도된 트레이드오프지만 눈으로 보고
  L이 무의미하게 느껴지면 상한만 40으로 되돌리면 된다.
- **v0.117.0 (A95)**: - **v0.117.0 (A95)**: ※ **Check now 버튼은 v0.148.0(A125)에서 제거됐다** — 아래 "Check now →" 확인은
  설정 진입 체크(A114 ②)로 대체해서 본다. **첫 실행 후 30초·10분이 지나도 네트워크 요청이 안 나가는지**(타이머 제거의 핵심),
  Check now → 최신이면 버튼 **밑**에 "You are on the latest version.", 새 버전이면 적용 버튼,
  창 2개에서 한쪽 Check now가 다른 창에 반영되는지. **토스트가 더 이상 안 뜨는지.**
  구 settings.json에 `update.autoCheck`가 남아 있어도 무해한지(마이그레이션 없음 — 의도).
- **v0.118.0 (A54)**: ⚠️ **16px 트레이에서 2줄 텍스트가 읽히는지가 최대 관건**(`JPEG`·`1080p` 등 4~5자).
  유휴(1줄·저채도) ↔ 열림(2줄·모듈 색) 구분이 밝은/어두운 작업표시줄 양쪽에서 서는지.
  **"Restart as admin" 후 옛 트레이 아이콘이 안 남는지**(이번 감사 수정의 핵심), 탐색기 재시작 후
  창 수만큼 정확히 복구되는지, 창 5개 장시간 운용 시 **GDI 개체 수가 안 늘어나는지**.
  오디오 이퀄라이저(1초 갱신·재생 중만)가 자연스러운지 — **실제 주파수 분석이 아니라 장식**이다.
  정보 창의 `INF` 아이콘과 A18 센서 아이콘이 **나란히 2개** 보이는 게 사양대로다.
- **v0.119.0 (A79)**: **기본은 레벨 0이라 겉보기 변화가 없어야 정상** — 먼저 v0.118.0과 같은 모습인지 확인.
  그다음 `%AppData%\KOTU\settings.json`에 `"branding.assetLevel": 1`(→2→3)을 넣고 **재시작**해서 본다
  (실시간 반영 없음 — 시작 시 1회 캐시). ⚠️ **레벨 1의 16px 발바닥 가독성이 이 항목의 성패**다 —
  32px 이상은 발가락 4개가 또렷하지만 16~24px에서는 붙는다(수치 확인됨). 안 읽히면 레벨 1의 ①만
  글자로 되돌리는 게 최소 수정이다. 레벨 2 워드마크(시작 메뉴 136폭·설정)·발바닥 스피너 회전,
  레벨 3 웰컴 마스코트. 고DPI 150/200%도.

**v0.120.0~v0.122.0 (2026-08-13 6회전, Fable ①) — 각 버전 확인 포인트**
- **v0.120.0 (A93)**: S1(모듈만 실행)에서 좌/중/우 **25:50:25** 정렬(불투명 도크 컬럼과 픽셀 정렬),
  썸네일 **열 수 = 도크 둘 다 열림 4 / 하나라도 닫힘 8**·창 리사이즈 추종, 이미지 실미리보기(psd 등
  실패 시 확장자 타일 폴백), 폴더 더블클릭 시 좌 리스트와 중앙이 **함께** 이동하는지, 드랍 4위치
  (좌·중=무동작 커서, 우 인포=열기, 콘텐츠 영역=같은 종류 그 자리/다른 종류 새 창), 오버레이 여닫기
  반복에 0x800F1000 없음. ⚠️ **수천 개 파일 폴더에서 타일 일괄 생성 성능**(가상화 없이 전체 생성 —
  느리면 후속 등재 후보). S2(파일 인자 시작)는 종전과 동일해야 함(회귀 확인).
- **v0.121.0 (A86)**: ⚠️ **Z/X 오토리피트가 핵심** — Z를 꾹 눌러도 홀드(반투명)가 유지되고 2연타
  판정이 오염되지 않는지. 열림 상태(고정·도크)에서 Z/X 1회 = 닫힘, 닫힘에서 2연타 = 불투명 도크,
  열림에서 시작한 2연타 = 닫힌 채 유지(재열림 없음이 사양). Enter 일괄 토글: S2에서 Enter=전부 닫기
  →다시 Enter=**직전 구성 그대로** 복원(좌만 열었었으면 좌만), 문서 에디터 줄바꿈·영상 전체화면·
  탐색기 선택 열기는 Enter를 안 뺏는지. **문서 에디터에서 z/x 문자가 정상 입력**되는지, 리스트/트리/
  썸네일 포커스에서 Z/X = 첫 글자 점프인지. 경계 버튼: 경계선 ±48px 근접 시만 표시·클릭=불투명 도크
  토글·닫힌 상태에선 창 가장자리에서 꺼내기. 이미지·오디오 Enter 전체화면이 사라진 것(F11은 유지) 체감.
- **v0.122.0 (A90)**: 하단 바 시작 메뉴 옆 '오픈 파일' 버튼 — 콘텐츠 표시 중 누르면 기존 구획은
  그대로 두고 없는 쪽만 **반투명** 추가 + 중앙 반투명 썸네일(포커스가 그리드로), Esc/재누름 = 진입 전
  상태로 정확히 복귀(원래 불투명이던 쪽은 불투명 그대로), 파일 열면 S4 자동 종료. S1에서 누르면
  중앙 테두리 강조 450ms만. S4 중 Z/X·문자 핫키·경계 버튼 무동작, 숫자 키 모듈 전환은 동작(전환 시
  S4 닫힘). 전체화면+S4 중첩 시 Esc 순서(첫 번째=전체화면 해제). ※ **알려진 한계 2건(수용됨)**:
  ① S4 진입으로 추가된 반투명 구획에 A92 힌트 "Pinned — press Z/X to close"가 2.5초 떴다 사라짐
  (S4에선 Z/X 무동작이라 문구 부정확 — 거슬리면 후속 등재) ② 필터 입력란이 Esc를 삼키면 복귀에
  Esc 2회 필요할 수 있음.

**v0.160.0~v0.161.0 (2026-08-18) — 각 확인 포인트**
- **v0.160.0 (A134)**: 좌 도크 열림 상태에서 하단 리스트·중앙 썸네일로 폴더 진입 → 상단 트리가
  펼침·선택·스크롤로 따라오는지. 감시 폴더에서 파일 추가/삭제(재스캔)·정렬/필터 변경 시 **트리가
  튀지 않는지**(같은 폴더 가드의 실효). 트리 클릭 항해 반복 시 이상 순환 없음.
- **v0.161.0 (A135) ⚠️ 가설 검증이 핵심**: 재현 절차 = 콘텐츠 열림 → F1 2연타(사이드바) →
  리스트 클릭(포커스 진입) → F1(닫기) → **F1 재누름이 듣는지**(비디오·이미지·PDF에서 각각).
  듣게 됐으면 가설(포커스 고아) 확정·수리 유효. **여전히 안 들으면 보고** — 포커스 null 갈래로
  가설 기각·재조사(잔여 후보 = audit 문서 §4 ②③④: 전체화면 진입 직후 버튼 Collapse·libvlc
  네이티브 포커스 탈취·Fn/OEM 키보드). F2 이름변경 편집 중 패널 닫힘 = 편집이 커밋으로 끝나는지
  (신규 부수 효과 — 어색하면 보고).

**v0.154.0~v0.159.0 (2026-08-16 14회전) — 각 확인 포인트**
- **v0.154.0 (A127·A128)**: 센터 타일·좌 대형에 "max 100%" 접두 가독(폭 90 임계에서 잘림 여부),
  하단 바 긴 그래프 = 채널명+현재값만(축 라벨·"5m" 소멸 확인 — 겹침 해소 체감).
- **v0.155.0 (A133)**: 힌트 판(다크·라운드)이 배경 글씨 위에서 읽히는지, 페이드가 판째 자연스러운지
  (⚠️ Storyboard 대상 Border 교체 — CI 검증 불가 부류: 페이드가 안 돌면 문구가 판째 안 사라짐 →
  보고), 라이트 테마에서 다크 판 위 흰 글씨, A94 실패 안내(같은 채널)도 판을 얻은 모양.
- **v0.156.0 (A129) ⚠️ 렌더 판정이라 실기기가 최종**: 비디오 재생 중 S4/F1/F2 반투명이 **영상이
  비치는 단색 틴트**로 보이는지(구 불투명 판 소멸 — 블러 없음이 사양), 오디오(파형)도 동일,
  이미지·문서·압축은 종전 아크릴 그대로, 비디오→이미지 전환 시 아크릴 복귀(원복 구조), 다크/라이트
  틴트 농도(6B 쌍 — 조정은 App.xaml 두 값).
- **v0.157.0 (A130)**: EOF 후 최대화/스냅/드래그 = **검정 정리**(잔상 고정 소멸 — 사양), 리사이즈
  안 하면 마지막 프레임 유지, ▶ 재시작 시 새 크기로 정상 재생, 수동 일시정지 리사이즈는 종전대로
  추종(회귀 없음 확인).
- **v0.158.0 (A131)**: 압축 모듈 좌 리스트·중앙 썸네일에서 zip 더블클릭 = ArchiveView 열림(핵심
  재현 — 활동 중 폴더에서 반복), 한 번에 한 번만 열림(이중 발화 억제), Ctrl+클릭 연타 = 선택
  토글만, 이미지·영상 모듈 더블클릭 회귀 없음.
- **v0.159.0 (A132)**: docs/USER-GUIDE.md·site/guide.html 내용 검토(사실 오류 발견 시 보고 —
  전 서술 grep 검증했으나 실기기 교차 확인 환영), site/index.html 버전 표기 v0.159.0.

**v0.150.0~v0.153.0 (2026-08-16 13회전, A94 후속) — 각 확인 포인트**
- **v0.150.0 (3차)**: 충돌 대화상자 3버튼·all 체크(다건에서만)·Esc = 취소("cancelled - n of m
  completed" 문구), **폴더 충돌 Replace = 병합**(내부 충돌 개별 질문·이동 병합 후 빈 원본만 삭제),
  Keep both = "이름 (2)". 진행 문구가 3개 이상 조작에서만 뜨고 완료 요약이 대체하는지.
  같은 창에서 문서 저장 대화상자와 겹치는 순간(다른 경로) = 취소 강등이 설계된 degrade.
- **v0.151.0 (4차)**: Shift+Del 확인창 **Enter 기본이 Cancel인지**, F2 편집 중 Shift+Del 무동작.
  잘라내기 0.5 흐림(리스트·타일)·선택 강조와의 겹침 모양, Esc/새 복사/붙여넣기 소진으로 해제,
  **외부(OS 탐색기)에서 그 클립보드를 쓴 뒤 표시가 남는 것 = 알려진 한계**. 접근 거부 유발(보호
  폴더 이동 등) → "Access was denied..." 대화상자 → Restart as admin → **창 세트 복원까지**
  하드웨어 버튼 때와 동일한지(공용화 회귀 확인). WinRT 삭제의 권한 실패가 0x80070005로 오는지
  (빗나가면 종전 일반 안내로만 — 보고).
- **v0.152.0 (5차)**: OS 탐색기에서 파일 추가/삭제/이름변경 → **약 0.4초 뒤 KOTU 좌 리스트·중앙
  썸네일 자동 갱신**(도크 닫힘 상태에서도 썸네일 갱신). F2 편집 중 외부 변경 = 편집 안 끊기고
  커밋/취소 후 반영. 보던 폴더를 밖에서 삭제 = 존재하는 상위로 자동 이동. 네트워크/USB 드라이브
  = 감시 없이도 종전 동작(수동 조작 후 갱신). 창 여러 개 = 각자 자기 폴더만 반응. **창을 여러 번
  여닫아 메모리 증가 없는지**(watcher 해제 — §3.4 부류).
- **v0.153.0 (6차) ⚠️ 이번 묶음 최대 확인**: ① 빈 영역 우클릭 = 메뉴 **하나만**(항목 위 우클릭과
  안 겹침), 항목 아래 여백에서도 뜨는지(Background=Transparent 전제의 실효), Paste 활성/비활성.
  ② 다중 선택 더블클릭/Enter = 전부 열림(첫 창 재사용 규칙·나머지 새 창·11개 이상 = 앞 10개+안내),
  S4에서도 동일. ③ **KOTU에서 Ctrl+X → OS 탐색기 붙여넣기 = 이동**(구 버전은 복사로 떨어짐),
  역방향(탐색기 Ctrl+X → KOTU Ctrl+V)도 이동·원본 삭제 주체 = KOTU. ④ 폴더 타일 메뉴 Paste가
  그 폴더로 들어가는지.

**v0.146.0~v0.149.0 (2026-08-16 12회전) — 각 확인 포인트**
- **v0.146.0 (A120·A121)**: A120 — 900px 초과 창에서 본문 컬럼 중앙·양측 대칭 여백, **포커스 시
  중앙 컬럼에만 불투명 배경(페이지 모양)이 그려지는 게 어색하지 않은지**, A115 장식선(가이드·¶)이
  본문과 계속 정렬되는지(150% 배율 포함 — 좌표계 동기의 실효). 900 이하 창은 종전과 픽셀 동일.
  A121 — ⚠️ **에디터 화살표 무간섭이 합격선**: 텍스트 편집 중 ↑↓/PgUp/Dn/Home/End가 캐럿 원 기능
  그대로인지. PDF에서 ↑↓(12.5%)/PgUp·Dn(90%)/Home/End 스크롤·꾹 누름 연속 스크롤, 오버레이
  필터·리스트 포커스에서는 원 기능 우선. **하단 바 버튼 클릭 직후엔 포커스가 뷰 밖이라 무동작
  (사양 — PDF 면 클릭으로 복구)**이 거슬리는지.
- **v0.147.0 (A122)**: 센터 타일(2/3/4열 전부)·좌 대형 그래프에서 y축 라벨("100%" 등)이 채널명
  줄 **아래**로 내려가 겹침이 사라졌는지, 하단 바 긴 그래프는 S/M/L 전 단계 종전 동일(무변경 확인).
- **v0.148.0 (A125·A126)**: 설정 Updates 섹션 — 버튼이 빠진 자리 줄 간격(헛간격 없어야), **설정
  진입만으로 Last checked가 갱신**되는지(이게 이제 유일한 수동급 확인 수단). A126 — ⚠️ **16px
  타이틀바 아이콘 본체가 12px로 줄어든 뒤 "KO/TU" 가독**(A46 하한 재검 — 안 읽히면 inset을
  줄이는 조정 등재), 32px 태스크바에서 본체 24px + 하단 3자 라벨 동시 표시 균형, 트레이 아이콘도
  같은 inset(창과 한 모습)이 어색하지 않은지.
- **v0.149.0 (A124) ⚠️ 이번 묶음 최대 변경**: H/W 창 + 파일 창 2~3개 상태에서 Restart as admin →
  UAC 승인 → **전 창이 모듈·파일·위치·크기·최대화까지 재현**되는지(생성 순서 = 인스턴스 번호
  일치), 복원된 창들의 트레이 아이콘·태스크바 그룹 분리(A100·A105 자동 배정). UAC **취소** 시
  아무 창도 안 닫히는지(세션 파일만 되지워짐 — %AppData%\KOTU\restart-session.json 잔존 없음).
  UAC 프롬프트 2분 방치 후 승인 = **1창 폴백이 사양**. 미저장 문서가 있어도 묻지 않고 내려가는
  건 현행 유지(변경 아님).

**v0.144.0~v0.145.0 (2026-08-15 11회전 계속) — 각 확인 포인트**
- **v0.144.0 (A118)**: ⚠️ **F2 이름변경 공존이 최악 회귀 후보** — 탐색기 리스트/썸네일에서 파일
  선택 후 F2 = 이름변경만(우 패널 동시 발화 금지), 선택 없으면 F2 = 우 패널. 문서 에디터 편집 중
  F1/F2 = 패널 동작 + 문자 미입력(이 변경의 목적). F1 꾹(오토리피트) = 홀드 유지·2연타 오염 없음.
  이름변경 편집 중 F1 = 편집 커밋 여부 체감(포커스 이동 — 거슬리면 보고). Alt+`·Alt+숫자 정상 +
  Alt 단독 = OS 메뉴 모드 정상(부기 존치 검증). Fn 잠금 키보드에서 F1이 OS 도움말을 부르는지.
- **v0.145.0 (A119) ⚠️ 이번 묶음 최대 변경**: ① 정보 모듈 진입 기본 = 좌 그래프·우 스펙 양쪽
  사이드바 + 센터 2열 정사각형 ② F1/F2 왕복 — 홀드=반투명 덮기(열 수 불변)·2연타=사이드바(열 수
  3→2 등 즉시 전환)·열림 1회=닫기·Enter 일괄 토글·경계 버튼, 파일 모듈과 체감 동일한지 + 힌트
  "press F1" 문구 ③ 창 리사이즈 중 타일 정사각형 유지·CPU 스파이크(무한 레이아웃 징후) 없음·
  2열 세로 스크롤 ④ H/W(패널 열림)→비디오→H/W 왕복 — 이전 패널 잔존·그래프 멈춤·메모리 증가
  없는지 ⑤ F11·핀 접힘 왕복 정상(긴 그래프 스트립 이관 무변경).
- **A110 감사 낙수 (버전 무관)**: ① 창 2개 → A에서 모듈 전환(힌트 표시) → 2.5초 안에 A 닫기 →
  3초 관찰, B 정상이면 무해 확정(분리된 요소 지연 틱 — WinUI 해체 타이밍은 실기기 영역) ② 창 2개
  겹침 → A 경계선 근처 마우스 정지(버튼 표시) → 그대로 Alt+Tab → A의 경계 버튼 잔존 여부(코스메틱)
  ③ A에서 Alt 누른 채 B 클릭 → B 위에서 Alt 뗌 → B가 창 메뉴 모드로 가는지(가도 OS 기본 동작 확인만)
  ④ 정보+사진 2창에서 Alt+Z/X·2연타·Enter 토글·S4가 포커스 창에만 작용하는지(코드상 보장 — 스팟 체크).
- **v0.140.0 (A112)**: ⚠️ **XButton1이 WinUI PointerPressed로 오는지가 관건**(일부 마우스 유틸은
  WM_APPCOMMAND로 가로챔) — 영상 열고 뒤로가기 버튼 1회 = S1 복귀. 영상 재생 중 닫기 → 소리 즉시
  정지 → 곧장 OS 탐색기에서 그 파일 삭제(잠금 없어야 함). 미저장 문서 → 대화상자 → Cancel=그대로
  (● 유지). 전체화면 2단(1회=창 모드·재생 유지, 2회=닫힘). 좌만 연 채 닫기 → 좌만 유지(사이드바
  보존). S4 중 = S4 복귀만. Browser Back 키(멀티미디어 키보드): 동일 + 문서 에디터 포커스 중엔
  무동작(사양 — XButton1은 동작).
- **v0.141.0 (A113)**: 혼합 줄바꿈 .txt(CRLF+LF 섞어 제작) 열고 Ctrl+S → "Saving will normalize
  this file", 취소 시 파일 무변경. 편집 → 메모장으로 같은 파일 수정 → KOTU Ctrl+S → "File changed
  on disk"의 **Enter 기본이 Cancel인지**(파괴 방지). ⓐ 강제 실패는 USB 뽑기/외부 잠금으로 —
  Retry/Save as.../Cancel 3버튼. Save as... 후 상태바 파일명·트레이 갱신(창 제목은 미갱신 — 알려진
  한계). 대용량(4MB 근처) 파일에서 1글자 치환 연타 시 끊김 여부(250ms 디바운스). undo 원복 → ●가
  250ms 내 꺼지는지(셸 제목 ● 포함).
- **v0.142.0 (A115)**: ⚠️ **가이드-글자 정렬이 성패** — 순수 텍스트·한글/이모지 혼재 줄·**150%
  배율**에서 가이드가 실제 글자 상하단에 붙는지(실측 방식이라 어긋나면 보고). 플릭 스크롤 중 랙
  (패스당 rect 수백 회). 첫/끝 잘린 줄의 장식 클립. ¶(하드 개행)·`·EOF`(파일 끝) 위치와 0.25
  불투명도가 "읽기 방해 없음" 수준인지 — 거슬리면 상수 2곳(EditorDecor 상단) 조정. 템플릿 변경
  등으로 장식이 아예 안 뜨면 그게 의도된 폴백(크래시만 아니면 정상 — 보고).
- **v0.143.0 (A104+A105)**: ⚠️ **① AUMID 그룹 분리 실효가 관건**(Win11에서 창 2개가 태스크바
  별개 버튼인지 — "항상 결합" 설정에서도). 고정(pin) 부작용: 인스턴스 버튼 고정 → 재실행 → 같은
  슬롯 창하고만 재결합·죽은 버튼 여부(수용 트레이드오프 — 심하면 보고). ② 태스크바·Alt+Tab
  32px 아이콘 하단 3자(IMG/ARC/INF) 가독(150% 포함 — 조정 노브 `InstanceIcon.LabelFontScale`).
  **archive = 중앙 "ZIP"+하단 "ARC", hardware = "HW"+"INF" 공존**이 어색한지(단일 출처 매핑의
  의도적 결과 — 어색하면 사용자 결정). 정보 창 16px 타이틀바 아이콘이 종전과 동일한지(재합성 경유).

**v0.132.0~v0.139.0 (2026-08-15 10회전 후반) — 각 버전 확인 포인트**
- **v0.132.0 (A106)**: 32px 버튼에서 FontIcon(18) 세로 잘림 여부(⛶·핀·복사 등 — Padding 2 처방의 실효),
  배속 ComboBox "1.0×" 잘림, 시작 메뉴 항목 36 오선택률, 플라이아웃 밀착 -6에 틈이 생겼는지(생기면 -8),
  정보 바 1196~1212px·영상 바 698~718px 폭 전이 구간.
- **v0.133.0 (A83+A111)**: ⚠️ **영상 Contain 축소만이 실동작 변경 본체** — 작은 영상(480p)이 큰 창에서
  확대되지 않고 원본 크기인지, 작은 창→큰 창 리사이즈·전체화면 왕복에서 재판정되는지, 재생 시작 직후
  "확대→원본" 한 번 튐(트랙 파싱 전 — 알려진 한계), 새 영상 = Contain 회귀. 플라이아웃 4항목(100%만
  아이콘 없음) 정렬, 본체 84×32에서 "1:1"·글리프 잘림, A 키 = 100% 적용 + 본체 표시 갱신, F = 재적용.
- **v0.134.0 (A107)**: ⚠️ **Alt 메뉴 모드 차단이 핵심**(코드로 재검증 불가 부류) — Alt+Z 홀드 후
  Z→Alt 순서로 떼도 창 메뉴로 포커스를 안 뺏기는지, Alt+Tab·Alt+F4·Alt+Space는 그대로인지,
  Alt+` 후 플라이아웃이 열린 채 Alt를 뗄 때 이상 동작(있으면 보고 — 후속 패치 준비됨), 외부 탐색기에서
  파일 열어 시작 → 즉시 Alt+Z/X(계기 시나리오), Alt+Z+X 동시(한 프레임), 문서 에디터에서 z·x·숫자
  타이핑 정상, 단독 `·숫자·Z/X 무동작 확인.
- **v0.135.0 (A108+A116)**: ⚠️ **파일 열기·S4 진입 후에도 사이드바 25% 유지가 핵심 재현**(종전 30%로
  벌어짐 — A57 구 사양을 대체했으므로 25%가 어색하면 보고 → 상수 1곳 복귀), 틴트 0.06/0.24가
  사이드바와 확실히 구분되는지(어두운 영상 위 가독성 포함), "Sidebar - press Alt+Z to close" 힌트가
  경계 버튼 옆에 뜨는지·클릭 방해 없는지.
- **v0.136.0 (A109+A114+A117)**: 모듈 전환마다 좌·우 사이드바 기본(파일 더블클릭 창은 무사이드바),
  S4 중 전환 시 잔상 없음, 시작 약 2분 뒤 "Last checked" 자동 갱신·**토스트 절대 없음**·설정 진입 즉시
  체크·2분 주기 체감 끊김 여부(있으면 워커 이관 등재), Date created 정렬·재시작 복원.
- **v0.137.0 (A101)**: 센서 전용 트레이 아이콘이 사라지고 H/W 창 아이콘이 값 2줄 표시(모듈 파랑 1색 —
  구 채널색 대비 가독성 체감), 선택 1개 = `값/—` 표기 수용 여부, H/W 창 없으면 트레이에 값 없음(사양),
  구 SensorTray의 NotifyIconSettings 잔재 항목은 무해(청소 코드 없음).
- **v0.138.0 (A60 3차) ⚠️ 이번 세션 최대 변경**: 720×540에서 3구획 꽉 참·8열 타일(~40px, 축 라벨
  숨김이 정상)·우측 라벨 120px 가독성, 타일 클릭=선택(최대 2·오래된 것 밀림)·핀 배지, 타일 드래그
  순서 변경(클릭과 오발 없는지·재시작 복원), 좌 큰 그래프 즉시 렌더, 하단 긴 그래프 2개 가운데 정렬·
  S/M/L에서 글씨만 변함(폭 152 고정), A61 접힘 바에 긴 그래프 표시(A72 흡수 가치), 전체화면 이관,
  50ms에서 5분 창이 30초로 줄어드는 표기(사양), 맥박 숨김 676 전이.
- **v0.139.0 (A87)**: docs/USER-GUIDE.md·site/guide.html 내용 검토(사실 오류 발견 시 보고),
  guide 링크·site 렌더.

**v0.131.0 (2026-08-15 10회전, A70) — 확인 포인트**
- H/W 창 2개를 띄우고: ① 창 A에서 카드 핀 토글 → **창 B의 핀은 불변**, 트레이 센서 아이콘 = A의
  선택(마지막 조작) ② 창 B에서 바 크기 순환(B 키/버튼) → **A 불변**(이 창만 커짐) ③ Shift+N 새 창 →
  마지막 조작 상태(선택·바 크기)로 시작 ④ 앱 재실행 → 마지막 조작 1벌 복원 ⑤ 트레이 아이콘 우클릭
  "Hide tray sensors" → 아이콘만 사라지고 **열린 창들의 핀은 유지**, 아무 창에서 핀 토글하면 아이콘
  부활(그 창 선택 전체로) ⑥ **창 1개 운용은 현행과 체감 동일**해야 함(회귀 확인 — 토글·저장·트레이 갱신
  순서 불변) ⑦ 하드웨어 창에서 Shift+N이 빈 하드웨어 인스턴스 새 창으로 열리는지(코드로는 성립 확인).

**v0.126.0~v0.130.0 (2026-08-14 8·9회전) — 각 버전 확인 포인트**
- **v0.129.0 (A99)**: 전 모듈 하단 바에서 모듈별 열기 버튼이 사라지고 **S4 'Open file'(☰ 옆)만**
  남았는지, O 키가 어느 모듈에서도 무동작인지(다른 문자 키는 그대로), **각 바의 요소 정렬이
  안 깨졌는지 ⚠️ 재번호 배치라 레이아웃 회귀가 최대 위험** — 특히 압축(19컬럼)·영상 바.
  영상 바는 좁은 창에서 볼륨·시간 숨김 임계가 718로 내려갔으니 720~760px 폭에서 이제 안 숨는 게 정상.
- **v0.130.0 (A102+A103)**: ① 트레이·창 아이콘에서 원형 번호 배지가 사라졌는지(번호는 제목
  `2KOTU`가 담당) ② 링 색이 인스턴스 색이 아니라 **모듈 색**인지 — 창 1개여도 링이 보이는 게
  사양, 정보(H/W)·설정만 무링 ③ 트레이 값 글자 0.85배가 링과 안 겹치는지(조정 =
  `TrayStatusIcon.FontScale` 상수 하나) ④ 제목이 `2KOTU - 파일명` 형식인지(모듈명 단어 소멸 —
  **설정 화면도 `KOTU`만**, 어색하면 보고) ⑤ 안내 문구들의 em-dash가 하이픈으로 바뀐 게 어색하지
  않은지("값 없음 —" 글리프는 그대로가 정상).
- **v0.126.0 (A100 1차)**: 작업표시줄 설정 > 기타 시스템 트레이 아이콘에서 — ① 앱 재시작·창 여닫기를
  반복해도 **KOTU 항목이 더 늘지 않는지**(기존 잔재는 무시) ② 항목 상태가 유지되는지.
  ※ 수동 켜기는 v0.128.0(2차)이 대신한다 — 2차가 실패할 때의 후퇴선으로만 유효.
- **v0.128.0 (A100 2차) ⚠️ 이번 건의 본체**: ① **지금까지 안 띄워 본 개수째 인스턴스**(예: 처음으로
  3개째)를 열고 **아무 조작 없이** 약 10초 안에 트레이 아이콘이 보이는지 — 신규 슬롯 항목 자동 승격의
  핵심 재현. ② 작업표시줄 설정에서 KOTU 항목을 일부러 '끔'으로 바꾸면 다음 아이콘 생성(새 창·탐색기
  재시작) 때 다시 켜지는지 — **사양이다**(자동 표시가 수동 '끔'보다 우선, 수용된 리스크 (c)).
  ③ AhnLab 상주 환경에서 레지스트리 쓰기에 오탐 반응이 없는지(리스크 (b) — A38 때와 같은 우려).
  ④ 실패해도 앱 동작에는 영향이 없어야 한다(아이콘만 '끔'으로 남음 — 그 경우 1차의 수동 1회 켜기로
  후퇴하고, OS가 키 구조를 바꿨을 가능성(리스크 (a))을 의심할 것).
- **v0.127.0 (A85)**: ① 이미지 모듈 중앙 썸네일·좌 리스트의 이미지 파일 **더블클릭 → 현재 창에서
  열림**(핵심 재현) ② 한 번의 더블클릭에 **한 번만** 열림 — 특히 설정 "Always open files in a new
  instance" ON에서 새 창이 2개 뜨지 않는지 ③ 폴더 더블클릭 진입(중앙=좌 리스트 동기) ④ Shift+더블클릭
  =새 창·우클릭 새 인스턴스=종전대로 ⑤ 좌 리스트 포커스 Enter = 선택 파일 열기/폴더 진입(신설),
  선택 없으면 기존 오버레이 Enter 동작 ⑥ F2 편집 중 편집 상자 더블클릭 = 텍스트 선택만
  ⑦ S4('오픈 파일') 썸네일 더블클릭 = 열리며 S4 자동 종료.

**v0.123.0~v0.125.0 (2026-08-13 7회전, Fable ②) — 각 버전 확인 포인트**
- **v0.123.0 (A98)**: 이미지 뷰어에서 **휠 단독 = 줌**(구 휠 이전/다음 탐색이 사라진 것 체감 — ←/→는
  그대로), Ctrl+휠도 줌, Shift+휠은 이제 무동작. PDF는 Ctrl+휠=줌·휠 단독=스크롤. ⚠️ **영상 Ctrl+휠
  줌이 이번 최대 미검증 지점** — libvlc Scale 직접 설정이라 실기기에서만 확인 가능: 줌 인/아웃이
  실제로 화면 배율을 바꾸는지, 자동 맞춤(레터박스)에서 시작한 첫 노치가 튀지 않는지(유효 배율 계산의
  정합), 줌 뒤 창 리사이즈에 **안 따라오는 게 정상**(Fit 추종 해제), 새 영상 열면 자동 맞춤 회귀,
  휠 단독 볼륨은 그대로인지. 전체화면에서 `Zoom n%` 칩. 썸네일 뷰·오버레이 위 Ctrl+휠이 줌으로 안 새는지.
- **v0.124.0 (A94 1차)**: 다중선택(Ctrl 토글·Shift 범위·Ctrl+A)·트리는 단일 선택 유지. 드래그 아웃 →
  OS 탐색기에 복사되는지·**탐색기가 이동을 택했을 때 원본 삭제 주체가 어느 쪽인지 관찰**(대조표 명기
  항목). 좌 리스트·중앙 썸네일로 드랍: 같은 볼륨=이동/다른 볼륨=복사, Ctrl/Shift 강제와 커서 표기,
  폴더 항목 위=그 폴더로, 같은 폴더 이동=커서부터 막힘, 폴더를 자기 하위로 못 넣음, 충돌 시 "이름 (2)".
  **OS 탐색기→KOTU 드랍 기본이 복사인 것**(외부 소스 볼륨 미상 — 사양). Ctrl+C/X/V 앱 내 동작,
  KOTU→OS 탐색기 붙여넣기(잘라내기가 복사로 떨어질 수 있음 — 대조표 확인 항목). 조작 후 좌·중 동시 갱신.
- **v0.125.0 (A94 2차)**: F2 인라인 편집 — 확장자 제외 선택, Enter/딴 곳 클릭=커밋·Esc=취소,
  잘못된 문자·충돌 시 안내 문구+원복, **편집 중 문자 키가 첫 글자 점프·핫키로 안 새는지**.
  Ctrl+Shift+N 새 폴더 → 곧장 이름 편집으로 이어지는지(썸네일 쪽은 좌 리스트 경유 재스캔 뒤라 한 박자
  늦을 수 있음 — 관찰), **Shift+N(새 창)과 안 섞이는지**. Del=휴지통(확인창 없음 — 휴지통에서 복원되는지),
  Shift+Del은 무동작(후속 — 영구 삭제 미구현). 우클릭 Rename·Delete(폴더 포함), Delete가 다중 선택을
  따르는지. ⚠️ 우클릭 Rename은 플라이아웃 닫힘 후 편집 진입 — **포커스 경합으로 편집이 곧장 닫히면 보고**.

**그 밖에 오래 밀린 것**: 오버레이 아크릴이 비디오 스왑체인 위에서 비치는지(v0.31.0),
Alt 리스트가 Zoom 화면공유에 보이는지(v0.32.0), 시크바 길이 불변(v0.49~0.53).

---

## 5. 사용자에게 물어야 할 미결 사항

`docs/REQUIREMENTS.md` 부록 B 끝의 "확인 필요"에도 있다.
**2026-08-13 사용자 답변(부록 B 41~48)으로 대부분 해소됐다.** 남은 것만 적는다:

- ~~A86 키 동작 표~~ → **합의 완료(2026-08-13, 부록 B 53). 확정 사양 = `docs/A86-keymap.md` — 착수 가능.**
  A87 단축키 장 = A34 키 맵 + 이 파일.
- **A83 — 초안 v1 제시했으나 사용자 보류**(2026-08-13 6회전: "오토핏 결정하기 어려우니 그것만 빼고
  진행"). 초안·미결 쟁점 Q1~Q7 = `docs/A83-autofit-draft.md` — 재론 때 여기서 시작, **확정 전 구현 금지**.
  이미지 모듈 Fit 규격(A30 이전 규격 잔존) 처리도 A83 결론 뒤에.
- **착수 시점에 확인할 것**: A87 버전 표기 방식(릴리스마다 갱신 vs "vX.Y.Z 기준").
  ※ ~~A79 단계 구획·지정 UI~~ · ~~A96 번호 배열~~ · ~~A67 sponsors.json 반영~~ → **2026-08-13 5회전에서
  사용자 확정 후 구현 완료**(각각 v0.119.0 · v0.116.0 · v0.115.0).
- **5회전에서 새로 생긴 판단 대기 2건** (실기기 확인 후 사용자가 결정):
  ① **A62 L 단계** — A97이 카드 상한을 36으로 내려 L 높이가 M과 같아졌다. 유지할지, 상한만 40으로 되돌릴지.
  ② **A79 레벨 1의 16px 발바닥** — 안 읽히면 ①(중립 표식)만 "KO/TU" 글자로 되돌릴지.
- **6회전에서 새로 생긴 후속 등재 후보** (실기기 확인 후 사용자가 결정 — §4 v0.120~122 항목 참조):
  ① A93 썸네일 대용량 폴더 성능(가상화) ② A90 S4의 A92 힌트 문구 부정확 ③ A86 홀드 표시 지연(A58
  시절부터의 잔여 검토 — Z/X로 바뀌며 오타이핑 노출은 줄었지만 여전히 유효).
- **7회전에서 새로 생긴 판단 대기** (실기기 확인 후 사용자가 결정):
  ① **A94 후속 범위** — 영구 삭제(Shift+Del)·권한 상승(UAC)·충돌 대화상자·폴더 감시(FileSystemWatcher)·
  잘라내기 반투명 표시·빈 영역 우클릭 메뉴·다중 일괄 열기. 전체 목록 = `docs/A94-matrix.md`의 ▫/❌ 행.
  A94 항목은 이것들이 결정될 때까지 **미완료 유지**(결번 처리 안 함).
  ② **A98 이미지 휠 탐색 제거 체감** — 휠 이전/다음이 사라진 것이 불편하면 대안(예: 미줌 상태 한정 복원,
  다른 키 할당) 재론.
- **10회전(v0.131.0~v0.139.0)에서 새로 생긴 판단 대기·후속 등재 후보** (실기기 확인 후 사용자 결정):
  ① **A116 처방 = 사이드바 25% 전 상태 단일화** — 콘텐츠 상태 30%(A57 구 사양)를 대체했다. 의도와
  다르면 `MainWindow.xaml.cs`의 `SidebarPercent` 상수 1곳 복귀. ② **틴트 0.06/0.24 체감**(A108) —
  조정은 App.xaml 한 곳(다크·라이트 쌍 동일 값). ③ ~~site/index.html이 v0.89.0을 광고~~ → **A132(v0.159.0)가 수동 갱신으로 해소**(자동화는
  도입 안 함 — 필요 시 별도 등재). ④ ~~트레이 채널 색 소실(A101 — TrayStatus 계약에 색 없음)~~ →
  **A169(v0.172.0)가 `uint?` ARGB 계약 확장으로 복원해 해소.** ⑤ A107 Alt+` 플라이아웃 포커스 엣지(후속 패치 준비됨 — §4
  v0.134.0). ⑥ 2분 업데이트 체크의 UI 스레드 체감(A114 — 끊기면 워커 이관).
- **11회전(v0.140.0~v0.143.0)에서 새로 생긴 판단 대기·후속 등재 후보** (실기기 확인 후 사용자 결정):
  ① **A115 ④ 커서(형광 녹색+검정 테두리) = 불가 판정으로 잔여 존치** — WinUI 캐럿 색 공개 API
  없음(#9005 not planned·RichEditBox 동일 한계). 대안 = 커스텀 캐럿 오버레이(이중 캐럿 문제) /
  OS 접근성 "텍스트 커서 표시기"는 사용자 설정 몫. 진행하려면 별도 등재 필요.
  ② **A113 낙수** — Save as... 후 창 제목 미갱신(셸 OnContentOpened가 제목을 안 만지는 기존 동작).
  ③ **A112 범위 밖 후보** — XButton2(앞으로) 매핑 / S1에서 XButton1 = 폴더 Up.
  ④ **A110 낙수** — 비활성 창 경계 버튼 표시 잔존(코스메틱 — 방어선 Deactivated `HideEdgeButtons()` 한 줄).
  ⑤ **A105 고정(pin) 부작용 체감**(수용 트레이드오프 — §4 v0.143.0) / 아이콘 "ZIP+ARC" 공존 어색함.
- **A38 관련 미해결 관찰 — ①만 남았다** (2026-08-10 실기기, v0.88.0 시점):
  ① **.png 더블클릭 팝업이 토글 껐다 켠 뒤에도 남았다는 보고** — v0.89.0(A78)이 스스로 고치는지 우선 확인.
    그래도 남으면 UserChoice 해시를 Windows가 거부하는 것일 수 있다(§3.2의 자기일관성 맹점).
    그 경우 후속 항목 등재 필요: 쓰기 후 `AssocQueryString` 같은 **OS 측 확인**으로 검증을 바꾸고 실패 시 폴백.
  ② ~~KOTU-doc 토글 때 뜨는 Windows 설정 창 / 빈 "기본 앱 > KOTU" 페이지~~ →
    **A166(v0.184.0)으로 소화·종결.** UCPD 보호 확장자는 강제 지정이 **원리적으로 불가**로 판정됐고,
    보호 확장자는 이제 **강행 시도·딥링크 자동 오픈을 생략**하고 안내 + Set default로 유도한다.
    빈 페이지는 **KOTU 등록 결함이 아니라 OS 인덱싱 아티팩트**(Capabilities 등록은 정상)이며,
    이 변경이 그 페이지 노출 자체를 우회한다.

---

## 6. 다음에 할 만한 것

**2026-08-13 사용자 일괄 답변(부록 B 41~53)으로 사용자 답 대기 항목이 사실상 소진됐다 — 전부 착수 가능.**
**개발은 새 세션에서, 항목별로 모델을 달리 쓴다(사용자 방침 2026-08-13): Fable / Opus 중 택1.**
추천 기준 — **Fable**: 설계 판단·상태 머신·원인 불명 디버깅·API 조사가 낀 것(잘못 잡으면 되돌리기 비싼 것).
**Opus**: 사양이 표·수치로 완전히 확정돼 기계적으로 옮기면 되는 것(대조표·연쇄 목록을 프롬프트에 명시할 것).
**(2026-08-14 사용자 지시)** 권장 모델은 **등재 시점에 REQUIREMENTS 항목에 `[미반영 An · 모델]`로 병기**한다
(운용 규칙 5). 기존 미반영 16건에는 이 표의 추천을 소급 반영 완료 — 개별 추천의 정본은 이제 REQUIREMENTS,
이 표는 배치(순서·묶음) 제안용.

**※ 2026-08-13 5회전(Opus ①·② → v0.114.0~v0.119.0)·6회전(Fable ① → v0.120.0~v0.122.0)·
7회전(Fable ② A98+A94 1·2차 → v0.123.0~v0.125.0)은 완료.** 아래 표에는 **남은 것만** 둔다.

**※ 2026-08-15 11회전으로 잔여 착수 가능분(A110→A112→A113+A115→A104·A105)과 같은 날 신규
등재분(A118 v0.144.0 · A119 v0.145.0)까지 전부 완료됐다.** 등재된 미반영 중 바로 착수 가능한 것
없음 — 남은 것은 전부 사용자 결정 또는 조사·설계 선행:

⚠️ **아래 표는 2026-08-19 (4)~08-20 세션(v0.185.0~v0.201.0)까지로 완전히 소진됐다** — 착수 가능분은
**0건**이고, 잔여 3건은 전부 사용자 입력 대기다(§1). **새 일감은 §1 위의 낙수(등재 후보) 목록에서
골라 등재하는 것이 다음 수순**이다.

| 후보 | 규모 | 모델 | 메모 |
|---|---|---|---|
| ~~A136·A139·A140·A141·A143·A146·A147·A148·A149·A154·A156·A157·A160·A162·A165·A167·A168·A169·A171·A172~~ | 소~중 | Opus | **전부 완료·결번**(v0.162.0~v0.174.0, 핫픽스 v0.169.1·v0.174.1) |
| ~~A137·A138·A144·A145·A150·A151·A152·A153·A155·A158·A163·A164·A166~~ | 중~대 | Fable | **전부 완료·결번**(v0.175.0~v0.184.0 11배치) |
| ~~A173~A191(19건)~~ | 소~대 | Fable·Opus 혼합 | **전부 완료·결번**(v0.185.0~v0.201.0 17배치 + A178 감사 — 코드 변경 0) |
| **A142 ④ · A159 · A170** | 소~중 | Fable | **사용자 입력 대기** — ¶/EOF 스크린샷 / 압축 뒤로가기 증상 구분 / 종료 시 밝기 실기기 판별 5종 |
| **A41·A48·A47·A43·A11** | 중~대 | Fable | 조사·설계 선행(휠 충돌·비공식 API·드라이버 서비스화·포터블·연속 재생) |
| A4·A14·A15·A19·A45 | 조사~대 | — | 후순위·보류(부록 A 참조) |

**※ 2026-08-18 등재분(A136~A172, 37건)의 배치 힌트**: 같은 파일을 만지는 묶음이 뚜렷하다 —
`TrayStatusIcon.cs`+`InstanceIcon.cs`(A137·A138·A139·A140**+A169**), 제목·배지(A136+A141),
Fit 3뷰(A143+A144), 편집기 2파일(A142 전체**+A171**), 좌 리스트 `MakeListItem`(A155+A156+A157**+A160**),
오디오 하단 바 칸 신설(A163+A164), H/W(A146+A168+A172), 설정 화면(A162+A167).
**순서 의존이 있는 것**: ① **A151(전체화면 3단) → A158(F1/F2 → F11/F12)** — F11이 비어야 성립.
② **A142는 ②(줄 간격 = TextBox 한계) 판정을 먼저** 내야 ③④⑤가 헛일이 안 된다.
③ **A152(콘텐츠 배치 기준)는 A151과 한 몸** — Full window 모드가 생겨야 "바 없는 상태 기준"이 의미를
갖고, A83 Fit 실효 배율에 회귀 위험이 있다.
④ **A145(공통 요소 전수조사) 결과가 A165(All Readable)·A149(이미지 바)와 겹친다** — 표를 두 번 만들지 말 것.
**확인 필요 25건이 부록 B에 모여 있다** — 사용자 답을 받은 뒤 큰 배치를 열고, 그 전에는 확인 불요
소형(A139·A141·A143·A148·A156·A160·A162·A167·A172)으로 채우는 편이 낫다.

**※ 2026-08-16 등재분(A120~A126)은 같은 날 12회전(v0.146.0~v0.149.0, 4배치)으로 전부 완료** —
착수 가능분 다시 소진. **13회전(v0.150.0~v0.153.0)으로 A94 후속까지 완주·결번 —
매트릭스는 영구 기록(스코프 제외 5건 사유 병기, COM 계열은 별도 등재 후보).** 남은 미반영 = 위 표(A94 후속 = 사용자 결정 대기 / A41·A48·A47·A43·A11 =
조사·설계 선행 / 후순위·보류군)뿐이다.

이 밖의 재료: §5의 판단 대기(A115 ④ 커서 대안·A113/A112/A110 낙수·site v0.89.0 표기 등)는 사용자
답을 받으면 소형 등재→배치가 가능하다.

**다음 세션 배치 제안**:
1. ~~**Fable ③**: A85 디버깅 단독~~ → **완료(v0.127.0, 2026-08-14 8회전).**
2. **2026-08-14 일괄 등재(A101~A117 + A60 3차)로 후보가 크게 늘었다 — 개별 추천은 REQUIREMENTS
   `[미반영 An · 모델]` 병기가 정본**(운용 규칙 5). 배치 제안(확인 필요 답 수신 후):
   **Opus 후보** = A106(치수 개정) / A109 / A114 / A117 — 사양이 수치·문구로 확정된 것들
   (~~A102+A103~~ → v0.130.0 완료). **Fable 후보** = A107(키 전면 개편 — 단독 대형) /
   A60 3차+A101(H/W 재구성 묶음 — 최대 덩어리, ~~A70~~ → v0.131.0 완료로 선행 충족) /
   A108+A116(사이드바 개념·비율 버그) / A112 / A111+A83 / A113+A115(문서). A110은 A107·A108 뒤에 검증으로.
2. **Opus**: A87(사용자 문서) — 선행 조건 전부 충족(A34 키맵 + A86-keymap.md 확정·구현 완료).
   착수 시 버전 표기 방식(릴리스마다 갱신 vs "vX.Y.Z 기준")만 사용자에게 확인.
A83 표 합의·A94 후속 범위 결정은 아무 세션에서나 Fable 대화로 재개 가능(각각 초안·대조표 보관됨).

작은 것들을 묶어 한 버전에 내는 방식이 잘 통한다(v0.87.0 4건, 2026-08-12 배치도 2건 묶음 2회).
**같은 파일을 건드리는 항목끼리 묶으면** 문자열을 두 번 만지지 않아도 된다.
서브에이전트 파이프라인 운용법: 오케스트레이터가 배치 정의·diff 검수·커밋·푸시·CI 확인을 맡고,
서브에이전트에게는 저장소 경로(파일 도구/bash 별도)·dotnet 없음(정적 검사 필수)·XML 주석 `--` 금지·
레거시 식별자 불가침·커밋 금지·버전/REQUIREMENTS 결번 처리까지 명시해 넘기면 한 배치가 그대로 닫힌다.

**3회전에서 추가로 배운 것 (2026-08-12)**:
- **CI를 깬 것은 `TextBlock.IsEnabled`(CS1061)** — TextBlock은 `Control`이 아니라 `FrameworkElement`라
  `IsEnabled`가 없다. "비활성으로 보이게" 요구는 TextBlock에서 `Opacity`로 풀어야 한다.
  서브에이전트의 정적 검사(파싱·grep·중괄호)로는 **타입 멤버 존재 여부를 못 잡는다** — 이게 이 방식의 한계다.
  4배치 중 1건만 실패했으니 비용 대비 여전히 유리하지만, **UI 속성을 새로 대입할 때는 그 속성이
  그 타입에 있는지** 프롬프트에 못 박거나 오케스트레이터가 diff에서 따로 볼 것.
- **"구현 시 결정" 항목은 프롬프트에서 미리 답을 주는 게 결정적으로 잘 통했다** — 서브에이전트는 되물을 수
  없으므로, 치수·설정 키 이름·문구·실패 시 동작까지 정해 주면 배치가 한 번에 닫힌다.
- **WMI처럼 무거운 의존은 이미 참조가 있는 프로젝트에 붙인다** — A22에서 `System.Management`가
  하드웨어 모듈에만 있어 조회 로직을 거기 두고 App이 델리게이트로 넘기게 했다. csproj 변경 0.
- **App → 모듈 → Core 단방향 참조**는 공용 UI를 만들 때마다 걸린다. 해법은 A57 ②/A22가 쓴
  **"컨트롤은 셸에, 계약(object 타입 인터페이스)은 Core에, 모듈은 슬롯만 내준다"** 패턴이다.

**4회전에서 추가로 배운 것 (2026-08-13, 5배치 CI 5/5 초록 — 핫픽스 0)**:
- **프롬프트에 "이 배치의 최대 함정"을 미리 적어 주면 그대로 막힌다.** A61의 최소 창 크기(A40)처럼
  **놓치면 기능이 통째로 무동작이 되는 지점**을 오케스트레이터가 먼저 찾아 넘긴 배치는 한 번에 닫혔다.
  3회전의 교훈("구현 시 결정은 미리 답을 준다")의 확장판이다.
- **"기존 사용례 grep으로 API 실재를 증빙하라"를 규칙으로 못 박으니 v0.108.1 같은 CS1061이 0건**이 됐다.
  서브에이전트가 저장소에 선례가 없는 API를 쓸 때는 **CI 1순위 위험 후보 + 최소 복구법**을 보고하게 하면
  실패해도 복구가 1분이다(이번엔 4건 보고, 전부 실제로는 통과).
- **서브에이전트의 "요구사항에 없던 판단" 보고가 실제로 사양을 고쳤다** — A74에서 "A17 셀과 하단 바
  카드가 같은 객체"라는 코드 사실 때문에 내가 준 적용 범위가 틀렸고, 에이전트가 폭 규칙으로 바꿔 살렸다.
  **지시가 코드와 어긋나면 지시를 고치라고 명시**해 두는 편이 낫다.
- **KOTU.Core는 UI 비의존(net8.0)** 이라 UI 공용 코드를 어셈블리로 못 만든다. A34는 `src/Shared/*.cs`를
  **7개 csproj가 Compile Link로 공유**해 풀었다(어셈블리마다 internal 사본 — 이름 충돌 없음).
  **UI 공용 헬퍼가 또 필요하면 이 패턴을 쓸 것.**
- **새 모듈 프로젝트를 추가할 때 빠뜨리기 쉬운 자리**: sln 프로젝트 항목 + **구성 매핑 4줄**, App의
  ProjectReference, DI/라우터 등록(**등록 순서 = 라우팅 우선순위**), 번호 키·시작 메뉴·창 아이콘 매핑,
  Branding 액센트, `.ico` Content + 실제 파일, 아이콘 생성 스크립트, ARCHITECTURE. A59에서 이걸
  **대조표로 증빙**하게 해 한 번에 통과했다.

**5회전에서 추가로 배운 것 (2026-08-13, 6배치 — 핫픽스 0)**:
- **"이 배치의 통과 조건"을 프롬프트에 이름 붙여 못 박는 게 가장 잘 통했다.** A96 = 번호 연쇄 대조표,
  A95 = 삭제 심볼별 grep 표, A54 = 수명 감사표 + 모듈 배선 대조표, A79 = 레벨 매핑 대조표.
  **표를 내라고 하면 에이전트가 스스로 빠진 곳을 찾는다** — A54에서 "Restart as admin의
  `Application.Exit()` 경로에 트레이 잔존" 실제 버그를, A79에서 "발가락이 16px에서 붙는다"를 이렇게 찾았다.
- **제거 위주 배치는 "지운 것"보다 "지우면서 미아가 되는 것"이 위험하다.** A95에서 스크롤 경로를 지우자
  `_updatesHeader`가 "대입만 되고 읽히지 않는 private 필드"가 되어 **CS0414로 빌드가 깨질 자리**였다
  (`TreatWarningsAsErrors`). 삭제 배치는 **삭제 대상 + 그 삭제로 고아가 되는 것**을 같이 훑게 할 것.
- **오케스트레이터가 준 수치가 틀렸는데 에이전트가 고쳐 온 사례 2건.** A97의 "간격 4/10 혼재"는 실제로
  4가 없었고(10과 12였다), A79의 "124/62 하드코딩"은 실제로 120×60이었다. **"지시가 코드와 어긋나면
  지시를 고치라"는 문장이 실제로 값을 했다** — 계속 넣을 것.
- **파생 상수를 찾아 같이 고치게 해야 한다.** A97에서 버튼 40→36을 시키자 `ModuleBarHost.Margin 52`,
  `InstanceBadge.Margin 32,2`, 광고 이미지 120×60이 전부 그 40에서 나온 값이었다.
  **"이 값에서 파생된 곳을 찾아 함께 고치고 근거를 주석에 남겨라"**를 지시에 넣으면 한 번에 닫힌다.
- **CI로도 정적 검사로도 못 잡는 부류가 이번에 3개 늘었다** — ① `CompositionTarget.Rendering` 같은
  프레임워크 static 이벤트의 실제 발화, ② `Storyboard.SetTargetProperty("Opacity")` 경로 유효성,
  ③ WinUI 플라이아웃의 음수 Margin이 실제로 위치를 옮기는지. 셋 다 **실패해도 앱이 죽지는 않고
  "그 장식만 안 도는" 형태**로 설계해 두게 했다(§4의 각 항목에 최소 복구법을 적어 뒀다).
- **큰 배치일수록 신규 파일이 `git diff`에 안 보인다.** A54(Core 계약 2개 + 셸 1개)·A79(C# 4개 +
  Python 2개 + PNG 4개)에서 `git status --short`의 `??`를 따로 봐야 검수가 성립했다.

**6회전(Fable ①)에서 추가로 배운 것 (2026-08-13, 3배치 — 핫픽스 0)**:
- **"다음 항목이 쓸 훅을 미리 심어 두게 하는" 배치 설계가 잘 통했다.** A86에게 S4 자리(enum·
  `IsOpenFileBrowsing` 훅·경계 버튼 숨김 조건)를 만들게 하고 A90이 그 훅을 실제 상태로 바꿨다 —
  A90 배치에서 셸 상태 분배 코드를 다시 뒤집을 필요가 없었다. 직렬 연쇄 배치에서는 앞 배치 프롬프트에
  "뒤 배치가 얹힐 자리"를 명시할 것.
- **등재문이 코드 실상과 다른 사례가 또 나왔다**(A93의 "중앙에 Drop here 문구만 있다" — 실제는
  ExplorerPane). "지시가 코드와 어긋나면 지시를 고치라"는 문장이 이번에도 값을 했고, 에이전트가
  착오 사실을 REQUIREMENTS 완료 서술에 남기게 했다.
- **이벤트 순서(모듈 액셀러레이터 > 셸 루트 handledEventsToo 핸들러)는 셸이 역전 못 한다** — A90의
  Esc 우선순위(전체화면+S4 중첩)를 사양대로 못 만들고 "첫 Esc=전체화면 해제, 다음 Esc=S4 복귀"로
  조정했다. 키 우선순위 사양은 이 순서 제약 안에서만 성립한다는 걸 프롬프트에 미리 줄 것.
- **문자 키를 셸 전역 키로 쓸 때의 3종 세트**: ① 오토리피트(`KeyStatus.WasKeyDown`) 무시 ② 텍스트
  입력 통과(A32) ③ 타이핑 탐색 표면 통과(`PassThroughTag`/`ShouldPassThrough`) — A86이 Z/X·Enter에
  전부 적용했고, 새 전역 키를 추가할 때 이 셋을 빠뜨리면 안 된다.
- **UI 인스턴스는 부모가 둘이면 공유하지 말고 하나 더 만든다** — S4 썸네일을 S1 인스턴스와 공유하지
  않은 결정(reparent 함정 회피)이 코드도 검수도 단순하게 만들었다. 데이터 경로(좌 리스트 단일 원본)만
  공유하면 중복 비용이 거의 없다.

**7회전(Fable ②)에서 추가로 배운 것 (2026-08-13, 3배치 — 핫픽스 0)**:
- **서브에이전트가 "선례 없음 위험"으로 보고한 API 중 실제 위험은 소수다** — 오케스트레이터가 보고서의
  최소 복구법 중 **컴파일이 보장되는 형태(인터페이스 캐스트 등)를 선제 적용**하면 CI 재시도 비용이
  0이 된다(A94 1차의 `DataPackagePropertySetView.TryGetValue`에 실제로 했다). 반대로 지시가 "선례
  없음"이라 단정한 `StorageDeleteOption`은 에이전트가 grep으로 선례(ImageViewerView)를 찾아 뒤집었다 —
  **선례 유무는 지시가 아니라 grep이 판정**한다.
- **`DragItemsStarting`은 await 불가** — StorageItems 수집처럼 비동기가 필요한 드래그 데이터는
  컨테이너 `CanDrag`+`DragStarting`의 `GetDeferral()` 경로로. UI 스레드 WinRT 동기 대기 금지 관례와
  묶이는 제약이다.
- **handledEventsToo로 구독한 표면 KeyDown에는 `e.OriginalSource is TextBox` 가드가 필수** — 인라인
  편집 상자가 Handled를 걸어도 표면 핸들러까지 오기 때문. 인라인 편집을 다른 표면에 이식할 때 같은
  3중 격리(편집 상자 Handled / 표면 OriginalSource 가드 / HotkeySupport 텍스트 통과)를 확인할 것.
- **연쇄 배치의 2차 프롬프트에는 "1차가 만들어 둔 자리"를 명시**(OnSurfaceKeyDown·Notice·CurrentFolder 등) —
  6회전의 "다음 항목이 쓸 훅을 미리 심는" 설계의 역방향으로, 2차 에이전트가 코드를 다시 발굴하는
  비용을 없앤다. A94 1→2차가 이 방식으로 접합부 재작업 0이었다.

---

## 7. 브랜딩·구조가 크게 바뀐 버전들 (배경만 — 상세는 git log)

- **v0.86.0 (A46)** ZP → KOTU. 범위는 표시명 + 시스템 등록 ID. **기존 사용자 설정 이관 없음**(사용자 확정) —
  대신 구 브랜드 레지스트리는 첫 실행에 1회 자동 청소. 앱 아이콘은 4글자가 16px에서 뭉개져 **"KO"/"TU" 2줄**로 확정.
- **v0.87.0 (A35·A52·A53·A56)** 표시명 단수형 + ZIP→Archive, New Instance 문구, 제목에 인스턴스 번호,
  연결 섹션 순서. 우클릭 메뉴 라벨의 하드코딩을 없애 모듈 `BrandName`을 따르게 함.
- **v0.88.0 (A64)** exe·어셈블리·프로젝트·네임스페이스 전면 rename. A46의 "표시명만" 결정을 대체.
  계기는 UAC에 `WinUtil.App.exe`가 보인 것 — 사용자 관심사는 **"WinUtil이라는 단어"** 이지
  "게시자: 알 수 없음"(코드 서명)이 **아니다**. 저장소도 `zpro` → `kotu`로 변경됨.
- **v0.89.0 (A78)** 시작 시 셸 등록의 exe 경로 어긋남 자동 복구. 계기는 실기기에서 .png 더블클릭이
  '어떤 앱으로 열까요'로 빠진 것(A64의 예고된 부작용). 순수 로직은 Core `ShellCommand`로 분리해
  단위 테스트(ShellCommandTests). 구현 결정은 REQUIREMENTS 부록 B #25.
- **v0.90.0 (A82)** 개인 실명 제거 + 저작 주체를 **ZP Studios** 한 이름으로 통일
  (LICENSE·`Directory.Build.props`·release.yml `packAuthors`·site 푸터 — **표기를 바꿀 땐 이 4곳을 함께 본다**).
  git 이력은 재작성하지 않기로 확정 → **v0.89.0 이전 커밋의 작성자 표기는 GitHub에 그대로 남는다**(수용된 한계).
  로컬 `git config`에 이름·메일이 고정돼 있으니 커밋할 때 `-c user.name=...` 오버라이드를 쓰지 말 것.
  ※ 함정 하나: XML 주석에 `--packAuthors`라고 적었다가 하이픈 2연속으로 props 파싱이 깨졌다(커밋 전에 잡음).
