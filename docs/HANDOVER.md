# KOTU 인수인계 문서

> **읽는 법**: 이 문서만 읽고 다음 세션이 이어서 일할 수 있게 유지한다. 일하는 방식은 `CLAUDE.md`,
> 남은 작업의 사양은 `docs/REQUIREMENTS.md`가 정본이고, 여기에는 **지금 상태·함정·사용자 몫·다음 수순**만 둔다.
> 세션별 작업 일지(무엇을 어떤 순서로 했는가)는 `docs/SESSION-LOG.md`, 실기기 확인 대기 누적분은
> `docs/DEVICE-CHECKS.md`로 분리했다(2026-08-28 문서 정리 — 본문은 git 이력으로 복원 가능).
>
> **최종 갱신: 2026-09-01 (v0.290.0 시점 — 2차 묶음 완주)** — 2026-08-31~09-01 세션에서
> ① 사용자 신규 요구 16건을 **A292~A307**로 등재하고 ② A291 판정 스크린샷 2장을 수령해
> **판정 종료·잔여는 A307 승계**(§4)한 뒤 ③ **1차 묶음 A279·A280·A282·A278을 v0.283.0~v0.286.0으로**,
> 이어서 ④ **2차 묶음 A293·A297·A298·A299·A306을 v0.287.0~v0.290.0으로 완주**했다
> (§4에 확인 포인트 두 블록 — 둘 다 미확인이라 밀어내지 않았다).
> 직전 갱신(2026-08-29): A257~A277 21건 전량 완주(v0.257.0~v0.273.0·17릴리스)
> + A283~A291 완주(v0.274.0~v0.282.0 — 에디터 장식 9배치. **결함이 다섯**이었고 서로를 가리고 있었다:
> X축 누락(A286)·줄 탐색 정지(A287)·Y축 누락(A288)·가이드 밑변(A289)·빈 마지막 줄(A290).
> A285 계측 시설 없이는 못 잡았다).
> 태그·릴리스는 **v0.290.0까지 발행 — CI 부채 0**. 단 v0.272.0 태그는 미발행: 두 커밋을 한 push로
> 보내 release.yml이 HEAD 버전(0.273.0)만 발행(내용은 v0.273.0에 포함 — 규칙 "커밋마다 push" 재준수 교훈).

## 1. 지금 상태

앱 이름 **KOTU (King Of The Util)**, 저장소 **zpstudios/kotu**, 실행 파일 **KOTU.exe**,
최신 **v0.290.0**(CI 초록 확인. CI 부채 0 — v0.272.0만 동시 push로 태그 생략·내용은 v0.273.0에 포함).

**코드 상태**: 2026-08-31~09-01 세션에서 두 묶음을 완주했다 — **1차 A279·A280·A282·A278
(v0.283.0~v0.286.0)** 에 이어 **2차 A293·A297·A298·A299·A306(v0.287.0~v0.290.0)**. 구 5건 중
A281(조건부)만 남았고, 신규 16건 중 5건이 소진됐다. 직전 묶음은 A283~A291(v0.274.0~v0.282.0·에디터
장식 9배치)과 A257~A277(v0.257.0~v0.273.0·17릴리스). 세션별 배치 구성은 `docs/SESSION-LOG.md`가 정본.
2차 묶음의 핵심 발견 3건(다음 세션이 같은 착각을 반복하지 않게): ① A293의 좌측 빈칸은 레이아웃 여백이
아니라 **Busy 링 전용 칸**이었다 ② A297의 헤더 크기 차이는 패딩이 아니라 **Name 열만 `2*`** 였다
③ A298의 "이미지 모듈에만 적용" 전제는 사실이 아니었고 **이미 3모듈이 동일**해 작업이 통합으로 바뀌었다.
②③은 등재문 전제가 코드와 어긋난 사례다(규칙 9 — 코드를 따르고 보고).

**착수 대기열 = A281 1건 + 신규 잔여 11건 = 12건.**
- **A281**(브랜드 에셋 레벨 1의 16px 중립 표식 가독성 — Opus·소형): **조건부**다. 지금은 레벨 0이라
  화면에 안 나오므로 **레벨을 올릴 때 착수**한다(A79 잔여).
- **신규 잔여 11건(2026-08-31 등재 16건 중 A293·A297·A298·A299·A306 완주 — 사양 정본은 REQUIREMENTS
  본문·부록 A 표)**: 설정 A292 · 문서 A294·A295·A307 · 아카이브 A296(+A159 결박) · 하단 바 A300 ·
  오디오 A301·A302·A303·A304(업그레이드 확정) · **화면 모드 A305(대형 — A186 반전)**.
  참조 스크린샷 6장은 미수령 — 서술만이 사양이다(사용자 확정: 서술로 진행).

그 밖에 `docs/REQUIREMENTS.md`에 남은 `[미반영]` 코드 항목:
- **사용자 입력 대기 1건** — 부록 B "확인 필요"에 **2026-09-01 신규 1건**: A298·A299 잔여 =
  **Fit 버튼의 빈 화면 표시 통일 방향**(문서 = `1:1` 상자 / 이미지·영상 = 마지막 옵션 글리프 / 현행 유지 3택).
  그 밖은 전부 소진 상태다(A304는 2026-08-31 "업그레이드"로 확정. A159도 같은 날 증상 확정 —
  버튼 disabled. A142 ④는 2026-08-29 소진 → A283 승계).
- **장기·후순위**: A14·A15(녹화·녹음) · A19(HW 랭킹) · 보류 A4·A43·A45.
- 새 일감은 **REQUIREMENTS 부록 A-2 "낙수(등재 후보)"** 에서 골라 등재한다(§6).

**판단 대기(사용자 지시 없이는 손대지 않는다)**: A234 종결 후 남은 진단 스트립(`diag.shellKeyOverlay`)의
"계측 토글 제거 or 존치" — 미등재.

**방향 전환 주의**(완료 항목의 서술이 이미 뒤집혀 있다 — 규칙 1.6 "재확인"과 혼동 금지):
A257=A235② 반전 · A260=A253 5차 전환 · A264=A245 개정 · A267=A247 개정(팝업 3분기 전면 폐기) ·
A265=A249 예외 공식 해제 · A259=A146/A128 반전 · A273=A109/A81/A202 개정 · A269=A217 첫 재계수 ·
A274=A203 외부 경로 보완. 옛 항목의 구현 서술을 읽을 땐 이 반전들이 **이미 코드에 반영돼 있음**을 전제할 것.

**문서 상태**: 2026-08-29 3차 다이어트로 REQUIREMENTS 2,499줄 → 1,969줄(완료 상세 42건 571줄 ARCHIVE 이관).
이후 A292~A307 등재분이 얹혔고, 1차(A279·A280·A282·A278)와 2차(A293·A297·A298·A299·A306) 완료 상세를
규칙 5대로 **즉시 ARCHIVE 이관**해(본문에는 한 줄 요약만) 현재 **약 2,480줄**이다.
남은 완료 상세는 둘뿐이고 **둘 다 의도적 존치**다 — 미진행 항목이 A번호를 언급하는 20건, 그리고
**A257~A277 등 실기기 확인 대기 24건**(확인이 끝나면 같은 기준으로 이관할 것). 앞선 08-28 다이어트는
3,763줄 → 2,499줄이었고, 그때 세션 일지·실기기 누적분을 별도 파일로 분리해 HANDOVER가 이 크기가 됐다.
같은 날 실기기 확인 누적분도 정리했다: DEVICE-CHECKS 594줄 → **232줄**, v0.161.0 이하 20묶음은
`docs/DEVICE-CHECKS-ARCHIVE.md`로 "미확인 종결"(CLAUDE.md §4 종결 규칙 신설).

---

## 2. 일하는 방식

**정본은 `CLAUDE.md`(협업 규칙·파이프라인·빌드 환경·브랜딩 불가침)** — 여기에는 CLAUDE.md에 없는
저장소 운용 세부만 둔다. CLAUDE.md와 어긋나면 CLAUDE.md가 이긴다(2026-08-21 개정: push는 **버전 커밋마다 즉시 1회**).

- **개발 우선순위: UI·코어 기능 우선**(사용자 확정). 녹화/녹음(A14·A15), HW 랭킹(A19), 포터블(A43),
  HWP 뷰어(A45)는 후순위.
- **push remote 이름은 클론마다 다르다 — `git remote -v`로 먼저 확인할 것**(소스트리 PC = `zp`,
  2026-08-29에 붙인 GitHub Desktop PC = `origin`). 인증은 저장소 로컬 git 설정에 있음 — 401이면
  사용자에게 토큰 재발급 요청. 새 PC 클론이면 identity·PAT 2단계가 필요하다(CLAUDE.md §3).
- **CI 판정 정본 = release 워크플로**(실제 빌드·패키징을 하므로 초록 = 컴파일 통과 실증). build 워크플로가
  Actions 장애로 Queued/Startup failure여도 release가 초록이면 무해. 확인은 사용자 Chrome(브라우저 MCP)으로
  `github.com/zpstudios/kotu/actions`(최신 run의 `svg[aria-label]`이 성공/실패) — 활성 탭을 뺏지 말 것.
  build.yml·release.yml이 동시에 돌고 각 3~4분.
- **완료 처리 절차**: 항목을 `※ A?? 완료 — 결번`으로 바꾸고 부록 A 표에서 행을 빼며, 완료 상세는 미진행 항목이
  참조할 때만 본문에 남긴다(아니면 ARCHIVE로). 미구현 항목의 내용이 바뀌면 새 번호를 주지 않고 그 자리에서 갱신.
- **개인 실명·개인 메일 금지**(A82) — 실명을 지우는 작업의 기록에 실명을 인용하는 실수가 잦다(v0.90.0 실사례).

---

## 3. 반드시 알아야 할 함정

### 3.1 레거시 식별자 — 절대 일괄 치환하지 말 것
이름이 세 번 바뀌었다: `WinUtil`(초기 코드명) → `ZP`(v0.33.0) → `KOTU`(v0.86.0~).
**구 이름을 그대로 둬야 하는 곳**이 있다. 바꾸면 "구 등록 청소" 로직이 깨진다:

- `ExplorerIntegration.LegacyBrands = ["ZP", "WinUtil"]`
- `%AppData%\ZP`·`%AppData%\WinUtil`·`ZP-Main`·`WinUtil-Main`을 언급하는 주석
- `release.yml`의 구 packId 안내 문구

**교훈**: A46에서 "ZP" 문자열만 훑어 `%TEMP%\WinUtil\subtitles`를 놓쳤다(A64에서 발견).
리브랜딩 스캔은 **현재 이름이 아니라 모든 과거 이름으로** 해야 한다.

### 3.2 셸 등록은 "등록한 그 순간"으로 굳는다
`ExplorerIntegration`이 레지스트리에 쓰는 값 중 **exe 절대 경로와 표시 문구는 재등록될 때만 갱신**된다.
- 그래서 A52로 모듈명을 바꿔도 이미 켜 둔 사용자의 우클릭 메뉴는 옛 문구 그대로다.
- A64로 exe명이 바뀌면서 등록된 경로가 없는 파일을 가리키게 됐고, 실기기에서 실제로
  .png 더블클릭이 '어떤 앱으로 열까요' 팝업으로 빠졌다 → **v0.89.0의 A78이 자동 복구를 넣음**
  (매 실행 시 `ReRegisterIfExeMoved`가 경로 어긋남을 감지해 켜져 있는 등록만 조용히 재기록).
- **Capabilities와 UserChoice(A38)는 ProgID만 참조**해서 경로 변화의 영향을 받지 않는다.
- A38의 자체 검증(`VerifyUserChoice`)은 **우리 해시 알고리즘 기준의 자기일관성 검사**다 —
  Windows가 실제로 수용하는지는 열어 봐야 안다. OS가 알고리즘을 바꾸면 "전부 성공"으로 보이면서
  실제로는 전부 무효가 되는 맹점이 있다(§5 첫 항목의 의심 지점).

### 3.3 하드웨어 모듈은 거의 전부 static (프로세스 공유)
`HardwareModule`의 폴러·섹션 캐시·구독자 수·리프레시 주기가 모두 `static`이고,
센서 선택(A18)·주기(A29)도 프로세스 단위 설정 키다. **A42에서 의도한 설계**(드라이버 핸들·WMI 조회 공유)라
통째로 뒤집으면 안 된다. **A70(v0.131.0)이 그 분리선을 그었다** — 폴러·주기는 공유 유지, 선택(A18)·바
크기(A62)는 `HardwareInstanceState`(인스턴스 상태 + 전역 1벌 저장 깔때기)로 창별 분리. A60 3차의 창별
상태(피닝·순서·모드)는 **이 클래스에 필드를 얹는 방식**으로 간다(새 static을 만들지 말 것).

### 3.4 빌드를 실제로 깨뜨린 것들 (재발 방지)
- **csproj/XAML 주석 안에 하이픈 두 개(`--`)** → MSB4025로 빌드 실패. XML 주석 문법 제약.
  (A64에서 `--mainExe`를 주석에 적었다가 실제로 깨짐.)
- **PowerShell 백틱 줄바꿈 뒤에 주석** → continuation이 깨진다. 백틱은 그 줄의 마지막 문자여야 한다.
- `resources.pri` — unpackaged WinUI는 exe 옆의 **고정 이름 `resources.pri`** 만 찾는다.
  없으면 시작 시 `XamlParseException`으로 조용히 죽는다(v0.5.5~0.5.7에 실제로 겪음).
  `AssemblyName`을 바꾸면 `$(TargetName).pri` 폴백 이름도 따라 바뀌므로 `release.yml`을 같이 고쳐야 한다.
- **`FrameworkElement.Parent`를 "이미 추가했나" 가드로 쓰지 말 것** — 라이브 트리 부착 전
  (뷰 생성자 안)에는 `Children.Add` 뒤에도 Parent가 null이라, 같은 요소를 두 번 Add해
  `COMException 0x800F1000`으로 죽는다(v0.111.0/A62가 유발, v0.113.2에서 수정).
  중복 방지는 `Children.Contains`(또는 자체 플래그)로. 이것도 **CI·정적 검사로 못 잡는 부류**다.
- **XAML 요소를 다른 컨트롤로 옮겨 붙일 때(reparent)** 는 **반드시 옛 부모의 `Children`에서 먼저 제거**해야
  한다. 판정은 `Children.Contains`(위 항목과 같은 이유로 `Parent` 금지). 선례가 셋 있다 —
  `ImageViewerView.TakeBottomBar()`(v0.27.0), `ExplorerPane.DetachPathBar()`(A91/v0.115.0).
  ※ **x:Name 필드는 부모에서 떼어도 살아 있다**(`InitializeComponent`의 `Connect()`가 잡아 둔 참조).
  그래서 옮긴 요소의 이벤트 핸들러·코드비하인드 참조는 손댈 필요가 없다.
- **정적 이벤트 구독은 반드시 Unloaded에서 해제한다** — `CompositionTarget.Rendering`(A88/v0.114.0)처럼
  프레임워크 static 이벤트에 뷰 메서드를 걸면, 해제를 빠뜨렸을 때 **뷰와 그 창이 통째로 샌다**.
  이 저장소의 관용구는 `Loaded`에서 `-=` 후 `+=`(이중 구독 방지), `Unloaded`에서 `-=`다.
- **`{StaticResource Default*Style}` BasedOn 참조는 런타임에만 터진다** — v0.107.0의
  `DefaultDropDownButtonStyle`이 실기기 시작 크래시를 냈다(v0.113.1에서 수정). WinAppSDK 1.8이
  WinUI 컨트롤 기본 스타일을 지연 로딩해 App.xaml 파싱 시점에 키가 없다. **CI(컴파일)로는 못 잡고,
  정적 검사로도 못 잡는 부류** — 새 XAML에서 프레임워크 키를 StaticResource로 참조할 때는
  "그 키가 파싱 시점에 로드돼 있는가"를 의심할 것. 키 없이 setter만 둬도 기본 템플릿은 유지된다.

---

## 4. 실기기 확인 대기 (사용자 몫) — 최신 묶음

> 누적분(v0.162.0~v0.273.0)은 `docs/DEVICE-CHECKS.md`. 이 절에는 **가장 최근 완주 묶음**의 확인 포인트만 두고,
> 다음 묶음이 완주되면 이 절 내용을 DEVICE-CHECKS.md 상단으로 밀어낸다.
> ※ **2026-09-01 예외**: v0.283.0~v0.286.0과 v0.287.0~v0.290.0 **둘 다 미확인**이라 두 블록을 함께 둔다.
> 다음 묶음이 완주되면 **앞 블록(v0.283.0~v0.286.0)부터** 밀어낼 것.
> **v0.161.0 이하는 2026-08-29에 `docs/DEVICE-CHECKS-ARCHIVE.md`로 "미확인 종결" 이관**(CLAUDE.md §4 종결 규칙).

✅ **v0.282.0 (A291) — 스크린샷 2장 2026-08-31 수령·판정 완료. 후속 = A307.**

판정 = **`trailingEdge: true` 부분 채택**. 즉 A284의 "쓰레기" 판정은 좌표 결함 셋(X `pad.Left` 누락 ·
Y `pad.Top` 누락 · 줄 탐색 정지)에 오염돼 있었고, 좌표계가 정리된 지금은 **글자가 있는 마지막 줄에서
`eofSrc=trailing`이 정상 동작**한다(A291의 목적 달성). 다만 **빈 마지막 줄에서는 개행 문자의 `trail`이
이전 줄의 Y를 주기 때문에 가드에 기각되고 `eofSrc=head` 폴백**으로 떨어져, 본문·밑줄·줄 번호와 y가
어긋난다. **잔여 증상은 A307로 승계**했고 **진단 값 2벌의 전사는 `docs/REQUIREMENTS.md` A307 본문**에
있다(다음 세션이 스크린샷 없이 판정할 수 있다).

> 판독법(A307 착수 시에도 그대로 쓴다 — 남겨 둔다):
> - **① 한 글자만 있는 문서.** 볼 것 = **`trail=`** 과 **`eofSrc=`**. `trail=`의 X가 글자 뒤쪽 모서리이고
>   `eofSrc=trailing`이면 부활 성공(`·EOF`가 글자 바로 뒤에 붙는다). `trail=`이 `0,0` 계열이거나
>   `eofSrc=advance`/`step`이면 `trailingEdge`가 무효인 것.
> - **② 한 줄 치고 엔터만 친 문서.** 볼 것 = **`endLine=`**. `trailing`이면 개행 문자의 뒤쪽 모서리가
>   빈 줄 시작을 준 것(`trail=`의 Y가 다음 줄 값이어야 한다). `height`면 그 길이 막힌 것 —
>   **2026-08-31 결과가 이 쪽이다**(`endLine=height`, `trail=`의 Y가 이전 줄 16.0).
> - **③ 회귀 확인**: 두세 줄 친 상태에서 EOF·`¶`·줄 번호·줄 사이 선이 이전 버전과 같은지.

**남은 미세 조정**(A307과 한 배치로 처리): `GuideOpacity`(0.14 — 진하면 0.11·흐리면 0.20) ·
`GuideGap`(2 — 글자에 닿으면 3~4).

⚠️ **v0.283.0~v0.286.0 (2026-08-31 A279·A280·A282·A278 완주분) 실기기 확인 포인트**

① **v0.283.0 Save as 후 창 제목(A279).** 기존 파일을 열어 **ⓐ 재검증 실패 다이얼로그의 "Save as..."**
   로 다른 이름 저장 → **타이틀바가 새 파일명으로 바뀌는지**, 그리고 **미저장 표식(●)이 함께 꺼지는지**.
   **All Readable 안에서도 같은지**(중계 한 겹이 더 있다 — 여기서만 안 바뀌면 중계 축 보고).
   무제 문서의 첫 저장도 같은 경로다. **덮어쓰기는 제목이 안 바뀌는 게 정상**(경로 불변).
② **v0.284.0 비활성 창 경계 버튼(A280).** 포인터를 창 위에 둔 채 **Alt+Tab**으로 다른 앱에 포커스를 주면
   **경계 버튼이 즉시 사라지는지** · 다시 활성화한 뒤 포인터를 움직이면 **정상 복귀**하는지 ·
   **(알려진 잔여)** 비활성 창 위에서 포인터를 움직이면 **다시 뜬다** — 의도적 존치이니 거슬리면 보고.
③ **v0.285.0 탐색기 홈 버튼(A282).** PathBar에 **집 모양 아이콘(E80F)** 이 보이는지 · 클릭하면
   **`%UserProfile%`로 이동**하는지 · **이미 홈이면 무동작**(버튼이 회색으로 죽지 않는 게 정상) ·
   열이 하나 늘었으므로 **경로 텍스트·필터 버튼 위치가 어긋나지 않는지**(재계수 확인 = 이 배치의 1순위).
④ **v0.286.0 업데이트 체크 워커 이관(A278).** 설정 화면을 열고 **2분 대기** → 자동 체크가 정상 동작하는지
   (Checking... 표시 → 결과 반영) · **설정을 닫았다 바로 다시 열어도 낡은 결과가 새 상태를 덮지 않는지**
   (1→0 경계 취소) · 체크가 도는 동안 **타이핑·스크롤 등 UI 체감에 변화가 없는지**(이관의 목적).

⚠️ **v0.287.0~v0.290.0 (2026-09-01 A293·A297·A298·A299·A306 완주분) 실기기 확인 포인트**

① **v0.287.0 H/W 하단 그래프(A293).** 긴 그래프 왼쪽의 **상시 빈칸이 사라졌는지**(Copy 버튼 바로 옆부터
   그래프가 차야 한다) · **기간 표기("5m")가 각 그래프의 가로 중앙**인지(바 전체 중앙이 아니다 — 그래프
   2개 각각) · **첫 로딩 Busy 링이 그래프 위에 겹쳐** 떠도 어색하지 않은지(칸을 없애고 star 칸 위에 겹치게
   했다 — 링이 꺼질 때 형제가 밀리지 않아야 한다) · 폭 재계수(BarFixedWidth 262 · 맥박 임계 574) 부근에서
   레이아웃이 튀지 않는지. ※ **가동 5분 전 그래프 왼쪽이 비는 것은 링 버퍼 정상 동작(A146 설계)** 이고
   이번 수정과 별건이다 — 사용자가 거슬렸던 것이 이쪽이면 별도 보고할 것(현재 미등재).
② **v0.288.0 좌 패널 리스트 헤더(A297).** 헤더 **5버튼의 폭이 동일**한지(Name만 두 배이던 것을 고쳤다) ·
   **Name 정렬 동작은 그대로**인지 · 협폭에서 **Created/Modified 잘림이 완화**됐는지 · 화살표가 있는 열과
   없는 열의 **행 높이가 같은지**.
③ **v0.289.0 1:1 아이콘(A298·A299).** 이미지/영상/문서 세 모듈의 **1:1 표시가 동일**한지(세 벌 복제를
   공용 팩토리 한 벌로 합쳤다 — 어느 한 모듈만 모양이 다르면 보고) · **비활성일 때 글자와 테두리가 함께
   회색**이 되는지 · **활성으로 돌아오면 함께 정색**되는지(왕복 대칭) · **라이트/다크 양쪽**에서 그런지.
   ※ **빈 화면(파일 미오픈) 표시가 문서(=`1:1` 상자)와 이미지·영상(=마지막 옵션 글리프)으로 갈리는 것은
   §5의 사용자 결정 대기 질문**이다 — 확인이 아니라 답을 주면 된다.
④ **v0.290.0 화면보호기 억제(A306) — OS 동작이라 코드로 판별 불가·1순위.** 영상 **재생 중 화면보호기·화면
   꺼짐이 오지 않는지**(짧은 대기 시간으로 설정해 시험) · **일시정지/정지/창 닫기 후 정상 복귀**하는지
   (억제가 남으면 화면이 계속 안 꺼진다) · 설정 Playback의 **"Keep the display awake…" 토글이 재생 중에도
   즉시 반영**되는지 · **오디오 재생은 영향 없음**(무접촉이 정상) · 창 두 개로 각각 재생·정지했을 때
   한쪽 정지가 다른 쪽 억제를 풀지 않는지(전역 카운터).

> 이전 묶음(v0.257.0~v0.273.0 A257~A277 21건)은 2026-08-31에 `docs/DEVICE-CHECKS.md` 상단으로 밀어냈다.

---

## 5. 사용자에게 물어야 할 미결 사항

**2026-08-29 일괄 정리 완료.** 2026-08-13~15에 쌓여 2주 넘게 답이 없던 14건을 사용자에게 전부 물어
종결하거나 등재했다. 이 절은 **살아 있는 것만** 둔다 — 종결 사유는 REQUIREMENTS 각 항목에 기입돼 있다.

**남은 것 1건** (정본 = `docs/REQUIREMENTS.md` 부록 B "확인 필요"):
**A298·A299 잔여 — Fit 버튼의 빈 화면(파일 미오픈) 표시를 어느 쪽으로 통일할 것인가.** v0.289.0의 전수
조사에서 드러난 행동 차이다: **문서 = 항상 `1:1` 상자** / **이미지·영상 = 마지막으로 고른 Fit 옵션의 글리프**.
3택 = ⓐ 전 모듈 `1:1` 상자 · ⓑ 전 모듈 마지막 옵션 글리프 · ⓒ 현행 유지. 아카이브는 A296이 Fit을
제거하므로 대상이 아니다.
2026-08-31 당일 답변 2건: **A304 = 업그레이드**(자체 렌더) · **A296 = Print도 제거**("Print 할 거
없을테니 빼자" — A271 존치·부록 B 78 규격 반전). A159는 같은 날 증상 확정(버튼 disabled —
파생 사양 결정은 착수 시 오케스트레이터 몫).

**등재로 전환한 6건(A278~A283) 중 5건은 이미 완주**했다: A283(v0.274.0) · **A279(v0.283.0) ·
A280(v0.284.0) · A282(v0.285.0) · A278(v0.286.0)**. 남은 것은 **A281**(A79 레벨 1 16px 가독성)뿐이고
**레벨을 올릴 때 착수**하는 조건부 항목이다(§1).

**종결분** — 재발하면 새로 등재할 것: A62(그래프 표시 크기 = 현행 유지. 카드 높이 M·L 동일은 A106
버튼 정렬에 맞춘 결과라 되돌리면 정렬이 깨진다) · A116(사이드바 25% 단일화 유지) · A108(틴트 현행) ·
A98(이미지 휠 단독 = 줌 유지) · A86(홀드 표시 지연 — 안 거슬림) · A107(플라이아웃 포커스 엣지 — 미발생) ·
A38(기본 앱 팝업 — v0.89.0이 해소한 것으로 확인) · A90(S4 문구 충분) · A112·A105(등재하지 않음) ·
A170(종료 시 밝기 — 미확인 종결·결번, 감사 결과는 ARCHIVE).
※ A83(Auto-fit)은 부록 B 56으로 종결·완료(v0.133.0). 재론 시 `docs/A83-autofit-draft.md` Q1~Q7에서 시작.

---

## 6. 다음에 할 만한 것

**착수 대기열 12건 = A281(조건부) + 신규 잔여 11건**(§1). 우선순위는 이 순서를 권한다:

1. **남은 신규 항목을 번호순으로 배치** — 전부 사양이 닫혀 있어 지시가 오면 즉시 배치 가능하다.
   묶기 좋은 쌍이 남아 있다: **A296+A159**(같은 버튼) · **A301~A304**(오디오 비주얼라이저 축 — 순서
   조율 필요). 대형은 **A305**(화면 모드 재정리 — 배치 분할 권장).
   ※ **A298+A299는 v0.289.0으로 소진**됐다.
2. **§4 실기기 확인 회수** — 미확인 묶음이 **둘**이다(v0.283.0~v0.286.0 · v0.287.0~v0.290.0).
   눈으로만 판별되는 축 = **A282 열 재계수**(경로 텍스트·필터 위치) · **A279 All Readable 중계** ·
   **A293 좌측 빈칸 소멸** · **A306 화면보호기 억제**(OS 동작이라 코드로 판별 불가).
   이전 묶음 21건은 `docs/DEVICE-CHECKS.md` 상단. 이슈가 나오면 그 유형을 REQUIREMENTS에 등재하고
   자체 검수 체크리스트에도 추가한다.
3. **REQUIREMENTS 다이어트** — A257~A277 21건의 완료 상세는 실기기 확인 전이라 본문에 존치 중이다.
   확인이 끝난 항목부터 ARCHIVE로 이관(부록 A 완료 행 포함).
4. **새 일감 등재** — `docs/REQUIREMENTS.md` **부록 A-2 "낙수(등재 후보)"**. 등재 전에 최근 완료
   항목이 이미 소화했는지 grep으로 확인할 것(목록 기준일이 오래됐고, 최근 묶음들이 문서 바·오디오
   바·사이드바 기본값을 크게 건드렸다).
5. **판단 대기 1건**: 진단 스트립(`diag.shellKeyOverlay`) 제거 여부 — 사용자 지시 전에는 손대지 않는다.

파이프라인·프롬프트 필수 항목·통과 조건 표 요구 등 운용법은 `CLAUDE.md` §2가 정본이다.

---

## 7. 브랜딩·구조가 크게 바뀐 버전들 (배경만 — 상세는 git log)

- **v0.86.0 (A46)** ZP → KOTU. 범위는 표시명 + 시스템 등록 ID. **기존 사용자 설정 이관 없음**(사용자 확정) —
  대신 구 브랜드 레지스트리는 첫 실행에 1회 자동 청소. 앱 아이콘은 4글자가 16px에서 뭉개져 **"KO"/"TU" 2줄**로 확정.
- **v0.87.0 (A35·A52·A53·A56)** 표시명 단수형 + ZIP→Archive, New Instance 문구, 제목에 인스턴스 번호,
  연결 섹션 순서. 우클릭 메뉴 라벨의 하드코딩을 없애 모듈 `BrandName`을 따르게 함.
- **v0.88.0 (A64)** exe·어셈블리·프로젝트·네임스페이스 전면 rename. A46의 "표시명만" 결정을 대체.
  계기는 UAC에 `WinUtil.App.exe`가 보인 것 — 사용자 관심사는 **"WinUtil이라는 단어"** 이지
  "게시자: 알 수 없음"(코드 서명)이 **아니다**. 저장소도 `zpro` → `kotu`로 변경됨.
- **v0.89.0 (A78)** 시작 시 셸 등록의 exe 경로 어긋남 자동 복구. 계기는 실기기에서 .png 더블클릭이
  '어떤 앱으로 열까요'로 빠진 것(A64의 예고된 부작용). 순수 로직은 Core `ShellCommand`로 분리해
  단위 테스트(ShellCommandTests). 구현 결정은 REQUIREMENTS 부록 B #25.
- **v0.90.0 (A82)** 개인 실명 제거 + 저작 주체를 **ZP Studios** 한 이름으로 통일
  (LICENSE·`Directory.Build.props`·release.yml `packAuthors`·site 푸터 — **표기를 바꿀 땐 이 4곳을 함께 본다**).
  git 이력은 재작성하지 않기로 확정 → **v0.89.0 이전 커밋의 작성자 표기는 GitHub에 그대로 남는다**(수용된 한계).
  로컬 `git config`에 이름·메일이 고정돼 있으니 커밋할 때 `-c user.name=...` 오버라이드를 쓰지 말 것.
  ※ 함정 하나: XML 주석에 `--packAuthors`라고 적었다가 하이픈 2연속으로 props 파싱이 깨졌다(커밋 전에 잡음).
